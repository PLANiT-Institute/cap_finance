# Raw data

Raw files are immutable and date-stamped. They are excluded from Git by default.

For each raw file, add a record to the source manifest containing the publisher, URL or DOI, retrieval date, reporting period, licence, redistribution status, local file name, and SHA-256 checksum.

Only explicitly redistributable sources may be force-added to a public release.
