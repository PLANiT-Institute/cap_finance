# macOS and editor noise
.DS_Store
**/.DS_Store
.idea/
.vscode/

# Python environments and caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Secrets and local configuration
.env
.env.*
!.env.example

# Raw, restricted, and generated data
data/raw/**
!data/raw/README.md
data/interim/**
!data/interim/.gitkeep

# CAP v2 pipeline outputs — regenerable; facility layer is CONFIDENTIAL (설계서 §8-2)
out/**
out/render/facility_confidential/**
# ...except the sensitivity screening, which the site build needs and which costs
# 7 minutes to regenerate. One output root (out/) — v1 artefacts live in archive/.
!out/sensitivity/
!out/sensitivity/*.csv
tmp/


# test artefacts
out_test/

# archived v1 generated artefacts — kept on disk, out of the repo
archive/cap_kj_v1/outputs_generated/
