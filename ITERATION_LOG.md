# ITERATION LOG

## Cycle 0 (준비, 2026-08-07 오후) — AUTOPILOT.md 배치. FIN 상태: v2.1(수소 외부조달·전력 이원화·retire) 코드 완료, retire 퇴행(A1) 미해소, 마지막 전체 실행 15:01. EFF 상태: 커밋 0개, remote 없음. 다음: A1부터.

## Cycle 1–2 (A1+A2 완료)
**한 일**: retire 퇴행 근본원인 2건 수정.
1. 전해조 전력 이중계상 — v2.1에서 수소를 외부조달로 전환했으나 `steel_h2dri`/`steel_hyrex`의 elec_intensity가 Vogl 총 SEC(수전해 포함)로 남아 수소 대금+전해조 전력 동시 부과. 0.85 MWh/t(샤프트+EAF+부대)로 보정 → 수소환원 연간 자원비용 958→498천원/t.
2. 무상할당 미반영 — 탄소비용을 배출 전량 부과(2050 726천원/t vs 철강 마진 70천원/t)해 "전 설비 폐쇄"가 최적해. `carbon_auction_share` 도입(K-ETS 4차 발전외 15% 앵커, 2030 이후 ESTIMATE 램프). 배출량은 예산 제약에 전량 계상 유지.

**검증**: 테스트 13/13 그린. 완료 기준 충족 — CAPEX>0(POSCO NZ15 11.0조), 경계 5–10점(NZ15), ② 96–331천원/t. POSCO 최적 = 효율개선 4기 + 수소환원 2기(2036/2038) + 폐쇄 2기(광양1고로 2028 = 재투자 창과 일치, FINEX3 2026) — 혼합 해로 정상화.

**다음**: B1(EFF 커밋·remote 완료됨) 확인 후 C1(crosswalk) → D1(Vercel 사이트 통합).
**백로그 변경**: A1·A2·B1 완료 처리.

## Cycle 3–4 (B2·C1·C2) — 상세는 EFF 로그. 요약: crosswalk 33행 생성(FIN 고로 ↔ EFF 블록), 기술비용 대조표 작성. EFF의 H2-DRI 전력 0.58이 FIN A1 수정(0.85)을 교차확증. 능력 기준 12% 차이는 추적 항목.

## Cycle 5 (D1 Vercel 사이트 통합)
**한 일**: `scripts/build_site.py` — FIN 보고서 + EFF 대시보드(국·영) + 랜딩을 `web/`으로 조립하는 단일 명령. vercel.json에 noindex·no-referrer 헤더 추가(시설 표 포함본 보호).
**검증**: 로컬 서브 index/report/dashboard 전부 200, 렌더 육안 확인. 중첩 앵커(a 안 a)로 카드 레이아웃 깨진 것 수정. web/ 12MB (대시보드 6MB×2).
**Vercel 연결 절차**(사용자 작업): Vercel > New Project > PLANiT-Institute/cap_finance > Framework: Other, Output Directory `web` > Deploy. 시설 단위 표 포함이므로 Settings > Deployment Protection 활성화 권장.
**다음**: C3(시나리오 정의 대조) 또는 D2(데이터 패키지).

## Cycle 6 (F1 파라미터 인벤토리) — AUTOPILOT v2 체제 시작
**한 일**: `scripts/build_parameter_inventory.py` — FIN·EFF 전 입력 파라미터 408건을 증거등급(T1~T5)·출처·사용처와 함께 단일 표로. `docs/parameter_inventory.csv` 양 저장소.
**결과**:
| 모형 | T1 | T2 | T3 | T4 | T5 |
|---|---|---|---|---|---|
| FIN | 3 | 130 | 41 | 79 | 42 |
| EFF | 0 | 7 | 0 | 0 | 113 |

**발견 2건**:
1. **EFF 모형 입력 113건이 전부 자기선언 `model_estimate`(T5)** — README가 명시한 설계대로지만, 별도 증거 파일(`technology_cost_evidence` 실제 프로젝트 7건 = T2)이 **모형 입력에 연결돼 있지 않다**. 증거는 있는데 쓰이지 않는 구조. → 백로그 신규 항목.
2. **T5 중 범위 미지정 139건** — FIN 26건(EST_v0 예산·가격 경로), EFF 113건. AUTOPILOT §1 규칙 위반이므로 F3에서 범위 부여 대상.
**결론 영향**: 없음(측정만). 단 다음 사이클 F2의 작업 순서를 이 표가 결정.
**다음**: C7 = F2 사전 민감도 스크리닝 — 어느 파라미터가 헤드라인을 좌우하는지 측정, 상위 10의 tier가 데이터 승급 순서가 됨.

## Cycle 7 (긴급 수정 — 테스트가 실산출물 오염)
**발견**: `tests/test_pipeline.py`가 `data/sample`(합성)로 전 파이프라인을 돌리며 `out_dir`을 실행본과 공유 → **실데이터 산출물 `out/`을 합성 결과로 덮어씀**. 그 상태에서 생성된 보고서·웹이 합성 수치를 게재(석화 ② −960천원/tCO₂ = "전환하면 돈 번다"는 불가능한 값).
**조치**: 테스트 `out_dir="out_test"` 격리 + 재발 방지 테스트 `test_outputs_isolated_from_production` 추가 + .gitignore.
**실데이터 정상 재실행 결과** (NZ15, support=none):
| 기업 | CAPEX(조) | 피크 | P50(조) | ②(천원/tCO₂) | TCaR(조) |
|---|---|---|---|---|---|
| POSCO | 21.7 | 2030 | 73.8 | 130 | 51.5 |
| NSC | 33.4 | 2032 | 83.8 | 173 | 57.8 |
| LOTTE | 1.4 | 2046 | 0.5 | 257 | 1.7 |
| MCI | 0.6 | 2045 | 0.2 | 264 | 0.8 |
경계 점 NZ15 4–6. ② 130–264천원/tCO₂ = 문헌 대역 내(H3에서 정식 대조 예정).
**교훈**: v1 기준("테스트 그린")으로는 통과했을 결함. v2의 "출처·재현·검증" 기준이 잡아냄. 재현성 게이트를 품질 게이트에 명시적으로 추가할 것.
**다음**: C8 = F2 사전 민감도 스크리닝.

## Cycle 8 (F2 사전 민감도 스크리닝)
**방법**: E2 계획 집합 고정(perturbation마다 MILP 재해는 7분 소요라 비현실적) → E4/E5 경제성만 재평가. 파라미터 25종 ×(±30%) × 4사, n=3,000. 계획 선택 채널은 I1/I2 전체 재실행으로 별도 확인.
**기준선(NZ15, 계획 고정)**: POSCO LCOA 131 / NSC 165 / MCI 191 / LOTTE 233 천원/tCO₂.
**상위 12 (4사 중 최대 |Δ|%)**:
| 순위 | 파라미터 | tier | ΔLCOA% | ΔTCaR% |
|---|---|---|---|---|
| 1 | fac.ef_inc (기존 시설 배출 원단위) | **T5** | 86.4 | 0 |
| 2 | tech.emission_factor | T3 | 85.8 | 0 |
| 3 | cfg.discount (할인율) | T5 | 7.3 | 42.5 |
| 4 | vol.h2 (수소 변동성) | **T5** | 10.8 | 41.9 |
| 5 | tech.h2_intensity | T3 | 12.1 | 30.7 |
| 6 | price.h2 | **T5** | 12.1 | 30.7 |
| 7 | vol.elec | T3 | 0.9 | 25.8 |
| 8 | fac.capacity | T2/T5 | 17.8 | 1.4 |
| 9 | tech.capex | T3 | 17.4 | 1.4 |
| 10 | tech.elec_intensity | T3 | 7.4 | 13.6 |

**핵심 발견**: 1위가 `fac.ef_inc` — **기존 설비의 배출 원단위**이고 현재 **T5(루트 표준값 주입, BF 2.15 tCO₂/t)**. LCOA를 86% 움직이는 파라미터가 가장 약한 증거 위에 있다. 이것이 G1(시설 실측 배출) 최우선 근거.
수소 관련 3종(vol.h2·price.h2·h2_intensity)이 TCaR의 30~42%를 좌우 — 수소 가격 시계열 부재(사전값 vol 0.25)가 두 번째 승급 대상.
**승급 작업 순서 확정**: ① fac.ef_inc → G1 시설 실측 ② price.h2/vol.h2 → 수소 계약가 시계열 ③ fac.capacity → G3 ④ 할인율은 데이터가 아닌 선택이므로 I1 강건성으로 처리.
**다음**: C9 = H1 내부 일관성 테스트(순서 조정 — G1 웹수집 전에 검증층부터), 이어서 G1.

## Cycle 9 (J6 일부 — 증거·민감도 시각화 페이지)
**한 일**: `scripts/build_evidence_page.py` → `web/evidence.html`. 3개 차트: ① 우선순위 매트릭스(가로 영향력 × 세로 증거등급, 위험구역 음영) ② 토네이도(LCOA/TCaR 영향 이중 막대, 등급별 색) ③ 등급 분포(FIN vs EFF 누적). build_site.py에 통합돼 사이트에서 도달 가능.
**설계 근거**: 등급은 순서형이라 단일 색상 램프를 쓰되 T4/T5는 상태색(경고/위험)으로 분기 — "증거가 약하다"는 상태 정보를 색으로 전달. 매트릭스 우하단이 위험구역.
**다음**: C10 = H1 내부 일관성 테스트.

## Cycle 10 (데이터 진위·활용 감사 + 조달부담 지표 ⑥) — 사용자 5개 점검 체제 시작
**한 일**
1. `scripts/audit_data.py` — 입력 9파일 88컬럼을 (a) 채움률 (b) 엔진 참조 여부 (c) 출처 해소 여부로 전수 판정. `docs/data_audit.csv` + `.md`. 합성 샘플과 바이트 동일하면 실패 종료(가짜 주입 게이트).
2. **config 기본값 결함**: `data_dir: data/raw`인데 파이프라인은 `D*.csv`(=`data/prepared`)를 읽어 `python -m cap` 무인자 실행이 SchemaError로 죽었다. `data/prepared`로 수정 — "한 명령 재현"이 실제로 성립.
3. **CAPEX 시점 결함(근본)**: `plancost.build_profile`이 CAPEX 전액을 채택연도 1년에 계상, `build_years`는 가동개시만 이동시켰다. 공사기간 균등 분산으로 수정. 피크가 최대 공사기간 배수만큼 과대였음.
4. **지표 ⑥ 조달부담 신설**: D6 재무 6개 컬럼(revenue/ebitda/total_debt/net_debt/interest/cash)이 **전량 미사용**이던 것을 소비 → `out/e5/affordability.csv` + 보고서 2절(표+피크배수 막대).

**검증(수치)**
- 감사 판정: ok 70 / UNUSED 15 / CONSTANT 3 / EMPTY 0 / 합성누출 0 / 미해소 출처 0 / 추정라벨 4(EST_D2A_V0·EST_D2B_V0·PREP_ALLOC·PREP_BOTTOMUP).
- CAPEX 분산 전후 피크(NZ15, none): POSCO 18.6조→**5.6조**, NSC 23.8조→**8.6조**, LOTTE 0.75→0.71, MCI 0.34→0.32.
- 헤드라인 ②(천원/tCO₂) 전후: POSCO 130→128, NSC 173→170, LOTTE 257→251, MCI 264→258. **결론 불변**(할인시점 이동분만).
- 테스트 14/14 그린.

**결론 영향(신규)**: 조달부담이 순위를 뒤집는다. 비용/tCO₂로는 POSCO(128)가 가장 싸지만 피크배수 3.1×·총 CAPEX/EBITDA 12.2×로 부담은 최대. LOTTE는 CAPEX 1.4조로 최소지만 **기준 EBITDA가 음수(−0.72조)라 자체 조달 자체가 불가**. MCI만 피크 0.3×로 여유. → "얼마 드는가"와 "감당 가능한가"가 다른 기업을 지목한다.

**데이터 공백(정직 기록)**: POSCO·LOTTE의 net_debt/total_debt 공시 미확보 → 사후 레버리지 미산출. D6 보강 대상.
**백로그 신규**: 미사용 15컬럼 중 실질 3건 — `D1b.emissions_s2`(전 행 0, Scope2 미모형화), `D3.capex_uncertainty`(기술별 불확실성 미전파 → F3), `D5.param_type`(유상할당 비율이 데이터에 있는데 config 하드코딩 사용, 2030년 값 데이터 50% vs config 15% **충돌**).
**다음**: C11 = D5 유상할당 충돌 해소(데이터를 정본으로) + H1 내부 일관성 테스트.

## Cycle 11 (정책 입력 정본화 · 가격경로 결함 · 실행가능성 · MCP)
**한 일**
1. **D5 유상할당 충돌은 충돌이 아니라 정보 소실이었다.** 원자료는 발전부문 50%(2030)와 **발전외(철강·석화) 15%(2026–2030)**를 구분해 두었는데 `prepare_raw`가 `instrument`를 전부 `"other"`로 눌러 구분이 사라졌고, config는 별도 하드코딩 램프를 썼다. `instrument`를 `auction_share`(발전외)/`auction_share_power`/`price_cap`/`price_floor`로 분류하고, `plancost.auction_share`가 **확정 할당계획을 config 추정 램프보다 우선**하도록 했다. 2026–2029 유상할당 0.11→0.15(확정값).
2. **가격 경로 결함(중대)**: D2b의 Korea `h2_price` 2025 셀이 비어 있었고 E1이 그 NaN을 그대로 보간해 경로 전체를 오염시켰다. E2는 오염된 경로를 거부하고 **v2.1에서 폐기한 전해조 구조식으로 조용히 되돌아가 있었다** — 안내는 로그 14줄 뒤 `note` 한 줄뿐. 즉 한국 2사(POSCO·LOTTE)의 수소 비용이 설계와 다른 모형으로 계산되고 있었다. E1이 결측 앵커를 제거·평탄 외삽하고 **그 사실을 경고로 알리며**, 보간 결과에 NaN이 남으면 즉시 실패하도록 수정. 실행 후 구조식 대체 발동 **14회 → 0회**.
3. **실행가능성**: E2가 20분 넘게 무출력으로 돌아 정지와 구분이 불가능했다. 조합별 진행 표시 추가 + `mip_gap_rel: 0.005` 도입 — E2 목적함수는 계획 **순서만** 정하는 대리이고 정본 비용은 E4가 낸다. 조합당 36초로 안정.
4. **감사 스크립트 자체 결함 2건**: (a) `schemas.py`가 엔진 스캔에 포함돼 전 스키마 컬럼이 "사용됨"으로 오판. (b) pandas 3에서 `astype(str)`이 NaN을 `"nan"`으로 바꾸지 않아 **빈 셀이 전부 채워진 것으로 계수**됐다. 둘 다 수정, `PARTIAL` 판정 신설.
5. `tests/test_consistency.py` (H1) — 실산출물 대상 12개 항등식: 자원비용 정의, TCaR 정의, 분산 몫 합=1, 기준선 배출의 원자료 왕복, 전환이 배출을 늘리지 않음, CAPEX 피크≤총액·공사기간 분산 회귀방지, 조달비율 재계산 가능, 가격경로 무결성, 수소 구조식 미발동, 확정 할당계획 우선, 발전부문 값 미유입, 경계점 파레토 비지배.
6. **MCP 서버** `src/cap/mcp_server.py` (stdlib, stdio JSON-RPC) + `docs/mcp_server.md`. 10개 도구, 모든 응답에 정의·한계 동봉, `get_validation_summary`는 **없는 검증을 missing으로 명시**, 시설 단위는 기본 거부.
7. E2가 매 실행 이전 실행의 잉여 계획 파일을 남기던 것 정리(`plans/` 초기화).

**데이터 감사 재판정** (결측 계수 수정 후): ok 67 / UNUSED 11 / PARTIAL 6 / EMPTY 2 / CONSTANT 2.
- **EMPTY 2건**: `D1b.energy_naphtha` 0/69 — 석화 NCC의 **주 원료가 미수집**. `D7.coverage_pct` 0/12.
- **PARTIAL 6건**: `D2b.value` 222/224(위 §2 원인), `D6.capex_total` 11/22·`net_debt` 9/22, `D7.facility_id/tech_id` 8/12(= 공시 해상도 부족으로 gap 미산출되는 그 행들).

**미해결·다음 사이클로**: `prepare_raw`가 **원자료의 Scope 2 배출을 0으로 덮어쓰고 있다** — raw 36/37행에 값이 있고 NSC는 11.9 MtCO₂(scope 1의 19%). 수집해 놓고 버리는 중이며, 기존 설비는 계통전력을 쓰고 전환설비는 재생 PPA를 쓰는 모형 구조상 **경계 불일치**다. C12에서 보존 + 경계를 config로 명시.
**다음**: C12 = Scope 2 보존·경계 명시 → 시나리오 러너 실행 → 시나리오 페이지.

## Cycle 12 (실데이터 전체 재실행 · 시나리오 분석툴 · Scope 2 보존)

**전체 재실행 (수소 경로 수정 반영, NZ15·support=none)**

| 기업 | ② 천원/tCO₂ | 이전(폐기 모델) | ① 총 CAPEX(조) | 이전 | 피크(조) | ③ TCaR(조) | ⑥ 판정 |
|---|---|---|---|---|---|---|---|
| POSCO | **115** | 128 | **20.6** | 21.7 | 3.4 (2041) | 26.9 | 피크 1.91× — 외부조달 필수 |
| NSC | **164** | 170 | **31.7** | 33.4 | 2.6 (2040) | 34.3 | 피크 0.51× |
| MCI | **237** | 258 | **0.07** | 0.63 | 0.03 (2041) | 0.86 | 피크 0.03× |
| LOTTE | **275** | 251 | **0.16** | 1.42 | 0.08 (2041) | 2.21 | EBITDA 음수 — 자체 조달 불가 |

석화 CAPEX가 9배 줄고 ②는 오히려 오른 것이 핵심 변화다. 원인은 C11의 수소 경로 수정 —
한국 수소가 D2b 실경로(2030 3,500원/kg)로 잡히면서 LOTTE·MCI의 최적해가
`petchem_h2fuel`(연료 수소전환, 2041) 하나로 바뀌었다. **MIP 갭 인공물이 아님을 직접
확인**: 갭 2%와 0.1%에서 LOTTE NZ15 최적해·NPV·슬랙이 완전히 동일(계획
`petchem_h2fuel` 2041 ×2기, npv 9,201.5조, 슬랙 9.91 MtCO₂). 이전 1.4조는 폐기된
전해조 구조식이 만든 값이었다.

**실행 시간**: E2 20분(이전 40분+에서 미완). 조합별: LOTTE 31/37초, MCI 33/32초,
NSC 753/146초, POSCO 90/87초. `solve_status` 55개 계획 전부 `Optimal`.

**시나리오 분석툴 (항목 ②)**: `scripts/run_scenarios.py` — 명명된 가정 묶음 10종을
E3–E5 재평가(묶음당 2~3초). `web/scenarios.html`에서 묶음을 고르면 4사 값·기준 대비
변화율·순위 이동(슬로프 차트)·판정이 즉시 갱신된다.

| 묶음 | ② 변화 (최대) | 판정 |
|---|---|---|
| h2_cheap (−30%) | 석화 −27%, 철강 −5% | 결론 불변 |
| h2_expensive (+30%) | 석화 +27%, 철강 +5% | 결론 불변 |
| elec_high (+30%) | 철강 +6%, 석화 0 | 결론 불변 |
| disc35 / disc65 | ② ±1%, **TCaR ±34%** | 결론 불변 |
| carbon_fast / slow | ② 0 (구조상), 탄소포함 P50 POSCO −17.9조→**−41.9조** | — |

**전 묶음에서 감축단가 순위 불변** (POSCO < NSC < MCI < LOTTE). 값은 크게 움직여도
지목되는 기업이 바뀌지 않는다 = 결론이 이 가정들에 의존하지 않는다.

**결함 2건 자체 발견·수정**
1. 시나리오 가격 오버라이드가 변수명 불일치(`h2` vs `h2_price`)로 **아무것도 안 바꾸고
   있었다** — 모든 묶음이 기준값을 그대로 반환. 이제 없는 변수명은 즉시 실패한다.
2. 구조상 ②를 못 움직이는 묶음(유상할당·PPA·폐쇄상한)이 0.0%로 표시돼 고장처럼
   읽혔다. 각 묶음에 **왜 안 움직이는지**를 화면에 붙였다.

**Scope 2 보존**: `prepare_raw`가 0으로 덮어쓰던 것을 전력 소비 가중으로 배분
(NSC 11.42 / POSCO 1.40 / MCI 0.38 MtCO₂ @2024). 회귀 확인: production·emissions_s1·
energy_* 전부 변화 0 → 기존 파이프라인 결과 유효. **엔진 경계는 Scope 1 유지**(A-21) —
기업 예산이 자체 base 배출에 앵커되므로 수준 중립이고, 넣으면 전기화가 더 유리해지는
방향의 구조 대안이라 별도 검증 대상.

**산출물**: `METHODOLOGY.md`(가정 대장 A-01~A-21), `web/scenarios.html`,
`web/memo.html`(A4 1페이지 이사회 메모), `src/cap/mcp_server.py`(10개 도구),
저장소 정리(v1 → `archive/`, `outputs/` → `out/sensitivity/`).
**검증**: 테스트 27/27 그린 (합성 end-to-end 14 + 실산출물 일관성 13).
**다음**: C13 = H3 외부 대조(실제 프로젝트 CAPEX·문헌 LCOA 범위 대비 ② 위치 판정).

## Cycle 13 (H3 외부 대조 — 우리 값은 바깥과 맞는가)

`scripts/validate_external.py` → `docs/validation_external.md`. EFF의 실제 프로젝트 공시
7건 + 문헌 앵커(DIW DP2082, Vogl 2018, Nature 2025, 한은·금감원 NGFS)에 견줬다.

**판정**

| 대조 | 우리 값 | 바깥 | 판정 |
|---|---|---|---|
| `steel_h2dri` CAPEX | 863 천원/t | DIW DRP+EAF 858~1,089 (€2022) | **범위 안** |
| `steel_eaf` CAPEX | 240 천원/t | DIW EAF 368~677 | 범위 밖 하단 ×0.65 — POSCO 광양 실적(247)과 동일 출처, 모형에서 미사용(A-10) |
| ② 감축단가 | 115~275 천원/tCO₂ (US$85~204) | NGFS 섀도가격 2030 US$150 / 2050 US$1,700 | 2030과 같은 자릿수, 2050보다 ×8.3 낮다 |
| `incumbent_capex_unit` | 200 천원/t | 고베 3고로 실제 개수 47 천원/t | **×4.2 과대** |

**핵심 발견**: 모형의 중심 전환기술(수소환원)이 문헌 범위 안에 들어간다 — 결과의 신뢰도를
직접 지지한다. 반대로 **개수 재조달가가 실적 대비 4.2배**이고, 이 값이 좌초비용(A-13)을 통해
투자 시점을 정한다. 과대하면 조기 전환의 벌점이 과대해져 투자가 재투자 창으로 몰린다 —
우리 결과의 CAPEX 피크가 2040–41에 집중된 것과 방향이 일치한다.

**검증 가능하게 만듦**: `cfg.incumbent_capex_scale` 도입 + 시나리오 묶음 `reline_cheap`
(×0.235 = 실적 수준). 계획 메뉴 고정 상태에서 ② −1%, 탄소 포함 P50은 NSC −1,297→−1,863조.
**이것은 효과의 하한**이다 — 본 효과인 "채택 시점 앞당김"은 E2 재탐색(`--replan`)에서만
나타나므로 화면과 문서에 그렇게 적었다. 단일 관측(2016, 외피 재사용 = 저비용 사례)이라
값을 교체하지 않고 [47, 200] 범위 부여의 근거로 쓴다.

**아직 못 한 것(명시)**: 문헌 LCOA 직접 대조(Vogl·Agora·IEA ISTR·MPP 수치 미추출),
석유화학 프로젝트 대조 자체가 없음(증거 파일이 철강뿐 — 석화 결과의 가장 약한 고리),
프로젝트 범위(scope) 정합 재계산 미실시.

**검증**: 테스트 27/27. MCP `get_validation_summary`의 missing이 4개 → 2개
(`cross_model_check`, `validation_backtest`)로 감소.
**다음**: C14 = H2 후향 검증(2020–24 실적 재현) 또는 석화 프로젝트 증거 수집.

## Cycle 14 (H2 후향 검증 — 주입 표준값이 실적을 재현하는가)

**설계 판단이 먼저다.** "시설 배출 합계 = 회사 공시 합계"는 배분 규칙상 항상 참이라
검증이 아니다. 순환하지 않는 질문은 하나뿐이었다: **능력가중 루트 표준 배출계수가 회사가
공시한 배출강도를 재현하는가.** `scripts/validate_backtest.py`가 이것만 잰다.

| 기업 | 공시 원단위 (tCO₂/t) | 능력가중 루트 표준 | 평균 오차 | 판정 (±10%) | 가동률 |
|---|---|---|---|---|---|
| POSCO | 1.98~2.09 | 2.032 | **+1.0%** | **통과** (최대 2.7%) | 95% |
| NSC | 1.83~1.91 | 2.150 | **+15.7%** | **초과** (최대 17.5%) | 79% |
| LOTTE·MCI | — | 0.95 | — | **대조 불가** (생산량 미공시) | — |

**NSC 초과의 원인과 결론 방향**: D1a의 NSC 설비 목록이 `{'BF': 10}` — 고로 단일 구성이라
능력가중 EF가 BF 표준값 2.15에 그대로 붙는다. 실제 NSC에는 전기로·스크랩 장입 몫이 있고
그만큼 공시 원단위가 낮다. 즉 **모형은 NSC의 기존 설비를 실제보다 더 더럽다고 보고 있고,
그 결과 전환의 감축량을 과대평가해 감축 단가 ②(164천원/tCO₂)를 과소평가**한다. F2에서
`fac.ef_inc`는 ±30%가 ②를 86% 움직이는 1위 파라미터이므로 16% 편차는 무시할 수 없다.
**보정값은 추정하지 않는다** — 시설 실측 배출(G1) 확보가 유일한 해법이고, 그 전까지는
NSC의 ②를 하한으로 읽어야 한다는 사실을 METHODOLOGY §9-1에 명시했다.

**가동률로 본 능력 추정(A-01)**: POSCO 95%(91~102%) — 타당. NSC 79%(74~87%) — 능력 과대
또는 가동 중단분 잔존. 둘 다 G3 작업 대상.

**석유화학은 원단위 검증이 성립하지 않는다.** LOTTE·MCI 모두 생산량 미공시라 배출 총량만
실적에 앵커돼 있고 원단위·능력은 검증되지 않았다. H3에서 석화 프로젝트 CAPEX 대조도
불가였으므로, **석화 결과는 현재 어떤 외부 검증도 통과하지 않은 상태**다 — 문서 3곳에 명시.

**못 한 검증 명시**: 에너지 원단위(A-03) 재현 — 사업장별 에너지 소비 미공시로 불가.
비용 구조 후향 검증 — 실적 에너지 지출 시계열 없음.
**검증**: MCP `get_validation_summary`의 missing이 2개 → 1개(`cross_model_check`).
**다음**: C15 = 석화 증거 보강 또는 I1 `--replan` 할인율 강건성.

## Cycle 15 (석유화학 외부 증거 수집 — 확보한 것과 못 한 것)

C14에서 "석화 결과가 어떤 외부 검증도 통과하지 않았다"가 최대 구멍으로 남아 웹 수집을 했다.

**확보 (1차 공시)**: 미쓰이화학 오사카공장 탄소중립 발표(2023-06-01, `MCI_OSAKA_CN_2023`).
Scope1+2 약 160만 tCO₂, 나프타 크래커 **암모니아 연료전환으로 약 70만 tCO₂ 감축 전망**
= 공장 배출의 약 44%.

**대조 결과**: 우리 `petchem_h2fuel`은 EF 0.48 vs 기존 0.95 → **49% 감축**. 회사가 자기
공장에 대해 제시한 44%와 **5%p 차이**. 연료를 수소로 바꾸느냐 암모니아로 바꾸느냐는
다르지만 크래커 가열로의 연료 탄소를 걷어낸다는 메커니즘이 같다. **석화 기술 가정 중
유일하게 외부에서 확인된 항목.** (분모가 공장 전체 vs 크래커 공정으로 달라 자릿수·방향
확인이지 동일 정의의 일치는 아님 — 문서에 명시)

**채택하지 않은 것 (중요)**
- 검색 요약에 도는 미쓰이 **"1,400억엔"**: 1차 출처(보도자료)에 금액이 없다. 본문은
  "large-scale capital investment will be needed"라고만 쓰고 금액 논의를 유보했다.
  **출처로 확인되지 않아 채택하지 않음** — 등록부 `quality_note`에 그 사실까지 기록.
- 롯데케미칼 **"2030년까지 그린사업 11조원"**: 수소(6조)·전지소재(4조)·리사이클(2조)
  **신사업 투자**이지 기존 크래커의 전환 CAPEX가 아니다. 비교 대상이 아니므로 미사용.

**결론**: 석화 **CAPEX**는 여전히 외부 대조 불가 상태이고, 그 사유(공시가 비교 가능한
형태로 존재하지 않음)를 `docs/validation_external.md` §5에 명시했다. 감축률 하나만
검증됐다.
**다음**: I1 할인율 `--replan` 완료 확인 → H4 교차대조.

## Cycle 16 (H4 두 모형 교차대조 — 마지막 미완 검증)

`scripts/cross_model_check.py` → `docs/cross_model_check.md` (양 저장소 동일 사본).
FIN `NZ15`·지원없음 비용최소 계획 ↔ EFF `ACCELERATED_15C`의 **실행가능 후보 96개 중**
감축단가 최소.

| 축 | POSCO FIN/EFF | NSC FIN/EFF |
|---|---|---|
| 생산 (Mt) | 35.0 / 34.5 (+1.4%) | 34.3 / 34.3 (0.0%) |
| 배출 S1+2 (Mt) | 71.1 / 69.8 (+1.9%) | 75.1 / 72.6 (+3.4%) |
| 총 CAPEX (조) | 20.6 / 24.1 (−15%) | 31.7 / 26.3 (+20%) |
| 감축단가 (천원/tCO₂) | 115 / 26.6 | 164 / 42.3 |
| EFF 실행가능 후보 범위 | 26.6 ~ 155.9 | 42.3 ~ 152.3 |

**입력은 일치한다** — 생산 ±1.4%, 배출 ±3.4%. 같은 회사를 같은 크기로 보고 있다는 뜻이고,
이것이 먼저 맞아야 나머지 비교가 성립한다.

**감축단가 차이의 성격**: FIN 115가 EFF의 실행가능 후보 분포(26.6~155.9) **안에 있다**.
두 모형이 서로 다른 값을 내는 것이 아니라 **다른 계획을 고르는 것**이다 — FIN은 예산 제약
하 비용최소, EFF는 강건성 선별. NSC(164)는 EFF 상단(152.3)을 약간 넘는다.

**설명되는 구조 요인 5가지**: 배출 경계(FIN S1 vs EFF S1+2), 기술 집합(FIN은 BF→EAF 전면
전환 불허 = 값싼 스크랩 EAF가 선택지에 없음), 탄소비용 처리(FIN ②는 자원비용 기준으로
탄소지출을 분리), 설비 해상도(시설 vs 블록), 시나리오 정의(EFF는 GCAM 정합을 주장하지 않음).

**결함 1건 자체 발견**: 첫 실행에서 EFF 후보를 실행가능 필터 없이 최소값으로 골라
POSCO 감축단가가 **−5.7**로 나왔다. FIN이 예산 실행가능 집합에서 고르는 것과 선택 규칙이
어긋나 이상치를 집은 것. 예산·시나리오·물리·자원 제약 전부를 걸어 217→96개로 좁힌 뒤
재비교.

**아직 못 한 것**: 각 구조 요인이 차이의 몇 %를 설명하는지 정량 분해. 한쪽 모형에서 요인을
하나씩 상대 정의로 바꿔 재실행해야 한다. 현재는 **방향 일치만** 확인 가능하고 수준 일치는
주장하지 않는다.

**MCP `get_validation_summary` missing = 0** — 계획한 검증 4종(데이터 감사·후향·외부·교차)이
전부 존재하는 상태가 됐다.

## Cycle 17 (자체 결함 — `--replan`이 실산출물을 파괴했다)

**무슨 일**: I1 할인율 강건성을 위해 `run_scenarios.py --replan disc35 disc65`를 돌렸다.
`_link_shared(dst, ["e1"])`은 e1만 링크하고 **이전 실행이 남긴 `<bundle>/e2 → out/e2`
심볼릭 링크는 그대로 뒀다**. 그 상태에서 E2가 `out_dir=<bundle>` 로 돌면서
`shutil.rmtree(<bundle>/e2/plans)`가 링크를 따라가 **공유 `out/e2/plans`를 지우고**
disc35의 계획을 그 자리에 썼다.

**어떻게 드러났나**: `build_site.py` → `build_report.py`가 `plan_P0034.csv` 없음으로
실패. 산출물 정합이 깨졌다는 신호였다.

**근본 수정**: `_link_shared`가 전 단계를 순회해 **공유 목록에 없는 단계의 낡은 링크를
반드시 제거**한다. 링크를 남기면 원본에 쓰게 된다는 것이 이 결함의 전부였다.

**회귀 테스트 2개**
- `test_scenario_bundles_never_write_into_shared_out` — 묶음이 직접 쓰는 e3/e4/e5가
  공유 `out/`을 링크하고 있지 않은가.
- `test_production_plan_index_matches_plan_files` — `plan_index.csv`의 계획 파일이 전부
  실재하는가. **이번 손상을 정확히 검출**했다(추가 즉시 red).

**영향 범위**: 커밋된 C12~C16의 수치는 손상 이전 정상 실행본에서 나온 것이라 유효하다.
파괴된 것은 `out/` 작업본뿐이며 재실행으로 복구했다(`out/scenarios` 전체 삭제 후
`python -m cap all`).

**교훈**: 심볼릭 링크로 산출물을 공유하는 설계는 "쓰기 단계에서 링크를 지운다"가
불변식이다. 공유는 읽기 전용일 때만 안전하다. 시나리오 러너가 원본 `out/`을 절대
건드리지 않는다는 것이 이제 테스트로 고정됐다.

## Cycle 18 (I1 할인율 강건성 — 계획 재탐색 포함)

`run_scenarios.py --replan disc35 disc65` — E2 MILP까지 다시 풀어 **계획 선택 채널을
포함한** 강건성. 묶음당 약 22분.

| 묶음 | POSCO | NSC | MCI | LOTTE | 순위 |
|---|---|---|---|---|---|
| base (5.0%) | 115 / 20.6조 / 피크 2041 | 165 / 31.7조 / 2036 | 237 | 275 | POSCO < NSC < MCI < LOTTE |
| disc35 (3.5%) | 116 / 20.7조 / **2036** | 164 / 31.7조 / **2040** | 226 | 260 | **동일** |
| disc65 (6.5%) | 114 / 20.6조 / 2041 | 161 / **29.9조** / 2039 | 240 | 277 | **동일** |
*(감축단가 천원/tCO₂)*

**결론 불변**: 세 할인율 전부에서 감축단가 순위가 같다. 어느 기업이 싸고 어느 기업이
무거운가는 자본비용 선택에 의존하지 않는다.

**바뀌는 것은 시점과 규모**: POSCO 피크가 3.5%에서 **2041 → 2036으로 5년 당겨진다**
(자본이 싸면 조기 투자가 유리). NSC는 6.5%에서 총 CAPEX가 31.7조 → **29.9조**로 줄어든다
(자본이 비싸면 프로그램 축소). TCaR은 POSCO 기준 36.3조(3.5%) ~ 20.3조(6.5%)로 **±34%**
움직인다 — 할인율은 순위가 아니라 **자본 계획의 모양**을 정한다.

**격리 확인**: 이번 실행은 C17에서 실산출물을 파괴했던 것과 동일한 명령이다. 수정 후
`out/e2/plans` 52개 그대로, 일관성 테스트 15/15 그린 — 회귀 없음이 실증됐다.

**한계**: I1의 세 값은 전부 `--replan`이지만, 다른 8개 묶음은 계획 메뉴 고정 평가다.
화면과 표에 `⟳` 표시로 구분한다.

## Cycle 19 (NSC 편차의 원인 특정 — 가설 하나를 계산으로 배제)

C14가 남긴 NSC +15.7%의 자연스러운 설명은 "설비 목록에 전기로가 빠져서"였다. 웹에서
NSC 전기로 현황을 수집하고 **그 가설을 배제했다**.

**수집**: 히로하타 720,000 t/yr 전기로(고급 전기강판, 2022-10 가동 중). 야하타 2.0Mt·
히로하타 0.5Mt·슈난 0.4Mt = 총 2.9Mt는 **FY2029 가동 예정**이고 이미 D7 공시계획에 있다
(모형은 신설 경로로 미해석 — A-10).

**계산으로 배제**

| 가정 | 능력가중 EF | 공시(1.856) 대비 |
|---|---|---|
| 현행 (고로 10기, 44.3 Mt) | 2.150 | +15.8% |
| 히로하타 EAF 0.72 Mt 추가 | 2.123 | +14.4% |
| FY2029 신설분 2.9 Mt까지 추가 | 2.046 | +10.2% |
| 가상 4.5 Mt까지 | 1.993 | +7.4% |

가동 중인 전기로를 전부 넣어도 **15.7%p 중 1.3%p만 닫힌다**. 능력 추정을 줄이는 것은 EF를
전혀 못 바꾼다(전부 고로라 가중이 그대로).

**결론**: 원인은 설비 목록이 아니라 **`BF = 2.15 tCO₂/t` 루트 표준값 자체**다. POSCO에서
잘 맞는 이유는 POSCO 가중에 FINEX(2.05)·EAF(0.45)가 섞여 2.03으로 내려가기 때문이지 값이
맞아서가 아니다. **작업 방향이 G3(능력 정합)에서 G2(원단위 승급)·G1(시설 실측)으로
바뀐다** — A-03의 결론 영향을 '중'에서 '대'로 격상.

**하지 않은 것**: 히로하타 0.72Mt를 D1a에 넣는 것. 편차를 1.3%p밖에 못 닫으면서 "고쳤다"는
인상만 주기 때문이다. 실측 배출(G1) 없이는 이 편차를 닫을 수 없다.

## Cycle 20 (정정 — C14·C19의 후향 검증 해석이 틀렸다)

**무엇이 틀렸나.** C14가 "NSC 배출 원단위가 실적보다 15.7% 높다 → 모형이 기존 설비를 더
더럽다고 보므로 ②를 과소평가한다"고 결론냈고 C19가 그 원인을 루트 표준값으로 특정했다.
**둘 다 결과 편향으로 오독한 것이다.**

`prepare_raw`의 철강 분기는 시설 배출을 **회사 공시 총량에 맞춰 재척도**한다
(`s1 = s1_total × w_f / Σw`). 따라서 루트 표준값은 *수준*이 아니라 *배분 가중치*로만 쓰인다.
모형이 실제로 쓰는 값을 직접 확인했다:

| 기업 | 모형이 쓰는 가중 `ef_inc` | 공시 원단위 | 차이 |
|---|---|---|---|
| POSCO | 1.987 | 1.988 | **−0.0%** |
| NSC | 1.846 | 1.856 | **−0.6%** |
| LOTTE·MCI | 0.950 (주입값 그대로) | 생산량 미공시 | **대조 불가** |

**NSC의 ②(165천원/tCO₂)는 과소평가돼 있지 않다.** 게다가 NSC 설비 목록이 고로 단일
구성이라 가중치가 균일해져 **표준값이 무엇이든 배분이 능력 비례로 같다** — C19가 계산한
"전기로를 넣어도 1.3%p만 닫힌다"는 사실 자체는 맞지만, 애초에 닫아야 할 편향이 아니었다.

**정정 후 진짜 빈 곳**: **석유화학**이다. 상향식이라 재척도가 없어 주입값 0.95가 곧 수준인데,
LOTTE·MCI 모두 생산량을 공시하지 않아 확인할 방법이 없다. 커버리지도 25~35%다.
A-03의 결론 영향을 **철강 소 / 석화 대**로 분리 기재했고, A-02는 "철강은 총량이 맞고
시설 간 분포가 미검증, 석화는 총량부터 미검증"으로 고쳤다.

**교훈**: 검증 스크립트가 무엇을 재는지는 준비 단계의 변환 규칙에 달려 있다. 재척도가
있는 파이프라인에서 "입력 표준값 vs 공시"를 비교하면 결과와 무관한 양을 재게 된다.
문서 첫머리에 **무엇이 검증 대상이 아닌지**를 먼저 적도록 바꿨다.

## Cycle 21 (석유화학 배출 수준 — 공시로 역산해 불일치 정량화)

C20이 "석화가 진짜 빈 곳"이라고 판정한 뒤, C15에서 확보한 미쓰이 오사카 공시를 **감축률이
아니라 절대량**으로 다시 읽었다.

| 항목 | 값 |
|---|---|
| 모형: 오사카 크래커 능력 0.455 Mt × 가동률 0.9 | 생산 0.409 Mt |
| 모형: 오사카 크래커 배출 (EF 0.95) | **0.389 MtCO₂** |
| 공시: 오사카공장 Scope1+2 | 1.60 MtCO₂ |
| 공시: 암모니아 연료전환 감축 전망 | **0.70 MtCO₂** |

**공시된 감축량(0.70)이 우리가 모형에 넣은 크래커 배출 총량(0.389)보다 크다.** 감축분 중
크래커 몫을 100%로 보면 크래커 감축 원단위가 1.71 tCO₂/t로 우리 EF 전체의 1.8배, 70%로 봐도
1.3배다. 50%까지 내려야 0.85로 우리 값과 만난다.

**가르지 못한 세 설명**: (a) NCC EF 0.95가 낮다 (b) 크래커 능력 0.455 Mt가 낮다(에틸렌 기준
vs 총 처리량) (c) 공시 감축량이 크래커 밖(공장 유틸리티·다운스트림 가열로)을 포함한다.

**결론 방향**: (a)라면 석화 감축량을 과소평가하고 **② 237~275천원/tCO₂를 과대평가**하고 있다.
(b)·(c)를 배제할 자료가 없어 **값은 고치지 않고 불일치를 기록만 했다** — 세 설명 중 어느
쪽이든 "석화 배출 입력이 검증되지 않았다"는 판정을 강화한다. METHODOLOGY §9-1에 반영.

**남은 작업(우선순위 순)**: MCI 오사카 크래커의 공표 처리능력·에틸렌 능력 구분, 오사카공장
배출의 설비별 분해(일본 SHK 사업소별 공시), LOTTE 여수·대산 NCC 능력·배출 1차 공시.

## Cycle 22 (능력 가설 배제 — 1차 자료로)

C21이 남긴 세 설명 중 (b) "크래커 능력이 낮다"를 **1차 자료로 배제**했다.

석유화학공업협회(JPCA) 게재 **경제산업성 조사(2024-12-31)**: 大阪石油化学(미쓰이 100%)
**455,000 t에틸렌/yr**, 三井化学 **553,000 t/yr**. 우리 D1a의 `MCI_OSK_CR` 0.455 Mt,
`MCI_ICH_CR` 0.553 Mt와 **정확히 일치**한다. 능력 추정 오류가 아니다.

- `JPCA_CAPACITY_2024` 출처 등록, MCI 2기의 `source_id`에 병기 (T5 자체추정 → 정부조사 확인)
- 남은 설명 둘: (a) NCC EF 0.95가 낮다 (b) 공시 감축량이 크래커 밖을 포함한다
- **문헌 대역을 인용하지 않았다** — "NCC는 보통 1.0~1.5"는 알고 있어도 등록된 출처가
  없으면 이 프로젝트 규칙상 쓸 수 없다. 그 사실을 문서에 명시했다.

**다음에 필요한 자료(구체)**: 오사카공장 배출의 설비별 분해, 일본 SHK 사업소별 공시,
NCC 배출원단위 문헌값(추출·등록 필요), LOTTE 여수·대산 NCC 1차 공시.

## Cycle 23 (NCC 원단위 — 문헌 앵커 수집 실패를 실패로 기록)

C21의 남은 두 설명 중 (a) "우리 NCC EF가 낮다"를 문헌으로 가르려 했으나 **인용 가능한
형태의 자료를 확보하지 못했다.**

**찾은 것**: 한 업계지가 재래식 스팀크래커를 **0.63 tCO₂e/t HVC**(Scope 1+2)로 제시.
**쓰지 않은 이유 둘** — (1) 출처 표기가 없다 (2) 분모가 에틸렌이 아니라 HVC다. HVC→에틸렌
환산 계수를 우리가 임의로 곱하면 **없는 근거를 만드는 것**이므로 중단했다.

**대신 공시 구조만으로 판정을 좁혔다**: 0.70 Mt는 공장 전체 1.60 Mt의 **44%**다.
오사카공장은 크래커 외에 폴리프로필렌·페놀 유도품 설비를 함께 두고 있고(공시 본문),
공장 전체 감축의 44%를 크래커 한 기에 전부 귀속시키는 것은 공시가 말하는 범위("암모니아를
연료로" = 공장 가열로 전반)와 맞지 않는다. **분모가 다른 두 수를 비교했을 가능성이 높다** —
외부 수치 없이 공시 자체의 구조에서 나오는 추론이다.

**남은 상태**: (b) 능력 = 배제(경산성 조사), (c) 범위 불일치 = 유력하나 확정 불가(설비별
분해 미공시), (a) EF 과소 = 배제 못 함. ②의 방향 편향 가능성은 문서에 남는다.
**필요 자료 명시**: 에틸렌 기준·출처 있는 NCC 배출원단위(IEA ISTR·Material Economics·MPP에서
추출·등록), 오사카공장 설비별 배출 분해, 일본 SHK 사업소별 공시.

## Cycle 24 (석화 상한 검증 — "맞다"는 못 해도 "틀리지 않았다"는 할 수 있다)

석화는 생산량 미공시라 원단위 대조가 불가능하다. 그러나 **상한은 검증된다**: 크래커 한 기의
배출이 그 사업장 전체 공시 배출을 넘으면 명백한 오류다. 롯데케미칼은 사업장별 배출을
공시하므로(원자료에 이미 있었다) 이 검증이 가능했다.

| 사업장 (2024) | 모형 크래커 배출 | 공시 사업장 배출 | 크래커 비중 | 판정 |
|---|---|---|---|---|
| 여수1 | 1.05 MtCO₂ | 1.89 | **56%** | 정합 |
| 대산 | 0.94 MtCO₂ | 1.53 | **61%** | 정합 |

상한 위반 없음. NCC 중심 사업장에서 나프타 분해로가 최대 배출원인 것은 자연스러워 비중도
부자연스럽지 않다. **이것은 '틀리지 않았다'는 확인이지 '맞다'는 확인이 아니며**, 그렇게
적었다. 미쓰이는 사업장별 배출 공시가 없어 같은 검증조차 못 한다.

**석화 검증 현황 정리**: 감축률 1건 확인(C15), 능력 1차 자료 확인(C22), 배출 상한 확인(C24),
원단위 수준 **미검증**, CAPEX **대조 불가**. C14가 "어떤 외부 검증도 통과하지 않았다"고 한
상태에서 세 항목이 채워졌고, 채워지지 않은 두 항목이 무엇인지 명확해졌다.

## Cycle 25 (F3 일부 — 기술별 설비비 불확실성 연결 + 수렴 상향)

**미사용 컬럼 하나를 소비**: `D3.capex_uncertainty`(30~60%, 13개 기술 전부 채워져 있었으나
엔진이 안 읽었다). E3의 설비비 충격은 전 기술 공통 하나였다. 이제 기술별 진폭 배율
(`capex_uncertainty / 40`)을 공통 충격에 곱한다 — 수소환원 플랜트와 폐열회수 리트로핏이
같은 공사비 위험을 지지 않는다는 것을 데이터가 말하고 있었다.

**수준이 아니라 상대 분산으로만 쓴다**: 스펙이 이 컬럼의 구간 규약(±X%가 90% 구간인지 전폭인지)을
정의하지 않으므로, 그 규약을 우리가 정하면 없는 근거를 만드는 것이다. 캘리브레이션된 공통
변동성이 **수준**을 담당하고 이 컬럼은 **기술 간 상대 위험**만 담당한다. A-22로 등재.

**수렴 검사 실패 → 스펙 대응 적용**: 변경 후 n=5,000에서 P50/TCaR 수렴 이탈이 2.64%로
기준(1%)을 넘었다. 스펙이 정한 대응대로 `n_sims: 10000`으로 올렸고 이탈 **0.55%**로 복귀.

**헤드라인 전후** (NZ15, none, 천원/tCO₂): POSCO 114.9→115.0, NSC 165.3→165.4,
MCI 237.2→241.7, LOTTE 274.9→279.2. 석화가 1.6~1.9% 오른 것은 표본 확대분이고 순위 불변.
설비비는 분산의 약 1%라 기술별 배율의 효과 자체는 작다 — **작다는 것을 측정해서 안 것**이
결과다.

**검증**: 테스트 29/29, 시나리오 11묶음 재실행, 데이터 패키지·검증 문서·사이트 전부 재생성.

## Cycle 26 (석화 배출계수 — 답이 미사용 컬럼 안에 있었다)

C21~C23이 "우리 NCC EF 0.95가 낮은가"를 웹에서 풀려다 실패했다. **답은 우리 데이터
안에 있었다** — `D7.quote`는 감사가 계속 UNUSED로 표시하던 컬럼이라 아무도 읽지 않고 있었다.

**미쓰이 에틸렌 설비 집약 공시**(D7 `timing` 행, `MCI_RELEASES`): AMEC 미즈시마를 정지하고
大阪石油化学(OPC)로 집약 — 統合前 95.1万t → 統合後 45.5万t, **CO₂ ▲50.6万t/年**.
45.5万t이 JPCA 게재 OPC 에틸렌 능력 455,000 t/yr과 정확히 같아 이 수치들이 **능력**임이
교차 확인된다.

| 계산 | 값 |
|---|---|
| 정지 에틸렌 능력 | 951,000 − 455,000 = 496,000 t/yr |
| 공시 CO₂ 감축 | 506,000 tCO₂/yr |
| **함의 배출원단위** | **1.020 tCO₂/t-에틸렌** |
| 우리 주입값 0.95 | **−6.9%** |

**석유화학 배출 원단위가 처음으로 1차 공시에 대조됐고 7% 안에서 맞는다.**

**C21의 0.70 Mt 수수께끼도 풀린다**: 같은 원단위로 OPC 크래커 총배출은 0.464 Mt인데
연료전환만으로 0.70 Mt를 줄인다는 것은 총배출보다 크다. 두 공시를 정합하게 만드는 유일한
읽기는 **0.70 Mt의 경계가 OPC 크래커보다 넓다**는 것이다. 설명 (c) 지지, **(a) 배제** —
우리 EF가 낮은 것이 아니었다. METHODOLOGY §9-1의 "② 과대평가 가능" 경고를 철회했다.

**교훈**: 감사가 UNUSED로 표시한 컬럼은 "안 써도 되는 컬럼"이 아니라 **아직 안 읽은
증거**다. 웹 수집 3사이클로 못 푼 것을 이미 가진 데이터가 풀었다.
**남은 미검증**: 석화 생산량 시계열(가동률 0.9 가정), NCC 원단위의 독립 문헌 범위.

## Cycle 27 ("측정 불가"의 사유를 구분 — 공시 부실이 아니라 모형 경계였다)

**발견**: POSCO의 공시 커밋(광양 전기로, `resolution=high`)이 gap 미산출로 처리되고 있었고,
E2 로그는 그 이유를 "resolution too low to locate the plan"이라고 말했다. **틀린 설명이다.**
진짜 이유는 우리 모형이 **BF→EAF 전면 전환을 불허(A-10)**해 `steel_eaf`가 어떤 설비 유형에도
적용되지 않기 때문이다. LOTTE도 마찬가지로 공시 수단이 CCUS인데 **우리가 CCUS를 D3에서
제외**했다.

즉 이사회 메모가 "경계까지 거리: 측정 불가"라고 쓰는 두 기업은 **공시를 부실하게 한 것이
아니라 우리 모형이 그 회사가 실제로 하고 있는 일을 모형 밖에 둔 것**이다. 그대로 두면
기업 책임으로 오독된다.

**조치**
- `_disclosed_fixed`가 커밋을 버릴 때 **사유를 구분해 수집**하고, E2가
  `out/e2/disclosed_skipped.csv`로 내보낸다.
- 이사회 메모의 "측정 불가"에 각주가 붙어 사유를 그대로 표시한다.
- A-16을 "사유를 구분해 기록한다"로 갱신, METHODOLOGY 한계절에 §9-6으로 신설.

**현재 사유 표**

| 기업 | 사유 |
|---|---|
| POSCO | `POSCO_GWY_EAF1/steel_eaf`: 모형 규칙상 적용 불가(A-10) — 공시 해상도 문제 아님 |
| LOTTE | `LOTTE_DAE_NCC/petchem_ccus`: 기술이 D3에 없음(CCUS 제외) |

**검증**: 전 파이프라인 재실행(E2 20분), 시나리오 11묶음, 테스트 29/29, 사이트·패키지·
검증문서 재생성. 헤드라인 불변.

## Cycle 28 (같은 계열의 결함 하나 더 — 평가 전용 실행이 재계획본을 덮어썼다)

C25에서 파이프라인을 재실행한 뒤 `run_scenarios.py`를 인자 없이 돌렸다. 그 실행이
**C18의 I1 재계획 결과(disc35·disc65, 각 22분 계산)를 평가 전용 결과로 조용히 덮어썼다.**
`summary.csv`의 `replanned`가 전 묶음 False가 되면서 시나리오 화면의 `⟳` 표시도 사라졌고,
**I1 강건성 결과가 산출물에서 재현 불가능해졌다** — 로그에는 남아 있으나 아티팩트에는 없다.

C17(심볼릭 링크가 실산출물 파괴)과 같은 계열이다: **비싼 계산 결과를 값싼 실행이 덮어쓰는
경로**가 열려 있었다.

**수정**: `run_scenarios.py`가 인자 없이 실행될 때 `replanned=True`인 묶음을 **보존하고
건너뛴다**. 덮어쓰려면 `--force` 또는 `--replan`을 명시해야 한다. 건너뛴 사실은 로그에 찍힌다.

**복구**: `--replan disc35 disc65` 재실행 중.

**교훈 반복**: 산출물 파이프라인에서 "덮어쓰기"는 기본값이면 안 된다. 계산 비용이 다른
결과가 한 파일에 섞일 때는 값싼 쪽이 비싼 쪽을 이길 수 없게 막아야 한다.

## Cycle 29 (이사회 메모에 "무엇을 언제" 추가 — 공개 경계 안에서)

메모에 빠져 있던 것은 **의사결정에 가장 필요한 정보**였다: 얼마 드는지·감당되는지는 있는데
**무엇을 언제 하는지**가 없었다. 보고서에는 시설 단위 간트가 있지만 그것은 §8-2 비공개
등급이라(보고서에도 CONFIDENTIAL 배너가 붙어 있다) 메모에 옮길 수 없다.

**해법**: 시설 식별자를 빼고 **기술 단위로 집계**하면 공개 가능하고, 자본계획에 필요한
해상도는 유지된다 — "몇 기를 언제 착수하는가".

| 기업 | 비용최소 계획 (NZ15) |
|---|---|
| POSCO | 설비 폐쇄 2기 2026–30 · 에너지효율 1기 2027 · **수소환원 4기 2033–41** · HyREX 1기 2041 |
| NSC | 설비 폐쇄 2기 2026–31 · **수소환원 8기 2031–44** |
| LOTTE | 연료 전환 2기 2041 |
| MCI | 연료 전환 2기 2040–41 |

A4 한 장 안에 유지(1123px). 폐쇄는 빨강으로 구분해 "전환"과 "축소"가 섞이지 않게 했다.

**읽히는 것**: 철강 2사는 2026~31년에 **폐쇄부터** 하고 수소환원은 2030년대 중반 이후다 —
초기 감축은 전환이 아니라 축소로 온다. 석화 2사는 2040년대에 단 한 번의 연료전환뿐이다.

## Cycle 30 (감사의 신호 정리 — 설명된 공백과 설명 안 된 공백을 가른다)

감사가 매 실행 21개 컬럼을 UNUSED/PARTIAL/CONSTANT/EMPTY로 찍어왔다. 그중 상당수는
**설계상 그게 맞는 것**이었고, 사유가 코드에 없으니 같은 경고가 반복되며 진짜 공백을 덮고
있었다. `EXPECTED_GAP`에 **컬럼별 사유를 명시**하고 판정을 `설계상 정상`으로 분리했다.

**설계상 정상으로 분류한 것 16개** (사유는 `docs/data_audit.md`에 표로 나간다)
- `D1b.energy_naphtha` — 석화 원료비는 물량×가격이 아니라 **에틸렌-납사 스프레드 마진**으로
  들어오고, 전환이 원료를 바꾸지 않는 한 증분비용에서 상쇄된다. (다만 원료가격 변동이
  TCaR에 안 잡히는 것은 한계로 남긴다.)
- `D1b.emissions_s2` — 보존하되 지표 경계는 Scope 1 (A-21)
- `D1a.commissioning_year`·`D5.param_type` — **준비 단계에서 소비**되고 엔진이 직접 안 읽을 뿐
- `D5.support_scenario/tech_id` CONSTANT — 확정된 직접 지원이 `current` 하나뿐이고 전부 업종
  전반 적용이라는 뜻. **상수인 것 자체가 발견**
- `D7.facility_id/tech_id` 67% — `target` 행은 회사 전체 목표라 시설이 없는 것이 정상
- `D7.quote` — 엔진 미사용이지만 **C26에서 NCC 배출계수를 역산한 근거**가 여기 있었다
- `D6.total_debt/cash` — 쓰이는 `net_debt`의 구성요소, `interest_expense` — 22행 중 2행만
  공시라 지표 불가(**공시 부재를 기록으로 남긴다**)

**남은 진짜 공백 4개** — 이제 이것만 본다.

| 컬럼 | 채움 | 무엇이 없나 |
|---|---|---|
| `D2b.value` | 99.1% | Korea `h2_price` 2025 셀 (E1이 경고 후 평탄 외삽) |
| `D6.revenue` | 95.5% | LOTTE 2021 |
| `D6.capex_total` | 50.0% | POSCO·LOTTE 미공시 |
| `D6.net_debt` | 40.9% | POSCO·LOTTE 미공시 |

**교훈**: 경고가 21개면 아무도 안 본다. 4개면 본다. 감사의 가치는 잡아내는 개수가 아니라
**남기는 것이 전부 조치 대상인가**에 있다.

## Cycle 31 (POSCO 레버리지 — DART까지 갔다가 넣지 않기로 한 이유)

감사가 남긴 진짜 공백 4개 중 `D6.net_debt`(POSCO·LOTTE)을 DART로 채우려 했다. **채우지
않았다.**

**확인한 것** (DART 1차 공시, 2024 사업보고서)
- POSCO홀딩스 **연결**: 차입금 11.12조 + 장기차입금 14.88조 − 현금 6.77조 = **순차입금 약 19.2조**
- POSCO홀딩스 **별도**: 순수지주 형태(자산 50.6조 중 45.6조가 종속기업투자), 차입금 0.04조

**넣지 않은 이유 — 경계 불일치**: D6의 POSCO 행은 **철강 사업회사 별도**다(매출 37.6조,
2024). 홀딩스 연결 매출은 약 72조로 다른 실체다. 지주 연결 부채를 사업회사 EBITDA로
나누면 **분자와 분모의 실체가 다른 비율**이 나온다. 이 도구로는 철강 사업회사(비상장,
별도 공시)를 특정하지 못했다.

**조치**: 값 대신 **사유를 기록**했다 — `_affordability` 독스트링, 감사 `EXPECTED_GAP`,
이사회 메모 유의사항 3번. "미공시"가 아니라 "**경계가 맞지 않아 비웠다**"로 쓴다.
전자는 기업 탓이고 후자는 우리 상태다.

**남은 진짜 공백 4 → 4** (줄지 않았다). 줄이려면 철강 사업회사 별도 재무제표를 특정해야
한다 — 다음 수집 항목으로 명시.

## Cycle 32 (I1 복구 완료 + 보호장치 작동 확인)

C28에서 잃은 I1 재계획 결과를 복구했고, **보호장치가 실제로 작동하는 것을 확인**했다:
복구 후 `run_scenarios.py`를 인자 없이 돌리자
`[scenario] 재계획본 보존: ['disc35', 'disc65'] 건너뜀 (덮어쓰려면 --force 또는 --replan)`
— 같은 사고가 다시 나지 않는다.

**I1 최종 (계획 재탐색 포함, NZ15·지원없음)**

| 할인율 | POSCO ② / 피크 / TCaR | NSC ② / CAPEX / 피크 | 순위 |
|---|---|---|---|
| 3.5% | 116.0 / **2036** / 35.9조 | 165.3 / 31.7조 / 2036 | POSCO < NSC < MCI < LOTTE |
| 5.0% | 115.0 / 2041 / 26.8조 | 165.4 / 31.7조 / 2036 | **동일** |
| 6.5% | 114.1 / 2041 / 20.2조 | 161.3 / **29.9조** / 2037 | **동일** |

**순위 불변, 자본계획의 모양이 바뀐다** — 자본이 싸면 POSCO 피크가 2041→2036으로 5년
당겨지고, 비싸면 NSC 프로그램이 31.7조→29.9조로 줄고, TCaR은 POSCO 기준 35.9조~20.2조로
**±34%** 움직인다. 할인율은 "누가 싼가"가 아니라 "언제 얼마나"를 정한다.

**검증**: 테스트 29/29, 시나리오 화면 `⟳` 표시 복원.

## Cycle 33 (I4 표본 안정성 — 보고한 자릿수가 실제인지 처음 쟀다)

`scripts/seed_stability.py` — 시드 5개로 E3–E5만 다시 돌려 헤드라인의 변동계수(CV)를 잰다.
E1·E2는 시드와 무관하므로 공유하되, **공유 로직은 `run_scenarios._link_shared`를 그대로
재사용**했다(C17에서 실산출물을 파괴한 바로 그 로직 — 두 번 구현하지 않는다). 실행 후
`out/e2/plans` 52개 무결 확인.

| 지표 | CV | 판정 |
|---|---|---|
| ② 감축단가 | 0.26~0.81% | 안정 — 표기 자릿수 유효 |
| ② P50 | 0.26~0.81% | 안정 |
| ③ TCaR | 1.08~1.75% | 주의 — **두 자리까지만** |
| ⑤ 유연성 | **10.6~14.8%** | **불안정** |

**⑤가 못 쓸 수준이었다.** 원인은 재최적화 부표본 `n_sims_flex: 300`. 1,500으로 올리자
9.2%까지 내려갔고(√5 개선과 일치) 비용은 e4 1.2초로 변화 없다. 3% 밑으로 내리려면 15,000이
필요한데, **⑤는 정의상 하한값**이라 그만한 계산을 태울 값이 없다.

**그래서 정밀도를 쫓는 대신 표기를 고쳤다**: 보고서 지표표의 ⑤를 소수점 없이 표기하고,
측정된 CV를 품질 주석에 넣었다("②는 표기 자릿수 유효, ③은 두 자리, ⑤는 자릿수 하나").
METHODOLOGY §9-1 신설, A-23 등재.

**이 검증이 재지 않는 것**을 문서에 명시: 계획 선택(E2)의 안정성은 시드와 무관하고,
시드 안정성은 **정밀도이지 정확도가 아니다** — 모든 시드가 같은 잘못된 사전 변동성(A-17)을
쓰면 결과는 일관되게 틀린다.

## Cycle 34 (I2 구조 대안 — 오늘 나온 가장 정책적인 발견)

`scripts/robustness_structural.py` — 파라미터도 할인율도 아니고 **모형이 무엇을 '최적'이라
부르는지**를 흔든다. 위험중립(P50 최소) vs 위험회피(P90 최소)로 같은 계획 집합에서 재선택.

**1) 순위 불변**: P90 기준에서도 POSCO < NSC < MCI < LOTTE.

**2) 꼬리 두께가 섹터별로 다르다**

| 기업 | P50 기준 | P90 기준 | 꼬리 배수 |
|---|---|---|---|
| POSCO | 115 | 170 | ×1.5 |
| NSC | 165 | 235 | ×1.4 |
| MCI | 242 | 911 | **×3.8** |
| LOTTE | 279 | 1,085 | **×3.9** |

**석화의 문제는 비용 수준이 아니라 비용의 분산**이다. 기대비용으로는 철강의 1.7배인데
나쁜 경우로는 격차가 훨씬 벌어진다.

**3) 기저 계획은 그대로, 계약만 바뀐다** — 위험 태도는 *무엇을 지을지*가 아니라 *어떻게
조달할지*를 바꾼다. 설계서 주장 **P2가 산출물에서 성립**한다.

**4) 그런데 석화가 고른 헤지는 자기 위험을 전혀 안 덮는다** — 오늘 가장 중요한 발견

| 기업 | 꼬리위험 (전력/수소/설비비) | P90이 고른 헤지 | 덮는 분산 |
|---|---|---|---|
| POSCO | 47% / 52% / 1.1% | PPA 75% | 47% |
| NSC | 64% / 35% / 1.4% | PPA 75% | 64% |
| MCI | 0% / **100%** / 0.1% | EPC | **0%** |
| LOTTE | 0% / **100%** / 0.0% | EPC | **0%** |

석화는 꼬리위험의 **100%가 수소 가격**인데 위험회피 기준이 고르는 헤지는 EPC(분산 기여
0.1%)다. 모형이 나빠서가 아니라 **계약 수단 집합에 수소 헤지가 없기 때문**이다 — PPA는
전력만 고정하고 수소는 `no power hedge on H2`로 시장에 그대로 노출된다. 위험회피
의사결정자는 **쓸 수 있는 유일한 헤지를 사되 자기 위험은 그대로 안고 간다.**

**이것은 모형 한계이자 그 자체로 결과다**: 석유화학 전환의 위험관리는 현재 수단 집합으로
불가능하다. 수소 장기 공급계약(오프테이크·가격 고정)이 수단으로 들어오기 전까지 석화의
TCaR은 줄일 방법이 없다. METHODOLOGY의 반증가능 주장 P2에 이 단서를 붙였다.

---

## 정기 점검 (30분 간격, cron `83ca3d8c`)

C34까지 연속 사이클로 진행했으나 **시간 기반 스케줄러가 없었다** — 지시의 핵심 메커니즘이
빠져 있었고 21:35에 지적받아 등록했다.

**정직하게 적을 것 두 가지**
1. **오늘(2026-08-09)의 19:00 창은 복구 불가능하다.** 11:00~21:35 동안 연속으로 25사이클
   (C10~C34)을 돌려 예정된 16회보다 많은 작업을 했지만, **요구된 메커니즘(시간 기반 반복)이
   아니었다.** 결과물의 양이 절차의 부재를 대신하지 못한다.
2. 첫 등록(`83ca3d8c`)은 `*/30 * * * *`로 **19:00 종료 조건이 없었다** — 지시의 "저녁 7시까지"를
   구현하지 않은 것이다. 창으로 묶어 교체했다.

**현재 스케줄**
- `567a4ca8` — `*/30 0-18 * * *` (00:00~18:30, 30분 간격)
- `c3e08807` — 2026-08-10 19:00 일회성: 마감 사이클 실행 후 창 요약 기록, `567a4ca8` 삭제로 종료

즉 다음 창(00:00~19:00)은 **끝에서 끝까지 스케줄로 덮인다**. 오늘 21:40~24:00 사이에는
아무것도 발화하지 않는다 — 창 밖이기 때문이다.

이후 사이클은 아래에 한 줄씩 누적한다. 변화가 있을 때만 별도 절을 만든다.

| 시각 | ① 데이터 (ok/설계정상/공백) | ② 순위 | ③ gap 산출 | ④ MCP·테스트·푸쉬 | ⑤ 산출물 | 발견 |
|---|---|---|---|---|---|---|
| 21:40 | 68/16/4, 미해소 출처 0 | 11묶음 전부 동일 | 2/4사 | 11도구·missing[]·클린 | 7종 | 없음 |

## G1 수집 시도 — 막힌 지점을 기록 (스케줄 창 밖, 실질 진행)

민감도 1위 `fac.ef_inc`(T5, 배분값)를 실측으로 올리려 한국 사업장별 배출을 찾았다. **못 채웠고,
그 사실을 `docs/data_gap_registry.md`에 경로별로 기록했다.**

| 시도 | 결과 |
|---|---|
| 공공데이터포털 배출권거래제 데이터셋 | 기업 단위까지만 (업체 명단·업종·주소) |
| KIER 할당대상업체 배출량 분석 | 688개 업체 각 한 줄. POSCO 78.5 MtCO₂(2021) — **포항·광양 분리 없음** |
| 법정 명세서 서식(별지 제11호) | **사업장별 배출량이 법정 기재 항목임을 확인** — 데이터는 NGMS에 존재하나 공개 발행 안 됨 |

**대체 경로**: ① 기업 지속가능경영보고서의 제철소별 표 ② 정보공개청구(온실가스종합정보센터)
③ 지자체 대기오염물질 공시와의 규모 비율 교차. 일본(온대법 사업소별)은 미시도 — 다음 첫 항목.

**부수 확인 (값 있음)**: POSCO 2021 Scope1이 우리 **77.1** MtCO₂ vs KIER **78.5** MtCO₂로
**−1.8%**. 경계가 다른데도(우리=철강 별도, KIER=법인 전체) 근접한다. **회사 단위 앵커는
독립 출처와 정합**하며, 문제는 총량이 아니라 **총량을 시설에 나누는 규칙**임이 재확인됐다
— H2 후향 검증과 같은 방향. METHODOLOGY §9-2에 반영.

`docs/data_gap_registry.md` 신설: 진짜 공백 4개 + 문헌 앵커 미확보 2건까지 한 곳에 모아
"아직 없다"를 "찾아봤고 여기서 막혔다"로 바꿨다. MCP `get_validation_summary`에 연결.

## G1 일본 확보 — 모형 최대 약점이 NSC에 한해 해소된다

한국은 막혔지만(공개 발행 없음) **일본은 뚫렸다.** EEGS(온대법 전자보고시스템) 공표 화면이
**2021년도부터 사업소 단위**로 배출량을 공개한다. 일본제철 **27개 사업소 FY2023** 확보 —
`data/raw/jp_site_emissions.csv`, 출처 `EEGS_GHG_2023` (T1 법정 공시).

| 사업소 | MtCO₂ | 우리 고로 |
|---|---|---|
| 大分 | 16.16 | 2기 |
| 君津 | 14.59 | 2기 |
| 名古屋 | 13.50 | 2기 |
| 鹿島 | 12.95 | **1기** |
| 八幡 | 9.24 | **1기** |
| 和歌山 | 5.03 | **1기** |
| 室蘭 | 0.68 | **1기** |

**정합성 확인**: 사업소 합계 74.4 MtCO₂ vs 우리 D1b NSC 2023 S1+S2 76.4 = **−2.6%**.
온대법 산정배출량이 에너지기원 CO₂(구매전력 포함)라 S1+S2에 대응한다.

**왜 중요한가**: 지금은 회사 총량을 **10기 전체에** 능력×루트EF로 배분한다. 사업소 실측을
쓰면 배분이 **사업소 안**에서만 일어나고, 고로 1기 사이트(鹿島·八幡·和歌山·室蘭)는 **사실상
실측값**이 된다. F2 민감도 1위 `fac.ef_inc`(T5)가 NSC에 한해 T1 기반으로 올라간다.

**수집 방식**: 다운로드 링크가 있었으나 쓰지 않고 **공표 화면의 표를 읽었다** — 파일 내려받기
없이 같은 데이터를 얻었다.
**다음**: 三井化学 같은 화면 검색(미시도) → `prepare_raw`에 사업소 실측 배분 경로 추가 →
전체 재실행으로 결론 영향 측정.

## G1 일본 — MCI까지 확보, 그리고 이전 판정 하나를 뒤집는다

같은 EEGS 화면에서 三井化学 8개 사업소 FY2023 확보. 합계 3.47 MtCO₂ vs 우리 회사 총량
3.537 = **−1.9%**.

**C24의 판정이 틀렸다**: "미쓰이는 사업장별 배출 공시가 없어 같은(상한) 검증을 못 한다"고
적었으나, EEGS를 찾기 전 이야기였다. 지금은 되고 **통과한다** —

| 사업장 | 모형 크래커 | 공시 사업장 | 크래커 비중 |
|---|---|---|---|
| 미쓰이 오사카 | 0.39 | 1.455 | **27%** |
| 미쓰이 이치하라 | 0.47 | 1.107 | **43%** |
| 롯데 여수1 | 1.05 | 1.89 | 56% |
| 롯데 대산 | 0.94 | 1.53 | 61% |

**C21의 0.70 Mt 수수께끼를 세 번째로 지지한다**: 크래커가 오사카공장 배출의 27%인데 공시된
연료전환 감축 전망은 공장의 48%다. 크래커 하나로는 설명되지 않으므로 **그 감축량의 경계가
크래커보다 넓다.** (앞선 두 근거: 공시 문언의 범위, 설비집약 공시 역산 1.020 tCO₂/t-에틸렌.)

**남은 데이터 공백에서 한국만 남는다** — 일본은 NSC·MCI 모두 사업소 실측이 T1으로 확보됐고,
한국은 명세서가 공개 발행되지 않아 막혀 있다.

## G1 적용 — 사업소 실측 분포가 NSC 감축단가를 6% 내린다

**설계**: 수준은 회사 Scope1 공시, **분포는 사업소 공시**. 온대법 산정배출량은 S1+S2라
수준을 그대로 못 쓴다 — 몫만 취해 회사 총량에 재척도한다.

| 기업 | ② 전 | ② 후 | Δ | TCaR Δ | CAPEX Δ | 피크 |
|---|---|---|---|---|---|---|
| POSCO | 115.0 | 115.0 | 0.0% | 0.0% | 0% | 2041 |
| **NSC** | 165.4 | **155.8** | **−5.8%** | **−4.4%** | +2.9% | 2036 → **2042** |
| MCI | 241.7 | 241.7 | 0.0% | 0.0% | +1% | 2041 |
| LOTTE | 279.2 | 279.2 | 0.0% | 0.0% | 0% | 2041 |

**순위 불변.** POSCO·석화가 정확히 0% 움직인 것이 수정이 NSC에만 걸렸다는 확인이다.

**왜 내려갔나**: 八幡 2.355 tCO₂/t(회사평균 1.856 대비 +27%), 名古屋 1.743·和歌山 1.732(−6~7%).
더 더러운 설비가 특정되니 같은 CAPEX로 더 많이 감축한다. 투자 시점도 2036→2042로 밀린다 —
어느 고로를 먼저 칠지가 바뀌었다.

**내가 넣은 버그 하나** (커밋 별도): 폴백 분기가 `fb_share`(충돌 없으면 0)를 곱해
**사업소 데이터가 없는 회사의 배출을 전량 0으로** 만들었다. POSCO 탄소예산이 저절로 충족돼
CAPEX 0·감축단가 NaN. **20분 MILP가 0 입력 위에서 다 돌 때까지 아무것도 못 잡았다.**
분기를 셋으로 분리하고 **배분 항등식**(시설합 = 회사 공시 총량, 전 연도)을 `prepare_raw`에
넣었다 — 비용 0이고 즉시 실패한다. 처음부터 있었어야 했다.

**검증**: 테스트 29/29, 감사 ok 68/설계정상 16/공백 4, 시나리오 재계획본 보존 확인,
검증문서 5종·데이터패키지·사이트 재생성.

---

# 창 2026-08-10 01:00 → 09:00 (D1–D17)

사용자 지시(00:41): 폴더를 하나로 통합, 30분 간격 9시까지, **오늘 창만**. 목표 네 개 —
데이터 갱신·검증 / 다른 보고서·논문의 인사이트로 우리 발견 개선 / MCP·GitHub 도구 작동 / 논문화.

**창 시작 헤드라인** (NZ15, 지원 none, 지표 ② 감축단가 천원/tCO₂):
POSCO 115.0 · NSC 155.8 · MCI 241.7 · LOTTE 279.2. TCaR(십억원): POSCO 26,753 · NSC 32,973 · MCI 864 · LOTTE 2,242.

## D1 (01:00) — 폴더 통합 + ④ 게이트 자동화

**한 일**

1. **저장소 통합.** `cap-efficient`를 `git subtree`로 `cap-efficient/`에 이력째 편입. 이제 저장소는 하나(`PLANiT-Institute/cap_finance`), 커밋도 사이클당 하나.
   - **통합이 깰 뻔한 것**: H4 교차대조는 FIN·EFF가 **독립 구현**이기 때문에 증거가 된다. 지금까지 그 독립성은 폴더가 달라서 *우연히* 지켜졌다. `tests/test_independence.py`로 **의도적으로** 강제한다 — 어느 쪽도 상대를 import하면 실패. 공유는 CSV로만.
   - `cap-rebuild`는 통합하지 않았다. 5개 문서 전부 `archive/cap_kj_v1`의 **더 오래되고 작은** 부분집합이다(DATA_CATALOG.md 19KB vs 27KB). 가져올 것이 없다.
2. **`scripts/gate.py` 신설.** ④ 점검(도구로 작동하는가)을 한 명령으로. stdlib 전용, `--json` 지원.
   - 하드 실패: tests / independence / audit / MCP tools-list / CLI 배선.
   - 경고: `out/`이 `data/raw`보다 오래됐는가(= 재실행 필요), 커밋·푸쉬 상태.
   - **`python -m cap all`은 게이트에 넣지 않았다** — 20분 MILP는 30분 사이클에 못 들어간다. 대신 **신선도**를 재서, 산출물이 낡은 사이클이 조용히 지나가지 않게 했다.
3. **AUTOPILOT.md 개정**: §5-A 오늘 지도(D1–D17), 워크스트림 **L(문헌 좌표화)**·**M(논문화)** 신설. §3 동기화 규약을 단일 저장소 기준으로 재작성.

**왜 L이 필요한가**: 지금까지의 검증(H1 내부 항등식, H2 후향, H4 교차대조)은 전부 **우리가 우리를 확인**하는 것이다. 그중 어느 것도 "이미 남이 발표한 발견인지", "남의 방법이 우리보다 나은지"를 말해주지 못한다.

**검증**: `scripts/gate.py` 전 항목 PASS — 테스트 31/31(29+독립성 2), audit ok 68 / PARTIAL 4 / 설계상 정상 16, MCP 11도구 응답, out/ 신선, 푸쉬 완료.

**결론 영향**: 없음. 파이프라인 수치 무변화 — 이번 사이클은 배관과 절차만 건드렸다.

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16 | 미실행(무변화) | 미변화 | 게이트 전항목 PASS, 1저장소 | 7종 유지 |

**인계**: D2 = L1 선행연구 지도(`docs/literature_map.md`). 우리 3대 주장을 문헌 좌표에 놓는다.
**백로그 변경**: L1–L3, M1–M7 신규. J4는 M으로 흡수.

## D2 (01:30) — L1 선행연구 지도: 우리 자리를 문헌 좌표에 놓는다

**한 일**

`docs/literature_map.md` 신설. 3대 주장별 판정과 근거, 각 판정에 `source_id`.

| 주장 | 판정 |
|---|---|
| ① 자본배분 효율경계 | **부분 있음** — 위험-비용 경계는 금융·전력투자에 있고, 산업 기업의 *계획집합*에 얹어 공시계획을 그 평면에 좌표화한 사례는 확인 못함 |
| ② TCaR = P90−P50 | **부분 있음, 그리고 이 축에서 문헌이 우리보다 앞선다** |
| ③ 공시–모형 gap | **배출 공간은 이미 있음 / 비용·위험 공간은 우리가 처음** |

**③이 이 사이클의 핵심 발견이다.** `NATCOMM_APA_2026`(Nature Communications, 2026-05-13)이
CA100+ 철강 10사·전력 10사의 공시 전환계획을 자산 단위로 NGFS 탄소예산과 대조한다 — 우리 ③과
대상·질문이 겹친다. 다만 그 논문은 **비용을 모형화하지 않는다고 스스로 밝힌다**(BF 개수 €48/t,
H2-DRI-EAF €574/t를 언급만 하고 선택에 간접 영향이라 적은 뒤 넣지 않는다). 즉 우리 자리는
"아무도 안 한 것"이 아니라 **그들이 비워 둔 자리**이고, 그 편이 방어하기 쉽다.

**②에서는 우리가 뒤진다.** 문헌의 확률화 대상은 주로 탄소가격·정책인데 우리는 그것을
시나리오로 고정하고 투입가만 흔든다. **우리 TCaR은 정책 위험을 빼고 잰 위험**이고 그 사실이
지금까지 어디에도 적혀 있지 않았다. L2(D6) 1순위로 지정.

**검증 (수치)**

문서 정리에 그치지 않도록 두 개를 코드에 넣었다 — `scripts/validate_external.py`가 자동 생성한다.

1. **개수 단가 앵커 1점 → 3점.** 이전 판정("우리 200 천원/t는 고베제강 실적 47보다 ×4.2 크다")은
   단일 관측에 기댄 판정이었다.

   | 앵커 | 천원/t능력 | 출처 |
   |---|---|---|
   | 고베제강 3고로 실적 | 47 | KOBELCO_HBI_BF |
   | 문헌 개수 단가 €48/t | 70 | NATCOMM_APA_2026 |
   | 1기당 300~1,000 M USD ÷ BF 능력 중앙값 5.02 Mt | 81 ~ 269 | ACCR_BF_RELINE_2025 |
   | **우리 값** | **200** | — |

   우리 200은 앞의 둘보다 ×2.9~×4.2 크지만 **세 번째 대역 안에 있다.** 바뀐 결론: 점 추정을
   내리는 것이 아니라 **[47, 269]의 5배 폭을 민감도로 전파**해야 한다.
   *경계*: NATCOMM의 H2-DRI-EAF €574/t가 VOGL_2018과 정확히 같다 — €48/t도 같은 계보의 2차
   인용일 수 있어 3점이 아니라 2.5점으로 취급한다고 코드 주석·문서에 명시.

2. **재투자 창이 처음으로 외부 대조를 받았다.** `last_reline_year + 15/20년`은 순수 내부
   가정이면서 CAPEX 피크 연도를 직접 정한다.

   | 지표 | 우리 (고로 17기) | 문헌 | 출처 |
   |---|---|---|---|
   | 2030년까지 재투자 도래 | 8기 / 능력 48.9% | 철강 자산 42% | NATCOMM_APA_2026 |
   | 2026–2035 도래 비중 | 12/17기 = 71% (능력 73.8%) | 개수 의사결정 70% 이상 | ACCR_BF_RELINE_2025 |

   표본이 달라 검증으로 승격할 수는 없으나 **어긋나지 않는다**는 것이 처음 확인됐다.

게이트: 테스트 31/31, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
출처 등록부 78 → 81건(NATCOMM_APA_2026, ACCR_BF_RELINE_2025, CA100_BENCHMARK_V22).

**결론 영향: 없음 (수치 무변화).** 모형 입력(`data/prepared/*.csv`)은 바이트 동일이고
`out/`을 재실행하지 않았다. 헤드라인 ②(NZ15·지원 none, 천원/tCO₂) 전후 동일 —
POSCO 115.0 · NSC 155.8 · MCI 241.7 · LOTTE 279.2, TCaR(십억원) 26,753 / 32,973 / 864 / 2,242,
CAPEX 피크 2041 / 2042 / 2041 / 2041. 게이트가 STALE을 경고하지만 이는 `source_register.csv`
mtime 변화 때문이며 모형 입력이 아니다 — 20분 재실행을 하지 않은 이유를 여기 적어 둔다.

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0 | 미실행(입력 무변화) | 문헌 좌표가 생겨 "왜 우리 그림을 사야 하나"가 처음 문서화 | 게이트 전 항목 PASS(STALE 경고 1, 사유 위 기재) | 8종 (literature_map 추가) |

**인계**: D3 = M1 페이퍼 골격. 기여 3개는 이 지도의 판정을 그대로 쓴다 — ③의 "비용·위험 공간"이
주 기여, ②의 정책 위험 누락은 한계절에 선제적으로 적는다.
**백로그 변경**: L1 [x]. L2 1순위 = 탄소가격 확률화. **G7 신설** = GEM Global Blast Furnace
Tracker로 `last_reline_year` 출처 승격(NATCOMM이 쓰는 것을 우리는 안 쓰고 있다).
스니펫 수준 판정 5건은 `data_gap_registry.md`에 "본문 미확보"로 이관 — 뒤집힐 수 있는 판정이다.

## D3 (02:00) — M1 페이퍼 골격: 골격이 경계의 정체를 드러냈다

**한 일**

`paper/working_paper.md` 신설. 연구질문 한 문장 / 기여 3개 / 반증가능 주장 FC1–FC5 /
절별 재료 지도 / 미리 적는 한계 7개 / 타깃 저널. 기여 진술은 L1 판정보다 넓지 않게 잘랐다 —
C1은 "아무도 안 한 것"이 아니라 **`NATCOMM_APA_2026`이 안 한다고 적은 것**이다.

**이 사이클의 실제 발견은 골격이 아니라 §4다.**

16개 (기업×시나리오×지원) 묶음 중 **14개에서 효율경계 위 점들이 전부 같은 기술 일정**을
공유한다. 경계를 따라 움직이는 것은 무엇을 언제 짓느냐가 아니라 **PPA 비중 0→100%**뿐이다.
LOTTE는 후보 기술 일정이 애초에 1개라 선택집합이 없다.

| 기업 | 경계 위 점 | 서로 다른 기술 일정 | 움직이는 축 |
|---|---|---|---|
| POSCO | 5 | 1 (NZ15) / 3 (B20) | PPA 0→100% |
| NSC | 5 | 1 | PPA 0→100% |
| MCI | 8 | 1 | PPA + EPC |
| LOTTE | 6 | 1 | PPA + EPC |

지금까지 이 그림을 "기업 내 자본배분 효율경계"라고 불러 왔다. 심사자가 *"PPA 헤지비율 곡선
아닌가"* 라고 물으면 **지금은 답할 수 없다.** 갈래는 둘 — (1) 계획 생성이 얇다(E2가 기업당
1–5개 일정만 만든다: NZ15·none 기준 LOTTE 1, MCI 3, NSC 3, POSCO 2), (2) 재투자 창과
A-10(BF→EAF 전면 전환 불허)이 진짜로 자유도를 없앴다. **(2)면 결과가 아니라 모형 경계에 관한
발견**이고 제목의 "자본배분"부터 정정해야 한다. → **M8 신설**, M5보다 먼저.

**새 수치 하나 — 위험 1원의 가격**

같은 계산에서 나왔다. 최소비용 계획 → 최소위험 계획 이동의 교환비 = 줄어드는 TCaR ÷ 늘어나는 P50.

| 기업 | P50 (십억원) | TCaR (십억원) | 교환비 |
|---|---|---|---|
| POSCO | 41,569 → 45,418 (**+9.3%**) | 26,753 → 17,364 (**−35.1%**) | **2.44** |
| NSC | 53,395 → 58,021 (**+8.7%**) | 32,973 → 17,834 (**−45.9%**) | **3.27** |
| MCI | 310.9 → 484.0 (+55.7%) | 864.1 → 828.2 (−4.2%) | 0.21 |
| LOTTE | 775.9 → 1,239.0 (+59.7%) | 2,242.2 → 2,129.1 (−5.0%) | 0.24 |

**같은 헤지 수단이 철강에서 10배 이상 잘 듣는다.** 기여 C3으로 올렸다. 다만 규모 효과일
가능성이 있어 FC3에 "전력집약도·계약 가능량을 통제했을 때 차이가 사라지면 업종 효과가
아니다"를 반증 조건으로 명시했다.

**검증 (수치)**

- `tests/test_paper_numbers.py` 신설 — 원고 §0 수치 대장 19개 key를 파싱해 `out/`에서 재계산한
  값과 대조. 손으로 적은 숫자가 파이프라인 변경 뒤 조용히 낡는 것이 이 저장소의 반복 실패
  방식이라, 원고 첫 줄을 쓰는 시점에 넣었다. 본문 인용은 대장 등록 key로만.
- 게이트: 테스트 **33/33**(31+2), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- 교차대조 1건(§3 규약): FC5로 `docs/cross_model_check.md`의 "FIN ② POSCO 115가 EFF 실행가능
  후보 범위 26.6–155.9 안" 판정을 반증조건 형태로 고정.

**결론 영향: 없음 (수치 무변화).** 모형 입력 `data/prepared/*.csv` 무수정, `out/` 재실행 없음.
헤드라인 ②(NZ15·지원 none, 천원/tCO₂) 전후 동일 — POSCO 115.0 · NSC 155.8 · MCI 241.7 ·
LOTTE 279.2. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242. CAPEX 피크 2041 / 2042 / 2041 / 2041.
게이트 STALE 경고는 D2와 같은 사유(`source_register.csv` mtime)로 모형 입력이 아니다.

**바뀐 것은 수치가 아니라 그 수치를 무엇이라고 부를 수 있는가다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0 | 미실행(입력 무변화). 경계 위 선택집합이 14/16 묶음에서 계약 축뿐이라는 것이 새 판정 | 위험 1원의 가격(철강 2.44·3.27 대 석화 0.21·0.24)이 처음 계산됨 — 파는 그림 하나 추가 | 게이트 전 항목 PASS(STALE 경고 1, 사유 위 기재) | 9종 (working_paper 추가) |

**인계**: D4 = G5 월별 가격 + ADF/Hurst 검정. 다만 **M8이 M4(결과 절)의 전제**다 — 경계가
계약 축뿐이면 결과 절의 주력 그림이 성립하지 않는다. §5-A 우선순위(M > L > ④ > G)를 그대로
따르면 D4에서 M8을 먼저 해야 하나, G5는 가격 확률과정을 정하는 항목이라 TCaR 자체의 근거이고
D5(F3 불확실성 전파)의 입력이다. **D4는 지도대로 G5, M8은 D6(L2)와 묶어 처리**한다 — L2의
탄소가격 확률화와 M8의 ε-constraint가 같은 e5 재실행을 공유하므로 20분 안에 함께 들어간다.
**백로그 변경**: M1 [x]. **M8 신설**(경계 퇴화 해소, M5 선행). FC3 통제 검정이 M5 작업목록에 추가.

## D4 (02:30) — G5 평균회귀 검정: 검정을 돌려 보니 이 검정은 원리적으로 답을 줄 수 없다

**한 일**

`scripts/price_process_test.py` 신설 — ADF(τ_μ)·Hurst(R/S)를 D4 전 시계열에 돌리고,
**임계값을 표에서 베끼는 대신 같은 n에서 랜덤워크를 시뮬레이션해 유한표본 귀무분포를 만든다**
(n=19에 점근 임계값 −2.86을 쓰면 과대기각한다 — 실제 5% 임계값은 −3.06이다). 그리고 I3이
적지 않았던 것을 계산했다: **검정력**.

| 시계열 | n | 연 σ | ADF τ_μ | 5% 임계값(유한표본) | 기각 | **검정력(진실이 OU 반감기 10년)** |
|---|---|---|---|---|---|---|
| `smp_monthly` | 19 | 0.250 | −1.16 | −3.06 | 아니오 | **4.9%** |
| `smp_krw_mwh` | 11 | 0.322 | −1.51 | −3.23 | 아니오 | 5.2% |
| `kau_krw` | 9 | 0.363 | −1.34 | −3.36 | 아니오 | 5.1% |
| `jepx_spot` | 7 | 0.386 | −2.49 | −3.67 | 아니오 | 5.0% |

검정력이 유의수준(5%)과 같다 = **동전던지기**. '기각 못함'은 랜덤워크의 증거가 아니라
정보가 0이라는 뜻이다.

**이 사이클의 발견: G5의 원래 계획이 반증됐다.**

지도는 "월별 확보 → ADF로 GBM 유지 여부 결정"이었다. 월별을 얼마나 모아야 하는지 계산했다.

| n(월) | 연수 | 검정력 |
|---|---|---|
| 120 | 10 | 5.9% |
| 240 | 20 | 5.7% |
| 480 | 40 | 8.4% |

정지 AR(1)에서 τ_μ는 국소단위근의 n(a−1)이 아니라 **√n(a−1)/√(1−a²)**로 커진다. 반감기 10년
(월 a=0.99424)에 τ≈−3.7을 만들려면 **n ≈ 4,740개월 ≈ 395년**이다. 뒤집어 물으면, 월별 10년
(120obs)으로 80% 검정력에 닿는 반감기는 **0.5년짜리도 안 된다**(49.0%).

**즉 확률과정 선택은 데이터로 메울 공백이 아니다.** TCaR을 40~48% 움직이는 가정(I3 §2)이
원리적으로 반증 불가능하다는 뜻이고, 그러면 이것은 검정 항목이 아니라 **명시적 선택**으로
논문 방법 절에 적혀야 한다. GBM을 유지하되 이유를 바꿔 적었다 — "검정 결과 랜덤워크"가 아니라
**"검출 불가능하므로 보수적(위험을 크게 잡는) 쪽을 고른다"**. 전자는 심사자가 깨고 후자는 못 깬다.

**교차대조 1건 (§3 규약) — 두 구현이 다른 과정을 쓰고 있었다**

| 항목 | FIN | EFF (`data/price_process.json`) |
|---|---|---|
| 전력 연 σ | 0.242 (3계열 평균) | 0.22 (`illustrative_estimate`) |
| 전력 평균회귀 | **없음 (GBM)** | **κ=0.35/yr → 반감기 2.0년** |
| 자본비 연 σ | 0.060 | 0.14 |

σ는 10% 이내라 문제가 아니다. **과정이 다르다.** H4 교차대조표의 위험 지표 차이에 이 몫이
섞여 있는데 지금까지 분리되지 않았다 → **G8 신설**. 어느 쪽도 데이터로 지지되지 않으므로
(위 검정력) 값을 맞추는 것이 아니라 차이를 분해해 보고하는 것이 완료 기준이다.

**웹 수집 결과: 막혔다.** KPX `smpMonthly.es`는 최근 2년(우리가 이미 가진 19obs)만 주고 연도
파라미터가 없다(`year=2016` 무시). EPSIS 격자 엔드포인트 404, 공공데이터포털 파일은 인증키
필요, JEPX 시장데이터는 JS 렌더링. `data_gap_registry.md`에 G5 절로 기록하고 **수집 목적을
'과정 선택'에서 'σ 정밀화'로 축소**했다 — 우선순위 강등.

**검증 (수치)**

- 게이트: 테스트 **37/37**(33+4 신규), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- 신규 테스트 `tests/test_price_process.py`: 강한 회귀(a=0.5) τ<−5.0 / 랜덤워크 τ>−2.86 방향
  확인, 랜덤워크 수익률 Hurst 0.4–0.65, n=19 유한표본 임계값이 점근값보다 엄격함, 관측 부족 시 NaN.
- 시드 20260810, 시뮬레이션 20,000회 — 재실행 시 동일.

**결론 영향: 없음 (수치 무변화).** `data/prepared/*.csv`·`config.yaml` 무수정, `out/` 재실행
없음. 헤드라인 ②(NZ15·지원 none, 천원/tCO₂) 전후 동일 — POSCO 115.0 · NSC 155.8 · MCI 241.7 ·
LOTTE 279.2. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242. CAPEX 피크 2041 / 2042 / 2041 / 2041.
게이트 STALE 경고는 D2·D3과 같은 사유(`source_register.csv` mtime).

**바뀐 것은 TCaR 숫자가 아니라 그 숫자에 붙일 수 있는 방어 문장이다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 월별 3종 `access_blocked` 기록 | 미실행(입력 무변화) | "TCaR 상한선"이라는 읽는 법이 근거를 얻음 — 파는 그림의 신뢰 문구가 바뀐다 | 게이트 전 항목 PASS(STALE 경고 1) | 10종 (price_process_test 추가) |

**인계**: D5 = F3 파라미터 불확실성 전파(TCaR 분해). 오늘 결과가 F3의 틀을 정한다 — 가격
변동분/파라미터분에 더해 **확률과정 선택분**을 세 번째 칸으로 두어야 한다. 크기는 I3 표(−40~−48%)가
이미 있으므로 재계산 불필요, 분해 표에 열로 얹기만 하면 된다.
**백로그 변경**: G5 [~] 부분완료(검정 종료, 수집 목적 σ 정밀화로 축소·강등). **G8 신설**
(확률과정 이원화 해소, H4 차이 분해). 논문 한계 8번 신설(§6).

## D5 (03:00) — F3 불확실성 전파: ③ TCaR은 "파라미터를 다 안다고 가정한 세계의 위험"이었다

**한 일**

`scripts/uncertainty_propagation.py` 신설. 지금까지 ③ TCaR(=P90−P50)은 **가격 충격 하나**의
산물이었다. 파라미터는 F2에서 한 번에 하나씩(OAT) 흔들어 보기만 했고, 그 폭이 위험 지표
안에 들어온 적이 없다. F2 랭킹 상위 10개를 **동시에** 추첨(200회 × 가격경로 20,000, 시드 고정)해
네 칸으로 갈랐다 — 가격분(파라미터 고정) / 파라미터분(가격 중심경로 고정) / 결합 / 상호작용.

| 기업 | 가격분 | 파라미터분(±30%) | 결합 | 상호작용 | **파라미터 몫** |
|---|---|---|---|---|---|
| POSCO | 26,753 | 11,665 | 28,697 | −9,721 | **40.6%** |
| NSC | 32,973 | 15,578 | 35,523 | −13,028 | **43.9%** |
| MCI | 864 | 229 | 862 | −231 | **26.6%** |
| LOTTE | 2,242 | 591 | 2,238 | −595 | **26.4%** |

단위 십억원. **분위수는 가법적이 아니다** — 가격분+파라미터분 ≠ 결합이고, 그 잔차를 지우지
않고 상호작용 열에 남겼다(전부 음수: 두 축이 부분적으로 상쇄된다). 석화는 결합이 가격분보다
오히려 **작다**(2,242 → 2,238) — 파라미터 추첨이 꼬리를 넓히는 것보다 중앙값을 흩뜨리는 쪽으로
더 작용한다는 뜻이다.

**그런데 이 숫자는 우리가 고른 폭이 만든 숫자다.**

추첨 폭을 절반(±15%)으로 줄여 다시 돌렸다. 파라미터분은 거의 정확히 절반이 된다 —
POSCO ×2.14, NSC ×2.08, MCI ×2.09, LOTTE ×2.10. **선형이다.** 즉 이 열은 '우리가 모르는
정도'를 그대로 되돌려 줄 뿐이고, 폭 ±30%는 근거가 아니라 F2에서 물려받은 **규약**이다.
왜 규약인지도 세어 봤다: `docs/parameter_inventory.csv` **415행 중 [low, high]를 가진 것이
18행**. → **G9 신설** (밴드 부재를 F3의 전제로 명시, 완료 기준은 전 파라미터가 아니라 F2 상위 10).

**이 사이클의 발견: 줄일 수 있는 불확실성보다 못 줄이는 쪽이 더 크다.**

D4가 남긴 인계대로 세 번째 칸(확률과정 선택분)을 I3 표에서 옮겨 나란히 놓았더니 순서가 뒤집혔다.

| 기업 | 과정 선택분 (GBM−OU 절대값) | 파라미터분 (±30%) | 큰 쪽 |
|---|---|---|---|
| POSCO | 12,745 | 11,665 | **과정 선택** |
| NSC | 16,521 | 15,578 | **과정 선택** |
| MCI | 352 | 229 | **과정 선택** |
| LOTTE | 909 | 591 | **과정 선택** |

**4사 전부에서 '무엇을 골랐는가'가 '무엇을 모르는가'보다 크다.** 그리고 앞쪽은 데이터로
줄일 수 있지만(G2·G3·G9), 뒤쪽은 D4에서 확인했듯 **원리적으로 줄일 수 없다**(검정력 ≈ 유의수준).
G2·G3의 기대수익을 이 표가 다시 매긴다 — 데이터 승급은 TCaR의 절반 이하만 건드린다.

**교차대조 1건 (§3 규약) — 자동화했더니 첫 실행에서 걸렸다**

I3 표의 GBM 열을 인용하면서 현재 `out/e5/metrics_company.csv`와 자동 대조하게 만들었다
(`_cited_vs_current`). **NSC 인용 34,488 대 현재 32,973 (+4.6%)**, 나머지 3사 일치. `docs/
process_alternative.md`가 그 이후의 파이프라인 변경 전에 생성됐다는 뜻이다. 손으로 옮긴 수치가
조용히 낡는 것이 이 저장소의 반복 실패 방식이라, 이번엔 문서에 경고 문단이 자동으로 찍히게
했다. → **I3-b 신설**(재실행).

**검증 (수치)**

- 게이트: 테스트 **42/42**(37+5 신규), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- 신규 `tests/test_uncertainty.py`: 정규분포에서 TCaR = 1.2816σ 복원, 상호작용 잔차가 정의대로
  계산되고 √(a²+b²)<a+b이므로 음수임, 곱수 1에서 교란이 항등, **vol 교란이 결정론적 경로를
  움직이지 못함**(이 성질이 깨지면 '가격분/파라미터분' 라벨이 거짓이 된다), 지정한 열만 스케일됨.
- 페이퍼 §0 대장에 8개 key 추가(`tcar_param30_*`, `param_share30_*`) — `tests/test_paper_numbers.py`가
  `out/uncertainty/decomposition.csv`와 대조하도록 확장.
- **가격분이 E5 헤드라인과 정확히 일치**한다: 26,752.6 / 32,973.1 / 864.1 / 2,242.2. 기준점 P50도
  41,569 / 53,395 / 311 / 776으로 일치 — 새 스크립트가 기존 파이프라인과 같은 수량을 재고 있다는 증거.

**결론 영향: 없음 (수치 무변화).** `data/prepared/*.csv`·`config.yaml` 무수정, `out/` 재실행 없음.
헤드라인 ②(NZ15·지원 none, 천원/tCO₂) 전후 동일 — POSCO 115.0 · NSC 155.8 · MCI 241.7 ·
LOTTE 279.2. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242. CAPEX 피크 2041 / 2042 / 2041 / 2041.
게이트 STALE 경고는 D2–D4와 같은 사유(`source_register.csv` mtime).

**바뀐 것은 ③의 값이 아니라 ③이 무엇의 위험인지다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. **밴드 보유 18/415를 처음 계량**(G9) | 미실행(입력 무변화) | ③에 오차막대가 생겼다 — "TCaR 26.8조"가 아니라 "가격 26.8 + 파라미터 11.7(±30% 가정)"로 팔린다 | 게이트 전 항목 PASS(STALE 경고 1, 미커밋 7건은 이 사이클 산출물) | 11종 (uncertainty_propagation 추가) |

**인계**: D6 = L2 문헌 방법 1개 이식(탄소가격 확률화, FC4). **오늘 결과가 L2의 완료 기준을 바꾼다** —
탄소가격을 확률 축으로 옮겨 TCaR이 커지는지만 보는 것으로는 부족하고, 그 증분을 오늘의 세 칸
(가격분·파라미터분·과정 선택분) 옆에 **네 번째 칸**으로 놓아야 비교가 된다. 분해기는 이미
`decompose()`로 분리돼 있으므로 증분 계산만 붙이면 된다. D3 인계의 M8(경계 퇴화)은 예정대로
D6에 L2와 함께 처리한다 — 둘 다 e5 재실행을 공유한다.
**백로그 변경**: **F3 [x]**. **G9 신설**(증거 밴드 18/415, F3의 전제). **I3-b 신설**(process_alternative
재실행, NSC +4.6% 어긋남). 페이퍼 한계 9번 신설(§6).

## D6 (03:30) — L2 문헌 방법 이식: 우리 TCaR에서 빠져 있던 정책 위험의 크기

**한 일**

L1이 "문헌이 우리보다 낫다"고 판정한 항목은 하나였다 — 그들은 **탄소가격·정책을 확률변수로**
두고, 우리는 그것을 시나리오 축에 고정한 채 투입가만 흔든다. 즉 지금까지 조원 단위로 적어 온
③ TCaR은 **정책 위험을 빼고 잰 위험**이었고(FC4), 그 크기가 논문 어디에도 없었다. 그 축을 켰다.

변동성은 추측하지 않았다. D4 `kau_krw`(K-ETS 연평균 2015–2023, 9obs, `NABO_KETS_2024`)에서
투입가와 **같은 추정기**(`calibration._annual_vol`)로 뽑아 **연 36.3%** — 전력 24.2%의 1.5배,
우리 위험인자 중 가장 크다. 규약이 다르면 두 축의 크기 비교 자체가 성립하지 않는다.

| 기업 | TCaR 가격분 | TCaR 탄소만 | 가격+탄소 | **정책 증분** (시드 3개 범위) | 증분/가격분 |
|---|---|---|---|---|---|
| POSCO | 26,753 | **16,510** | 29,197 | **+2,444** [+1,979, +2,715] | +9% |
| NSC | 32,973 | **15,465** | 34,196 | **+1,223** [+835, +1,479] | +4% |
| MCI | 864 | 75 | 827 | **−37** [−47, −27] | −4% |
| LOTTE | 2,242 | 163 | 2,142 | **−100** [−130, −62] | −4% |

단위 십억원, NZ15·지원 none. **FC4가 업종별로 갈렸다.** 철강에서는 반증되지 않았다 — 정책 축
하나가 F3 파라미터분(11,665 / 15,578)과 **같은 자릿수**다. 석화에서는 반증됐다 — 증분이 음수라
우리 단순화가 그쪽에서 실무적으로 무해하다. 부호가 결론이 되므로 탄소충격 시드를 3개 돌렸고,
부호는 세 시드 모두에서 같다.

**이 사이클의 발견: 정책 위험이 우리 지표에 들어오는 방향이 통념과 반대다.**

전환계획은 무대응보다 배출이 적으므로 탄소가격이 **오를 때** 상대적으로 싸진다. TCaR = P90−P50을
'추가 조달 여력'으로 정의한 우리 지표에서, 이 축의 나쁜 꼬리는 고탄소가격이 아니라 **탄소가격
붕괴**다 — 투자해 놓고 정책이 물러서는 경우. 즉 정책 위험은 '규제 강화 위험'이 아니라 **좌초
위험**으로 들어온다. 실물옵션 문헌(탄소세 상승이 투자를 앞당긴다)과 모순이 아니라 같은 메커니즘을
조달 관점에서 본 것이고, 이 진술은 우리 정의에서 **연역적으로 따라 나오므로** 심사자가 깨기 어렵다.

D5의 세 칸 옆에 네 번째 칸을 놓으면 POSCO 기준 순위는 이렇다 — 가격 26,753 > **정책 16,510** >
과정 선택 12,745 > 파라미터 11,665. **줄일 수 있는 것(파라미터)이 가장 작다**는 D5의 결론이
한 단계 더 강해졌다.

**교차대조 1건 (§3 규약) — 브로드캐스트가 조용히 깨져 있었다**

첫 전체 실행이 `(10000,26) vs (20000,26)`로 죽었다. `--sims 20000`을 줘도 e3가 저장한 경로는
`config.yaml`의 `n_sims: 10000`뿐이라 기존 충격은 잘리고 새 탄소충격만 요청값으로 만들어진
것이다. **D5 로그의 "가격 경로 20,000"은 실제로는 10,000이었다** — F3 수치 자체는 그 10,000으로
계산된 값이므로 변하지 않지만, 보고된 N이 틀렸다. 이제 실제 N을 쓰고 보고한다.

**검증 (수치)**

- 게이트: 테스트 **46/46**(42+4 신규), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- **헤드라인 무변화를 실측으로 증명했다.** `plancost.simulate_cost`를 고쳤으므로 e4·e5를
  재실행해 `out/e5` 전체를 전후 비교 — `diff -rq` **바이트 단위 동일**. 확률화는 opt-in
  (`shocks['co2']` 부재 시 (1,T) 그대로), `config.yaml` 무수정.
- F3 재실행이 D5 값을 정확히 복원: 가격분 26,753 / 32,973 / 864 / 2,242, 파라미터분(±30%)
  11,665 / 15,578 / 229 / 591, 몫 41 / 44 / 27 / 26%. 기준점 P50 41,569 / 53,395 / 311 / 776.
- 신규 `tests/test_uncertainty.py` 4건: co2 키 부재·전1이면 **기존 수치와 완전 일치**(헤드라인
  보호), 충격이 탄소비용 채널에만 닿고 배출 0이면 정책 위험도 0, 충격이 GBM·평균1 규약
  (로그공간 검정 — σ=0.36·29년이면 표본평균이 ±7% 흔들려 수준 검정이 성립하지 않는다),
  변동성이 사전값이 아니라 `kau_krw`에서 온다.
- 페이퍼 §0 대장에 8개 key 추가(`tcar_co2only_*`, `co2_increment_*`), `test_paper_numbers.py`가
  `out/uncertainty/decomposition.csv`와 대조.

**결론 영향: 헤드라인 수치 무변화, 그러나 ③의 읽는 법이 바뀐다.** ②(NZ15·none, 천원/tCO₂)
POSCO 115.0 · NSC 155.8 · MCI 241.7 · LOTTE 279.2 — 전후 동일. TCaR(십억원) 26,753 / 32,973 /
864 / 2,242 — 전후 동일. CAPEX 피크 2041 / 2042 / 2041 / 2041 — 전후 동일. 게이트 STALE 경고는
D2–D5와 같은 사유(`source_register.csv` mtime).

**바뀐 것은 ③의 값이 아니라 ③이 하한이라는 사실이다 — 철강에 한해.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. **새 위험인자를 사전값 없이 실측(`kau_krw`)으로 세움**, 탄소–전력 상관은 G10에 `access_blocked` 기록 | 미실행(입력 무변화, e5 바이트 동일) | "철강 TCaR은 하한이다"가 붙었고 그 근거가 원화로 있다 — 정책 시나리오를 파는 문이 열린다 | 게이트 전 항목 PASS(STALE 경고 1), 커밋·푸쉬 완료 | 11종 (신규 파일 없음, F3 보고서 §4 확장) |

**인계**: D7 = M2 방법 절. **오늘 결과가 M2에 두 문단을 강제한다** — (a) 탄소가격이 왜 시나리오
축에 있는지와 그 선택의 크기(§4 표), (b) 위험인자 4개 중 3개만 헤드라인에 있다는 사실. D3 인계의
M8(경계 퇴화)은 e2 MILP에 ε-constraint를 기술 일정 축으로 거는 작업이라 사이클 하나를 통째로
쓴다 — **D7에서 M2를 끝낸 뒤 M8을 별도 사이클로 잡는 것이 맞다**(M5 전까지 답이 필요, 늦어도 D11).
**백로그 변경**: **L2 [x]**. **L2-b 신설**(헤드라인 채택 판단 — `calibration.calibrate`의 corr가
`dropna()`라 4번째 계열을 넣으면 elec–capex 상관이 L2와 무관한 이유로 움직인다. 이걸 pairwise로
고치는 것이 선행조건). **G10 신설**(탄소–전력 상관, 관측 구간 비중첩). 페이퍼 한계 5번 확장.

## D7 (04:00) — M2 방법 절: 대리 목적함수를 믿었다면 여덟 번 다 틀렸다

**한 일**

`paper/working_paper.md`에 첫 본문 절을 넣었다 — 「본문 §3. 방법」 3.1–3.8 (E1 예산 앵커 /
E2 후보 생성 / **3.4 대리와 정본** / E3 확률과정 / E4 정본비용 / E5 경계·gap / 수치 규약).
METHODOLOGY의 수식을 논문 문체로 옮기는 작업이었지만, 지시된 핵심(§5 표 3행: "E2 대리목적함수와
E4 정본의 관계를 정면 기술")은 **서술로 끝낼 수 없어서 측정했다**.

**이 사이클의 발견: A-14가 방어적 각주가 아니라 실증된 필요조건이었다.**

기준 실행(NZ15·none)에서 기업×시나리오 묶음별로 E2 대리비용 순위와 E4 정본 P50 순위를 대조했다.

| 기업 | 후보 수 | ρ(대리비용, 정본 P50) | ρ(대리위험, 정본 TCaR) |
|---|---|---|---|
| POSCO | 3 | **0.00** | **0.00** |
| NSC | 5 | **−0.051** | 0.553 |
| MCI | 8 | 0.204 | 0.707 |
| LOTTE | 6 | 0.725 | 0.783 |

**철강에서 대리 순위는 정본 순위에 대한 정보를 거의 갖지 않는다.** 그리고 8개 묶음 **전부**에서
대리 최저비용 계획 ≠ 정본 최저비용 계획(**일치 0/8**). MILP 최적해를 "그 기업의 최적 계획"으로
보고하는 흔한 관행을 따랐다면 여덟 번 모두 틀린 계획을 보고했을 것이다. 결론에는 영향이 없다 —
E4가 정본이라서 애초에 그렇게 하지 않았기 때문이고, 그것이 A-14가 실제로 일하고 있다는 뜻이다.

원인이 몬테카를로 잡음이 아니라는 **대조군**을 같이 뒀다: 중앙경로 결정론 비용과 정본 P50의
순위상관이 여덟 묶음에서 **최소 0.941, 대부분 1.00**이다. 순위를 흔드는 것은 불확실성이 아니라
대리 목적함수의 구조(폐쇄 마진 상실·잔존가치·계약 프리미엄)다.

**교차대조 1건 (§3 규약) — 후보 다양성이 세는 것보다 작다**

대리/정본 대조를 돌리다 정본 비용·TCaR이 **소수점 6자리까지 동일한 계획 쌍**이 나왔다.
E2 51개 계획 중 정본 기준 서로 다른 것은 **43개**이고, 사라지는 8개는 여덟 묶음 각각의
**CCfD 체결 여부만 다른 쌍**이다. 지원 `none`에서는 `plancost._support_arrays`가
`ccfd_strike=None`을 반환해 CCfD가 정본에서 아무 일도 하지 않는데, E2는 프리미엄을 물리고
선형 위험 공제(A-15)를 되돌려 준다 — 대리 공간에서만 다른 점이다. E5 헤드라인은 중복을
제거하므로 **결과는 무영향**이지만, 페이퍼 §4의 경계 퇴화 진단("기업당 후보 1–5개")은
그 숫자조차 **상한**이라는 뜻이 된다. → METHODOLOGY §9-9 신설, 페이퍼 §4 보강, §6 한계 10 신설.

**정정 1건 — 페이퍼 §0 머리글의 N이 틀려 있었다**

D6가 브로드캐스트 오류를 쫓다 "D5 로그의 20,000은 실제로 10,000"이라고 적었는데, 페이퍼 §0은
아직 "N=20,000"이었다. `out/e3/price_sims.parquet`이 1,040,000행 = 4요인 × **10,000경로** ×
26년으로 확정한다. 수치는 전부 그 10,000으로 계산된 값이라 변하지 않고 **보고된 N만 틀렸었다**.
§0에 정정 문단을 박아 뒀다.

**D6가 M2에 강제한 두 문단도 넣었다** — §3.5가 (a) 탄소가격이 왜 시나리오 축에 있는지와 그
선택의 크기(POSCO +2,444 / NSC +1,223 / MCI −37 / LOTTE −100십억원, 정책 축만으로도 철강
16,510 / 15,465), (b) 위험인자 넷 중 헤드라인이 확률로 다루는 것은 셋이므로 **철강 ③은 하한**
이라는 사실을 함께 적는다.

**검증 (수치·테스트)**

- 게이트: 테스트 **46/46**, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- 페이퍼 §0 대장에 **13개 key 추가** — `e2_plans_total` 51, `e2_plans_canonical` 43,
  `surrogate_argmin_match` 0 / `..._groups` 8, `surrogate_rho_*` 4개, `riskproxy_rho_*` 4개,
  `rho_central_p50_min` 0.941. `tests/test_paper_numbers.py`가 `out/e4/summary.csv` ·
  `out/e2/plan_index.csv`에서 전부 재계산해 대조한다(scipy 부재라 순위 변환 후 피어슨).
- 본문 서술 자체도 테스트가 잡는다: `test_body_quotes_only_ledger_keys`에 §3.4의
  "일치 0/8"과 "51개 계획 중 … 43개" 문자열 대조를 추가. 손으로 적은 숫자가 낡는 것이
  이 저장소의 반복 실패 방식이라 본문 문장을 대장에 묶었다.
- **코드 변경 없음** (`src/` 무수정, `config.yaml` 무수정, `out/` 재실행 없음).

**결론 영향: 없음 (헤드라인 무변화).** ②(NZ15·none, 천원/tCO₂) POSCO 115.0 · NSC 155.8 ·
MCI 241.7 · LOTTE 279.2 — 전후 동일. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242 — 전후 동일.
CAPEX 피크 2041 / 2042 / 2041 / 2041 — 전후 동일.

**바뀐 것은 경계가 무엇인지에 대한 우리 진술이다 — "효율경계"가 아니라 "43개 후보의 하한 경계".**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 신규 수치는 전부 `out/`에서 재계산되는 파생값 | 미실행(입력 무변화) | ③의 정직한 이름표가 붙었다 — 경계는 하한이고 대리 순위는 팔 수 없다 | 게이트 전 항목 PASS, 커밋·푸쉬 완료 | 11종 (신규 파일 없음, 페이퍼에 본문 절 최초 추가) |

**인계**: 지도상 D8 = G3(설비 능력 정합, EFF 12% 괴리). 다만 **오늘 결과가 M8의 우선순위를
올린다** — 페이퍼 §6-10("경계가 아니라 하한 경계")은 데이터 승급으로는 좁혀지지 않고 오직
후보 생성을 넓혀야 좁혀진다. §5-A 우선순위 규칙(M > L > ④ > G)을 문자 그대로 적용하면 M8이
G3보다 앞선다. **D8에서 M8을 하고 G3를 D10으로 미루는 편이 맞다고 본다** — D6 인계도 "늦어도
D11"이라 했고, M8은 e2 재실행을 요구해 사이클을 통째로 쓰므로 뒤로 밀수록 위험하다.
**백로그 변경**: **M2 [x]**. METHODOLOGY §9-9 신설(후보 다양성 51→43, CCfD 중복).
페이퍼 §6 한계 10 신설(대리 순위 비보존, 하한 경계). 페이퍼 §0에 N 정정 문단.

## D8 (04:30) — M8 경계 퇴화: 다른 길은 있었다, 우리 자가 그것을 위험이라 불렀을 뿐

**한 일**

지도상 D8은 G3(설비 능력)였으나 **§5-A 우선순위 규칙(M > L > ④ > G)대로 M8을 먼저 했다** —
D7 인계의 판단 그대로다. M8은 e2 재해를 요구해 사이클을 통째로 쓰고, 페이퍼 §6-10("경계가
아니라 하한 경계")은 데이터 승급으로 좁혀지지 않는다. G3는 D10으로 민다.

`_solve_company`에 **`emis_cap`**(누적 배출 상한)을 넣었다. 위험 대리에 건 epsilon은 계약의
선형 공제로 살 수 있어서 좁히면 계약만 움직인다. 기술 일정을 강제하려면 **계약이 건드릴 수 없는
축**이 필요하고 그것이 배출이다 — PPA·EPC·CCfD는 배출을 1 tCO₂도 바꾸지 않는다.
`scripts/frontier_tech_epsilon.py`가 기업×시나리오 8묶음 각각에 기준 비용최소해 누적배출의
95/90/80/70%를 걸어 다시 풀고, 얻은 일정을 **E4와 같은 정본 절차**로 재산출해(E5와 같은 계약
격자) 기존 후보 전체가 만드는 파레토 집합에 대해 비지배인지 본다. → `docs/frontier_degeneracy.md`.

**이 사이클의 발견: 퇴화는 후보 생성의 결함이 아니라 위험 규약의 귀결이었다.**

**강제 상한 32개가 전부 실행가능하고 전부 최적해이며 전부 새 기술 일정이다.** 70%까지 밀어도
`Optimal`이 나온다. "다른 일정이 없다"는 가설(H-thin의 전제)은 틀렸다 — 자유도는 있다.

그런데 헤드라인 규약에서 그 32개 중 정본 (P50, TCaR)에 **비지배로 남는 것은 4개**뿐이고,
그 4개도 한 묶음(LOTTE NZ15)이며 P50이 기존 경계보다 한 자릿수 큰 영역에 있다. 나머지 일곱
묶음에서 강제 일정은 **두 축 모두에서** 진다. 감축을 더 하면 기대비용이 오르는 것은 당연하지만
**꼬리위험도 같이 오르기** 때문이다. 이유는 회계에 그대로 있다 — 감축은 노출을 탄소에서
전력·수소·건설비로 옮기는데, 헤드라인에서 **탄소가격은 결정론**이다. 위험 없는 항목을 위험 있는
항목으로 바꾸는 일이니 감축이 위험을 키운다. 계약은 반대로 위험만 줄이고 배출은 그대로 둔다.
**그래서 경계를 만드는 것은 오직 계약이다.**

**같은 32개를 L2 규약(탄소가격 σ=36.3% 확률화, D6)에서 다시 재면 25개가 비지배**가 되고,
여덟 묶음 중 **일곱**에서 기술 축이 돌아온다. 부호가 뒤집히는 곳이 가장 선명하다 — LOTTE B20에서
TCaR이 헤드라인 2,266 → 6,611로 커지는 그 감축이, L2에서는 1,209 → **637**로 줄어든다.
탄소가격이 확률변수가 되는 순간 감축이 **헤지**가 되기 때문이다.

**따라서 M8의 답은 (H-thin)도 (H-bound)도 아니다.** 경계에 기술 일정 차원이 있는지 없는지는
탄소가격을 확률로 두는지에 달려 있다. M8의 완료 기준("후자면 제목의 '자본배분'을 '계약·자본
혼합 배분'으로 정정")은 **현행 규약에서 발동한다** — 페이퍼 §6 한계 7이 그대로 적는다.

**검증 (수치·테스트)**

- 게이트: 테스트 **47/47**(46+1 신규), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구.
- 신규 `test_emis_cap_is_an_axis_contracts_cannot_buy`: (a) 상한이 실제로 물리고, (b) 제약이
  붙었으므로 대리비용이 내려가지 않으며, (c) **PPA=1·EPC=1·CCfD=1과 전부 0의 배출 배열이
  바이트 동일**하다. (c)가 깨지면 이 축은 일정 축이 아니게 되고 진단 전체가 무의미해진다.
- 기준선을 강제 일정에 **불리하게** 잡았다: E5의 `budget_ok` 필터 없이 기존 후보 전체 ×
  계약 격자의 파레토 집합을 썼다. 그래도 헤드라인에서 4/32만 살아남는다.
- 페이퍼 §0 대장에 5개 key 추가(`m8_caps_total` 32, `m8_new_schedules` 32,
  `m8_nondominated_headline` 4, `m8_nondominated_l2` 25, `m8_groups_nondominated_l2` 7).
  `test_paper_numbers.py`가 `out/m8/tech_epsilon.csv`에서 전부 재계산해 대조한다.
- **코드 변경은 `emis_cap` 인자 하나뿐이고 기본값 None에서 기존 경로와 동일**하다.
  `config.yaml` 무수정, `out/e1`–`out/e5` 재실행 없음(신규 산출은 `out/m8/`만).

**결론 영향: 헤드라인 수치 무변화.** ②(NZ15·none, 천원/tCO₂) POSCO 115.0 · NSC 155.8 ·
MCI 241.7 · LOTTE 279.2 — 전후 동일. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242 — 전후 동일.
CAPEX 피크 2041 / 2042 / 2041 / 2041 — 전후 동일.

**바뀐 것은 이 경계를 무엇이라 부를 수 있는가다 — 현행 규약에서는 "자본배분 경계"가 아니라
"계약·자본 혼합 배분 경계"다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 신규 수치는 전부 MILP 재해 + 정본 재산출의 파생값 | 미실행(입력 무변화, e5 무재실행) | 경계의 이름표가 정확해졌고, L2 채택이 무엇을 되돌리는지가 원화로 있다 | 게이트 전 항목 PASS, 커밋·푸쉬 완료 | 12종 (`docs/frontier_degeneracy.md` 신설) |

**인계**: D9 = M3 데이터 절(지도대로). **L2-b의 성격이 바뀌었다** — 정밀화가 아니라 제목이
걸린 선택이므로, M4(결과 절, D11) 전에 답이 필요하다. `calibration.calibrate`의 corr를
계열쌍별 pairwise로 고치는 것이 여전히 선행조건이다. G3는 D10으로, G2는 그 다음으로 민다.
**백로그 변경**: **M8 [x]**. L2-b에 D8 판단 추가. METHODOLOGY §9-10 신설. 페이퍼 §6 한계 7
채움(빈 항목이었다). `docs/frontier_degeneracy.md` §4에 시점 축 epsilon을 부차 항목으로 기록.

## D9 (05:00) — M3 데이터 절: 출처가 좋은 값일수록 범위가 없었다

**한 일**

지도대로 M3. `paper/working_paper.md`에 「본문 §4. 데이터」 4.1–4.5를 넣었다 — 원자료 10종 표 /
신뢰등급 분포 / F2 상위 10의 등급 / 인용 무결성 / 진짜 공백 넷. 다만 데이터 절의 표를 **손으로
적으면 다음 수집에서 조용히 낡는다**는 것이 이 저장소의 반복 실패 방식이라, `scripts/data_section_table.py`가
원자료·등록부·인벤토리·민감도 랭킹에서 전부 다시 세게 하고 본문은 그 산출을 인용만 한다
(`out/m3/{datasets,tier_summary,source_integrity,top10_tier,summary}.csv`).

**발견 1 — 신뢰등급과 불확실성 폭이 직교한다. G9를 잘못 적어 뒀었다.**

| 등급 | 행 | [low, high] 보유 |
|---|---|---|
| T1 | 3 | **2** |
| T2 | 137 | **0** |
| T3 | 41 | **0** |
| T4 | 79 | **0** |
| T5 | 155 | **16** |
| 계 | 415 | 18 |

D5가 "415행 중 밴드 18행"이라고 적었을 때 그것은 진도 부족으로 읽혔다. 아니었다. **증거로
인용하는 등급 T2·T3·T4의 257행 전부가 점 추정**이고, 범위를 가진 것은 T1 두 개와 **우리가 만든
값** 열여섯 개다. 즉 결함은 "덜 채웠다"가 아니라 **수집 규약이 값만 받고 범위를 버렸다**는 것이다
— 문헌은 대개 범위를 함께 싣는데 우리는 중앙값만 옮겨 적었다. F3의 파라미터분이 근거 밴드가
아니라 ±30% 규약 위에 서 있는 직접 원인이 여기 있고, **G2·G3 수집에서 값이 아니라 범위를 함께
받으면 밴드는 부산물로 따라온다**. G9의 완료 경로가 바뀐다.

**발견 2 — F2 상위 10 중 §1 규약(T3 이상)을 만족하는 것은 5개**

`fac.ef_inc`(1위) T5 · `cfg.discount`(3위) T5 · `vol.h2`(4위) T5 · `price.h2`(6위) T5 ·
`fac.capacity`(8위) T2/T5 혼합. 다섯의 성격이 서로 달라 처방도 다르다 — (a) `fac.ef_inc`는
공시제도가 정하는 공백이다(일본은 EEGS로 해소, 한국은 NGMS 비공개), (b) `cfg.discount`는
밴드가 아니라 **선택**이고 I1이 3.5/5/6.5%로 이미 처리했다, (c) `vol.*`·`price.h2`는 표본
길이 문제이며 과정 선택은 검정으로 결정되지 않는다(G5), (d) `fac.capacity`는 T2와 T5가 섞인
채라는 것 자체가 문제이고 EFF 12% 괴리와 같은 지점이다(G3). 결론 영향은 F3이 이미 정량화했다
— 그래서 **③ TCaR의 절대값은 이 표가 개선되기 전까지 인용 대상이 아니고 순서·부호만 쓴다**.

**발견 3 (교차대조 1건, §3 규약) — 인용 7건이 FIN 등록부 밖을 가리킨다**

인벤토리 415행의 `source_id`를 세 갈래로 해소했다: 등록부 **279** / 표식(출처 아님) **129** /
EFF 이름공간 **7** / 해소 불가 **0**. 등록부 80건은 하나도 남김없이 인용된다(고아 0).

7행은 실제 프로젝트 비용(광양 EAF, NSC 야하타·히로하타·슈난, JFE 구라시키, 고베 BF3·가코가와
멜터)이고 T2 등급을 받으면서 `JFE_KURA_EAF_20` 같은 id를 인용하는데, 그 id는
`cap-efficient/data/transition_projects.csv`에만 있다. **§3 독립성 때문에 복사하지 않았다** —
검사기가 두 등록부를 CSV로 읽어 해소하고 어느 쪽에서 왔는지를 행마다 남긴다. 표식 129행은
결함이 아니라 선언이며(거의 전부 EFF `data_status = model_estimate`), 그 분포가 드러내는 것은
**EFF의 증거 성숙도가 모형 입력이 아니라 별도 증거 계층에 있다**는 구조다. §3이 "데이터 증거의
정본은 EFF"라고 할 때 그것은 EFF 입력값이 낫다는 뜻이 아니라 **EFF 등록부가 낫다**는 뜻이다.
이 구별을 흐리면 H4 교차대조를 잘못 읽는다.

**검증 (수치·테스트)**

- 게이트: 테스트 **47/47**, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구, `gate: OK`.
- 페이퍼 §0 대장에 **12개 key 추가** — `m3_raw_datasets` 10, `m3_sources_registered` 80,
  `inv_rows` 415, `inv_banded` 18, **`inv_banded_t2t3t4` 0**, `inv_src_registered` 279,
  `inv_src_sentinel` 129, `inv_src_eff` 7, `inv_src_unresolved` 0, `top10_t3plus` 5,
  `top10_t5` 4, `audit_partial_cols` 4. 전부 `out/m3/summary.csv`에서 재계산해 대조한다.
- 본문 표도 테스트가 잡는다: `test_body_quotes_only_ledger_keys`에 §4.2 등급 표 합계,
  §4.3 "열 중 5개만", §4.4 무결성 표 세 행의 문자열 대조를 추가.
- **코드 변경 없음** (`src/` 무수정, `config.yaml` 무수정, `out/e1`–`out/e5` 재실행 없음).
  신규 산출은 `out/m3/`만이고 신규 스크립트 1개.

**결론 영향: 없음 (헤드라인 무변화).** ②(NZ15·none, 천원/tCO₂) POSCO 115.0 · NSC 155.8 ·
MCI 241.7 · LOTTE 279.2 — 전후 동일. TCaR(십억원) 26,753 / 32,973 / 864 / 2,242 — 전후 동일.
CAPEX 피크 2041 / 2042 / 2041 / 2041 — 전후 동일.

**바뀐 것은 우리가 인용하는 증거의 성질에 대한 진술이다 — 등급이 높은 값일수록 폭이 없다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 인용 무결성 최초 계량 — 해소 불가 0, 고아 출처 0, 규약 이탈 7 | 미실행(입력 무변화) | 데이터 절이 약점을 표로 먼저 내놓는다 — 심사자가 찾기 전에 우리가 센다 | 게이트 전 항목 PASS, 커밋·푸쉬 완료 | 12종 (`out/m3/` 5종 신설, 신규 파일은 스크립트 1개) |

**인계**: 지도상 D10 = G2(에너지 원단위 범위 승급). **오늘 발견 1이 G2의 완료 기준을 바꾼다** —
값을 승급하는 것이 아니라 **[low, high]를 함께 받는 것**이 완료 기준이어야 한다. 그러지 않으면
T3 행이 늘어나도 밴드 열은 여전히 0이고 F3은 ±30% 규약 위에 그대로 남는다. G3(설비 능력, D8에서
D10으로 밀림)는 `fac.capacity`의 T2/T5 혼재를 푸는 같은 작업이므로 G2와 한 사이클에 묶는 것을
검토할 만하다. L2-b(제목이 걸린 선택)는 여전히 M4(D11) 전에 답이 필요하다.
**백로그 변경**: **M3 [x]**. G9 완료 경로 수정(밴드는 별도 작업이 아니라 G2·G3 수집의 부산물).
페이퍼 §6 한계 9에 D9 발견 추가. `docs/data_gap_registry.md` G9에 "18행의 정체" 보강.

## D10 (05:30) — G2 증거 밴드: 규약은 폭이 아니라 중심이 틀렸다

**한 일**

지도대로 G2. 다만 **완료 기준은 D9 발견 1이 바꿔 둔 것**을 따랐다 — 값을 승급하는 것이 아니라
**[low, high]를 함께 받는 것**. 문헌 PDF에서 밴드를 받아 세 칸에 붙였다
(`data/raw/tech_bands.csv` → `data/prepared/D3b_tech_bands.csv` → 인벤토리 `value_low/high`
→ F3 승수 구간). ScienceDirect 403로 막힌 PDF는 `pdftotext -layout`로 로컬 추출했다 —
L1이 등록해 둔 대체 경로("로컬 PDF 파서로 재시도")가 실제로 작동한다.

| 기술.항목 | 현행 | [low, high] | 출처 |
|---|---|---|---|
| `steel_h2dri.capex_unit` | 863 천원/t | [863, 1095] | DIW DP2082 Table 1 (문헌 592 / OECD 626 / 발표 751 €2022) |
| `steel_eaf.capex_unit` | 240 천원/t | [370, 835] | 같은 표 EAF 행 (254 / 467 / 573 €2022) |
| `steel_eaf.emission_factor` | 0.05 tCO₂/t | [0.04, 0.05] | IEA ISTR 2020 p.38 (스크랩 EAF 직접배출 0.04) |

€→천원 1.4578(= `USDKRW` 1350 × EURUSD 1.08)로 DIW 문헌 중앙값 592가 현행 863과 **정확히**
일치한다 — 원 입력이 그 값을 썼다는 것이 역산으로 확인된다. IEA ISTR의 연환산 CAPEX
(8%·25년·CF 0.9) 스크랩EAF 34~58 USD/t을 오버나이트로 환산하면 441~752천원/t이라 DIW EAF
밴드와 겹친다 — 두 출처가 독립적으로 같은 방향을 가리킨다.

**발견 — 세 칸 전부에서 현행 값이 밴드의 끝이거나 밖이다. 우연이 아니다.**

우리는 각 항목에서 가장 낮은 인용값을 골라 왔다(H2-DRI는 문헌 중앙값, EAF는 POSCO 광양 실제
프로젝트비 €170.15/t — DIW가 같은 표에 수록한 그 값이다). 그것이 문헌 분포의 어디인지 확인한
적이 없었다. 그래서 밴드를 base_param 승수로 옮기면 구간이 **한 방향으로만 열린다**:
`tech.capex` **[1.000, 3.479]**, `tech.emission_factor` **[0.800, 1.000]**.

**이것이 이 사이클의 결론이다 — ±30% 대칭 규약은 폭이 틀린 것이 아니라 중심이 틀렸다.**
F3은 1을 중심으로 대칭 추첨하므로 추첨의 절반이 증거가 배제하는 영역에 있었다. D5가 "폭에
선형(×2.08~2.14)"이라고 적었을 때 그것은 폭만 고치면 되는 문제로 읽혔다. 아니었다.

`--bands`로 F3을 다시 돌린 결과(정본 파일은 덮지 않는다 — `decomposition_bands.csv` 별도):

| 기업 | 폭 | TCaR_param 규약 → 증거 | 파라미터 몫 |
|---|---|---|---|
| POSCO | ±15% | 5,459 → **10,263** (×1.88) | 20% → **36%** |
| NSC | ±15% | 7,483 → **13,061** (×1.75) | 22% → **36%** |
| POSCO | ±30% | 11,665 → 15,791 (×1.35) | 41% → **50%** |
| NSC | ±30% | 15,578 → 21,626 (×1.39) | 44% → **54%** |
| LOTTE·MCI | 양쪽 | ×0.95~1.01 | 무변화 |

석유화학이 안 움직이는 것은 밴드가 붙은 기술이 철강뿐이기 때문이고, 그 사실 자체가 다음
수집 대상을 정한다. **값은 고치지 않았다** — 광양은 기존 부지·인프라 재활용일 개연이 크고,
경계 확인 없는 교체는 데이터가 아니라 취향이다. 밴드로 남기고 판단은 경계 확인 후에 한다.

**검증 (수치·테스트)**

- 게이트: 테스트 **48/48**(47+1 신규), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16,
  MCP 11도구, `gate: OK`.
- 신규 `test_evidence_bands_carry_literature_not_a_convention`: (a) 밴드를 붙였다고 D3 값이
  밴드 안으로 끌려 들어가지 않았고(밖에 있는 것이 하나 남아 있다), (b) 승수 구간이 파일의
  `[low/value, high/value]` 포락과 1e-9 안에서 일치하며, (c) **1이 구간 안쪽이 아니라 끝에
  있다**. (c)가 깨지면 이 사이클의 결론이 성립하지 않는다.
- 페이퍼 §0 대장에 5개 key 추가(`g2_bands_added` 3, `g2_bands_outside` 1,
  `f3_param_share_steel_conv_w15` 21, `f3_param_share_steel_band_w15` 36,
  `m3_sources_registered` 80→81) + `inv_banded` 18→**21**, `inv_banded_t2t3t4` 0→**3**.
  `test_paper_numbers.py`가 `out/g2/summary.csv`에서 전부 재계산해 대조한다.
- **모형 입력은 바뀌지 않았다**: `git diff --quiet data/prepared/D3_tech_options.csv`가
  통과한다(바이트 동일). 밴드는 별도 파일 `D3b`로만 들어가고 `config.yaml` 무수정이다.
  `--bands`는 명시 플래그이며 기본 실행 경로는 규약 그대로다.

**결론 영향: G2 자체는 헤드라인을 바꾸지 않는다** — `D3_tech_options.csv`가 바이트 동일하므로
필연이다. **그러나 이 절이 처음 적은 "전후 동일"은 틀렸다.** 대조에 쓴 "전"이 나흘 낡은
산출물이었다. 정정과 실제 수치는 아래 D10-b에 있다.

**바뀐 것은 ③ TCaR의 파라미터분을 우리가 어떻게 인용할 수 있는가다 — "±30%를 가정했을 때의
몫"이라는 단서가 이제 "그 가정의 중심이 증거와 어긋난다"로 강해진다.**

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. T2·T3·T4 밴드 0 → 3, 등록부 80 → 81 | 미실행(D3 입력 바이트 동일) | 파라미터 위험이 규약이 아니라 문헌 대비 어디에 있는지 처음으로 한 표에 있다 | 게이트 전 항목 PASS, 커밋·푸쉬 완료 | 13종 (`docs/tech_band_upgrade.md` 신설) |

**인계**: 지도상 D11 = M4(결과 절 + 표·그림). **M4 전에 L2-b(제목이 걸린 선택)가 여전히
미결이고, 오늘 결과가 그 판단에 재료를 하나 더 얹는다** — 파라미터 축의 위험이 규약보다
크다면 "자본배분 경계"의 불확실성 진술 자체가 넓어진다. G3(설비 능력, D8→D10에서 다시 밀림)는
`fac.capacity`의 T2/T5 혼재를 푸는 작업이고 오늘 방식(값이 아니라 범위)을 그대로 쓰면 된다.
석화 `tech.*` 밴드(BASF·RSC·ChemRxiv 원문)는 한 번의 수집으로 LOTTE·MCI 열을 채운다.
**백로그 변경**: **G2 [~]**(완료 기준 변경 + 부분완료). G9에 D10 진척 기록. `docs/data_gap_registry.md`
에 G2 절 신설(막힌 것 4건: `h2_intensity` Vogl 403 · `elec_intensity` 경계 불일치 · 석화 전체 ·
`fac.capacity`). 페이퍼 §4.6 신설, §4.2 표에 D9→D10 열 추가, 한계 9에 D10 결과 추가.


## D10-b (06:00) — 정정: 대장은 나흘 낡은 out/에 대해 '검증됨'이었다

**무엇이 틀렸나**

D10 끝에 `python -m cap all`을 돌린 것은 mtime 경고를 지우기 위한 형식이었다. 그런데 수치가
움직였다. `git diff --quiet data/prepared/D3_tech_options.csv`가 통과하는데도 움직였다면
바뀐 것은 입력이 아니라 **코드**다 — `out/e2/plan_index.csv`는 **8월 9일 22:28**자였고,
그 뒤 D6(L2)·D7(M2)·D8(M8 `emis_cap`)이 `src/cap/`을 고쳤지만 아무도 파이프라인을 다시
돌리지 않았다. 네 사이클 동안 페이퍼 §0 대장은 **낡은 산출물에 대해** 통과하고 있었다.

`scripts/gate.py`가 못 잡은 이유가 정확히 여기 있다. freshness 검사는 `out/`을 **`data/raw`
하고만** 비교했다. 코드는 입력이 아니라고 본 것이다. 고쳤다 — `src/cap/*.py`와 `config.yaml`을
같이 센다. 이 한 줄이 없었으면 D11도 같은 것을 물려받았을 것이다.

**정정된 수치** (정본 `cap all`, 05:43)

| 대장 key | D10까지 인용 | 실제 |
|---|---|---|
| `tcar_nsc` | 32,973.1 | **33,047.2** |
| `gap_cost_nsc` / `gap_risk_nsc` | 1,164.4 / 4,364.3 | **1,097.4 / 4,038.7** |
| `e2_plans_total` / `_canonical` | 51 / 43 | **48 / 41** |
| `surrogate_rho_nsc` | −0.051 | **−0.105** |
| `riskproxy_rho_nsc` | 0.553 | **0.632** |
| `rho_central_p50_min` | 0.941 | **0.412** |
| `frontier_single_schedule_groups` | 14 | **16** |

②(천원/tCO₂) POSCO 115.0 · NSC 155.8 · MCI 241.7 · LOTTE 279.2 — **무변화**. CAPEX 피크
2041/2042/2041/2041 — **무변화**. 움직인 것은 NSC 한 회사이고, 전부 NSC 후보집합이 5→4개로
줄어든 데서 나온다.

**두 서술이 강해지고 하나가 약해진다.**

- **강해짐**: 경계 퇴화가 16개 묶음 중 14개 → **16개 전부**다. §4의 진단("경계를 따라 움직이는
  것은 PPA 비중뿐")에 예외가 없어졌다.
- **강해짐**: 후보 중복도 51/43 → 48/41로, 여전히 여덟 묶음 중 일곱에서 CCfD만 다른 쌍이다.
- **약해짐**: §3.4의 대조군이다. "중앙경로 비용 대 정본 P50 순위상관 최소 0.94"가 **0.41**
  (NSC B20)이 됐다. 재정렬이 몬테카를로 잡음이 아니라는 논거는 나머지 일곱 묶음(0.94~1.00)이
  여전히 지지하지만, **한 묶음에서는 그 대조가 성립하지 않는다**고 본문에 적었다.

**G2의 결론은 재실행 뒤에도 그대로다**: 승수 구간 `tech.capex` [1.000, 3.479] ·
`tech.emission_factor` [0.800, 1.000], 철강 파라미터 몫 ±15% **21% → 36%**, TCaR_param
×1.75~1.88. 밴드는 D3 값의 상대량이라 e2 재실행에 영향받지 않는다.

**검증**: `out/uncertainty`(양 모드)·`out/m8`·`out/m3`·`out/g2` 전부 새 e2/e4/e5 위에서
재생성했다. M8 대장 5개 key(32/32/4/25/7)는 **재실행 후에도 동일**하다 — D8의 결론은 낡은
산출물의 산물이 아니었다. `test_paper_numbers.py` 2/2 통과.

**미완 — 이 사이클에서 끝내지 못한 것**

06:00부터 **다른 세션이 같은 저장소에서 `python -m cap all`을 돌리고 있다**(D11로 보인다).
그 실행이 05:53에 `out/e2/plans`를 31개로 갈아치웠고, 그래서 지금
`test_consistency.py::test_production_plan_index_matches_plan_files`가 실패한다 —
plan_index(48행)와 plans 디렉터리(31개)가 다른 실행의 것이다. **내 변경 때문이 아니고,
그 실행이 끝나면 해소된다.** 같은 자원을 두고 두 사이클이 겹쳐 도는 것을 피하려고 재실행을
걸지 않았다. 같은 세션이 `src/cap/__main__.py`에 실행 설정 스탬프(`out/run_manifest.json`)와
`gate.py` provenance 검사를 추가하는 중이며, 그 두 파일의 미커밋 변경은 건드리지 않았다
(`gate.py`는 내 freshness 수정과 한 파일이라 함께 커밋됐다).

**인계**: **D11은 시작 전에 `scripts/gate.py`를 먼저 돌려라.** freshness가 이제 `src/cap`을
보므로, 코드를 고친 사이클은 `python -m cap all` 없이는 WARN이 뜬다. 그리고 out/e2 클로버가
해소됐는지(plan_index 행수 = plans 파일 수) 확인한 뒤 M4를 시작하는 것이 안전하다.
**백로그 변경**: gate freshness 범위 확대(코드도 입력). I3-b(`process_alternative.md` 재실행)는
같은 부류의 부채이고 이번 재실행으로 더 벌어졌을 수 있다 — D11 이후 확인 대상.

## D11 (06:00) — 대장이 흔들린 이유는 낡음이 아니라 병렬 솔버였다

**한 일**

지도상 D11은 M4(결과 절)였다. 시작 시 `gate: FAILED (tests)`였고 §2 규약대로 그것을 먼저
완결했다 — 그리고 실패의 원인이 M4가 인용할 표 자체였으므로 순서를 바꿀 이유가 없었다.

**처음 판정은 틀렸다.** `out/e2`·`out/e4`가 D10 커밋(05:30) 이후 05:43에 갱신된 것을 보고
"`--sims`를 줄인 실행이 정본을 덮었다"고 읽었다. 그 판정으로 `python -m cap all`을 기본
설정으로 다시 돌렸는데, **대장과 또 어긋났다 — 이번에는 다른 방향으로**. 그때 드러난 것:

- 05:43 실행(동시 실행 중이던 D10-b 인스턴스): `e2_plans_canonical` **41**, `surrogate_rho_nsc` **−0.105**
- 05:47 실행(내가 돌린 기본 설정): 같은 커밋·같은 시드인데 **42**, **−0.400**
- 06:15 실행(`solver_threads: 1`): **40**, **−0.564**

세 실행 모두 `data/prepared` 무변경, `config.yaml` 무변경, `seed: 20260806`, 48계획
전부 `solve_status = Optimal`. **시간제한 절단이 아니다.** 원인은 `mip_gap_rel: 0.02`
안에 동률에 가까운 최적해가 여럿 있고, `solver_threads: 6`의 병렬 CBC가 스레드 경합에 따라
**그중 다른 것을 반환**한다는 것이다. 즉 페이퍼 §0 대장은 기계 부하의 함수였다.

D10-b가 "대장은 나흘 낡은 out/에 대해 검증되고 있었다"고 적었을 때 그 진단은 맞았지만
절반이었다. 낡음을 고쳐도 **매 실행이 새 대장을 만든다**. 고친 것:

| 조치 | 파일 |
|---|---|
| `solver_threads: 6 → 1` (재현성을 속도로 산다 — e2 5분 → 16분) | `config.yaml` |
| 단계별 실행 설정 기록 (`data_dir`/`n_sims`/`n_sims_flex`/`frontier_points`/`seed`/`solver_threads`) | `src/cap/__main__.py` → `out/run_manifest.json` |
| 게이트 `provenance` 검사 — out/이 정본 설정의 6단계 전체 실행인가 (WARN) | `scripts/gate.py` |
| 대장 불일치 시 **어느 쪽이 틀렸는지**를 먼저 말한다 | `tests/test_paper_numbers.py` |

마지막 항목이 이 사이클의 실제 교훈이다. 05:44에 본 실패 메시지는 "페이퍼 §0 대장이 out/과
어긋난다"였고, 그 문장은 원고를 고치라는 뜻으로 읽힌다. **틀린 쪽은 out/이었다.** 산출물이
자기 출처를 들고 있지 않으면 진단은 매번 원고를 향한다.

**검증 (수치·테스트)**

- 게이트: 테스트 **50/50**, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구,
  freshness PASS, **provenance PASS**(6단계 전부 n_sims 10000·seed 20260806·threads 1), `gate: OK`.
- 신규 `test_run_manifest_records_the_settings_a_run_actually_used`: 축소 실행이 자기 이름을
  남기는가 + 단계 기록이 **병합**되는가(e1만 다시 돌렸다고 e2의 출처가 지워지면 같은 착오가 돌아온다).
- `out_test/`를 pid로 갈랐다 — 두 pytest가 겹치면 한쪽이 쓰는 중인 계획 파일을 다른 쪽이
  지우고 FileNotFoundError 10건이 난다. 이 사이클에 두 번 났고 두 번 다 실재 결함이 아니었다.
- 재실행: `cap all`(기본 10000 sims, threads 1) → `uncertainty_propagation.py`(F3 기본) →
  `--bands` → `g2_band_impact.py`. 계획 집합이 바뀌었으므로 F3·G2도 같이 다시 냈다.

**결론 영향: 헤드라인 ② 무변화(소수 첫째 자리까지), ③ TCaR NSC −0.3%.**

| 지표 (NZ15·none) | 전 (05:43 실행) | 후 (06:15 실행, threads 1) |
|---|---|---|
| ② 천원/tCO₂ POSCO / NSC / MCI / LOTTE | 115.0 / **155.8** / 241.7 / 279.2 | 115.0 / **155.6** / 241.7 / 279.2 |
| ③ TCaR 십억원 POSCO / NSC / MCI / LOTTE | 26,753 / **33,047** / 864 / 2,242 | 26,753 / **32,961** / 864 / 2,242 |
| gap NSC 비용 / 위험 (십억원) | 1,097 / 4,039 | **1,255 / 4,651** |
| `e2_plans_canonical` | 41 | **40** |
| `surrogate_rho_nsc` / `riskproxy_rho_nsc` | −0.105 / 0.632 | **−0.564 / 0.553** |
| L2 탄소 증분 NSC / POSCO (십억원) | +1,207 / +2,444 | **+2,159 / +3,064** |

**순위·부호는 세 실행 전부에서 같다** (② POSCO < NSC < MCI < LOTTE, L2 증분 철강 +·석화 −).
흔들린 것은 후보집합의 내부 구성이지 결론이 아니다 — 그러나 **L2 증분은 동률 계획 한 쌍의
교체에 79% 움직였다**. 이 축의 절대값을 인용하려면 후보 생성이 먼저 넓어져야 한다(M8).

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 산출물이 처음으로 자기 실행 설정을 들고 있다 | 미실행(입력 무변경, 계획 집합만 재현 가능해짐) | ③의 인용 가능 범위가 좁아졌다 — 부호·순위는 되고 L2 증분 절대값은 아직 아니다 | 게이트 전 항목 PASS(신설 provenance 포함), 커밋·푸쉬 완료 | 13종 (`out/run_manifest.json` 신설) |

**인계**: **M4(결과 절)를 하지 못했다** — D12로 밀린다. §5-A 우선순위가 M > G이므로 D12는
G4가 아니라 **M4**부터다. 두 가지가 M4 전에 정리돼야 한다: (a) `out/m8/`은 옛 계획 집합에서
만들어졌고 이번 재실행에 따라오지 않았다 — `docs/frontier_degeneracy.md`의 32개 상한과
비지배 4/25는 다시 내야 한다, (b) L2-b(제목이 걸린 선택)는 D8부터 미결이고 오늘 결과가
그 판단을 더 밀어붙인다.

**동시 실행 사고**: 05:57 커밋 `e2700ea`(D10-b)는 **다른 인스턴스가 이 세션과 겹쳐 돌면서**
만든 것이고, 그 커밋에 이 세션이 작업 중이던 `scripts/gate.py`·`tests/test_paper_numbers.py`
편집분이 함께 실려 갔다. 같은 저장소에서 두 사이클이 동시에 도는 상태를 스케줄이 허용하고
있다 — 다음 창을 열기 전에 잠금이 필요하다. **백로그 변경**: **K-lock 신설**(사이클 동시
실행 방지), M4 D11→D12, 페이퍼 §6 한계 **11** 신설, 한계 7·10의 낡은 수치 정정.

## D11-b (07:30) — 재현성은 주장됐지만 검증되지는 않았다

**한 일**

D11 커밋(a86f2ad)은 `solver_threads: 6 → 1`로 "재현 가능"을 샀다고 적었다. 근거는 병렬
실행 세 번이 서로 달랐다는 것이었다 — **단일 스레드 두 번이 같다는 확인은 없었다.** 그것은
원인 진단이지 수정의 검증이 아니다. `threads=1`도 60초 시간상한에 걸리면 절단 시점이
기계 부하의 함수가 되고, 단일 스레드는 병렬보다 **느리므로 그 상한에 더 가깝다** — 고친 것이
같은 병을 다른 문으로 들일 수 있었다. 그래서 A/B를 돌렸다.

같은 커밋·같은 시드·같은 설정으로 `python -m cap e2`를 두 번 (06:15–06:37, 07:03–07:25).

**검증 (수치·테스트)**

| 대조 | 결과 |
|---|---|
| `out/e2/plans/*.csv` 48개 | **48/48 바이트 동일** (`diff -r -q`) |
| `out/e2/plan_index.csv` | **바이트 동일** (`cmp`) |
| `solve_status` | **48/48 Optimal** — 시간상한 절단 0건, 두 실행 모두 |
| 묶음별 소요 | 785/790s (NSC NZ15), 219/222s, 102/104s, 88/94s — 최대 6% 차 |

**시간상한이 안 걸린다는 것이 재현성의 조건이다.** 가장 느린 해(NSC NZ15)가 10점을 785초에
푸는데 점당 상한은 60초다 — 즉 개별 해는 상한 한참 아래이고, 790초의 대부분은 1,110 이진변수
모형을 열 번 세우는 시간이다. 이 여유가 사라지면(설비 추가·연도 확장·상한 인하) `threads=1`
만으로는 재현이 보장되지 않는다. **`solve_status`에 `Optimal` 아닌 행이 하나라도 생기는 순간
대장은 다시 기계의 함수가 된다** — 이것이 앞으로 볼 지표다.

게이트: 테스트 **50/50**, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, freshness PASS,
provenance PASS(6단계 전부 정본, n_sims 10000, seed 20260806), MCP 11도구, `gate: OK`.

**결론 영향: 헤드라인 수치 무변화** — run2가 run1과 바이트 동일이므로 필연이다. ②
POSCO 115.0 · NSC 155.6 · MCI 241.7 · LOTTE 279.2, ③ TCaR NSC 32,961.3 — D11 커밋 값 그대로.
바뀐 것은 **그 값을 어떤 자격으로 인용할 수 있는가**다. D11은 "재현 가능하게 만들었다"였고,
D11-b 뒤에는 "같은 커밋·시드의 두 실행이 48개 계획 파일까지 바이트 동일임을 확인했다"이다.
페이퍼 §0의 "기준 실행" 문구가 그제서야 문자 그대로 참이 된다.

**이 사이클이 낭비한 것 — 두 세션이 같은 D11을 동시에 돌았다**

06:15에 내가 시작한 `cap all`과 다른 세션의 D11이 겹쳤다. 그쪽이 06:44에 a86f2ad를 커밋했고,
내 미커밋 편집(`tests/test_consistency.py::test_canonical_run_is_reproducible_not_load_dependent`)이
그 커밋에 함께 실려 갔다 — 내용은 살았지만 **저자와 커밋 메시지가 어긋난다**. 22분짜리 e2를
두 세션이 각자 돌아 컴퓨트도 두 배 썼다. D10-b가 같은 것을 보고 K-lock을 백로그에 올렸는데,
그 다음 사이클에서 곧바로 재발했다. **백로그 항목으로는 못 막는다 — 스케줄러 쪽 잠금이거나,
사이클 시작 시 `out/`에 실행 중 표식을 남기는 것이거나.** 다만 오늘 창은 09:00에 닫히므로
이번 실행에서 스케줄을 건드리지 않는다(태스크 §6 금지).

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0 | 미실행(e2 산출 바이트 동일) | 대장 인용 자격이 "주장"에서 "확인"으로 | 게이트 8항목 전부 PASS | 13종, 신규 없음 |

**인계**: **D12 = M4(결과 절 + 표·그림).** 이제 인용할 표가 흔들리지 않는다는 것이 확인됐으므로
막을 것이 없다. 시작 전 `scripts/gate.py`만 돌려라. **e2 재실행은 22분이다** — M4는 out/을
읽기만 하므로 재실행이 필요 없고, 필요해지면 그 시간을 사이클 예산에 넣어라.
**백로그 변경**: **K-lock 승급 — 백로그가 아니라 D12 착수 전 확인 항목**(다른 세션이 도는지
`ps`로 먼저 본다). G3(설비 능력)·석화 `tech.*` 밴드는 D10 인계 그대로 미착수.

## D12 (07:13) — 결과 절을 쓰자 지원 축이 비어 있다는 것이 보였다

**한 일**

§5-A 지도상 D11이 M4였고 D11이 밀렸으므로 이번이 M4다(우선순위 M > G, D11-b 인계 그대로).
시작 시 `gate: OK`, 다른 세션의 `cap all` 없음(K-lock 확인 — `ps`로 봤다).

§5 결과 절을 썼다. §4(M3)와 같은 규약이다 — 본문 표를 손으로 적지 않고
`scripts/results_section_table.py`가 `out/e5`에서 다시 낸다
(`out/m4/{headline,frontier_ladder,regret,summary}.csv`). 스칼라 **15개**를 §0 대장에
등록했고 `tests/test_paper_numbers.py`가 대조한다. 파이프라인은 돌리지 않았다 —
M4는 `out/`을 읽기만 한다(D11-b 인계).

절은 넷이다: 5.1 두 숫자(②③·gap), 5.2 경계는 계약 사다리, 5.3 정책 wedge와 후회비용,
5.4 빈 지원 축.

**절을 쓰면서 드러난 것 셋**

**(1) `support` 축이 정보를 하나도 담고 있지 않다.** `none`과 `current`가 기업×시나리오
8칸 **전부**에서 ②③ P50·CAPEX·유연성·정책노출까지 완전히 같다. 모형 결함이 아니라 데이터
공백이다 — `plancost.support_params`가 지원 시나리오에서 읽는 수단은 `subsidy_capex`·`ccfd`
둘뿐인데 D5에 그 행이 **각각 0개**고, D5가 실제로 담은 K-ETS 4기 유상할당(15%)·GX-ETS
가격 collar는 지원 여부와 무관하게 `auction_share`로 **항상** 적용된다. 표에 열이 있으면
독자는 "지원 정책을 봤다"고 읽는다. **보지 않았다.** 이 축은 C11(D5 정본화) 이후 계속
산출표에 있었고 아무도 그것이 비어 있는지 센 적이 없다 — 결과 절을 쓰기 전에는 셀 이유가
없었기 때문이다.

**(2) 완화 시나리오가 같은 계획의 꼬리위험을 더 크게 만든다.** 네 회사 **전부**에서
NZ15 TCaR < B20 TCaR. 통념과 반대지만 기제는 단순하다 — 위험은 남은 배출이 마주하는
탄소가격의 **분산**에서 오고, NZ15는 수준이 높은 대신 예산에 묶여 덜 흩어진다. 한계 5
("정책 위험은 규제 강화가 아니라 가격 붕괴로 들어온다")가 여기서 숫자가 됐다.

**(3) 후회비용이 계약 헤지보다 한 자릿수 크다.** NZ15 계획을 들고 B20이 실현되면 그
시나리오 최적 대비 POSCO **28,636.4**·NSC **31,629.1**십억원을 더 쓴다. 경계 사다리가 사는
위험(수천억–수조)과 자릿수가 다르다. 그러나 **역방향은 계산되지 않았다** —
`out/e5/policy_wedge.csv`가 NZ15 계획만 담는다. 비대칭을 모르면 "지연이 합리적"으로 읽힌다.

**검증 (수치·테스트)**

- 게이트: 테스트 **51/51**(신규 1), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16,
  MCP 11도구, freshness PASS, provenance PASS(6단계 전부 n_sims 10000·seed 20260806·threads 1),
  `gate: OK`.
- 신규 `test_support_axis_is_empty_and_the_paper_says_so`: D5의 지원 수단 행 유무와 축의
  작동이 **일치하는가**(`inert != has_rows`). 보조금 행이 들어오는 날 깨지고, 그때 §5.4의
  "이 축은 비어 있다"도 같이 고쳐진다. 값이 아니라 **서술**을 지키는 테스트다.
- `frontier_ladder`의 "위험 1원의 값"에서 LOTTE 0.25→0.5 칸이 **2306.479**로 나왔다.
  TCaR 감소가 보고 정밀도(0.1십억원) 아래라 분모가 0에 가까웠던 것이다. 그 칸은 비운다.
- 재실행 없음(읽기 전용 사이클). `scripts/results_section_table.py`만 3회 실행.

**결론 영향: 헤드라인 수치 무변화.** ② POSCO 115.0 · NSC 155.6 · MCI 241.7 · LOTTE 279.2,
③ TCaR 26,752.6 / 32,961.3 / 864.1 / 2,242.2 — D11-b와 같다(파이프라인 미실행이므로 필연).

바뀐 것은 **그 수치에 붙는 자격**이다.

| 항목 | 전 (D11-b) | 후 (D12) |
|---|---|---|
| 지원 정책 효과 | 표에 열이 있으므로 "봤다"로 읽힘 | **재지 않았다** — 한계 12로 명문화, gap registry 등록 |
| 시나리오 축 진술 | ②③의 수준만 | ② 순위 보존(`m4_m2_rank_preserved`=1) + 후회비용 28.6조/31.6조 |
| 꼬리위험의 방향 | L2에서 "가격 붕괴" 가설 | 네 회사 전부에서 NZ15 TCaR < B20 TCaR로 확인 |
| 페이퍼 절 | §3·§4 초안 | **§5 초안 추가** (남은 것 §1·§2·§6·§7·§8) |
| §0 대장 key | 88 | **103** (+15, 전부 `out/m4/summary.csv`) |

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. **공백 1건 신규 등록**(D5 subsidy·ccfd) | 미실행. 다만 시나리오 축의 **후회비용**이 처음으로 수치화됐고 ② 순위 보존이 확인됐다 | 셋 다 좋아졌다 — 계약 사다리(무엇을 사면 위험이 얼마 주는가), 후회비용(왜 지연하는가), 빈 지원 축(무엇을 아직 못 말하는가) | 게이트 8항목 PASS, 커밋·푸쉬 완료 | **14종** (`out/m4/` 신설) |

**인계**: 지도상 다음은 **D13 = L3(기여 진술 + 문헌 대비 우리 값 위치)**. 다만 §5가 만든
빚 두 개가 L3보다 싸고 결론에 더 가깝다 — (a) **역방향 후회비용**(B20 계획을 NZ15에서 평가):
`e5_metrics.policy_wedge`에 B20 경계 계획을 추가하면 되고 재실행 없이 e4 산출로 가능한지
먼저 확인할 것, (b) `out/m8/`은 06:44 재생성분이라 `docs/frontier_degeneracy.md`의 32개
상한·비지배 4/25 서술이 그 실행과 맞는지 대조가 남았다(D11 인계 이월, 이번에도 못 했다).
우선순위 규칙은 M > L이므로 M5(D14) 전에 (a)를 끼우는 것이 §5.3을 완성한다.

**백로그 변경**: **한계 12(빈 지원 축)·13(한 방향 후회비용) 신설.** data_gap_registry에
D12 절 추가(`subsidy_capex` 한국·일본, `ccfd`는 양국 모두 제도 부재 — 넣으면 실측이 아니라
가상 정책 실험으로 명시해야 한다). G3(설비 능력)·석화 `tech.*` 밴드·I3-b는 D10부터 미착수 그대로.

## D13 (07:47) — 후회비용의 반대 방향을 재자 결론의 근거가 옮겨갔다

**한 일**

§5-A 지도상 D13은 L3(기여 진술)이다. **L3을 하지 않고 D12 인계의 (a) 역방향 후회비용을
했다.** 근거는 §5-A의 우선순위 규칙 M > L이다 — (a)는 §5.3을 완성하는 M계열이고, L3은 그
위에 얹는 진술이다. 순서를 뒤집으면 아직 부호가 안 정해진 결과를 놓고 기여를 쓰게 된다.
시작 시 `gate: OK`, 다른 세션 `cap all` 없음(K-lock 확인).

`e5_metrics`의 `wedge_store`가 1차 시나리오 경계만 담던 것을 **모든 시나리오**로 넓히고
`scen_origin`으로 구분했다. 같이 `p50_incl_carbon`·`budget_gap_tco2` 두 열을 냈다.
`results_section_table.py`의 3번 표를 양방향으로 다시 썼다. e5는 2.1초라 재실행이 쌌다.

**절을 쓰면서 드러난 것 둘 — 둘 다 D12가 이미 적어 놓은 것을 뒤집는다**

**(1) D12의 후회비용은 시나리오 간 비교가 불가능한 기준으로 계산돼 있었다.** `policy_wedge`의
`p50`은 다른 산출표와 맞추려고 탄소지출을 뺀 **자원비용** 기준이다(설계서 §4, 시나리오
*안에서* 경계가 탄소회피에 지배되지 않게 하려는 규약). 그런데 시나리오를 **가로지르면**
덜 감축하는 계획일수록 내야 할 탄소요금이 함께 빠지므로 싸 보인다. D12는 그 기준을 그대로
후회비용에 썼다. 총비용 기준으로 다시 재면 POSCO 28,636.4 → **22,100.1**, NSC 31,629.1 →
**23,315.2**. 부호와 순서는 살았지만 인용할 값이 바뀐다.

**(2) 총비용 기준의 역방향 후회는 네 회사 중 셋에서 음수다.** B20 계획을 들고 NZ15가 와도
POSCO는 **7.8조를 덜 쓴다** — 예산을 초과하고 그만큼의 탄소요금을 내면 되기 때문이다.
역방향에서 실제로 다른 것은 돈이 아니라 **예산 초과량**(POSCO 213.0·NSC 251.9MtCO2)이다.
D12가 "비대칭을 모르면 지연이 합리적으로 읽힌다"고 경고했는데, **재어 보니 돈만 보면 실제로
지연이 이긴다.** 조기 전환을 지지하는 근거는 가격 전망이 아니라 **탄소예산이 수량으로
강제되는가**에 있다.

손익분기(초과 1톤을 얼마로 보면 역방향 후회가 0인가)는 POSCO 36.5·NSC 39.4·LOTTE
4.8천원/tCO2다. E2는 초과를 `max(2×탄소가격, 300천원/tCO2)`로 벌하므로 모형 안에서는 조기
행동이 8배 여유로 이기지만, **그 300은 `config.yaml`의 수렴용 바닥값이고 `evidence_tier`도
`source_id`도 없다.** 8배 여유는 데이터가 아니라 그 한 줄이 만든 것이다.

**검증 (수치·테스트)**

| 대조 | 결과 |
|---|---|
| e5 재실행 뒤 나머지 산출물 8종 | `metrics_company`·`gap` 등 **바이트 동일** (`cmp`) — 변경이 wedge에 격리됨 |
| `budget_gap_tco2` 교차검산 | 자기 출신 시나리오에서 E2 `budget_slack_tco2`를 **0.2tCO2 이내 재현** (POSCO NZ15 22,744,956.5 대 .690) |
| `scripts/build_report.py` | 그림 6은 `scen_origin=="NZ15"`만 그려 모양 무변화, 259,438바이트 생성 |
| 게이트 | 테스트 **52/52**(신규 1), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구, freshness PASS, provenance PASS, `gate: OK` |

신규 `test_regret_is_two_directional_and_its_sign_is_conditional`이 셋을 건다 — wedge가 두
출신을 모두 담는가(한 방향으로 되돌아가면 깨진다), `budget_gap_tco2`가 E2 slack을 재현하는가,
**손익분기가 벌칙 바닥보다 낮은가**. 셋째가 이 사이클의 핵심이다: 손익분기가 300을 넘는
순간 모형 안에서도 지연이 이기므로 §5.3.1과 한계 13을 다시 써야 한다.

**결론 영향: 헤드라인 수치 무변화.** ② POSCO 115.0 · NSC 155.6 · MCI 241.7 · LOTTE 279.2,
③ TCaR 26,752.6 / 32,961.3 / 864.1 / 2,242.2 — D12와 같다(`metrics_company.csv` 바이트 동일).

바뀐 것은 **§5.3이 무엇을 주장하는가**다.

| 항목 | 전 (D12) | 후 (D13) |
|---|---|---|
| 후회비용 (POSCO) | 28,636.4 (자원비용, 단방향) | 순방향 총비용 **22,100.1** / 역방향 **−7,766.5** |
| 정책 진술의 근거 | "시나리오 불확실성이 지연의 이유" | **"예산을 수량으로 강제하는가"** — 가격 전망이 아니다 |
| 역방향 | 미계산 (한계 13) | 계산됨. 셋에서 음수(`m4_rev_regret_negative`=3), 손익분기 36.5–39.4천원/tCO2 |
| 한계 13 | "한 방향만 계산됐다" | **"부호가 벌칙 가정에 조건부다"**로 대체 |
| `budget_violation_floor_thkrw` | 눈에 띈 적 없음 | 결론을 지탱하는 **무출처 T5 가정**으로 승격, gap registry 등록 |
| §0 대장 key | 103 | **116** (+13, 전부 `out/m4/summary.csv`) |

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. **공백 1건 신규 등록**(예산 초과 벌칙 한·일) | 미실행. 다만 시나리오 축이 처음으로 **양방향**이 됐고 순위 역전은 없다 | 좋아졌다 — "지연의 대가"라는 팔기 쉬운 문장이 "돈만 보면 지연이 이긴다, 예산 강제가 관건"이라는 **팔기 어렵지만 맞는 문장**으로 바뀌었다 | 게이트 8항목 PASS, 커밋·푸쉬 완료 | 14종, 신규 없음 (`out/m4/regret.csv` 스키마 확장) |

**인계**: 지도상 다음은 **D14 = M5(강건성·한계 절)**. 오늘 결과가 M5의 재료를 하나 만들어
줬다 — `budget_violation_floor_thkrw`는 이제 결론을 지탱하는 무출처 가정이므로 **민감도 축**
으로 올리는 것이 M5의 첫 항목이 되어야 한다(손익분기 36.5–39.4를 기준선으로). L3(기여 진술)은
D13에서 밀렸고 M > L이므로 M5 뒤다. **백로그 변경**: 한계 13 재작성(신설 아님, 대체).
`docs/data_gap_registry.md` D13 절 추가. `out/m8/` 대조는 D11부터 세 사이클째 미착수 —
`docs/frontier_degeneracy.md`의 32개 상한·비지배 4/25가 06:44 재생성분과 맞는지 아직 모른다.
G3(설비 능력)·석화 `tech.*` 밴드·I3-b는 D10부터 미착수 그대로.

## D14 (08:35) — 강건성 절을 쓰자 시나리오 묶음 넷이 헤드라인에 닿을 통로가 없었다

**한 일**

§5-A 지도상 D14 = **M5(강건성·한계 절)**. 지도대로 했다. 시작 시 `gate: OK`, 다른 세션
`cap all` 없음.

§6(강건성)·§7(한계·반증)을 썼다. §4(M3)·§5(M4)와 같은 규약이다 — 본문 표를 손으로 적지 않고
`scripts/robustness_section_table.py`가 `out/`에서 다시 낸다
(`out/m5/{checks,bundle_matrix,penalty_axis,summary}.csv`). §6은 저장소에 이미 있는 검증
여덟(I2·I4·F3×2·L1·M8·H2 + D14 신규)을 한 표로 모으고, §7은 한계 14개 중 **결론 방향을 바꿀
수 있는 다섯**과 반증가능 주장 다섯의 상태만 적는다.

D13 인계였던 `budget_violation_floor_thkrw`는 **두 통로로 나눠** 쟀다. 하나는 값매김(사후,
재실행 불필요), 하나는 탐색(`penalty_none` 묶음, E2 재계획 20분). 둘을 섞으면 "이 가정이
결론을 좌우한다"가 어느 뜻인지 알 수 없다.

**절을 쓰면서 드러난 것 둘**

**(1) 시나리오 묶음 넷이 헤드라인 ②③을 한 자리도 바꾸지 않는다.** `carbon_slow`·
`carbon_fast`·`ppa_costly`·`retire_free`. 이 축들(`carbon_auction_share`, `ppa_premium_pct`,
`retire_max_share`)은 계획 선택에 관한 한 **E2 MILP에서만 읽히는데**, `run_scenarios.py`는
계산을 아끼려 E1·E2를 심볼릭 링크로 공유하고 E3–E5만 다시 돈다. **평평했던 것이 아니라 흔든
적이 없다.** D12의 빈 지원 축과 같은 종류이고, 이번에도 결과 절을 쓰기 전에는 셀 이유가 없었다.

넷이 다시 둘로 갈린다 — `ppa_costly`·`retire_free`는 **모든 열에서** base와 같고,
유상할당 두 묶음은 E5의 탄소지출에는 닿아 **탄소 포함 P50을 22~24조원** 움직이면서도 계획은
못 바꾼다. 낼 돈은 바뀌는데 할 일은 못 바꾼다. 가장 아픈 곳이 `carbon_fast`(2040 전량
유상)다: 네 회사가 24조원을 더 무는데 그 압력에 대응해 계획을 다시 고를 자유가 없는 실행에서
"감축단가 순위가 안 바뀐다"는 안심을 팔 뻔했다.

**이 진단 자체가 한 번 틀렸다.** 처음 만든 `bundle_matrix`가 `p50_incl_carbon`을 빼고 재는
바람에 `carbon_slow`·`carbon_fast`를 "완전 무력"으로 판정했고, 그 상태로 §6.2 초안과 gap
registry를 썼다. `penalty_none` 결과를 받아 요약표를 다시 보니 그 두 묶음의 탄소 포함 P50이
크게 움직여 있었다. `KEYS`에 그 열을 넣고 다시 재서 "완전 무력 2"와 "헤드라인 무력 4"를
갈라 적었다. **묶음이 무엇을 못 바꾸는지는 어느 열을 보느냐에 달려 있었다.**

**(2) 벌칙 바닥은 탐색이 아니라 값매김에서 결론을 좌우한다.** 탐색 통로를 실제로 재 보니
(`penalty_none`, 300 → 0, `--replan`) 계획 메뉴는 바뀌지만 **순위는 보존되고 변화폭이 ② 1.5% ·
③ 5.9%**에 그친다 — 할인율(6.1%/39.2%)이나 수소가격(27.0%/30.4%)보다 작다. 값매김 통로는
반대다: 부호 전환점이 최대 39.4천원/tCO2로 인벤토리 밴드 [150, 600] 하단보다 **3.8배 아래**라
밴드 전 구간에서 네 회사 모두 조기 전환이 이기지만, **φ=0은 밴드 밖의 값이 아니라 다른
제도**이고 거기서는 넷 중 하나(MCI)만 이긴다. 즉 밴드 안의 강건성이 이 결론을 방어하지
못한다. 방어하는 것은 "초과에 실제로 제재가 붙는가"라는 제도 사실 하나다.

**검증 (수치·테스트)**

| 대조 | 결과 |
|---|---|
| 게이트 | 테스트 **55/55**(신규 3), 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구, freshness PASS, provenance PASS, `gate: OK` |
| `out/e5` 무변경 | 묶음은 `out/scenarios/`에 쓴다. 헤드라인 CSV 그대로 |
| `penalty_none` 재계획 | E2 20분. NSC ② 155.8→158.1 ③ 32,973.1→31,016.3, POSCO ② 115.0→116.8 ③ 26,752.6→27,164.7, 석화 2사 무변화, 순위 보존 |
| 순위 역전 | 묶음 11개 + 판정 가능한 검증 3개 **전부에서 0회** (`m5_bundle_rank_reversals` = 0) |
| F3 폭 혼입 수정 | `decomposition.csv`가 width 0.15·0.30을 함께 담는데 필터가 없어 "파라미터 몫 13~44%"로 나왔다. ±30%만 남겨 **26~44%**(밴드 26~54%) |

신규 `tests/test_scenario_bundles.py` 셋 — (a) `BUNDLES`에 E2 전용 축을 넣고
`REPLAN_REQUIRED`에 안 넣으면 깨진다, (b) 설명되지 않은 '변화 0'이 새로 생기면 깨진다,
(c) `--replan`으로 돌았는데도 base와 같으면 깨진다. **값이 아니라 대응을 지키는 테스트**다.

**결론 영향: 헤드라인 수치 무변화.** ② POSCO 115.0 · NSC 155.6 · MCI 241.7 · LOTTE 279.2,
③ TCaR 26,752.6 / 32,961.3 / 864.1 / 2,242.2 — D13과 같다(`out/e5` 미실행).

바뀐 것은 **강건성 주장의 범위**다.

| 항목 | 전 (D13) | 후 (D14) |
|---|---|---|
| 시나리오 묶음 | 10개 전부 "흔들어 봤다" | **넷은 헤드라인에 닿을 통로가 없었다**(둘은 모든 열에서 무력). 한계 14 신설 |
| 유상할당 가속 | 결론 불변으로 읽힘 | 탄소요금 **+24조원**, 계획 재선택 자유 없음. 순위 영향은 **모른다** |
| 벌칙 바닥 | "결론이 이 가정에 조건부" | 통로 분리 — 탐색은 ②1.5%/③5.9%, **값매김이 부호를 정한다** |
| 조기 전환 근거 | 밴드 [150,600]에서 방어된다고 말할 뻔 | **밴드가 아니라 제도 사실**이 방어한다(φ=0에서 1/4) |
| 페이퍼 절 | §3·§4·§5 초안 | **§6·§7 초안 추가** (남은 것 §1·§2·§8) |
| §0 대장 key | 96 | **107** (+11) |

> 커밋 본문에 대장 key를 "116 → 127"로 적었으나 실제 표 행 수는 96 → 107이다. 116은 D13
> 로그가 쓴 수이고 그 수의 출처를 확인하지 못했다. 대장 자체는 테스트가 대조하므로 값은
> 안전하고, 틀린 것은 개수 서술뿐이다.

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. 공백 1건 신규 등록(실행 규약) | **묶음 11 → 12**(`penalty_none`). 순위 역전 0. 다만 넷은 재계획 전까지 분석툴로 못 쓴다 | 좋아졌다 — "여덟 축을 흔들어도 순위가 안 바뀐다"는 팔 문장과, "그중 넷은 흔든 적이 없다"는 팔기 어려운 문장이 같은 절에 있다 | 게이트 8항목 PASS, 커밋·푸쉬 완료 | 15종 (`out/m5/` 신설), `web/scenarios.html` 갱신 |

**인계**: 지도상 다음은 **D15 = M6(서론·선행연구 절)**. 그 전에 D14가 만든 빚이 하나 있고
M계열이라 우선순위가 앞선다 — **`carbon_fast`·`carbon_slow`·`ppa_costly` 재계획**(묶음당
~10분, 셋이면 30분). `carbon_fast`가 순위를 건드리면 §6.2와 한계 14를 다시 써야 하고, 그것이
정책적으로 가장 인용될 축이다. 다만 M6도 M계열이므로 지도를 따를 것인지 빚을 갚을 것인지는
사이클 예산으로 판단한다 — 재계획 셋은 한 사이클에 안 들어간다.
**백로그 변경**: **한계 14 신설**(시나리오 묶음 넷에 통로가 없다). `docs/data_gap_registry.md`
D14 절 추가. **FC3(헤지 가격의 업종 효과 통제)은 M1이 M5에서 하겠다고 적었으나 D14에서 못
했다** — 4사 표본에서 업종과 규모를 가르는 설계가 성립하는지부터 판단해야 한다. `out/m8/`
대조는 D11부터 네 사이클째 미착수. G3(설비 능력)·석화 `tech.*` 밴드·I3-b는 D10부터 그대로.

## D15 (08:58) — 서론·선행연구를 쓰고, "흔들 통로가 없다"던 축을 열자 그것이 최대 축이었다

**한 일**

§5-A 지도상 D15 = **M6(서론·선행연구 절)**. 지도대로 하되, D14 인계였던 `carbon_fast`
재계획을 **같은 사이클 안에 병렬로** 돌렸다 — 재계획은 E2 MILP 20분이고 §1·§2 집필은
`out/`을 읽지 않으므로 충돌하지 않는다. 시작 시 `gate: OK`.

본문 §1(서론)·§2(선행연구)를 썼다. **원고 본문에서 남은 것은 §8 결론(M7) 하나다.**

§1은 §7 저널 표의 결정대로 Energy Policy 독자 기준이다. 기존 도구(`CA100_BENCHMARK_V22`,
`NATCOMM_APA_2026`)가 답하는 것은 "정합인가 아닌가"이고, 그 판정을 받은 기업이 다음에 묻는
"얼마인가"에는 답이 없다. §1.4에 **이 논문이 아직 할 수 없는 말 셋**을 결과 절보다 먼저
적었다. §2는 `docs/literature_map.md`의 판정 셋을 그대로 옮기되, 주장 ②에서는 **문헌이
우리보다 앞서 있었다**는 것과 그 지적대로 재어 보니 답이 업종별로 갈렸다는 것을 같이 적는다.

**드러난 것 — D14의 판정 하나가 틀렸다**

D14는 `carbon_fast`(2040년 전량 유상할당)를 "헤드라인 무력"으로 판정하고 그 위에 §6.2·한계
14·gap registry 항목을 썼다. 판정의 원인이 **그 실행이 E2를 공유했기 때문**이라는 것을
알면서도 재계획 결과를 기다리지 않고 절을 썼다. D15에서 `--replan`으로 다시 풀었더니
판정이 뒤집혔다.

| 기업 | ② base → carbon_fast | ③ base → carbon_fast |
|---|---|---|
| POSCO | 115.0 → **137.8** | 26,752.6 → **40,811.3** |
| Nippon Steel | 155.8 → **160.1** | 32,973.1 → **34,653.9** |
| Mitsui Chemicals | 241.7 → **263.9** | 864.1 → **1,205.3** |
| LOTTE Chemical | 279.2 → **317.0** | 2,242.2 → **3,620.3** |

`m5_d_m2_pct_carbon_fast` **0.0 → 19.7**(수소가격 다음으로 큰 축),
`m5_d_tcar_pct_carbon_fast` **0.0 → 61.5** — **흔든 모든 축 중 ③에서 가장 크다**. 할인율
3.5%(39.2%)를 넘는다. 메커니즘도 읽힌다: 계획 메뉴가 고정됐을 때는 24조원의 추가 탄소요금이
전부 비용으로만 쌓였는데, 재계획을 허용하자 기업이 더 비싼 감축을 앞당겨 사서 요금을 피하고
**감축단가와 조달부담이 함께 오른다.**

**순위는 보존된다** (137.8 < 160.1 < 263.9 < 317.0, `m5_bundle_rank_reversals` = 0). 즉
D14가 팔 뻔했던 "유상할당이 빨라져도 순위는 안 바뀐다"는 **결과적으로 참**이었다. 그러나
D14 시점에 그 문장은 근거 없이 참이었고, 근거가 생긴 지금 **같은 근거가 크기에 대해서는
정반대**를 말한다. 순위만 보고 "결론을 바꾸지 않는다"고 읽으면 조달부담 61.5% 증가를 놓친다.

**검증 (수치·테스트)**

| 대조 | 결과 |
|---|---|
| 게이트 | 테스트 **55/55**, 독립성 PASS, audit ok 68/PARTIAL 4/설계정상 16, MCP 11도구, freshness PASS, provenance PASS, `gate: OK` |
| `out/e5` 무변경 | 재계획은 `out/scenarios/carbon_fast/`에만 쓴다. 헤드라인 CSV 그대로 |
| `carbon_fast` 재계획 | E2 8묶음 20분. `replanned=True`로 요약표에 기록 |
| 순위 역전 | 묶음 11개 전부에서 **0회** — 재계획 후에도 유지 |
| `tests/test_paper_numbers.py` | 신규 대장 key 2개(`m5_d_m2_pct_carbon_fast`, `m5_d_tcar_pct_carbon_fast`)가 `out/m5/summary.csv`와 일치 |

`scripts/robustness_section_table.py`에 `m5_d_tcar_pct_<bundle>` 한 줄을 늘렸다 — ③ 변화폭이
이제 결론을 지탱하는데 대장에 등록할 key가 없었다. 신규 테스트는 없다(기존
`test_scenario_bundles.py`의 (c) 조항이 "재계획했는데 base와 같으면 깨진다"를 이미 건다).

**결론 영향: 헤드라인 수치 무변화.** ② POSCO 115.0 · NSC 155.6 · MCI 241.7 · LOTTE 279.2,
③ TCaR 26,752.6 / 32,961.3 / 864.1 / 2,242.2 — D14와 같다(`out/e5` 미실행).

바뀐 것은 **강건성 주장의 내용과 원고의 완성도**다.

| 항목 | 전 (D14) | 후 (D15) |
|---|---|---|
| 본문 절 | §3–§7 초안 (§1·§2·§8 없음) | **§1–§7 초안.** 남은 것은 §8 결론뿐 |
| `carbon_fast` | "헤드라인 무력, 통로 없음" | **②19.7% / ③61.5% — ③에서 최대 축.** 순위는 보존 |
| `m5_bundles_headline_inert` | 4 | **3** |
| `m5_bundles_replanned` | 3 | **4** |
| 한계 14 | "모른다 — 재계획하면 순위가 움직이는지 열려 있다" | **"순위는 안 움직였으나 크기는 크게 움직인다"** |
| 유상할당 가속의 정책 진술 | 할 수 없는 말 | **할 수 있다 — 단 순위와 크기를 갈라서만** |
| §0 대장 key | 107 (표 행 수) | **109** (+2) |

**5개 점검**

| ① 데이터 | ② 시나리오 | ③ 인사이트 | ④ MCP·GitHub | ⑤ 산출물 |
|---|---|---|---|---|
| ok 68 / PARTIAL 4 / 설계정상 16, 무출처 신규 0. gap registry **D15 절 추가**(D14 항목 1건 해소) | 묶음 12, 재계획 **3 → 4**. 순위 역전 0. 남은 미재계획 축 3개 | 좋아졌다 — "유상할당이 빨라져도 순위는 그대로다, 그러나 조달부담은 6할 오른다"는 **한 문장 안에 안심과 경고가 같이 있는** 인용 가능한 결과가 생겼다 | 게이트 8항목 PASS, 커밋·푸쉬 완료 | 15종, `web/scenarios.html` 갱신 |

**인계**: 지도상 다음은 **D16 = J5·J6(README·사이트 + ⑤ 산출물)**, 그 뒤 D17 총점검.
**M7(§8 결론)이 아직 없고 M > 나머지**이므로 D16에서 지도와 우선순위가 충돌한다 — 원고가
§1–§7까지 있는데 결론이 없는 상태로 창을 닫는 것보다 M7을 먼저 쓰는 편이 낫다고 본다.
재료는 다 있다(§1.3의 발견 셋 + §1.4의 못 하는 말 셋 + §7의 다섯).
**백로그 변경**: 한계 14 개정(신설 아님). gap registry D14 표에서 `carbon_fast` 행 **해소**,
`carbon_slow` 분리 신설. `carbon_slow`·`ppa_costly`·`retire_free` 재계획은 여전히 미착수 —
`carbon_fast` 하나가 이만큼 움직인 이상 나머지가 평평하다는 가정에 근거가 없다.
**FC3(헤지 가격의 업종 효과 통제)은 D14에 이어 D15에서도 못 했다.** `out/m8/` 대조는 D11부터
다섯 사이클째 미착수. G3(설비 능력)·석화 `tech.*` 밴드·I3-b는 D10부터 그대로.

## D17 (09:00 → 10:05, 지연 실행) — 창 마감 요약 (2026-08-10 01:00 → 09:00)

**먼저 실패부터.** 09:00 마감 태스크는 **발화했고**(`lastRunAt` 09:00:02) **아무것도 남기지
않았다.** 커밋 없음, 창 요약 없음 — 사용자가 아침에 볼 문서가 그것이었다. 그리고 30분
스케줄을 **삭제하지 않아** 내일 01:10에 다시 돌 상태로 무장돼 있었다. "오늘 창만"이라는
지시가 지켜지지 않을 뻔했다. 사용자 지적(09:25)으로 발견해 수동 삭제했다.

**두 번째 실패**: 그 마감 세션은 `python -m cap all`을 시작했다가 완료하지 못하고 끝났다.
`out/e2`가 반쯤 쓰인 채 남았고(계획 파일 39개 vs `plan_index` 48행), e3~e5는 07:06 정본이라
**산출물끼리 어긋났다.** D15가 08:58에 기록한 `55/55` 그린이 09:25에는 `54/55`였다 —
**창이 닫힌 뒤에 깨진 것이고, 깬 것은 마감 절차 자신이다.**

`python -m cap all` 전체 재실행으로 복원. 재실행이 헤드라인을 **소수점까지 재현**했다
(② 115.0/155.6/241.7/279.2) — D11의 `solver_threads=1` 고정이 실제로 재현성을 준다는
독립 확인이 덤으로 나왔다.

**내가 틀렸던 것 하나**: 진단 중 "테스트가 정본 `out/`을 덮어쓴다"고 의심했다. 복원된 깨끗한
상태에서 다시 재니 `plans` 48 → 48, `plan_index` 48 → 48, mtime 무변화 — **틀렸다.**
반쯤 쓰인 `out/` 위에서 잰 값이었다. 테스트 격리는 `test_outputs_isolated_from_production`이
지키고 있고 실제로 지켜진다.

### 창 헤드라인 — 8시간 동안 거의 안 움직였다

| 지표 (NZ15, support=none) | 창 시작 (D1) | 창 끝 (D17) |
|---|---|---|
| ② 감축단가 POSCO / NSC / MCI / LOTTE (천원/tCO₂) | 115.0 / 155.8 / 241.7 / 279.2 | 115.0 / **155.6** / 241.7 / 279.2 |
| ③ TCaR (십억원) | 26,753 / 32,973 / 864 / 2,242 | 26,752.6 / **32,961.3** / 864.1 / 2,242.2 |
| 순위 | POSCO < NSC < MCI < LOTTE | **동일** |

**이것이 이 창의 정직한 헤드라인이다.** 8시간에 17세션을 돌려 바뀐 수치는 NSC 두 자리뿐이다.
움직인 것은 수치가 아니라 **그 수치를 믿어도 되는 근거**, 그리고 **수치가 답하지 못하는
질문의 목록**이다.

### 사이클별

| 사이클 | 한 일 | 결론에 한 일 |
|---|---|---|
| D1 | 저장소 통합(subtree) + `scripts/gate.py` | 무 — 배관 |
| D2 | L1 문헌 지도 | 우리 gap은 배출이 아니라 **비용·위험 공간**에 있다 |
| D3 | M1 페이퍼 골격 | 우리 '효율경계'는 사실 **PPA 헤지곡선**이었다 |
| D4 | G5 평균회귀 검정 | 검정력을 재니 **이 검정은 답을 줄 수 없다** — 표본이 부족 |
| D5 | F3 불확실성 전파 | TCaR의 절반 가까이가 파라미터, 그보다 큰 것이 **우리 선택** |
| D6 | L2 문헌 방법 이식 | 탄소가격을 확률 축으로 올리니 FC4가 업종별로 갈렸다 |
| D7 | M2 방법 절 | 대리 목적함수를 믿었다면 **여덟 번 다 틀렸다** |
| D8 | M8 경계 퇴화 | 다른 기술 일정은 있었다 — 우리 자가 그것을 위험이라 불렀을 뿐 |
| D9 | M3 데이터 절 | **출처가 좋은 값일수록 범위가 없었다** |
| D10 | G2 증거 밴드 | ±30% 규약은 폭이 아니라 **중심**이 틀렸다 |
| D10-b | 정정 | 대장이 **나흘 낡은 out/** 에 대해 '검증됨'이었다 |
| D11 | 재현성 원인 규명 | 낡음이 아니라 **CBC 병렬 솔버** — `solver_threads=1` 고정 |
| D11-b | 정정 | 재현성은 주장됐지만 **두 번 돌려본 적이 없었다** |
| D12 | M4 결과 절 | 지원(support) 축은 **열만 있고 정보가 없었다** |
| D13 | 역방향 후회비용 | 돈만 보면 **지연이 이긴다** — 근거가 가격에서 예산 강제로 이동 |
| D14 | M5 강건성 절 | 시나리오 묶음 넷이 헤드라인에 **닿을 통로가 없었다** |
| D15 | M6 서론·선행연구 | D14가 무력하다던 축이 **③에서 최대 축**(+61.5%)이었다 |

**패턴**: 17세션 중 **자기 정정이 2회, 이전 판정 뒤집기가 3회**(D8·D14→D15·D10→D10-b).
사이클이 만든 가장 큰 가치는 새 결과가 아니라 **어제 참이라 적은 것을 오늘 반증한 것**이다.

### 목표 4개

| 목표 | 창 시작 | 창 끝 |
|---|---|---|
| **데이터 갱신·검증** | audit ok 68, 공백 4, T5 무범위 다수 | audit ok 68 유지. G5는 **"검정 불가"로 판정 종료**(추측 아님), G2 증거밴드 도입. 파라미터 불확실성이 TCaR에 전파됨 |
| **문헌으로 발견 개선** | 없음 | `docs/literature_map.md` 3주장 판정. 주장 ②는 **문헌이 우리보다 앞서 있었고**, 그 지적대로 재니 답이 업종별로 갈렸다 |
| **MCP·GitHub 도구** | 저장소 2개, 손 점검 | 저장소 **1개**, `gate.py` 8항목 한 명령, 테스트 31→**55**, MCP 11도구 |
| **논문화** | `paper/` 없음 | `paper/working_paper.md` **§1–§7 초안**. 남은 것은 §8 결론. §0 수치 대장은 `test_paper_numbers.py`가 `out/`과 대조 |

시나리오 묶음 8 → **12**.

### 다음 창 인계 (우선순위)

1. **§8 결론(M7) + 초록** — 본문에서 유일하게 빈 절.
2. **마감 절차를 신뢰할 수 있게** — 오늘 마감 태스크는 발화했으나 산출물이 없었고 `out/`을
   깨뜨렸다. 마감은 `cap all` 같은 장시간 작업을 시작하면 안 된다. 검증·요약·스케줄 정리만.
3. **한국 시설 실측(G1)** — 일본은 T1 확보, 한국은 명세서 비공개로 여전히 막혀 있다.
   `docs/data_gap_registry.md`의 대체 경로 3개 중 정보공개청구가 미시도.

## D17-b (10:20) — 총점검: 게이트 두 절을 전 항목 돌렸고, 창이 깨진 원인은 마감이 아니었다

**왜 절이 하나 더 붙는가.** 09:00 마감 태스크는 발화해서 이 세션을 열었고, 같은 시각에
사용자가 연 다른 세션이 09:25에 D17 절을 먼저 썼다. 그 절이 요구사항 넷 중 셋(사이클별
한 줄·헤드라인 표·목표 4개)을 이미 채웠으므로 다시 쓰지 않는다. 이 절은 **남은 하나**,
AUTOPILOT §6·§7을 전 항목 실제로 돌려 확인한 결과와, D17 절의 진단 하나를 정정한 것이다.

### 1. 정정 — `out/`을 깬 것은 미완료 실행이 아니라 동시 실행이다

D17 절은 "마감 세션이 `cap all`을 시작했다가 완료하지 못하고 끝났다"고 적었다. **그 실행은
완료했다** — 09:06:52 시작, e2 1,727.8초, 09:35:46 종료, `run_manifest.json` 6단계 전부
`n_sims 10000 / seed 20260806 / solver_threads 1`. 09:25에 관측된 "계획 파일 39개 vs
`plan_index` 48행"은 **그 실행이 아직 돌고 있던 중간 상태**다.

그리고 진짜 원인은 그 뒤에도 계속 있었다. 마감 점검 중 같은 저장소에서 `python -m cap all`이
**최소 다섯 번 겹쳐 돌았다** — 09:06(이 세션)·09:20·09:37·09:54·10:03, 서로 다른 pid.

| 시각 | 관측 | 뜻 |
|---|---|---|
| 09:42 | `gate.py` → **FAILED (tests)**, 2 failed / 53 passed | `plan_index`에 있으나 파일 없는 계획 12개, 대장 4키 불일치(`e2_plans_total` 48↔51, `surrogate_rho_nsc` −0.564↔−0.18) |
| 10:00 | 다른 프로세스의 전량 실행 종료, `plan_index` 48행 = 계획파일 48개 | 찢김 해소 |
| 10:04 | 같은 두 테스트 재실행 → **21 passed** | 실패는 결함이 아니라 **동시 쓰기의 찢긴 순간** |

즉 오늘 창에서 게이트가 빨갛게 난 두 번(09:25의 54/55, 09:42의 53/55)은 **둘 다 같은 원인**
이고 그것은 **K-lock**이다 — D11(06:00)에 신설되고 여섯 사이클째 미구현인 항목. 마감 절차가
장시간 작업을 하지 말라는 D17의 인계는 증상을 고치고, 잠금은 원인을 고친다.

**부수 확인 — 재현성은 이제 3중이다.** 서로 다른 프로세스의 `cap all` 전량 실행 셋
(D11-b · 09:35 · 10:00)이 헤드라인을 소수점까지 같게 냈다: ② 115.0 / 155.6 / 241.7 / 279.2,
③ 26,752.6 / 32,961.3 / 864.1 / 2,242.2. `solver_threads=1`은 실제로 재현성을 준다.

### 2. §6 품질 게이트 — 5항 전부

| 항목 | 판정 | 근거 (실행) |
|---|---|---|
| 두 저장소 테스트 그린, 커밋 완료 | **통과** | FIN `pytest tests/ -q` **55 passed** (09:06, 367s). EFF `pytest tests -q` **7 passed**, `validate-data` 4사·17시설·6기술·9공시계획·8기업시나리오·32계획. `git status` clean, `main...origin/main` 동기 |
| 재현성 (out ≠ out_test, 보고 수치가 실데이터 실행) | **통과** | 위 3중 재현. `gate.py` provenance PASS(6단계 전부 정본 설정), freshness PASS. `out_test/`는 pid로 분리(D11) |
| 신규·변경 수치에 tier + source_id + (T5면) 범위 | **부분 통과** | `audit_data.py` 무출처 신규 0. 창 안 신규 원자료는 `tech_bands.csv`(T3, D10) · `kau_krw`(T1, D6) 둘뿐이고 둘 다 등록. **미충족 1건**: `budget_violation_floor_thkrw` = 300천원/tCO₂가 §5.3 결론을 지탱하는데 `evidence_tier`·`source_id`가 없다(D13 발견, gap registry 등록, 미해소) |
| ITERATION_LOG에 헤드라인 전후 숫자 | **통과** | D1–D15 전 사이클에 전후 표. 무변화 사이클도 "무변화"를 숫자로 적었다 |
| 결론 서사 점검 | **통과, 단 서사가 창 안에서 뒤집혔다** | "기대값에서 싼 계획은 꼬리에서 비싸다"는 유지(FC2 미반증, 경계 24점·교환비 0.21–3.27). 그러나 D13에서 **"돈만 보면 지연이 이긴다"**가 나왔고 버그가 아니라 실재였다 — 조기 전환의 근거는 가격 전망이 아니라 예산의 수량 강제로 옮겨 적혔다 |

### 3. §7 학술적 방어 체크리스트 — 9항 전부

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 연구질문 한 문장, 답이 검증 가능한 형태 | **통과** | `paper` §1.2 · `METHODOLOGY` §0. 검증 가능성은 FC1–FC5로 형식화 |
| 2 | 선행연구 대비 기여 명시 | **통과** | `paper` §2 = `docs/literature_map.md` 판정 3개. 주장 ②에서 **문헌이 앞선다**는 것도 같이 적혀 있다 |
| 3 | 모든 결정변수·제약이 수식 | **통과** | `METHODOLOGY` §1–§7, 가정 식별자 **A-01 – A-24** 24개 |
| 4 | 데이터 출처·연도·등급이 표로 | **통과** | `docs/parameter_inventory.csv` **415행, tier 부여율 100%** (T1 3 / T2 137 / T3 41 / T4 79 / T5 155), 등록부 **82건** |
| 5 | 결론 좌우 가정 식별 + 민감도 검증 | **통과, 규약 미달 명시됨** | F2 상위 10 랭킹 · F3 분해 · I1–I4 · `paper` §6. 상위 10 중 §1 규약(T3 이상)을 만족하는 것은 **5개**이고 그 사실이 §4.3에 적혀 있다 |
| 6 | 후향 검증 제시 | **통과, 표본 절반** | `docs/validation_backtest.md` — 철강 2사 ±1%(재척도 결과), **석화 2사는 생산량 미공시로 대조 불가**이며 그렇게 적혀 있다 |
| 7 | 독립 구현(EFF)과의 교차대조 | **부분 통과 — 창 안 최대 미갱신** | `tests/test_independence.py` PASS(FIN⊥EFF). 그러나 `docs/cross_model_check.md`는 **08-09 22:31 생성분**으로 D11의 `threads=1` 정본 계획 집합 이전이다. 그 문서의 NSC CAPEX 총액 32,618·피크 **2042**가 현재 `out/e5`의 32,616.6·**2035**와 어긋난다. H4 방어가 낡은 숫자 위에 서 있다 |
| 8 | 한계와 반증가능 주장 명시 | **통과, 하나 미검사** | `paper` §6 한계 14개 · §7.2 FC 상태표. **FC3(헤지 가격의 업종 효과)은 M1이 M5에서 하겠다고 적고 D14·D15 연속 미실행** |
| 9 | 재현 가능 (데이터·코드·시드·환경) | **부분 통과 — 환경만 빈다** | 데이터 패키지 15파일 + `manifest.json`(SHA256), 시드 20260806 고정, `run_manifest.json`이 실행 설정 기록, 1명령 재실행 오늘 2회 확인. **환경 핀 없음**: `pyproject.toml`은 `>=` 하한만이고 락파일·constraints가 없다. Python 3.14.6·CBC 버전이 어디에도 기록되지 않는다 — 남이 재현할 때 오늘 발견한 솔버 비결정성이 그대로 재발할 수 있다 |

### 4. 창 요약이 적지 않은 것 — 못 한 것과 틀렸던 것

**D16이 통째로 실행되지 않았다.** 지도상 D16 = J5·J6(README·사이트 + ⑤ 산출물), 08:30.
사이클 시각이 창 내내 밀렸고(D12 예정 06:30/실제 07:13, D14 07:30/08:35, D15 08:00/08:58)
D16 슬롯이 D15와 D17 사이에서 소멸했다. 확인되는 결과 셋 —

| 대상 | 상태 |
|---|---|
| `README.md` | 마지막 수정 **08-09 21:31**(창 밖). 본문이 몬테카를로를 **N=5,000**으로 적는데 `config.yaml`은 **10,000**이다 |
| `web/index.html` | `github.com/PLANiT-Institute/cap-efficient`를 별도 저장소로 링크 — **D1의 통합 이후 유효하지 않다** |
| J6 사이트 확장 | 방법론·검증·강건성·데이터 패키지 페이지 없음. 8쪽 그대로 |

**AUTOPILOT §4 J3의 기술이 틀렸다.** "EFF `cap_efficient/mcp_server.py`"라고 적혀 있으나
그 파일은 없다(`python3 -m cap_efficient.mcp_server` → `No module named`). MCP 서버는 **FIN에만**
있고 11도구로 응답한다. J3은 완료가 맞지만 **어느 구현에 있는지가 반대로 적혀 있었다.**

**창 안에서 틀렸다가 고친 것 다섯** (D17 절이 센 3회보다 많다):
D10-b(대장이 낡은 out/에 검증됨) · D11(원인을 낡음으로 오진 → 병렬 솔버) ·
D13(D12의 후회비용이 시나리오 간 비교 불가 기준) · D14(자기 진단을 `p50_incl_carbon`
빼고 재어 "완전 무력" 오판) · D15(D14의 `carbon_fast` 무력 판정 뒤집기).
여기에 D17-b가 하나 더한다 — D17의 "미완료 실행" 진단.

**창 내내 미착수로 남은 것**: G1 한국(POSCO·LOTTE) · G3 설비 능력 · G4 시나리오 실데이터 ·
G7 GEM DB · L3 기여 진술 · L2-b 채택 판단 · I3-b `process_alternative.md` 재실행 ·
`out/m8` 재생성(D11부터 여섯 사이클) · K-lock · M7 §8 결론.

### 5. 다음 창 인계 — 우선순위 3개

D17 절의 셋(§8 결론 / 마감 절차 / G1 한국)과 겹치는 부분은 그대로 두고, 총점검이 새로
드러낸 것을 반영해 다시 매긴다.

1. **K-lock을 첫 항목으로.** D17 절은 "마감이 장시간 작업을 하지 말 것"을 인계했는데 그것은
   증상이다. 오늘 게이트가 두 번 빨갛게 난 원인은 **다섯 개의 `cap all`이 같은 `out/`을
   나눠 쓴 것**이고, 마감 절차를 고쳐도 사이클끼리 겹치면 그대로 재발한다.
   최소 형태는 D11이 적어 둔 그대로 — 시작 시 `.autopilot.lock`(pid+시각), 실패 시 로그만
   남기고 종료. 여기에 `gate.py`가 잠금 미보유 실행을 WARN으로 보고하면 된다.
2. **M7(§8 결론) + 낡은 방어 문서 셋 재생성.** 원고는 §1–§7이 있고 §8만 없다 — 결론 없는
   원고로 창을 닫았다. 같이 갈 것이 **`docs/cross_model_check.md`(§7 체크 7번, 08-09 생성분) ·
   `out/m8`(D11 이후 미재생성) · `docs/process_alternative.md`(I3-b)** 셋이다. 셋 다 §6·§7이
   인용하는 방어 근거인데 현재 `out/`과 어긋나 있다. 결론 절이 이것들을 인용하기 전에 고쳐야
   순서가 맞다.
3. **재현 패키지의 환경 핀 + D16(J5·J6).** §7 9번의 유일한 결손이 환경이고, 오늘 솔버
   비결정성을 겪은 저장소에서 이것은 형식 요건이 아니라 실질이다 — 락파일 또는
   `constraints.txt`, `run_manifest.json`에 Python·PuLP·CBC 버전 추가. 같은 손길에 D16이
   붙는다(README의 N=5,000 정정, 사이트의 `cap-efficient` 별도 저장소 링크 제거, J6 4쪽 추가).

**스케줄**: `list_scheduled_tasks` 결과 남아 있는 것은 `cap-autopilot-d17-close`
(one-time, 09:00 발화 완료, `enabled: false`) 하나뿐이다. 30분 사이클 태스크
`cap-autopilot-d-cycle`은 09:25에 이미 수동 삭제됐고 재확인했다 — 반복 발화는 없다.
남은 one-time 항목도 이 절과 함께 삭제한다.

## F1 (01:33) — 가이드 §3 필드 정의 검증 1회차: 존재하지 않는 값을 문서화하던 열이 넷이었다

**한 일.** `docs/TECHNICAL_GUIDE.md` §3의 D1a–D7 필드표를 `src/cap/schemas.py`의 `SCHEMAS`,
`data/prepared/*.csv` 실데이터, 그리고 그 열을 실제로 읽는 코드와 한 줄씩 대조했다. 산문만
고치는 것으로는 같은 오류가 재발하므로, 열거형 값은 손으로 쓰지 않고 생성기로 옮겼다
(`scripts/build_tech_guide.py`의 새 `GEN:vocab` 블록, §3.0).

### 가이드에서 고친 사실 오류 (하나씩)

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.1 `unit_type` | `BF`, `NCC`, **`CR`**, `EAF` | `BF` 17 · `NCC` 4 · `FINEX` 1 · `EAF` 1. **`CR`은 한 행도 없다** | `data/prepared/D1a_facility_static.csv` |
| 2 | §3.1 `status` | `operating` / `idle` / `closed` 열거형 | 공시 원문 그대로의 한국어 자유서술 6종(`가동` 18 등). **어떤 코드도 이 열을 읽지 않는다** — 폐쇄 예정이라 적힌 2행도 제외되지 않는다 | 같은 파일 + `grep status src/cap/` |
| 3 | §3.1 `capacity` | 단위 `t/yr` | 단위가 행마다 다르고 `capacity_unit` 열에 담긴다 — `t용선`(18) · `t에틸렌`(4) · `t조강`(1). **행 간 비교 불가**이고 환산하는 코드가 없다 | `D1a.capacity_unit` |
| 4 | §3.1 누락 | `unit_name`·`capacity_unit`(둘 다 스키마 필수), `margin_kthou_t`(추가 열) 자체가 표에 없었다 | `margin_kthou_t`는 E2의 폐쇄 결정을 켜고 끄는 열이다 — 한 행이라도 비면 그 회사 전체의 폐쇄가 꺼진다 | `schemas.py:15-19`, `e2_milp.py:161` |
| 5 | §3.4 `max_applicability_pct` | "부분 감축은 `max_applicability_pct`가 설비 커버리지를 제한한다" | **그런 열은 저장소 어디에도 없다.** 채택은 (설비×기술×연도) 이진변수이고 설비당 최대 1건. 부분성은 `emission_factor`(감축률이 아니라 사후 원단위)와 `retrofit`(가산이냐 대체냐)로 표현된다 | `grep max_applicability` = 가이드 1건뿐, `e2_milp.py:86,168` |
| 6 | §3.4 `capex_uncertainty` | 단위 "fraction" | **퍼센트**(30–60). `plancost.py:22`가 명시 | `D3_tech_options.csv` |
| 7 | §3.3 D2 출처 | "NGFS Phase 5 / GCAM 파생 공개 추정치, 전부 `EST` 라벨" | **GCAM 산출이 아니다.** `gcam_version` 열이 `EST_v0 (비GCAM 잠정)`이라 적고 있고, GCAM-KAIST 수령 전 잠정 구성물이다. 라벨도 전부가 아니라 **행별 혼재** — 앵커 행은 실출처(`IEA_GECM_DOC_2025` 등)를 유지하고 보간 행만 `EST_D2A_V0/EST_D2B_V0` | `data/manifests/estimation_notes_D2_v0.md`, `D2a/D2b.source_id` |
| 8 | §3.3 앵커 | "D2a·D2b 둘 다 5년 앵커" | `re_price`만 예외 — 26년 연간이며 **2050년까지 상수**(한국 175,000 KRW/MWh). PPA 채널에는 경로가 없다 | `D2b_scenario_prices.csv` |
| 9 | §3.6 `instrument` | 열거형에 `subsidy_capex`·`ccfd` 포함, `auction_share_power` 누락 | 실제 4종: `auction_share_power` 2 · `price_cap` 2 · `price_floor` 2 · `auction_share` 1. **D5 전 7행을 읽는 스테이지가 없다** — 모형이 쓰는 유상할당 램프는 `config.yaml` 쪽이다 | `D5_policy_support.csv`, `plancost.support_params` |
| 10 | §3.8 `resolution` | `high` / `medium` / `low`, 강제성 판정 기준 | 실제 값은 `high` 7 · `mid` 5, **`medium`도 `low`도 없다**. 그리고 이 열을 읽는 코드가 없다 — 고정 결정 여부는 `item_type`·`facility_id`·`year_stated`·D3 적용가능성이 정한다 | `D7_disclosed_plan.csv`, `e2_milp._disclosed_fixed` |
| 11 | §3.8 `coverage_pct` | "설비 커버리지 비율" | **12행 전부 공란.** 부분 이행을 강제할 방법이 현재 없다는 뜻이므로 그렇게 적었다 | 같은 파일 |
| 12 | §3.7 D6 | "Annual **consolidated** figures: revenue, **EBITDA**, …" | 네 회사 중 어느 것에 대해서도 완전히 참이 아니다 — POSCO는 별도, `ebitda` 열은 NSC 営業利益·MCI コア営業利益(감가상각 미가산) | 아래 별항 |

### 별항 — 수치 결함 1건 발견: D6 통화 단위가 섞여 있고 환산이 없다

가장 큰 소득은 산문 오류가 아니라 이것이다. `D6_company_financials.csv`는 한국 2사를 **십억원**,
일본 2사를 **億円**으로 담는다(NSC 2024 revenue 86,955 = 억엔). `PREP_LOG`는 "E1에서 환산"이라
적었으나 **그 환산은 구현된 적이 없고**, `e5_metrics._affordability`가 열을 그대로
`ebitda_ref_bnkrw`로 읽는다. 9.2원/엔에서 1억엔 = 0.92십억원이므로 **일본 2사 분모가 약 8%
과대, 지표 ⑥ 비율은 그만큼 과소**다. ①–⑤는 D6를 읽지 않으므로 영향 없다.

**이번 창에서 고치지 않았다.** 이 창은 가이드 창이고, 고칠 자리는 `prepare_raw.py`의 D6 블록이며
고치면 ⑥ 재실행이 따라온다 — D17이 실증한 대로 사이클 안에 못 끝낼 실행은 시작하지 않는다.
대신 세 곳에 적었다: 가이드 §3.7, `docs/data_gap_registry.md`(F1 항목), `METHODOLOGY.md` §⑥.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 7 blocks (vocab 신설)
.venv/bin/python scripts/gate.py               # gate: OK
```

창 시작 시 게이트는 **FAILED (tests)** 였다 — `test_tech_guide`가 stamp 블록의 커밋 해시
`f60cdd3`(직전 창)이 낡았다고 잡은 것으로, 생성기를 돌리는 것이 곧 수정이었다. 감지 장치가
의도대로 작동한 사례이므로 별도 조치는 없다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지, 단 **미사용 열이 문서화됐다** | gate audit `ok 68, PARTIAL 4, 설계상 정상 16`. §3.0이 이제 어느 열이 입력이고 어느 열이 기록인지 명시 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12 묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "데이터셋 정의를 자세히"에 대해 §3이 이제 스키마 필수/추가, 실제 값 집합, 읽히지 않는 열까지 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `gate.py` 8항목 통과 |
| ⑤ 산출물 — 다른 형식이 있는가 | **미충족** | md뿐. HTML은 F6 |

### 인계

- **F2(§4 가정 절 검증)**: A-xx 영어 서술 대 METHODOLOGY 정본 대조. F1에서 A-13(BF 200천원/t,
  unit_type별 단일값)·A-04(margin 섹터별 단일값)의 근거 구조를 확인했으므로 그 두 개는 "영향
  등급"보다 **값의 해상도**를 함께 적을 것.
- **F7(데이터 사전 통합)**에 F1의 [req]/[extra] 구분과 §3.0을 정본으로 넘긴다.
  `data/package/data_dictionary.csv`가 이 구분을 갖고 있는지 미확인.
- **D6 통화 환산**은 가이드 창 밖의 코드 수정이다. 창 마감(F16) 이후, 또는 `cap all`을 끝까지
  돌릴 수 있는 창에서 `prepare_raw.py` D6 블록 + ⑥ 재실행으로 처리.

**F1 후속 (커밋 직후 발견).** F1 커밋을 넣자 게이트가 다시 빨개졌다. `GEN:stamp`가 `HEAD`를
찍고 있었기 때문이다 — 스탬프를 쓰는 커밋 자신은 스탬프가 부르지 못하는 커밋이 되므로,
**이 문서는 커밋되는 즉시 구조적으로 낡는다.** 창 시작 때의 FAILED도 같은 원인이었고 그때는
"생성기를 돌리면 된다"로 넘겼는데, 그것은 증상이었다. 스탬프가 부르는 대상을 HEAD에서
**코드·입력·설정·결과에 마지막으로 닿은 커밋**(`git log -1 -- src data config.yaml out`)으로
바꿨다. 산문만 고친 커밋은 이제 스탬프를 건드리지 않는다. `build_tech_guide.py --check` 통과,
`tests/test_tech_guide.py` 3 passed.

## F2 (02:03) — §4 가정 절 검증: 흔들어 보지 않은 축 셋이 "안 흔들렸다"로 실려 있었다

**한 일.** `docs/TECHNICAL_GUIDE.md` §4의 A-xx 영어 서술을 `METHODOLOGY.md` §8 대장(A-01–A-24),
`out/m5/bundle_matrix.csv`, `out/sensitivity/ranking.csv`와 한 줄씩 대조했다. F1과 같은 방침으로,
묶음 수치는 손으로 쓰지 않고 생성기로 옮겼다(`GEN:axis_impact`, §4.3 신설).

### 가이드에서 고친 사실 오류 (하나씩)

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.1 A-07 | `carbon_fast`가 "②19.7% / ③61.5%, **흔든 모든 축 중 최대**" | ③은 맞고 **②는 3위**다 — `h2_cheap` 27.0 · `h2_expensive` 26.9가 위다. D15 로그는 "③에서 최대"라고만 적었고 가이드로 옮겨오며 한정어가 빠졌다 | `out/m5/summary.csv` |
| 2 | §4.2 A-09 | "상한이 없으면 전량 폐쇄가 이긴다" (근거 없이 단정) | 그 관찰은 1차 무제약 실행의 것이고, 이를 재는 `retire_free`(20%→40%)는 **`--replan` 없이 돌아 0.0%**다. 0.0은 측정이 아니다 | `out/m5/bundle_matrix.csv`, `scripts/run_scenarios.py:77` |
| 3 | §4.2 A-15 | 헤지 선형화의 한계만 서술 | 이를 재는 `ppa_costly`도 미재계획 — 경계가 없다는 사실을 같이 적었다 | 같은 파일 |
| 4 | §4.1 A-07(완화 방향) | `carbon_slow` 언급 없음 | 이것도 `REPLAN_REQUIRED`인데 미재계획. 유상할당 축은 **가속만 측정됐고 완화는 안 됐다** | 같은 파일 |
| 5 | §4 전체 | 대장 24개 중 **A-03·A-08·A-12·A-23이 가이드 어디에도 없었다** | A-03은 METHODOLOGY가 석유화학 **대**로 등급한 가정이다 — 석화 원단위는 회사 총량 재척도가 없어 **주입값이 곧 ②의 수준**이다. A-08(가격 앵커 결측 시 경고 후 평탄 외삽)·A-16(강제 가능한 커밋 없으면 좌표 미산출)도 **대**인데 빠져 있었다 | `METHODOLOGY.md:221-243` |
| 6 | §4.2 A-11 | "1차 실행에서 실증" | 인벤토리에 **T5 [150,600]으로 등록돼 있고 `source_id`가 `MODEL_CHOICE`** — 밴드에 외부 근거가 없다는 뜻. 그리고 300은 필요선보다 훨씬 높다: 조기 전환이 지는 지점은 **≈39천원/tCO₂**이고, 바닥 0에서 4사 중 3사가 뒤집히지만 50에서는 0사다 | `docs/parameter_inventory.csv:387`, `out/m5/penalty_axis.csv`, `out/m5/summary.csv` |
| 7 | §4.1 A-13 | 좌초비용 산식만 | 재조달가는 **자산별이 아니라 설비유형별 단일값**(BF 200 ×17 · NCC 150 ×4 · EAF 250 · FINEX 300). F1 인계 항목 | `data/prepared/D1a_facility_static.csv` |
| 8 | §4.2 A-04 | 가이드에 A-04 서술 없음(§3에 1회 언급뿐) | 마진도 **섹터별 단일값** — 폐쇄 결정이 한계 크래커와 우량 크래커를 구분하지 못한다. F1 인계 항목 | `METHODOLOGY.md:224` |

**확인해 틀리지 않은 것** (수정 없음): A-02 86% = `ranking.csv` fac.ef_inc 86.36, T5, 1위 ✓ ·
A-01 "민감도 8위" = fac.capacity 8위 ✓, "독립 구현 대비 12%" = 77.2 vs 68.8 Mtpa ✓ ·
A-05 "TCaR의 30~42%" = price.h2 30.7 / vol.h2 41.9 ✓ · A-17 "41~48% 감소" = `checks.csv` ③×0.52~0.59 ✓ ·
A-24 "+71~73%", "2050 중앙값 0.47배" = `process_alternative.md:41,47-48` ✓ ·
A-13 "×4.2" = 200/47 ✓ · A-14 "8묶음 중 0" ✓.

### 구조 변경

§4.3 **What we actually re-solved** 신설 — `out/m5/bundle_matrix.csv`에서 생성하고
`run_scenarios.REPLAN_REQUIRED`를 **import**한다(재기술하지 않는다). 묶음이 추가되면 표에
자동으로 나타나고, 미재계획 묶음은 `**no — required**`로 표시된다. 이 창에서 가장 중요한 줄은
표의 수치가 아니라 그 열이다. §4.4는 나머지 대장 항목(A-12·A-22·A-23)과 24개 전부의 소재를
적어, "결론을 움직이는 것만 옮겼다"는 문장이 검증 가능해졌다. 기존 §4.3(증거 등급)은 §4.5로.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 8 blocks (axis_impact 신설)
.venv/bin/python scripts/gate.py               # gate: OK (pytest 58 passed)
```

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4, 설계상 정상 16` |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **판정 하향** | `out/scenarios/summary.csv` 12묶음은 그대로지만, 그중 **3묶음은 흔든 적이 없다**. 축 12개가 아니라 9개다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "주요 가정" 질문에 대해 §4가 이제 측정된 축과 등급만 매긴 가정을 구분해 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `gate.py` 8항목 |
| ⑤ 산출물 — 다른 형식이 있는가 | 미충족 | md뿐. HTML은 F6 |

### 인계

- **F3(D1a·D1b 파생량)**: A-03이 §4.1에 들어갔으므로, 석화 원단위 주입값이 어느 코드 줄에서
  들어가고 철강 재척도가 어디서 일어나는지를 §3.1–3.2에 파일·행으로 적을 것. 지금은 산문뿐이다.
- **미재계획 3묶음**(`carbon_slow`·`ppa_costly`·`retire_free`)은 묶음당 ~10분이다. 이 창은
  가이드 창이라 시작하지 않았다(D17 규칙). 한 사이클을 통째로 쓸 수 있는 창에서
  `run_scenarios.py --bundles carbon_slow,ppa_costly,retire_free --replan`.
  끝나면 §4.3 표가 생성기만 다시 돌려도 갱신된다 — 문장은 손대지 않아도 된다.
- **F8(적대적 검토)** 목록의 첫 줄 후보: "축 12개 중 3개는 안 흔들었다"를 Arc가 먼저 발견하면
  나머지 9개의 신뢰도도 같이 떨어진다. §4.3이 먼저 말하도록 해 둔 것이 이번 대응이다.

## F3 (02:33) — D1a·D1b 정의 심화: 측정된 적 없는 에너지를 측정값으로 문서화하고 있었다

**한 일.** 가이드 §3.1·§3.2의 필드 정의를 `scripts/prepare_raw.py`(생성 규칙)와
`src/cap/e2_milp.py:41-56`(소비 규칙) 양쪽에서 추출해 대조했다. F1·F2와 같은 방침으로 수치는
생성기로 옮겼다 — `GEN:register_filter`(§3.1)·`GEN:d1b_intensity`(§3.2) 두 블록 신설.

### 가이드에서 고친 사실 오류 (하나씩)

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.2 `energy_coal`·`energy_gas` | 단위 `t/yr` | **`GJ/yr`.** E2가 각각 28.0·54.0 GJ/t로 나눠 톤을 만들고 그 톤에 석탄·가스 가격을 매긴다. 문서의 단위와 코드의 환산이 환산계수만큼 서로 모순이었다 | `e2_milp.py:36-37,54-55`; `prepare_raw.py:100-107` |
| 2 | §3.2 도출 | "에너지 원단위도 마찬가지(3개년 평균)" — 관측값이라는 함의 | **셋 다 관측이 아니다.** 수집 패널에 전면 결측이라 `production × ROUTE[unit_type]`로 주입된다. 따라서 `energy_x / production`은 정의상 루트 상수를 되돌려 준다 — BF 17기 전부 석탄 13.5 GJ/t·전력 0.08 MWh/t로 **동일**. 시설 단위 에너지 정보가 모형에 없다 | `prepare_raw.py:100-107,190,211`; 신설 표 |
| 3 | §3.2 A-03 주입점 | 가이드에 없음(F2 인계 항목) | 석화 Scope1 자체가 `capacity × 0.9 × ROUTE[NCC][0]`이라 **주입값 0.95가 곧 ②의 수준**이다. 철강은 회사 공시 총량 재척도가 있어 `ef_inc`가 1.72–2.34로 갈린다 — 상쇄되는 쪽과 아닌 쪽을 표가 보여준다 | `prepare_raw.py:198-212` 대 `169-190` |
| 4 | §3.1 행 수 | 23행이라고만 | **원자료 28행 중 5행이 준비 단계에서 탈락**한다. 준비본이 쓰이기 전에 걸러지므로 스키마 검사도 감사도 이 5행을 보지 못한다 | `prepare_raw.py:53-62` |
| 5 | §3.1 `status` (F1 정정) | F1: "`status`로 제외되는 행은 없다" | 모든 **모형** 단계에 대해서는 참이고 준비 단계에 대해서는 거짓이다. `폐쇄예정` 문자열을 담은 1행이 실제로 탈락한다. 그리고 그것이 **문자열 부분일치**여서 `휴지예정`·`가동중단 계획`은 남는다 — F1이 관찰한 두 행이 남는 이유가 "아무도 안 읽어서"가 아니라 "필터가 그 문자열만 봐서"였다 | 같은 파일 |
| 6 | §3.1 `capacity` 채움 | A-01 참조 한 줄 | 23행 중 **16행이 내용적 추정**이고, 그 내용적을 **설비명 문자열에서 정규식으로** 뽑는다. 승수 913.3 t/m³·yr는 캘리브레이션 점 **1개**(광양1고로) | `prepare_raw.py:43-51` |
| 7 | §3.1 재투자 필드 | 없음 | `next_reinvest_year` 결측은 `commissioning+20`(하한 2030), `last_reline_year` 결측은 `commissioning`으로 보정된다. 앞의 것은 A-13 좌초비용에 직결되므로 **보정된 앵커 = 보정된 좌초비용** | `prepare_raw.py:64-69` |
| 8 | §3.2 `emissions_s2`·`energy_naphtha` | "보존되나 부과하지 않음" | `emissions_s2`는 모형 프레임에 **조인조차 되지 않는다**(E2가 5개 열만 가져간다). `energy_naphtha`는 어떤 단계도 읽지 않는다 — 둘 다 비어도 시끄럽게 실패할 방법이 없다 | `e2_milp.py:50-51` |
| 9 | §3.1 `incumbent_capex_unit`·`margin_kthou_t` | "unit_type/sector당 단일값"(F2) | 값의 해상도만이 아니라 **수집물이 아니라 준비 단계 주입물**이라는 것을 파일·행으로 적었다 | `prepare_raw.py:73,80` |
| 10 | METHODOLOGY §2-1 | $\mu_f$ 출처 "D6 배분" | **D6에 마진 열이 없다.** `D1a.margin_kthou_t`이고 섹터 상수 주입이다. 같은 표의 에너지 원단위 행도 GJ→t 환산이 빠져 있었다 | `head -1 D6_company_financials.csv`; `prepare_raw.py:80` |

**결측·0 처리 규칙**(F3 지도 항목)을 §3.2에 절로 성문화했다. 핵심은 두 줄이다 — NaN은 LP 구성
전에 회사별로 걸려 실행을 세우지만(`e2_milp.py:111-118`), **`production == 0`은 걸리지 않는다.**
모든 원단위가 `production`으로 나누므로 0은 NaN이 아니라 `inf`를 만들고 그 검사를 통과한다.
현재 0인 행은 없다. 검사의 구멍이지 데이터의 성질이 아니라고 적었다.

### 생성기로 옮긴 것

- `GEN:register_filter` — 원자료 `facility_static.csv`와 준비본의 **차집합**을 찍는다.
  `prepare_raw.py`의 제외 로직을 **재구현하지 않는다**(재구현하면 파이프라인과 갈라진다).
  사유는 원자료 행에서 귀속한다. 규칙이 바뀌면 표가 따라 바뀐다.
- `GEN:d1b_intensity` — `_prep_company`가 만드는 계수를 unit_type별 범위로 찍는다.
  **범위 열에 값이 하나면 그 열은 관측이 아니다**는 판정이 표에서 바로 읽힌다.

### 데이터 공백 1건 신규 등록

`docs/data_gap_registry.md` **F3**: `POSCO_GWY_BF2`(광양 2고로, `개수중`)가 모형 밖에 있다.
폐쇄도 미가동도 아닌 가동 설비인데, 공시된 설비명에 내용적이 없어 능력을 추정할 수 없어서
탈락했다 — **데이터 결함이 아니라 표기 형식에 의한 제외**다. 크기 미검증(광양 고로 1기 ≈ 3~5
Mt/yr 대 현행 총 72.5 Mt/yr). 회사 총량은 공시 재척도라 수준은 유지되고 **POSCO 시설 간 몫이
바뀐다**. 이 창은 가이드 창이라 고치지 않았다 — 옳은 수정은 원자료 `capacity` 보충이다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 10 blocks (register_filter·d1b_intensity 신설)
.venv/bin/python scripts/gate.py               # gate: OK (pytest 58 passed)
```

수치 출처: 단위·환산 `src/cap/e2_milp.py:36-37,49-55` · 주입 상수 `scripts/prepare_raw.py:100-107`
· 제외 규칙 `scripts/prepare_raw.py:53-62` · 능력 추정 `scripts/prepare_raw.py:43-51` · 계수 범위와
Q 총계는 `data/prepared/D1a_facility_static.csv` + `D1b_facility_panel.csv`에서 생성기가 계산.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **판정 하향** | gate audit는 `ok 68, PARTIAL 4`로 그대로지만, 감사가 보는 것은 **준비본**이다. 원자료 5행은 감사 시야 밖에서 탈락하고, 에너지 3열은 "있다"로 세어지지만 시설 정보가 0이다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(그중 3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "데이터셋 정의를 자세히"에 대해 §3.1–3.2가 이제 도출식·결측 규칙·제외 규칙까지 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `gate.py` 8항목 |
| ⑤ 산출물 — 다른 형식이 있는가 | 미충족 | md뿐. HTML은 F6 |

### 인계

- **F4(D2·D3 정의 심화)**: F1이 D2 출처 계보를 이미 고쳤다(`EST_v0 (비GCAM 잠정)`). 남은 것은
  **D3b 밴드 규약**이고, G2가 "현행 값이 밴드의 끝이거나 밖"이라고 적은 3칸을 §3.4가 그대로
  말하게 하는 것이 F4의 최소 성과다.
- **F5(D4–D7)**: D7 `resolution`은 F1이 "읽는 코드 없음"까지 확인했으므로, F5는 실제 판정자인
  `e2_milp._disclosed_fixed`의 조건을 성문화하면 된다.
- **F7(데이터 사전)**: §3.1의 `GEN:register_filter`가 `data/package/data_dictionary.csv`에
  없는 정보다 — 사전은 준비본만 기술한다. 사전에 "수집 28 / 모형 23"을 넣을지 F7에서 판단.
- **미해결 코드 수정 2건**(창 밖): D6 통화 환산(F1), 광양 2고로 능력(F3). 둘 다 `cap all`
  재실행이 따라오므로 한 사이클을 통째로 쓸 수 있는 창에서.

## F4 (03:03) — D2·D3 정의 심화: 앵커가 규칙이라고 적혀 있었고, 공시된 최대 확약은 흔적 없이 빠지고 있었다

**한 일.** 가이드 §3.3(D2a·D2b)·§3.4(D3·D3b)를 준비본·`e2_milp` 소비 규칙·`source_register`와
한 줄씩 대조했다. F1–F3과 같은 방침으로 수치는 손으로 쓰지 않고 생성기로 옮겼다 —
`GEN:d2_provenance`·`GEN:d3_reach`·`GEN:d3b_bands` 세 블록 신설(총 13블록).

### 가이드에서 고친 사실 오류 (하나씩)

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.3 출처 | "앵커를 단 행은 진짜 `source_id`를 유지하고 **보간된 행만** `EST_*`" — 앵커가 규칙이라는 함의 | 반대다. 예산 **48행 중 46행**, 가격 **224행 중 82행**이 자작이고, 앵커 행은 예산 전체에서 **2행**(Korea `KR_NETZERO_2050`)뿐이다 | `data/prepared/D2a·D2b`, 신설 표 |
| 2 | §3.3 일본 예산 | 언급 없음 | **일본 예산 24행에 앵커가 0개다.** "정부 목표 사이 구간선형"이라는 서술이 일본에 대해서는 가리킬 목표 행이 없다 | 같은 표 |
| 3 | §3.3 전력 | "전력은 도매가가 아니라 LCOE 연구에서 온다" | 한·일 **양쪽 다 등록부 키가 없다**(24행 전부 `EST_D2B_V0`). 그리고 서술이 일본에만 맞다 — 한국은 3년 평균 발전단가 110.2원/kWh를 SNU 2050 시나리오로 선형 외삽한 것이고, **시나리오 강도의 대용이 원전 비중**이다. 즉 한국 NZ15와 B20 전력경로의 차이는 감축비용이 아니라 원전 비중 가정이다 | `estimation_notes_D2_v0.md:29-30` |
| 4 | §3.3 시나리오 분화 | 언급 없음 | **일본 `elec_price`는 6개년 전부 NZ15 = B20이다.** 분화되도록 만든 변수가 분화하지 않으면 시나리오가 닿지 못하는 입력이다 — NSC·MCI의 전화(電化) 경제성은 1.5도에서도 2도에서도 같은 전력가를 본다 | 신설 표(생성기가 매 빌드 판정) |
| 5 | §3.3 `re_price` | "한국 175,000 KRW/MWh" | 일본 198,000이 빠져 있었다. 두 지역 값이 다르다 | `D2b_scenario_prices.csv` |
| 6 | §3.3 `h2_price` | "한국 2025 결측" (참) | 참이되 불충분 — **D2b 전체에서 결측은 이 2행뿐**이고, 일본 2025는 10,240원/kg으로 존재한다. 근단에서 두 지역 수소경로는 비교 불가다 | 같은 파일 |
| 7 | §3.3 `source_id` 정의 | "행별 `source_register` 외래키" | 셀은 **단일 키가 아니다.** 여러 키(`KR_PPA_2026/REI_JP_PPA_2025`)와 괄호 안 파생 주석을 함께 담고, 그 주석에 실제 변형이 적혀 있다. 해소 규칙은 `; \| + /` 분리 + 괄호 제거 | `scripts/audit_data.py:source_parts` |
| 8 | §3.4 D3b | "(tech, field)별 문헌 밴드" — 밴드층이 있다는 함의 | **밴드는 3개, 13개 기술 중 2개에만** 붙어 있다. 그리고 **3개 중 어느 것도 밴드 내부에 있지 않다** — 2개는 경계 위, 1개는 밖(`steel_eaf.capex_unit` 240 대 [370,835]) | `D3b_tech_bands.csv` |
| 9 | §3.4 `applies_to_unit` | "`NONE`은 기존 설비에 적용 불가인 신설 수단" | 사실이되 결과가 빠졌다. E2는 `applies_to_unit == unit_type` **정확 문자열 일치**로 선택지를 만든다. 따라서 `steel_eaf`는 **어느 설비에도 제시되지 않고**, D1a의 유일한 EAF(`POSCO_GWY_EAF1`, 2.5 Mt)는 **선택지가 0개**다 | `src/cap/e2_milp.py:148` |
| 10 | §6.4 | "네 회사 중 둘은 공시 좌표가 없다" (POSCO·LOTTE) | 세 번째가 있다 — **NSC는 좌표가 없는 게 아니라 미완이다.** 야하타·히로하타 전기로 전환 2건이 `e2_milp.py:325`에서 탈락하는데, `facility_id`가 D1a에 없어서다. POSCO·LOTTE 탈락과 달리 **`disclosed_skipped.csv`에 줄이 남지 않는다**(파일은 4행, NSC 없음). NSC 좌표는 기미쓰 확약 하나로 만들어지고 gap은 그만큼 넓게 나온다 | `out/e2/disclosed_skipped.csv`, `out/e5/gap.csv` |

**확인해 틀리지 않은 것** (수정 없음): 5년 앵커 + `re_price`만 연간(각 조 26행) ✓ ·
`coal_price`는 연료탄 ✓ · 13행 = steel 7 / petchem 6 ✓ · retrofit 9/13 ✓ ·
`capex_uncertainty` 30–60 퍼센트 ✓ · **`source_id` 미해소 0건** — 처음 세었을 때 D3 13행 중
9행이 dangling으로 보였으나 그것은 감사의 분리 규칙을 쓰지 않은 내 오독이었다. 감사 규칙으로
다시 세면 전부 해소된다.

### 생성기로 옮긴 것

- `GEN:d2_provenance` — 지역·변수별 행수 / 앵커 행수 / 앵커 출처, 그리고 NZ15 = B20인 계열.
  출처 해소는 `audit_data.source_parts`를 **import**한다(재구현하지 않는다). 규칙이 두 벌이면
  가이드가 "출처 있음"이라 부르는 행을 감사가 dangling이라 부를 수 있다 — 그 분리 규칙을
  `audit_data.py`에서 함수로 뽑아내 양쪽이 쓰게 했다(감사 결과 불변: `ok 68, PARTIAL 4`).
- `GEN:d3_reach` — 설비유형별 설비수·능력·제시되는 수단. 양끝의 공집합(선택지 없는 설비 /
  설비 없는 수단)을 두 데이터 열의 교집합으로 매 빌드 재계산한다.
- `GEN:d3b_bands` — 밴드 3개 각각의 중심값 위치(내부/경계/밖)와 커버리지.

### 데이터 공백 1건 신규 등록

`docs/data_gap_registry.md` **F4**: D1a는 **현존 설비만** 담는 등록부라 신설 EAF 2기에 행이
없고, D3의 신설 수단 `steel_eaf`는 `NONE`이라 어느 행에도 붙지 않는다. 두 경계가 겹치는 자리에
**D7에서 투자액이 명시된 유일한 확약**(6,302억엔 + 1,400억엔, 250만 t/년)이 빠진다. 감사도
스키마도 못 본다 — 세 행 다 형식이 옳고 출처가 있고 `tech_id`가 D3에 실재한다. 이 창은 가이드
창이므로 기록만 했다. 한 줄짜리 수정(`_disclosed_fixed`가 미등록 `facility_id`를 `dropped`에
사유와 함께 적기)은 수치를 바꾸지 않으므로 **다음 코드 창의 첫 후보**다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 13 blocks (d2_provenance·d3_reach·d3b_bands 신설)
.venv/bin/python scripts/gate.py               # gate: OK (pytest 58 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 출처 분포 `data/prepared/D2a_scenario_budget.csv`·`D2b_scenario_prices.csv` ×
`data/raw/source_register.csv` · 전력 구성 `data/manifests/estimation_notes_D2_v0.md:29-30,34` ·
밴드 `data/prepared/D3b_tech_bands.csv` × `D3_tech_options.csv` · 선택지 일치 규칙
`src/cap/e2_milp.py:148,325,332` · NSC 좌표 `out/e5/gap.csv` · 탈락 기록 `out/e2/disclosed_skipped.csv`.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **판정 하향** | 감사 수치는 불변이나, D7 10행 중 **3행이 강제되지 않고 그중 2행은 기록조차 남지 않는다**. F3의 "감사 시야 밖" 문제와 같은 종류다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **판정 하향** | 묶음 12개(3개 미재계획 — F2)에 더해, **일본 전력은 시나리오 축이 아예 닿지 못한다** |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "데이터셋 정의"에 §3.3이 출처 계보를 행 단위로, §3.4가 선택지 집합을 두 열의 교집합으로 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `gate.py` 8항목 |
| ⑤ 산출물 — 다른 형식이 있는가 | 미충족 | md뿐. HTML은 F6 |

### 인계

- **F5(D4–D7)**: D7이 F4에서 반쯤 열렸다. F5의 최소 성과는 `_disclosed_fixed`의 판정 순서를
  §3.8이 그대로 적는 것이다 — (1) `facility_id`가 D1a에 있는가(**조용한 탈락**), (2) `tech_id`가
  D3에 있는가, (3) `applies_to_unit`가 맞는가, (4) 가용연도 클램프. `resolution` 열은 이 넷
  어디에도 들어가지 않는다 — 즉 `high`/`mid`는 **아무것도 결정하지 않는다**. 그것이 F5가 적을
  문장이다.
- **F8(적대적 검토)** 목록 2·3번째 줄: "일본 전력은 시나리오에 반응하지 않는다", "NSC gap은
  자기 최대 확약을 빼고 계산됐다". 둘 다 §3.3·§6.4가 먼저 말하도록 해 두었다.
- **미해결 코드 수정 3건**(창 밖): D6 통화 환산(F1), 광양 2고로 능력(F3), 미등록 `facility_id`
  조용한 탈락(F4). 세 번째만 재실행이 필요 없다.

## F5 (03:33) — D4–D7 정의 심화: "D5는 읽히지 않는다"가 거짓이었고, 공시 12행 중 3행은 흔적 없이 빠진다

**한 일.** 가이드 §3.5(D4)·§3.6(D5)·§3.7(D6)·§3.8(D7)을 `calibration.py`·`plancost.py`·
`e5_metrics._affordability`·`e2_milp._disclosed_fixed`와 한 줄씩 대조했다. F1–F4와 같은 방침으로
수치는 손으로 쓰지 않았다 — `GEN:d6_coverage`·`GEN:d7_enforcement` 신설, `GEN:price_series`
확장(총 15블록).

### 가이드에서 고친 사실 오류 (하나씩)

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.6 D5 | "일곱 행을 **어느 단계도 읽지 않는다**. 모형이 적용하는 유상할당 램프는 `config.yaml`의 것" | **거짓.** `plancost.auction_share`가 `auction_share` 1행(K-ETS 4기 발전외 15%, 2026–2030)을 읽어 config 램프를 그 구간에서 덮어쓴다 — 2026–2029가 보간값 0.11~0.14에서 **0.15로 올라간다**. 모든 경로·두 지원 시나리오·E2와 E5 전부. **METHODOLOGY A-07이 정본이고 가이드가 결함**이었다. 지원 축이 무신호인 것(참)과 파일이 불활성인 것(거짓)은 다른 말이다 | `plancost.py:33-61`; `METHODOLOGY.md:227`; `test_auction_share_follows_confirmed_allocation_plan` |
| 2 | `GEN:vocab` 산문 | "Nor is any `D5` row read" | 같은 오류. 생성기에서 고쳤다 — 읽히는 것은 `auction_share` 1행이고 지원 축이 아니라 `plancost`가 읽는다. 나머지 6행(발전부문 2·GX-ETS 콜라 4)은 읽히지 않는다 | `build_tech_guide.py:gen_vocab` |
| 3 | §3.5 문턱 | "6관측 미만인 **인자**는 사전값으로" | 문턱은 **계열당**이다. 인자는 지정 계열 중 하나라도 통과하면 추정하고, 전부 실패할 때만 사전값이다. `elec`은 3계열 전부 통과(σ=0.242, 세 계열 평균) | `calibration.py:73-88` |
| 4 | §3.5 사전값 | "수소 0.25, **건설비** 0.06" | 0.06은 `capex` **인자**의 사전값이지 `constr_cost_idx`의 값이 아니다. 그 인자가 지정한 두 계열 중 **`equip_import_idx`는 D4에 존재하지 않는다** — `if s in wide.columns`로 조용히 건너뛴다 | `calibration.py:26,47,75` |
| 5 | §3.5 상관 | "항등 상관행렬"(사실만 나열) | 이유가 빠졌다. 수익률 계열을 내는 인자가 `elec` 하나뿐이라 **상관을 추정할 대상이 없다.** 항등은 독립의 발견이 아니라 추정의 부재다 | `calibration.py:90-97` |
| 6 | §3.5 표 | `Volatility` 열이 6관측 이상을 전부 `estimated`로 표기 | 11계열은 **어느 인자도 이름을 부르지 않아 열리지 않는다.** 길이와 무관하다. `usdkrw` 11관측이 "estimated"로 읽히던 자리다. `Read as` 열로 교체 — 대응은 `FACTOR_SERIES`를 import한다 | `calibration.py:24-26` |
| 7 | §3.5 전해조 | 언급 없음 | 수소가격이 파생되는 전해조 CAPEX 경로가 **2관측**에서 앵커만 취하고 감소율 5%/yr·변동성 0.10은 사전값이다. 그리고 두 관측은 **+32% 상승**인데 부과되는 경로는 하강한다 — 방향이 반대다 | `calibration.py:100-106`; `D4_price_history.csv` |
| 8 | §3.7 D6 | 재무 7열을 채워진 표처럼 나열 | `ebitda`만 22/22. `net_debt`·`total_debt` 9/22(**일본 2사뿐**), `interest_expense` 2/22. 그래서 ⑥의 레버리지 절반이 POSCO·LOTTE에서 비어 있고, 그것은 미공시가 아니라 **법인 경계 결정**이다(POSCO 행은 철강 사업회사, 공개 대차대조표는 지주) | `D6_company_financials.csv`; `e5_metrics.py:95-99` |
| 9 | §3.7 미사용 열 | 없음 | 7열 중 **4열(`capex_total`·`total_debt`·`interest_expense`·`cash`)을 어느 단계도 읽지 않는다.** e5의 `capex_total_bnkrw`는 모형 전환 CAPEX이지 D6의 동명 열이 아니다 — 이름이 같아 읽히는 것처럼 보인다 | `e5_metrics.py:104-131,227` |
| 10 | §3.7 3년 평균 | "3-year mean EBITDA" | 고정 창이 아니라 **보고된 마지막 3년**이다(`ebitda.tail(3)`). 2025가 있는 3사는 2023–2025, 미쓰이만 2022–2024로 비교된다 | `e5_metrics.py:109-110` |
| 11 | §3.8 강제 규칙 | 다섯 조건을 한 문장으로 나열 | 순서가 있고 **순서가 흔적을 결정한다.** 2번(`facility_id`가 D1a에 있는가)과 5번(가용연도)은 **조용히** 탈락 — `disclosed_skipped.csv`에 줄이 없다. 3·4번만 사유가 기록된다. 12행 중 **강제 2 / 사유 기록 2 / 무흔적 3 / 문맥 5** | `e2_milp.py:317-357`; 신설 표 |
| 12 | §3.8 `resolution` | "필터가 아니라 skip 보고서용 해석 등급" | 절반만 맞다. **다섯 검사 어디에도 들어가지 않으므로 아무것도 결정하지 않는다** — 조용히 탈락한 3행 중 2행이 `high`다 | 같은 곳 |
| 13 | §3.8 계약수단 | 없음 | D7에 `ppa`·`epc`·`ccfd` **행 유형 자체가 없어** 모든 공시 좌표가 세 수단을 0으로 **고정한 채** 풀린다. 최적해는 셋을 살 수 있다. 따라서 gap의 일부는 기술 차이가 아니라 **아무도 공시한 적 없는 헤지 차이**다 | `e2_milp.py:179-184,353-356` |
| 14 | §3.8 클램프 | "가용연도보다 이른 확약은 최초 가능연도에 강제"(일반론) | 실제로 일어난다 — NSC 기미쓰 H2DRI가 공시 2026에서 **채택 2030 / 가동 2034**로 8년 이동한다. 살아남은 유일한 철강 확약이 그것이다. **공시 좌표는 공시가 아니라 우리 모형이 표현할 수 있는 공시**다 | 빌드 로그 + 신설 표 |

**확인해 틀리지 않은 것** (수정 없음): D5 7행 = 유상할당 3 + 콜라 4 ✓ · `support_params`가 읽는
것은 `subsidy_capex`·`ccfd` 둘뿐이고 D5에 0행 → `current` ≡ `none` ✓ · 발전부문 50%가 철강·석화에
새지 않음(별도 키 + 회귀 테스트 2개) ✓ · D7 `item_type` 3/7/2 · `resolution` high 7 / mid 5 ·
`coverage_pct` 12행 전부 결측 ✓ · D6 통화 혼재(F1) ✓ · §6.4의 NSC 서술 ✓(무흔적 3행 중 2행이
그것이다).

### 생성기로 옮긴 것

- `GEN:price_series` 확장 — `Read as` 열은 `calibration.FACTOR_SERIES`를 **import**한다.
  이름만 있고 D4에 없는 계열(`equip_import_idx`)을 매 빌드 지목하고, 사전값을 쓰는 인자와
  그 값을 각주로 찍는다. 전해조 관측 방향도 데이터에서 계산한다.
- `GEN:d6_coverage` — 열별 비결측 / 보유 회사 / 읽는 지표. 소비처 3열은 `e5_metrics.py:104-112`
  대조로 확정한 상수이고 나머지는 `—`다. 3년 창은 `out/e5/affordability.csv`의 `ebitda_years`에서
  읽는다(재계산하지 않는다).
- `GEN:d7_enforcement` — **`_disclosed_fixed`를 호출한다.** 전제 사슬을 산문으로 재현하면
  `e2_milp.py`가 움직이는 순간 갈라지고, 흥미로운 사례가 정확히 "흔적을 남기지 않는" 것들이다.
  두 시나리오 모두 돌려 판정이 갈리는지 확인한다(현재는 동일).

### 데이터 공백 1건 신규 등록

`docs/data_gap_registry.md` **F5**: 위험인자 3개 중 2개가 시장 관측을 하나도 쓰지 않는다.
`capex`는 지정 계열 두 개 중 하나가 **부재**(`equip_import_idx`)하고 하나가 3관측이라 사전값이
되는데, 경고 문장이 "짧다"와 "아예 없다"를 구분하지 않는다. 감사는 D4를 계열 단위로 세므로
"어느 인자가 이 계열을 읽는가"를 보지 못한다. 수정안 (b) — `FACTOR_SERIES`에서 부재 계열을
지우고 capex 사전값을 규약으로 확정 — 은 **수치를 바꾸지 않는다**.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 15 blocks (d6_coverage·d7_enforcement 신설)
.venv/bin/python scripts/gate.py               # gate: OK (pytest 58 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 인자↔계열 대응·문턱·사전값 `src/cap/calibration.py:24-26,47,73-97,100-106` ·
유상할당 덮어쓰기 `src/cap/plancost.py:33-61` 대 `config.yaml:65-71`(실행값은
`plancost.auction_share`를 직접 호출해 확인) · D6 열 커버리지 `data/prepared/D6_company_financials.csv`,
소비처 `src/cap/e5_metrics.py:104-131` · 3년 창 `out/e5/affordability.csv` ·
D7 판정 12행은 `_disclosed_fixed`를 `out/e1/tech_availability.csv`와 함께 호출해 생성.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **판정 하향** | gate audit는 `ok 68, PARTIAL 4` 불변이지만 **감사가 세는 단위가 계열·열이고 소비 여부가 아니다.** D4 11계열·D6 4열이 읽히지 않고, D4 1계열은 이름만 있고 없다. F3·F4의 "감사 시야 밖"과 같은 종류의 세 번째 사례 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2). D5 유상할당 행은 두 지원 시나리오에 **동일하게** 적용되므로 축을 늘리지 않는다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "가정이 무엇인가"에 §3.5가 인자별 증거 유무로, §3.8이 공시 12행의 행별 처분으로 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목 |
| ⑤ 산출물 — 다른 형식이 있는가 | 미충족 | md뿐. HTML은 F6 |

### 인계

- **F6(HTML 판)**: 지도대로 `scripts/build_site.py`·`web/`에 붙인다. 새 빌드 체계 금지.
  가이드가 15블록이므로 GEN 마커를 그대로 두고 md를 렌더하는 쪽이 안전하다.
- **F7(데이터 사전)**: `data/package/data_dictionary.csv`가 준비본의 **열 존재**만 기술한다.
  F5가 만든 두 정보 — "이 열을 어느 단계가 읽는가"(D6 4열 무소비)와 "이 계열을 어느 인자가
  읽는가"(D4 11계열 무소비) — 가 사전에 없다. 사전에 `read_by` 열을 넣을지 F7에서 판단.
- **F8(적대적 검토)** 목록에 두 줄 추가: "위험인자 2/3이 관측 0인데 ③을 인용해도 되는가",
  "공시 gap에 헤지 차이가 섞여 있다". 둘 다 §3.5·§3.8이 먼저 말하도록 해 두었다.
- **미해결 코드 수정 4건**(창 밖): D6 통화 환산(F1), 광양 2고로 능력(F3), 미등록 `facility_id`
  조용한 탈락(F4), `FACTOR_SERIES`의 부재 계열 정리(F5). **F4·F5 두 건은 재실행이 필요 없다** —
  수치를 바꾸지 않고 사실을 드러내기만 한다. 코드 창이 열리면 이 둘부터.

## F6 (04:03) — 기술 가이드 HTML 판: 웹에서 죽는 링크 5종, md에서 안 보이는 생성 블록 15개

**한 일.** 지도대로 `docs/TECHNICAL_GUIDE.md`를 `web/guide.html`로 렌더하는
`scripts/build_guide_page.py`를 만들고 `scripts/build_site.py`의 기존 파이프라인에 붙였다.
새 빌드 체계는 없다 — `_style.py`의 CSS를 그대로 쓰고, `build_evidence_page.py`·
`build_changelog_page.py`와 같은 자리에서 같은 명령으로 만들어진다.

### 가이드에서 고친 사실 오류

| # | 어디 | 가이드가 적었던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | 머리말 | "Every quantitative claim … (blocks marked *generated*)" | **거짓이었다.** 마커는 `<!-- GEN:x -->` **HTML 주석**이라 GitHub이 md를 렌더하면 아무 표시도 남지 않는다. 외부 독자는 어디까지가 손으로 쓴 산문이고 어디부터가 저장소에서 나온 수치인지 구분할 방법이 없었다. 웹 판이 15개 블록에 `generated · <이름>` 라벨을 붙이고, 머리말은 그 사실을 그대로 적는다 | `docs/TECHNICAL_GUIDE.md:7-10` |
| 2 | 링크 5종 | `../METHODOLOGY.md`·`../REDESIGN_SPEC.md`·`../data/manifests/estimation_notes_D2_v0.md`·`data_gap_registry.md`·`mcp_server.md` | md 안에서는 맞지만 **웹에서는 전부 404**다. 렌더러가 `docs/` 기준 상대경로를 GitHub blob URL로 다시 쓴다 | `build_guide_page.py:link_target` |

새 수치는 하나도 넣지 않았다. 이 창의 규칙대로 렌더는 원고를 바꾸지 않는다.

### 렌더러가 조용히 지지 않게 한 것

가이드가 실제로 쓰는 마크다운 부분집합만 아는 렌더러다(제목 h1–h4·표·펜스코드·인용·
번호/불릿 목록의 lazy continuation·인라인 code/bold/italic/link). **원고에 새 구문이 들어오면
HTML에서 조용히 사라지는 것이 기본 실패 모드**이므로 두 겹의 방어를 넣었다.

- `--check`가 원문의 **표 행(271)·코드블록(4)·제목(35)**이 전부 페이지에 도달했는지 세고,
  하나라도 잃으면 0이 아닌 코드로 죽는다. `tests/test_tech_guide.py::test_html_render_loses_nothing`이
  그 판정을 부른다(pytest 58 → 59).
- 표 행의 열 수가 헤더와 다르면 렌더가 **예외**를 던진다. 실제로 한 번 걸렸다 — §3.1
  `source_id` 행의 `; \| + /`가 이스케이프된 파이프여서 셀이 하나 더 쪼개졌다. `cells()`가
  이스케이프되지 않은 파이프로만 나눈다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py    # 15 blocks
.venv/bin/python scripts/build_guide_page.py    # 34 sections, 271 table rows, 105 KB
.venv/bin/python scripts/build_site.py          # index/report/guide/evidence/memo/changelog + EFF 2
.venv/bin/python scripts/gate.py                # gate: OK (pytest 59 passed, audit ok 68/PARTIAL 4 불변)
```

브라우저에서 실제로 확인한 것(로컬 `http.server`): h2 8 · table 28 · tr 271 · pre 4 ·
`.gen` 15 · TOC 34개 링크가 **전부 실재 앵커를 가리킨다**(끊긴 앵커 0). `body.scrollWidth ==
innerWidth`(1280) — 가로 스크롤 없음. 표는 `.tblwrap`이 자체 스크롤한다.

수치 출처: 표 행·제목·블록 수는 `docs/TECHNICAL_GUIDE.md` 스캔에서 계산(`build_guide_page.check`),
랜딩 카드의 KB는 `web/guide.html` 파일 크기, 커밋 스탬프는 `GEN:stamp` 블록에서 정규식으로 읽는다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | `gate` audit `ok 68, PARTIAL 4` 불변. 이 사이클은 수치를 만들지 않았다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 저장소를 클론하지 않고 URL 하나로 네 질문을 다 읽는다. 생성 블록에 라벨이 붙어 **손으로 쓴 주장과 기계가 쓴 수치가 눈으로 갈린다** |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목 |
| ⑤ 산출물 — 다른 형식이 있는가 | **충족** | md뿐이던 것이 md + HTML(`/guide`) + 랜딩 카드. 데이터 패키지는 F7 |

### 인계

- **F7(데이터 사전)**: `data/package/data_dictionary.csv`가 준비본의 **열 존재**만 기술한다.
  F5가 만든 "이 열/계열을 어느 단계가 읽는가"(D6 4열·D4 11계열 무소비)를 사전에 넣을지 판단.
  가이드 §3이 정본이면 사전이 가이드에서 파생돼야 한다.
- **F10(그림)**: 이제 붙일 자리가 있다 — `guide.html`의 §6.4 근처에 인라인 SVG를 넣으면
  md 판에는 없고 웹 판에만 있는 그림이 된다. 그러면 두 판이 갈리므로, 그림을 md에도 두려면
  `build_tech_guide.py`가 SVG를 GEN 블록으로 쓰게 하는 쪽이 낫다.
- **F14(README)**: 저장소 첫 화면에서 `docs/TECHNICAL_GUIDE.md`와 `/guide` 둘 다로 가야 한다.
  `web/README.md`는 이번에 고쳤다(랜딩=보고서라고 적혀 있었고, 지금은 `build_site.py`가
  전 페이지를 만든다는 사실을 적는다).
- **미해결 코드 수정 4건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES`의 부재 계열 정리(F5).

## F7 (04:33) — 데이터 사전 통합: 사전은 싣지 않는 파일을 기술하고 실은 열의 55%를 기술하지 않았다

**한 일.** 지도대로 `data/package/data_dictionary.csv`와 가이드 §3의 필드 정의를 하나로 묶었다.
**가이드가 정본이고 사전은 그 투영**이다 — `scripts/build_data_package.py`가 §3의 필드 표를
파싱해 실제로 쓴 파일의 헤더에 맞춰 사전을 낸다. 정의가 없는 열이 하나라도 실리면 빌드가 죽는다.

### 가이드·사전에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | `data_dictionary.csv` | `SCHEMAS`를 그대로 찍은 85행 | **패키지에 없는 두 파일을 기술하고 있었다.** `D1a_facility_static`·`D1b_facility_panel`은 패키지에 존재하지 않는다 — 실리는 것은 `D1a_company_capacity`·`D1b_company_panel` 집계본이다. 반대로 실린 134개 열 중 **73개가 사전에 없었다**: 집계 2파일 13열, result 4파일 43열, `source_register` 16열, `D3.retrofit` 1열 | `build_data_package.py` 대 `data/package/*.csv` 헤더 |
| 2 | 같은 곳 | (경계 위반) | 사전이 기술하던 24개 열에 `facility_id`·`site`·`unit_name`·`commissioning_year`가 들어 있다. **`manifest.json`이 "시설 단위 절대값은 포함하지 않는다"고 선언한 바로 그 필드들**이다. 값은 나가지 않았지만 스키마는 나갔다 | `manifest.json:boundary` |
| 3 | §3.9 | "manifest.json ... **a merged data dictionary**" | 병합된 것이 없었다. 무엇과 무엇을 병합했다는 것인지 문장이 가리키는 대상이 존재하지 않는다. 지금은 사전이 무엇에서 파생되는지를 §3.10이 적는다 | `TECHNICAL_GUIDE.md:651` |
| 4 | §3.3 D2 표 | `year` 행 없음 | D2a·D2b 둘 다 `year`가 스키마 필수인데 필드 표에 없었다. **새 빌드가 이것을 잡아 죽었다** — 사전을 낼 수 없다고. 5-앵커 보간이라는 사실을 같이 적어 넣었다 | 빌드 실패 메시지 |
| 5 | §3.5 D4 | 필드 표 없음 | 5열을 산문에서 이름만 부르고 정의가 없었다. 표 신설. `unit`이 **파싱되지 않는 자유 텍스트**이고 어느 단계도 그것으로 환산하지 않는다는 사실을 열 정의에 넣었다 | `calibration.py:24-26` |
| 6 | §3.7 D6 | 필드 표 없음 | 10열이 GEN 커버리지 표에만 있었고(그나마 `company_id`·`year`·`source_id` 3열은 그 표에도 없다) 정의가 없었다. 표 신설 + **통화 주의** 명문화: 환율이 어디에도 적용되지 않으므로 한국 행은 bn KRW, 일본 행은 億円이다(§8 주장 8). `capex_total`이 result 파일의 동명 열과 **다른 양**이라는 것도 열 정의에 적었다 | `D6_company_financials.csv`; `e5_metrics.py:104-131` |
| 7 | §3.10 (신설) | 없음 | **집계가 `source_id`를 파괴한다.** §3.9의 "모든 데이터 행이 `source_register`로의 외래키를 갖는다"는 그대로 실리는 7개 입력 파일에만 성립하고, 패키지의 두 집계 파일에는 성립하지 않는다. 패키지만 받은 독자는 그 두 파일의 출처를 추적할 수 없다 | `build_data_package.py:main` |
| 8 | §3.10 (신설) | 없음 | `result_emissions_pathway`에 `support` 열이 없는 이유 — 경로는 **첫 번째 지원 시나리오에서만** 계산되고 나머지는 재실행되지 않는다. gap 파일에 POSCO·LOTTE 행이 없는 것도 같은 자리에서 밝힌다 | `e5_metrics.py:290`; `result_gap.csv` 8행 = MCI·NSC뿐 |

### 생성기로 만든 것

- `scripts/build_data_package.py::guide_fields` — §3의 두 가지 표 머리(`| Field | Definition | ...`와
  §3.10의 `| File | Field | ...`)를 파싱한다. 한 셀에 여러 열 이름(`` `a`, `b` ``), `**[req]**`
  꼬리표, `(D2a)` 한정자를 처리한다. `dictionary()`가 그 정의를 **실제 파일 헤더**에 맞춰
  붙이고, 붙지 않는 열이 있으면 `SystemExit`.
- `GEN:package` — 패키지 파일별 종류·행·열·정의 위치. 파일 목록은 `manifest.json`,
  열 수는 생성된 사전에서 읽는다. 빌드가 쓰지 않은 파일을 주장할 수 없다.
- `tests/test_tech_guide.py::test_data_dictionary_describes_what_ships` — 산출물 자체를 검사한다.
  실린 열 ⊆ 기술된 열, 기술된 열 ⊆ 실린 열, 정의 빈 행 0. (pytest 59 → 60)

### 검증

```
.venv/bin/python scripts/build_data_package.py   # 15개 파일, 사전 134행
.venv/bin/python scripts/build_tech_guide.py     # 16 blocks (package 신설)
.venv/bin/python scripts/build_guide_page.py     # 35 sections, 344 table rows (271 → 344)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 60 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 사전 134행 = `data/package/*.csv` 14개 파일의 헤더 합(빌드가 계산) ·
이전 사전 85행 = `src/cap/schemas.py::SCHEMAS` 열 수 · 미기술 73열은 헤더 대 이전 사전 차집합 ·
패키지 행수는 `data/package/manifest.json` · `result_gap` 8행/2사는 그 파일 · 경로의 지원 축
부재는 `src/cap/e5_metrics.py:290`(`supp == cfg.support_scenarios[0]` 가드) · D6 통화는
`data/prepared/D6_company_financials.csv`(NSC 2024 revenue 86,955 = 億円, LOTTE 2025 18,483 = bn KRW).

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변. 이 사이클은 수치를 만들지 않았다. 다만 F5가 연 "감사 시야 밖" 목록에 **네 번째 종류**가 붙는다 — 감사는 `data/prepared/`를 보고 `data/package/`를 보지 않으므로, 패키지가 무엇을 싣는지는 아무도 검사하지 않고 있었다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 데이터를 받아 열어 보면 열 하나하나에 정의·단위·정본 위치가 붙는다. "데이터셋 정의를 자세히"라는 요구가 문서에서 산출물까지 이어진다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. `get_data_package_manifest`가 내주는 매니페스트의 해시가 이번에 전부 바뀌었다(사전 재생성) |
| ⑤ 산출물 — 다른 형식이 있는가 | **개선** | md + HTML + **자기기술적 데이터 패키지**. 사전이 코드에서 파생되던 것이 문서에서 파생되는 것으로 바뀌어, 산출물이 문서를 배신할 수 없다 |

### 인계

- **F8(적대적 검토 1)** 목록에 세 줄 추가:
  ① "패키지의 집계 파일은 출처를 추적할 수 없다"(§3.10이 먼저 말한다),
  ② "⑥의 통화 혼재가 §3.7 열 정의에도 이제 적혀 있는데, 그러면 ⑥ 국가 간 비교를 왜 아직 싣는가",
  ③ "`result_emissions_pathway`는 지원 축 1개만 계산한다 — 나머지 축의 경로를 묻는 질문에 답이 없다".
- **F9(숫자 추적성)**: 이번에 만든 `defined_in` 열이 그 작업의 절반이다. 남은 절반은 **가이드 산문의**
  수치이고, `GEN` 밖 문장에 박힌 숫자를 세는 쪽이 빠르다.
- **가이드 렌더 부하**: 표 행이 271 → 344로 늘었다. `build_guide_page.py --check`가 전부 세고 있으므로
  누락은 자동으로 잡히지만, §3.10이 길어져 웹 판 §3이 무거워졌다. F15 교정 때 §3.10을 접을지 판단.
- **미해결 코드 수정 4건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES`의 부재 계열 정리(F5).

## F8 (05:03) — 적대적 검토 1: gap이 무엇까지의 거리인지 문서가 말한 적 없었다

**한 일.** 지도대로 Arc·심사자 입장에서 가이드를 통독하고 **답이 없는 질문 10개**를 §9
"The objections we expect"로 세운 뒤 그 자리에서 답을 채웠다. 이전 사이클들이 인계로 넘긴
질문 8개는 이미 §3–§6 상세절이 선제적으로 말하고 있었으므로, 그것들은 §9에서 **"어디가
먼저 말하는가"로 축약**하고, 목록을 만드는 과정에서 **처음 드러난 세 개**에 자리를 줬다.

시작 시 gate가 `FAILED (tests)`였다 — `test_generated_blocks_are_current`가 STALE. 원인은
F7 커밋이 `data/package/*`를 건드려 `GEN:stamp`의 상태 sha가 움직인 것. 규칙대로 지도보다
먼저 완결했다(생성기 재실행).

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6.2 | "Ranking is robust … zero rank reversals" | **표본 크기를 밝히지 않은 강건성 주장이었다.** 그 순위는 **네 기업**의 순위다. 네 항목은 원래 뒤집기 어렵다 — 12개 섭동에서 역전이 0인 것은 파이프라인이 요동치지 않는다는 증거이지 다섯 번째 기업에서도 성립한다는 증거가 아니다. 같은 자리에서 밝히도록 고쳤다 | `out/e5/metrics_company.csv` 4사 |
| 2 | §9 O3 (신설) | 없음 | **공시 좌표 둘 다 확약 한 줄로 만들어진다.** D7 12행 중 강제되는 것은 2행이고 회사당 하나씩이다(NSC 기미쓰 H2 주입, MCI 오사카 H2 연료전환). 즉 CAP이 계산한 **모든** 공시 좌표는 단일 확약 좌표이고, gap은 "기업의 전환계획과 프론티어의 거리"가 아니라 "그 결정 하나와 프론티어의 거리"다. §3.8이 두 사실을 따로 적고 있었으나 합친 문장이 없었다 | `GEN:d7_enforcement`; `out/e5/gap.csv` 8행 = MCI·NSC |
| 3 | §9.1 (신설) | 없음 | **프론티어가 몇 점인지 문서 어디에도 없었다.** §6.3의 "32개 중 4개"는 기술 스윕에 대한 진술이지 gap이 재는 집합이 아니다. 실제 비지배 집합은 firm×scenario당 **2~8점**(후보 10~31개 중). gap을 가진 것 중 가장 얇은 **MCI B20은 2점**이고, 보고된 836/1,326 bn KRW는 그 두 점까지의 거리다 | 새 `GEN:frontier_shape` ← `out/e5/frontier_points.csv` |
| 4 | 같은 블록 | 없음 | gap이 재는 축의 근거를 같은 자리에 붙였다 — **h2가 비용 분산의 64~77%를 지는데 h2 변동성은 추정이 아니라 사전값 0.25**다. ③ 수준을 못 믿는다는 §8 주장 3이 `gap_risk` 수준에도 그대로 적용된다는 뜻이고, 그 연결이 없었다 | `out/e5/variance_decomp.csv`; `calibration.py:FALLBACK_VOL` |
| 5 | §9 O6 (신설) | 없음 | 가이드 §6 표는 통화 혼재 때문에 ⑥을 **일부러** 싣지 않는다. 그런데 `out/e5/affordability.csv`와 MCP `get_affordability`는 무보정으로 싣는다. **경고가 문서에만 있고 파일에는 없다** | `affordability.csv` 헤더에 통화 주석 열 없음 |
| 6 | §9 O9 (신설) | 없음 | `result_emissions_pathway.csv`는 첫 지원 시나리오만 계산할 뿐 아니라 **`support` 열 자체가 없다** — 파일 안 어디에도 지원 시나리오가 선택되었다는 표시가 없다. F7 인계는 "1개만 계산"까지만 알고 있었다 | `e5_metrics.py:290`; 그 파일의 6개 열 |
| 7 | 머리말 | 읽는 순서 안내 없음 | 시간이 없는 독자에게 §3(데이터)·§4.1(결론을 정하는 가정)·**§9(우리가 생각하는 가장 센 반론)** 세 곳을 지목한다 | — |

O1·O2·O5·O7·O8·O10은 새 사실이 아니라 **이미 상세절이 말하던 것을 반론의 형태로 재배치**한
것이다(각각 4사 표본 / 2사만 좌표 / 위험인자 사전값 / support 축 무신호 / 미재계획 3축 /
패키지 집계본 `source_id` 소실). 이 창의 이전 인계 5줄이 여기로 들어왔다.

### 생성기로 만든 것

- `scripts/build_tech_guide.py::gen_frontier_shape` — `frontier_points.csv`를 `support == none`으로
  접고(§3.6대로 축이 전 행을 복제한다) firm×scenario별 후보 수·비지배 수를 세어 `gap.csv`와
  붙인다. 좌표가 없는 조합은 `**no coordinate**`로 남아 4사 8조합이 한 표에 다 보인다.
- `tests/test_tech_guide.py::test_section9_hand_written_counts_still_hold` — §9 산문에 박힌
  두 수(강제 확약 2행, gap 8행 = 4개 gap의 복제)를 `out/`에서 다시 세어 대조한다. 생성 블록
  **밖** 산문이라 생성기가 갱신하지 않으므로 파이프라인이 바뀌면 조용히 거짓이 될 수 있었다.
  (pytest 60 → 61)

### 검증

```
.venv/bin/python scripts/build_tech_guide.py    # 17 blocks (frontier_shape 신설), 90,528 chars
.venv/bin/python scripts/build_guide_page.py    # 37 sections, 364 table rows (35/344 → 37/364)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                # gate: OK (pytest 61 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 비지배 계획 수·후보 수는 `out/e5/frontier_points.csv`의 `on_frontier` 집계 ·
gap 값은 `out/e5/gap.csv` · h2 분산 지분은 `out/e5/variance_decomp.csv`를 회사별 평균 ·
h2 사전 변동성 0.25는 `src/cap/calibration.py:47 FALLBACK_VOL`(하드코딩 대신 import) ·
강제 확약 2행은 `GEN:d7_enforcement`가 엔진을 호출해 얻은 판정 · 지원 축 복제는
`gap.csv` 8행 대 서로 다른 (회사, 시나리오) 4쌍.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변. 이 사이클은 수치를 만들지 않고 이미 있던 산출물을 처음으로 세었다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 192행(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 가장 센 질문 열 개에 대한 답이 한 절에 모였다. 특히 "gap이 무엇까지의 거리냐"는 질문에 이제 수로 답한다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. 다만 O6이 MCP 쪽 결함을 하나 지목한다 — `get_affordability`가 통화 경고 없이 ⑥을 내준다 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md + HTML(`/guide` 37절) + 데이터 패키지. 새 형식은 없다 |

### 인계

- **F9(숫자 추적성)**: 이번에 §9 산문이 새 수치를 몇 개 들여왔고 전부 파일 경로를 달았다.
  F9는 **생성 블록 밖 문장의 숫자를 전수**로 세는 작업이고, `test_section9_...`가 그 작업의
  축소판이다 — 같은 방식(산출물에서 다시 세기)을 §1·§2·§6·§8 산문에 확대하면 된다.
  P1의 "4 of 32", P2의 "petchem 0% variance", §2의 "−0.05~0.00 / 0.20~0.73", "0 of 8"이 후보다.
- **F12(적대적 검토 2)**: §9는 열 줄로 닫았다. 재점검할 것 — ① O6의 MCP 결함은 문서가 아니라
  코드에서 고쳐야 한다(응답에 통화 경고 필드), ② O3을 읽은 Arc가 "그러면 gap을 왜 싣느냐"고
  물으면 §9에 답이 없다, ③ 2점 프론티어에 대한 거리를 bn KRW 단위로 §6.4가 여전히 그대로 적는다.
- **F10(그림)**: §9.1 표가 그림의 후보다 — (P50, TCaR) 평면에 후보·비지배·공시 좌표를 얹으면
  O3·O4가 한 화면이 된다. `frontier_points.csv`에 `p50`·`tcar`·`is_disclosed`·`on_frontier`가
  전부 있으므로 데이터는 이미 준비돼 있다.
- **미해결 코드 수정 5건**(창 밖): D6 통화 환산(F1), 광양 2고로 능력(F3), 미등록 `facility_id`
  조용한 탈락(F4), `FACTOR_SERIES`의 부재 계열 정리(F5), **`get_affordability` 통화 경고(F8 신규)**.

## F9 (05:33) — 숫자 추적성: 손으로 쓴 순위상관이 한 자릿수 틀렸고 계획 수는 어느 파일에도 없는 수였다

**한 일.** 지도대로 가이드의 **생성 블록 밖** 수치를 전수로 `out/`·`config.yaml`·`docs/*.csv`·
`data/prepared/`에 대조했다. 확인한 수는 약 60개(§1 P1–P4, §2 대리 통계, §3 산문, §4.1–§4.4
A-xx 영향 문장, §5 설정, §6.1–§6.5, §7, §8, §9). **대부분은 맞았다** — 틀린 여섯 개와
근거가 약한 두 개를 아래에 적는다. 손으로 쓴 범위가 두 번 다 틀렸으므로, 그 두 자리는
산문을 고치는 대신 **생성 블록으로 바꿨다**.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §2, `METHODOLOGY.md:234` | 대리비용·정본 P50 순위상관 "**−0.05 to 0.00** in steel" | **−0.56**이다(NSC NZ15 ρ=−0.564, POSCO 0.000). 자릿수가 하나 빠진 채 두 문서에 실려 있었고, **더 나쁜 사실을 실제보다 좋게** 적고 있었다. 게다가 그 범위는 NZ15 한 시나리오의 것인데 그 사실을 적지 않았다 — B20까지 넣으면 −0.56~+0.94다 | `out/e4/summary.csv` × `out/e2/plan_index.csv`; `tests/test_paper_numbers.py::surrogate_rho_*` |
| 2 | §6.3 | "of **51** enumerated plans only **43** are distinct" | **48 중 40**이다. 51도 43도 어느 산출물에도 없는 수다. 차이 8 = 묶음당 정확히 한 쌍이라는 뒤 문장은 그대로 성립한다(8묶음 각각 1쌍) | `out/e2/plan_index.csv` 48행; `out/e4/summary.csv`의 `central_cost` 묶음별 유일값 합 40 |
| 3 | §3.10 | "85 columns of **two files** … 73 of the **89** columns" | 옛 사전 85행은 **아홉 파일**에 걸쳐 있었고 그중 싣지 않는 두 파일 몫이 **24열**이다. 실린 열은 89가 아니라 **134**다 | `git show 4b44202~1:data/package/data_dictionary.csv` 대 현재 패키지 헤더 |
| 4 | §1 P2 | "an **even** hedge covers 0% of the variance" | 오역이다. 근거 문서의 "**고른** 헤지"는 *uniform*이 아니라 *chosen* — 위험회피 기준이 고른 수단(고정가 EPC)이 석화 꼬리분산의 0%를 덮는다는 뜻이다. "even hedge"는 존재하지 않는 균등 배분 헤지를 가리키고 있었다 | `docs/robustness_structural.md` §3-1 |
| 5 | §4.1 A-02 | "see **§5.1**" | §5에는 하위 절이 없다. 문서의 유일한 끊긴 상호참조였고(전수 확인), EEGS 내용은 §6.5에 있다 | 가이드 제목 전수 대조 |
| 6 | §8 주장 8 | "off by roughly 8% … **in a known direction**" | 방향을 안다고 적고 방향을 적지 않았다. 9.2원/엔에서 일본 2사 **분모가 약 8% 과대** → ⑥이 그만큼 **낮게** 읽힌다. 출처 위치(F1)도 없었다 | `docs/data_gap_registry.md` F1 |

### 근거가 약해 같은 문장에서 밝힌 것 두 개

| # | 어디 | 무엇 | 근거 |
|---|---|---|---|
| 7 | §4.4 A-22 | "worth about 1% of variance"는 **헤드라인 칸(NZ15·none)의 평균 1.1%**일 뿐이다. 전 칸 평균은 6.9%이고, **수소를 하나도 채택하지 않는 석화 B20 계획 2개에서는 분산의 전부(100%)**다. "2차항"은 파일이 아니라 헤드라인에 대한 진술이라는 것을 적었다 | `out/e5/variance_decomp.csv` (capex 몫: 전체 평균 0.069, NZ15·none 평균 0.011, 최대 1.000) |
| 8 | §6.1 | 시드 표가 **현재 실행보다 낡았다**. `docs/seed_stability.csv`의 정본 시드(20260806) 행이 NSC에서만 out/과 어긋난다(② 165.4·③ 34,488 대 155.6·32,961) — 스윕 이후 NSC의 최소비용 계획이 바뀌었다. 나머지 3사는 자릿수까지 일치. CV를 "위 표의 오차막대"가 아니라 "시드 잡음의 자릿수"로 읽으라고 적었다 | `docs/seed_stability.csv` 대 `out/e5/metrics_company.csv` |

### 맞았던 것 (대조해 확인, 변경 없음)

`4 of 32`·`25`·`7 of 8`(`out/m8/tech_epsilon.csv`) · `0 of 8`·2% gap·60초(`config.yaml:38,42`) ·
gap 2/4사와 NSC 1,255/4,651(`out/e5/gap.csv`) · `disclosed_skipped.csv` 4행에 NSC 없음 ·
TCaR 41–48%·석화 ② 71–73%·2050 중앙값 0.47배(`docs/process_alternative.md`) ·
CV 0.3–0.8/1.1–1.8/3–9%(`docs/seed_stability.csv`에서 재계산) · A-01 민감도 8위·×913.3·16/23행 ·
BF 17기와 200/150/250/300천원(`D1a`) · A-11 ≈39천원 교차·floor 0에서 3사 역전(`out/m5/penalty_axis.csv`) ·
A-05 30–42%(`out/sensitivity/screening.csv`) · s2 63/69 · naphtha 0/69 · D4 1–19관측 ·
re_price 175,000/198,000 · 110.2 · 3,500 · 10,240 · D7 ¥630.2bn+¥140bn·2.5 Mt/yr · D5 7행 15% ·
D6 회사별 연도창 · A-13 ×4.2.

### 생성기로 만든 것

- `scripts/build_tech_guide.py::gen_surrogate` (`GEN:surrogate`, §2) — firm×scenario 8묶음의
  ρ(대리비용, P50)·ρ(위험대리, TCaR)·대리 최저=정본 최저 여부를 표로 낸다. **손으로 쓴 업종별
  범위가 오류 1을 만들었으므로 범위를 손으로 쓰지 않는다.**
- `::gen_plan_distinct` (`GEN:plan_distinct`, §6.3) — 열거 계획 수·정본 기준 유일 계획 수·
  한 쌍씩 붕괴하는 묶음 수. 오류 2의 자리.
- 둘 다 `_e4_base_plans()`/`_rho()` 공유. scipy 없이 순위→피어슨(`test_paper_numbers.py`와 동일 규약).
- `tests/test_tech_guide.py::test_seed_sweep_staleness_is_described_as_it_is` — §6.1의 새 단락은
  "NSC에서만 어긋난다"는 판정이고 그 판정 자체가 낡을 수 있다. 스윕을 다시 뜨거나 다른 회사가
  어긋나면 실패한다. (pytest 61 → 62)

### 검증

```
.venv/bin/python scripts/build_data_package.py   # 15개 파일, 사전 134행 (불변)
.venv/bin/python scripts/build_tech_guide.py     # 19 blocks (surrogate·plan_distinct 신설), 92,502 chars
.venv/bin/python scripts/build_guide_page.py     # 37 sections, 373 table rows (364 → 373)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 62 passed, audit ok 68/PARTIAL 4 불변)
```

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변. 이 사이클은 수치를 만들지 않고 이미 있던 수치를 출처와 대조했다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | 대리 순위상관이 실제로 얼마나 나쁜지가 8묶음 표로 보인다. Arc가 "왜 E4를 따로 도느냐"고 물으면 표 하나가 답이다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md + HTML(`/guide` 37절 373행) + 데이터 패키지 |

### 인계

- **F10(그림)**: §9.1 표 + 이번 §2 표가 같은 그림의 두 축이다. `frontier_points.csv`에
  `p50`·`tcar`·`is_disclosed`·`on_frontier`가 있고, 대리 순위는 `e4/summary.csv`×`e2/plan_index.csv`로
  같은 평면에 얹을 수 있다 — "대리가 고른 점"을 프론티어 그림 위에 표시하면 오류 1이 그림이 된다.
- **F11(한계 절)**: §8 주장 3·4는 이미 정량. 정량이 없는 것은 주장 1(시설 단위 = 순서 정보)·
  주장 6(재계획 채널)이고, 후자는 §4.3 표가 이미 반쯤 답한다.
- **F12(적대적 검토 2)**: 오류 1이 §9 O5의 무게를 키운다 — 대리가 정본 순위를 이만큼 못 지키면
  "후보 생성기가 프론티어를 결정한다"는 반론이 더 세진다. §9에 그 줄이 없다.
- **§6.1 시드 스윕 재실행**(신규 백로그): E3–E5 × 5시드. 지금 CV는 NSC 옛 계획 위의 값이다.
  30분 창에 들어갈지 미확인이므로 시작하지 않았다(중단 파이프라인 금지 규칙).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F10 (06:03) — 그림 한 장이 문서의 정의를 반증했다: gap은 최근접 거리가 아니고, 지금 산출된 모든 비용 다리는 경계 끝점까지의 거리다

**한 일.** 지도의 F10대로 **경계 + 공시 좌표 + gap을 한 장에** 담았다. 4사 × 2시나리오 8칸
소묶음 그림을 `scripts/build_tech_guide.py::gen_gap_figure`가 `out/e5/frontier_points.csv`와
`out/e5/gap.csv`에서 그려 `docs/figures/frontier_gap.svg`로 쓰고, 가이드 §2(“Efficient frontier”
정의 바로 아래)에 `GEN:gap_figure` 블록으로 걸었다. **그림을 그리는 과정이 문서의 오류를
잡았다** — 다리가 어디서 끝나는지 좌표로 찍어보니 경계 위가 아니었다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §9.1 마지막 단락 | gap은 “**nearest-point distance to a small finite set**”이고 그래서 “불연속적으로 움직인다” | **최근접 거리가 아니다.** `_gap`은 축에 평행한 두 거리이고, 비교 대상은 유한 점집합이 아니라 그 점들의 **구간선형 보간**(`np.interp`)이다. 최근접(유클리드) 거리로 읽으면 gap이 실제보다 작은 것으로 오해된다 | `src/cap/e5_metrics.py:61-81` |
| 2 | 같은 자리 · O4 · METHODOLOGY §정의 | 없음 | **지금 산출된 4개 gap의 비용 다리 4개 전부와 위험 다리 3개는 보간이 아니라 클램프 값이다.** 네 공시 좌표 모두 자기 경계의 꼬리위험 구간 **위**에 있다(초과 배수 1.01×~477×) — 그래서 비용 다리는 경계의 *가장 위험한 끝점*까지의 거리이고, 정의상 **하한**이다. 즉 보고된 gap은 과소평가된 값이다. 어느 문서에도 이 분기가 적혀 있지 않았다 | `out/e5/frontier_points.csv` × `out/e5/gap.csv`; 판정 코드 `e5_metrics.py:_gap` 클램프 분기 |
| 3 | `METHODOLOGY.md:200` | “공시 계획 좌표에서 경계까지의 수평·수직 거리”에서 끝 | 클램프(구간 위 → 끝점, 하한)와 **NaN**(구간 아래 → 날조 방지) 두 분기를 정본에 적었다. 가이드가 METHODOLOGY와 어긋난 것이 아니라 **양쪽이 같이 불완전**했으므로 정본을 먼저 고쳤다 | 같은 함수의 docstring이 이미 두 분기를 말하고 있었다 |

O4의 답도 고쳤다 — “그 한 줌까지의 최근접 거리”가 아니라 “경계 **내부의 점이 아니라 끝점**까지의
거리이고 그래서 하한”이다. P3(“지출 부족은 그 자체로 위험 포지션”)에 유리한 방향의 오차라는
점도 같은 자리에 적었다: 하한이므로 P3를 과대주장하지 않는다.

### 만든 것

- `gen_gap_figure` + `_panel` + `_x`(비율 표기) — 칸마다 자체 축, 후보(회색)·비지배 경계(파란
  선+점)·공시 좌표(빨간 마름모)·두 gap 다리(빨간 파선, 끝에 빈 원). 좌표가 없는 4칸은
  “no disclosed coordinate (§6.4)”라고 칸 안에 적어 **O2가 그림에서 바로 보인다**.
  캡션의 수(클램프 4/4·3/4, 초과 배수)는 전부 생성값이다 — 손으로 쓴 범위가 F9의 오류 1을
  만들었으므로 같은 실수를 반복하지 않는다.
- SVG는 자체 `<style>`(다크 모드 미디어쿼리 포함)을 들고 있어 GitHub 마크다운에서도 그대로 뜬다.
- `scripts/build_guide_page.py`: 마크다운 이미지 한 줄(`![alt](*.svg)`)을 **인라인 SVG**로
  렌더한다(에셋 복사·경로 재작성 없음). `check()`에 그림 수 대조를 추가해 렌더러가 그림을
  조용히 삼키면 빌드가 실패한다. 표·코드블록과 같은 규약이다.
- `tests/test_tech_guide.py` 2개 추가(62 → 64):
  `test_gap_legs_are_clamped_as_the_figure_and_prose_say`는 클램프 4/4·3/4를 산출물에서 다시
  세어 §9.1·O4의 손으로 쓴 판정과 대조한다(보간 분기로 넘어가면 “하한”이 거짓이 되므로
  반드시 실패해야 한다). `test_figure_is_built_and_referenced`는 그림 파일과 참조를 잡는다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 20 blocks (gap_figure 신설), 93,746 chars
.venv/bin/python scripts/build_guide_page.py     # 37 sections, 373 table rows, figure 1
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 64 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 후보·비지배·공시 좌표 = `out/e5/frontier_points.csv`(`support == none`) ·
gap 두 다리 = `out/e5/gap.csv` · 클램프 판정 = 같은 두 파일에서 `tcar_공시 > max(tcar_경계)`,
`p50_공시 > max(p50_경계)` · 초과 배수 1.01×(NSC NZ15: 33,276.8 / 32,961.3)~477×(MCI B20:
1,325.9 / 2.8) · 렌더 확인은 `web/guide.html`에서 `figure svg` 630px 인라인.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변. 새 수치는 없고 기존 산출물의 기하를 그렸다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | 방법 전체가 한 화면이다. 그리고 그 그림이 “gap은 하한”이라는, 지금까지 어느 문서도 하지 않던 말을 하게 만들었다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | **개선** | md / HTML / 데이터 패키지에 더해 **SVG 그림**이 생겼고, 두 형식 모두에서 같은 파일을 쓴다 |

### 인계

- **F11(한계 절)**: §8에 클램프 항목이 없다. “gap은 하한”은 한계이자 방향 있는 오차이므로
  §8 주장으로 승격할 후보다. 크기도 있다 — MCI B20은 경계 꼬리위험의 477배 위에 있어
  사실상 “경계와 비교 불가”에 가깝다.
- **F12(적대적 검토 2)**: 새 반론 후보 — *“공시 좌표가 경계 구간 밖이면 그 비교가 의미가 있나”*.
  477× 칸에서는 경계가 사실상 다른 세계다. O4를 한 번 더 세게 쓸 자리.
- **그림 확장**(선택): 대리(E2)가 고른 점을 같은 평면에 얹으면 F9 오류 1이 그림이 된다.
  `_panel`이 이미 임의 점을 받게 되어 있어 계열 하나를 추가하는 일이다.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability`
  통화 경고(F8).

## F11 (06:33) — 한계에 크기를 붙였더니 주장 1이 사실과 달랐다: 실측 시설 배출은 23기 중 0기다

**한 일.** 지도의 F11대로 §8 "What we do not claim"에 정량 근거를 붙였다. 다섯 개 주장에는
크기가 아예 없었고, 하나는 크기를 붙이려고 데이터를 보다가 **틀렸다는 것이 드러났다**.
크기는 `scripts/build_tech_guide.py::gen_limits`(`GEN:limits`)가 산출물에서 다시 계산한다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §8 주장 1 | 시설 절대값은 "per-site **measured emissions**를 얻을 때까지" 순서 정보다. "**For Japan they now are**; for Korea they are not" | **실측 시설 배출은 23기 중 0기다.** 사업소(site) 실측은 12기의 사업소에 있고(NSC 10·MCI 2), D1b에 들어가는 것은 **NSC 10기뿐**이며 그것도 사업소 간 **분포**로만 쓰인다 — 수준은 언제나 회사 Scope 1 총량의 재척도다(`prepare_raw.py:122-125`). 게다가 **MCI 2기는 두 사업소(ICH·OSK) 모두 실측 행이 원자료에 있는데도 상향식 추정**이다. 나라별 구분("일본은 되고 한국은 안 된다")이 아니라 업종·경로별 구분이었다 | `data/raw/jp_site_emissions.csv` × `D1a_facility_static.csv` × `D1b_facility_panel.csv:source_id` |
| 2 | §8 주장 9 | 없음 | **gap은 하한이다**를 §8에 넣었다. F10이 그림으로 밝힌 사실이 §9.1·O4에만 있었다 — 방향이 알려진 오차이므로 "못 하는 말" 목록에 속한다. 반대 분기(공시 좌표가 경계 구간 **아래**)가 NaN이고 현행 실행에서는 발생하지 않는다는 것, 그리고 §6.4의 빈칸은 그 분기가 아니라 **공시 좌표 부재**라는 것을 같은 자리에서 구분했다 | `src/cap/e5_metrics.py:61-81`; `out/e5/gap.csv` 8행 전부 값 있음 |
| 3 | §8 주장 2 | "one primary source … otherwise unverified" | 크기를 붙였다: 석화 4기 전부 **분자·분모가 모두 구성값**이고(내재 원단위 0.95 tCO₂/t = 주입한 NCC 루트 계수), 생산량이 2022–2024 내내 **4기 모두 불변**이며, 모형이 덮는 몫은 회사 Scope 1의 **MCI 25% · LOTTE 55%**다 | `D1b_facility_panel.csv` × `data/raw/facility_panel.csv` |

### 새로 붙인 크기 (GEN:limits, 전부 생성값)

| 주장 | 크기 | 출처 |
|---|---|---|
| 3 TCaR 수준 | 단위근 검정이 **8개 계열 0개**에서 기각(관측 7–19개), 반감기 10년 평균회귀 대안에 대한 **검정력 4.9–5.4%**(명목 크기 5%) — "untestable"이 수사가 아니라 측정값이다 | `docs/price_process_test.csv` |
| 5 `support` 축 | `gap.csv` 8행 = **4개 유일 gap**, `current`와 `none`의 최대 불일치 **0** | `out/e5/gap.csv` |
| 6 계획선택 채널 | **11묶음 중 4묶음** 재계획, 나머지 중 **3개**는 재계획 없이는 읽을 수 없다(`carbon_slow`·`ppa_costly`·`retire_free`) | `out/m5/bundle_matrix.csv` × `run_scenarios.py::REPLAN_REQUIRED` |
| 9 gap = 하한 | 비용 다리 **4/4**·위험 다리 **3/4** 클램프, 공시 좌표가 자기 경계 최대 꼬리위험의 **1.01×–477×** 위 | `out/e5/frontier_points.csv` × `out/e5/gap.csv` |

주장 4·7·8은 문장 자체에 크기가 있어 표에서 뺐고, 그 사실을 표 밑에 적었다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks (limits 신설), 97,697 chars
.venv/bin/python scripts/build_guide_page.py     # 37 sections, 380 table rows (373 → 380), figure 1
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 65 passed, audit ok 68/PARTIAL 4 불변)
```

`tests/test_tech_guide.py::test_no_facility_carries_a_measured_emission` 추가(64 → 65) —
D1b에 배분·상향식이 아닌 출처가 생기거나, 사업소 실측이 있는데 상향식으로 남은 시설 집합이
`{MCI_ICH_CR, MCI_OSK_CR}`에서 바뀌면 실패한다. 주장 1은 손으로 쓴 단정이므로 그때 다시 써야 한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **문제 발견(수정 아님)** | gate audit `ok 68, PARTIAL 4` 불변이지만, **MCI 두 사업소의 실측 배출이 원자료에 있는데 쓰이지 않는다**. audit은 열 채움률을 보므로 이 낭비를 잡지 못한다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음(3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "그래서 얼마나 틀릴 수 있느냐"고 물으면 §8 표 한 장이 답이다. 검정력 4.9–5.4%는 "TCaR 수준을 못 믿는다"는 말의 가장 강한 형태다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(37절 380행) / SVG / 데이터 패키지 |

### 인계

- **F12(적대적 검토 2)**: 새 반론 후보 — *"실측이 있는데 왜 안 쓰나"*(MCI ICH·OSK). 답은
  "석화 회사 총량에 비NCC 설비가 섞여 있어 사업소 배출로도 NCC 몫을 분리할 수 없다"이고,
  이것을 §3.2나 O 목록에 적은 문서가 없다. 크기는 커버리지 25%다.
- **MCI 사업소 실측 미사용**(신규 백로그, 코드): `prepare_raw.py`의 석화 분기는 `SITE_EM`을
  아예 보지 않는다. 사업소 배출을 상한으로 쓰는 것만으로도 상향식 추정의 검증이 된다
  (ICH·OSK 사업소 총량 대 모형 NCC 배출). 30분 창에 코드+재실행이 안 들어가 시작하지 않았다.
- **F13(용어집)**: TCaR·frontier gap·surrogate·evidence tier. §8 표가 새 용어를 늘리지는 않았다.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드. 지금 CV는 NSC 옛 계획 위의 값이다.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F12 (07:15) — 적대적 검토 2: 경계는 한 기술계획의 계약 변형이었고, 그 계획은 대리가 꼴찌로 매긴 것이다. 그리고 문서의 "twelve"는 세 곳에서 base를 묶음으로 잘못 센 수였다

**한 일.** 지도의 F12대로 §9 반론 목록(O1–O10)을 산출물과 다시 대조하고, 새 구멍 셋을
O11–O13으로 추가했다. 대조 과정에서 **세는 수가 틀린 곳 셋**과 **문서가 한 번도 하지 않은
가장 센 반론 하나**가 나왔다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6 헤드라인 `GEN:headline` | "**12** assumption bundles have been evaluated" | **11개다.** 생성기가 `summary.csv`의 `bundle.nunique()`를 셌는데 그중 `base`는 가정 묶음이 아니라 나머지 열하나를 차분하는 **기준**이다. 생성기를 고쳐 `base`를 빼고, 칸 수(16)를 같은 문장에 붙였다 | `out/scenarios/summary.csv` (12묶음 = 11 + base, 묶음당 16행) |
| 2 | §4.3 첫 단락 | "the **twelve** firm × scenario × support cells" | **16칸이다** (4사 × 2시나리오 × 2 support). 오류 1의 12가 여기로 복사됐다 | 같은 파일, `base` 16행 |
| 3 | §9 O8 | "Three of your **twelve** sensitivity axes" | **열한 축이다.** 같은 12의 세 번째 복사본. 반론 제목 자체가 틀린 수를 들고 있었다 | 같은 파일 |
| 4 | §9 O1 | 순위가 "**twelve** perturbations"에서 안 뒤집힌다 | **열셋이다** — 가정 묶음 11(`rank_preserved = 1` 11/11) + 평균회귀 + 중앙값 정규화. 12는 우연히 맞는 수가 아니라 오류 1의 네 번째 복사본이었다 | `out/m5/bundle_matrix.csv` × `out/process/{gbm,ou,gbm_median}/e5/metrics_company.csv` (② 순서 세 실행 모두 동일) |
| 5 | §6.2 "Ranking is robust" | 출처 없음 | 같은 자리에 파일을 붙였다. "GBM vs 평균회귀·양쪽 정규화"를 뒷받침하는 파일이 문서 어디에도 없었고, 이번에 세 실행의 ② 순서를 직접 대조해 확인했다 | 위와 같음 |
| 6 | §1 P1 상태칸 | "약하게 성립: 강제 기술일정 32개 중 4개만 비지배" | 축이 다르다. **보고된 실행에서 여덟 묶음 전부의 비지배 점이 한 기술계획의 계약 변형**이다. 경계가 기울어지는 이유는 기술이 아니라 계약이다 | `out/e5/frontier_points.csv` (`base_plan_id` nunique = 1, 8/8) |

### 새 반론 셋

- **O11 — "경계가 한 계획의 계약 변형이고, 그 계획은 최적화기가 꼴찌로 매긴 것 아닌가."**
  맞다. 8/8 묶음에서 비지배 점의 `base_plan_id`가 하나이고(시설·기술·연도·총 CAPEX 동일),
  점들은 PPA 비중과 고정가 EPC 여부로만 갈린다 — 현행 실행에서 CCfD를 서명한 경계 점은 없다.
  그래서 frontier gap은 "더 나은 **계획**을 고를 수 있었나"가 아니라 "같은 계획을 더 낫게
  **계약**할 수 있었나"를 잰다. 게다가 그 계획은 E2 대리 비용 순위에서 **여덟 중 다섯에서
  꼴찌**, **여덟 중 일곱에서 하위 절반**이다 — §2가 이미 측정한 대리 실패를 경계 쪽에서 본 것이다.
  수는 `GEN:frontier_shape`에 새 열(`Distinct schedules on frontier`)과 문장으로 생성된다.
- **O12 — "gap 넷 중 둘은 자기 경계 밖 수백 배인데 그게 비교인가."** MCI B20은 자기 경계
  최대 꼬리위험의 **477×** 위다. 나머지 셋은 1.01×·1.42×·2.08×로 비교가 성립한다. §8 주장 9는
  방향(하한)만 말했고, 방향으로 부족한 칸이 하나 있다는 말은 없었다.
- **O13 — "상향식으로 추정한 그 공장에 실측이 있는데 왜 원자료 폴더에 두는가."** MCI 두 기다.
  `jp_site_emissions.csv`의 FY2023 사업소 총량(市原 1,107,038 · 大阪 1,454,608 tCO₂)에 대해
  D1b의 상향식 NCC 추정은 각각 **49%·31%**다 — 사업소 행은 만족되는 **상한**이고 아무도 확인한
  적이 없다. 대체재는 아니다(사업소 행은 다공장 사업소의 에너지 CO₂ Scope 1+2, 모형 단위는
  크래커 하나). 상한 검증으로 쓰는 데는 비용이 안 드는데 하지 않는다. F11 인계의 답이다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks, 101,987 chars
.venv/bin/python scripts/build_guide_page.py     # 37 sections, 383 table rows (380 → 383), figure 1
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 67 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 경계 구성 = `out/e5/frontier_points.csv`(`support=none`, `on_frontier`, `base_plan_id`·
`ppa_share`·`epc`·`ccfd`) · 대리 순위 = `out/e2/plan_index.csv:npv_cost_bnkrw` · 묶음·칸 수 =
`out/scenarios/summary.csv` · 순위 불변 = `out/m5/bundle_matrix.csv:rank_preserved` +
`out/process/{gbm,ou,gbm_median}/e5/metrics_company.csv` · 초과 배수 = `frontier_points.csv` ×
`out/e5/gap.csv` · MCI 사업소 = `data/raw/jp_site_emissions.csv` × `data/prepared/D1b_facility_panel.csv`.

테스트 2개 추가(65 → 67). `test_frontier_is_one_schedule_per_bundle`은 경계에 두 번째
기술계획이나 CCfD 점이 생기면 실패한다 — 그때 O11·P1·§9.1 생성문을 다시 써야 한다.
`test_base_is_not_counted_as_an_assumption_bundle`은 11·16이 바뀌거나 "twelve sensitivity axes"
문자열이 되돌아오면 실패한다.

**METHODOLOGY.md도 고쳤다.** §9-6 항목 10(경계 퇴화)은 "원인은 후보 생성이 아니라 위험 규약"까지만
적고 있었고, 퇴화의 **크기**(8/8 한 계획, 계약 축만 기움, 대리 꼴찌 5/8)가 정본에 없었다.
가이드가 정본을 앞서지 않도록 같은 사실을 정본에 먼저 적었다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **문제 재확인** | audit `ok 68, PARTIAL 4` 불변. MCI 사업소 실측 미사용을 O13으로 문서에 올렸다(수정은 아직) |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "그래서 gap이 무엇의 거리냐"고 물으면 O11 한 줄이 답이다. 팔기 나쁜 답이지만 사실이고, 먼저 말하는 쪽이 낫다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(37절 383행) / SVG / 데이터 패키지 |

### 인계

- **F13(용어집)**: O11이 용어 부담을 늘렸다 — `base_plan_id`(기술계획)와 계약 변형의 구분이
  이제 P1·§9.1·O11 세 곳에서 쓰인다. 용어집 첫 항목 후보.
- **경계 그림 확장**(선택): §2 그림의 경계 점들이 전부 한 계획의 계약 변형이라는 사실은
  그림에 안 보인다. `_panel`에 계약 라벨(PPA 비중)을 붙이면 O11이 그림이 된다.
- **MCI 사업소 상한 검증**(백로그, 코드): O13이 제안한 검증은 `prepare_raw.py` 석화 분기에
  경고 한 줄이면 된다(사업소 총량 초과 시 실패). 재실행이 필요해 창 안에서 시작하지 않았다.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F13 (07:49) — 용어집을 쓰다가 "경계에 CCfD가 없다"가 관측이 아니라 구성이라는 것이 드러났다

**한 일.** 지도의 F13대로 외부 독자용 용어집을 §10으로 붙였다. 용어 하나하나에 "그래서 그 수는
무엇 위에서 세는가"를 붙이는 작업이었고, 그 과정에서 **세 문서가 공유하던 사실 오류 둘**과
**어느 문서에도 없던 축약 하나**가 나왔다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §1 P2 · §9 O11 · §9.1 `GEN:frontier_shape` · METHODOLOGY §9-6 항목 10 | "현행 실행의 경계 점 중 CCfD를 서명한 것은 없다" — **관측처럼** | **구성이다. 서명할 수 없다.** E5는 비공시 후보를 전부 `ccfd=0`으로 다시 만들고(`e5_metrics.py:200`), D5에는 `ccfd` 행이 **0개**라 서명해도 값이 같다(`plancost.py:258`). 두 겹으로 비활성이다. 그런데 **E2는 48개 중 26개 계획에서 CCfD를 서명한다** — 대리 목적함수가 프록시 탄소변동성에 대해 공제를 주기 때문(`e2_milp.py:263`). 즉 **P2의 세 수단 중 CCfD는 기각된 것이 아니라 시험된 적이 없다** | `out/e5/frontier_points.csv`(`ccfd` 전 행 0) × `out/e2/plan_index.csv`(`ccfd` 합 26/48) × `data/prepared/D5_policy_support.csv`(7행, `ccfd` 0행) |
| 2 | §1 P1 · O11 · §9.1 | "8묶음 **전부**에서 비지배 점이 한 기술계획의 계약 변형" | 맞지만 **넷은 산술이다.** 여덟 중 넷(LOTTE 양쪽·MCI B20·POSCO B20)은 후보 집합에 기술 일정이 **애초에 하나뿐**이라 두 번째가 나올 수 없다. 진단이 서는 곳은 일정이 둘·셋 있었던 **나머지 넷**이고, 거기서 4/4가 하나로 무너진다. 표에 `Schedules available` 열을 새로 생성한다 | `out/e5/frontier_points.csv` (비공시 `base_plan_id` nunique = 1,1,1,2,3,2,1,2) |
| 3 | §6.3 `GEN:plan_distinct` | "48개 중 40개가 정본 평가에서 구별된다"까지만 | **E5의 축약은 더 크고 방향이 다르다.** E5는 E2 계획을 (시설·기술·연도) 일정으로 중복 제거해 **13개 기술 일정**으로 줄인 뒤 고정 계약 격자(PPA 5수준 × EPC 2 = 10점)를 다시 씌운다(`e5_metrics.py:184-201`). **E2가 고른 계약은 보고되는 어떤 수에도 도달하지 않는다.** 어느 문서에도 없던 사실이다 | `out/e5/frontier_points.csv` × `out/e2/plan_index.csv` |
| 4 | §2 "Efficient frontier" 정의 | "the Pareto non-dominated set in the (P50, TCaR) plane" | 어느 집합 위인지가 빠져 있었다. **한 칸(기업×시나리오×support) 안에서, E5가 재구성한 후보 집합 위**다 — E2가 낸 계획 위가 아니다 | `e5_metrics.py:51-58`, 오류 3과 같은 근거 |
| 5 | §2 vs §9.1 "candidate plans" | 같은 말로 **다른 집합**을 세고 있었다 | §2 대리 표의 `Plans`는 **E2 산출**(8·6·9·8·5·5·4·3), §9.1의 `Candidate plans`는 **E5 재구성**(10–31)이다. 화해시키지 않고 용어집에서 둘 다 이름 붙였다 — 뒤의 집합은 계약 차원이 합성이다 | 같은 두 파일 |

### 새로 붙인 것

- **§10 용어집** (손으로 쓴 산문, 20항목): bundle/cell, technology schedule(`base_plan_id`),
  contract variant, candidate plan(모호성 명시), surrogate, authoritative revaluation, incremental,
  resource-cost basis, P50/P90, TCaR, efficient frontier, frontier gap, disclosed coordinate,
  enforceable commitment/`resolution`, support scenario, evidence tier, λ tangency, policy wedge,
  flexibility value, units. 항목마다 정의하는 코드 위치를 붙였고 **수치는 넣지 않았다** —
  F12의 "twelve" 복사 사고가 손으로 옮긴 수에서 났기 때문이다.
- 용어집에서 확인한 비자명한 사실 하나: **TCaR은 탄소지출 차감 여부와 무관하게 같다.**
  `dcarb`가 계획별 상수라 P50·P90을 같은 크기로 옮긴다(`e5_metrics.py:213-217`). ③을 탄소 처리가
  다른 연구와 비교할 때 필요한 문장인데 어디에도 없었다.
- **METHODOLOGY.md도 고쳤다** (정본 우선): §9-6 항목 9에 E5의 일정 축약·계약 격자·`ccfd=0` 강제를
  적고, 항목 10의 "CCfD 서명 없음" 괄호를 지웠으며 "여덟 묶음 전부" 옆에 넷은 산술이라는 단서를 붙였다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks, 111,189 chars (101,987 → )
.venv/bin/python scripts/build_guide_page.py     # 38 sections (37 → 38), 383 table rows, figure 1
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 68 passed, audit ok 68/PARTIAL 4 불변)
```

테스트 1개 추가(67 → 68). `test_ccfd_cannot_reach_the_frontier`는 E5 격자가 `ccfd=0`을 그만
강제하거나, 격자 크기가 바뀌거나, D5에 `ccfd` 행이 생기거나, E2가 CCfD를 그만 서명하면 실패한다
— 넷 중 무엇이든 §1 P2·O11·§9.1·§10을 다시 써야 하는 사건이다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지(+발견) | gate audit `ok 68, PARTIAL 4` 불변. 새 발견은 데이터가 아니라 **수단**의 미사용이다 — D5에 CCfD 행사가가 없어 계약 수단 하나가 통째로 비활성 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "PPA·EPC·CCfD 다 봤나"고 물으면 이제 "둘만 봤다"가 문서에 있다. 용어집은 미팅 중 되묻기를 줄인다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **CCfD를 시험 가능하게 만드는 일**(신규 백로그, 코드+데이터): 둘 다 필요하다 — D5에 CCfD 행사가
  행(한·일 각각 근거 있는 값), 그리고 E5 계약 격자에 `ccfd` 축 추가(격자가 10점 → 20점, E5 재실행
  필요). 지금 P2는 세 수단 중 둘만 시험한다.
- **③ 번호 오기**(코드 주석, 미수정): `e5_metrics.py` 모듈 독스트링과 `plancost.py:216`이 분산분해를
  ③이라 부르는데 정본(METHODOLOGY §7 표)에서 ③은 TCaR이다. 고쳤다가 **되돌렸다** — 주석 한 줄만
  바꿔도 gate의 `out/ vs data/raw`가 STALE로 뒤집혀 다음 창이 out/를 의심하게 된다. 파이프라인을
  다시 도는 사이클에 같이 넣어라.
- **F14(README 연결)**: 용어집이 생겼으므로 README에서 가이드로 가는 링크는 §10을 함께 가리키는 것이 낫다.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드.
- **MCI 사업소 상한 검증**(백로그 유지, O13): `prepare_raw.py` 석화 분기에 경고 한 줄.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F14 (08:12) — README에서 가이드로 가는 길을 냈더니, 첫 화면 요약이 정본과 세 곳에서 어긋나 있었다

**한 일.** 지도의 F14대로 저장소 첫 화면에서 `docs/TECHNICAL_GUIDE.md`에 도달 가능하게 했다.
링크는 한 줄이었지만, **링크가 없는 동안 외부 독자가 대신 읽던 README 요약**을 정본과 대조하니
사실 오류 셋이 나왔다. 랜딩 카드에서 둘 더 나왔다.

### 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | README 방법 요약 2 · `docs/literature_map.md` §2 | 상관 몬테카를로 "**N=5,000**" | **10,000이다.** 5,000은 `REDESIGN_SPEC.md:249`의 기본값이고, 그 수에서 P50/TCaR 수렴 검사가 2.6% 이탈해 스펙의 상향 조항을 따라 올린 값이 정본이다. 두 문서가 설계 기본값을 실행값으로 적고 있었다 | `config.yaml:14-15` (`simulation.n_sims: 10000` + 상향 사유 주석), gate의 `out/ 실행 설정` 줄 |
| 2 | README 방법 요약 3 | "선택 가능한 계획 **전체**를 (기대비용, TCaR) 평면에 놓고 **ε-constraint로 frontier 추적**" | 보고되는 경계는 E2가 ε-constraint로 열거한 계획 위가 **아니다**. E5가 E2 계획을 (시설·기술·연도) 기술일정으로 중복 제거한 뒤 고정 계약 격자(PPA 5 × EPC 2)를 재부여하고 `ccfd=0`을 강제한 **재구성 집합** 위의 좌하 포락선이다. README가 대리 열거 단계와 정본 재평가를 한 문장에 뭉개고 있었다 — 가이드 §2가 "가장 중요한 구조적 사실"이라 부르는 바로 그 구분이다 | `src/cap/e5_metrics.py:184-201`(격자 재부여), `:50-58`(`_frontier` 포락선), 가이드 §2·§6.3 |
| 3 | README 방법 요약 2 | "P50 = 기대비용, TCaR = P90−P50"까지만 | 무엇 위의 P50인지가 빠졌다. 둘 다 **무전환 기준선 대비 증분**이고 **탄소지출 차감 후**(자원비용 기준)다. 탄소 처리가 다른 연구와 비교하려면 이 조건이 있어야 한다 | `src/cap/e5_metrics.py:1-6` 독스트링, `:210-217` |
| 4 | `web/index.html` 가이드 카드 (`build_site.py:201`) | "· **8장**" | **10장이다.** F13이 §10 용어집을 붙인 뒤에도 상수 "8장"이 그대로였다. 이제 가이드의 `## ` 제목을 세서 쓴다 | `docs/TECHNICAL_GUIDE.md` |
| 5 | `web/index.html` 시나리오 카드 (`build_site.py:193`) | "묶음 **12종**" | **11종이다.** `bundle.nunique()`가 `base`를 가정 묶음으로 셌다 — **F12가 가이드 네 곳에서 고친 그 오류의 다섯 번째 사본**이고, 문서가 아니라 사이트 빌더에 남아 있었다. 이제 `- {"base"}` 한다 | `out/scenarios/summary.csv` (12묶음 = 11 + base) |

### 새로 붙인 것

- **README 상단 진입 블록**: 가이드 링크 + 어느 절을 읽을지(§3 데이터셋 / §4.1 결론을 좌우하는
  가정 / §6 결과가 선 자리 / §8 못 하는 말 / §9 가장 센 반론 / §10 용어집) + `<!-- GEN:x -->`
  블록을 손으로 고치지 말라는 규칙 + 웹 판 위치.
- **부속 도구 표에 3행 추가**: `scripts/gate.py`(매 사이클 돌리는 도구인데 README에 없었다),
  `scripts/build_tech_guide.py`, `scripts/build_guide_page.py`.
- **실행 절 검증 블록에 `scripts/gate.py`** 한 줄.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks, 111,189 chars (불변)
.venv/bin/python scripts/build_site.py           # guide 38 sections; 카드 "10장" · "묶음 11종"
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 70 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 표본 수 = `config.yaml:15` · 경계 구성 = `src/cap/e5_metrics.py:50-58,184-201` ·
장 수 = `docs/TECHNICAL_GUIDE.md`의 `## ` 제목 · 묶음 수 = `out/scenarios/summary.csv`.

테스트 2개 추가(68 → 70). `test_repo_front_door_reaches_the_guide_and_agrees_with_config`는
README가 가이드 링크를 잃거나 `config.yaml`의 `n_sims`와 어긋나면 실패한다.
`test_landing_card_counts_are_counted_not_typed`는 카드의 두 수가 다시 상수가 되거나
`base`를 묶음으로 세면 실패한다.

**가이드 본문은 건드리지 않았다.** F14는 도달 경로 작업이고, 가이드 수치는 F9·F12·F13에서
이미 대조했다. 생성 블록 21개는 바이트 단위로 불변이다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **표기 정정** | 11 가정 묶음 + base. 사이트가 12로 팔고 있던 것을 11로 고쳤다 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 GitHub 링크만 받아도 3초 안에 가이드에 닿는다. 그전에는 `docs/`를 뒤져야 했다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | `scripts/gate.py`가 README에 올라왔다 — 클론한 사람이 "아직 작동하는가"를 한 명령으로 물을 수 있다 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **F15(최종 교정)**: 영어 문장·표 정합·링크 전수. 이번에 확인한 것은 README → 가이드 방향뿐이다.
  가이드 → 외부(`METHODOLOGY.md`·`data_gap_registry.md`·`mcp_server.md`) 링크는 테스트가 있지만
  §3·§9의 파일 경로 인용(예: `docs/robustness_structural.md §3-1`)은 절 번호까지 확인한 적이 없다.
- **`web/README.md`**(사소, 미수정): 가이드를 "8장"이 아니라 장 수 없이 기술하는지 확인 안 했다.
- **CCfD를 시험 가능하게 만드는 일**(백로그 유지, F13): D5에 행사가 행 + E5 격자에 `ccfd` 축.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드.
- **MCI 사업소 상한 검증**(백로그 유지, O13): `prepare_raw.py` 석화 분기에 경고 한 줄.
- **③ 번호 오기**(백로그 유지, F13): 파이프라인 재실행 사이클에 같이 넣어라 — 주석만 고쳐도
  gate의 `out/ vs data/raw`가 STALE로 뒤집힌다.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F15 (08:49) — 링크·경로·행 번호는 전부 살아 있었고, 무너진 것은 출처가 아예 없던 두 문단이었다

**한 일.** 지도의 F15대로 가이드를 전수 교정했다. 링크·파일 경로·코드 행 번호를 하나씩 열어
확인했고, **거기서는 결함이 거의 나오지 않았다**(29개 코드 행 인용 중 1개가 한 줄 어긋남).
결함은 다른 곳에 있었다 — 가이드 머리말이 "모든 정량 주장은 생성되었거나 그것을 만드는 파일을
가리킨다"고 약속하는데, **그 약속을 어기는 문단이 둘 있었고 둘 다 P1·A-05를 떠받치고 있었다.**

### 전수 확인 결과 (결함 없음)

| 확인 대상 | 방법 | 결과 |
|---|---|---|
| 마크다운 링크 6개 | 파일 존재 확인 | 6/6 OK |
| 인용된 저장소 경로 (파일 60여 개 + 디렉터리 9개) | 존재 확인 | 전부 OK |
| 타 문서 절 번호 인용 (`robustness_structural.md §3-1`, `process_alternative.md §3`, `data_gap_registry.md` F1) | 대상 문서 제목 대조 | 3/3 실재 |
| 가이드 내부 `§N.M` 참조 30종 | 자체 제목 대조 | 전부 실재 |
| 코드 행 번호 인용 29개 | 해당 행 열람 | 28 정확, 1 어긋남 (아래 #3) |

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6.3 전체 (그리고 그것을 인용하는 §1 P1) | 32 / 4 / 25라는 세 수가 **출처 한 줄 없이** 서 있었다 | 이 수는 파이프라인 산출이 아니라 **별도 진단 실행**에서 나온다. `scripts/frontier_tech_epsilon.py` → `out/m8/summary.csv`(`caps_tried`, `nondominated_headline`, `nondominated_l2`), 서술은 `docs/frontier_degeneracy.md`. 그 파일 이름이 **가이드 어디에도 없었다** — Arc가 "그 32는 어디서 나왔나"고 물으면 문서 안에 답이 없었다 | `out/m8/summary.csv` |
| 2 | §6.3 · §1 P1 | "헤드라인 규약에서 32개 중 **4개**만 비지배로 살아남는다" | 맞지만 **가장 중요한 부분이 빠져 있었다. 그 4개는 전부 한 묶음(LOTTE NZ15)에 있다.** 나머지 **7개 묶음에서는 0개**다. 4/32는 "간신히 살아남았다"로 읽히고 실제는 "여덟 중 일곱에서 기술 축이 통째로 죽는다"이다 — 같은 데이터인데 P1의 강도가 다르다 | `out/m8/summary.csv`의 `nondominated_headline` = [4,0,0,0,0,0,0,0] |
| 3 | §3.10 표 · §9 O9 | `src/cap/e5_metrics.py:290` | **289다.** 290은 `constraints.csv`를 읽는 첫 줄이고, 주장("배출경로는 첫 support에서만 계산된다")을 만드는 `if supp == cfg.support_scenarios[0]:` 가드는 289다. 같은 파일에 그 가드가 넷(239·259·270·289) 있어 한 줄 어긋나면 다른 가드로 읽힌다 | `src/cap/e5_metrics.py:289` |
| 4 | §4.1 A-05 **그리고 `METHODOLOGY.md` A-05** | "대 — TCaR의 30~42%" | **현행 실행의 어떤 절단으로도 재현되지 않는다.** 그리고 애초에 성립하지 않는 종류의 진술이다 — TCaR은 분위수 차이라 요인별로 가법 분해되지 않는다(`docs/uncertainty_propagation.md` §1이 같은 이유로 상호작용 잔차를 남긴다). 실제로 잴 수 있는 것은 분산 몫이고, NZ15 비용최소 계획에서 **34%~100%**(NSC 34, POSCO 52, 석화 두 곳 100)다. 같은 가이드 §9.1이 이미 64~77%(전 계획 평균)를 적고 있어 **한 문서가 수소 비중을 두 값으로 팔고 있었다** | `out/e5/variance_decomp.csv` × `out/e5/frontier_points.csv` |

### 부속 문서에서 고친 것

- `docs/frontier_degeneracy.md` §1이 "16개 묶음 중 **14개**에서 경계 점이 같은 기술 일정을
  공유한다"로 시작하는데, 현행 실행에서는 **16개 전부**다(`out/e5/frontier_points.csv`의
  `on_frontier` 점 `base_plan_id` nunique가 16칸 모두 1). D3 시점의 계획 메뉴에서 나온 수이고
  그 뒤 메뉴가 한 번 움직였다. #1에서 이 파일을 가이드가 가리키게 만들었으므로, 가리키기 전에
  그 전제를 고쳤다 — 안 고쳤으면 가이드 §6.3이 §9.1(8/8)과 어긋나는 문서를 가리키게 된다.
- `METHODOLOGY.md` A-05 (#4, 정본 우선).

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks, 112,185 chars (111,189 → )
.venv/bin/python scripts/build_guide_page.py     # 38 sections, 383 table rows
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 72 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 32/4/25 = `out/m8/summary.csv` · 수소 분산 몫 = `out/e5/variance_decomp.csv` ×
`out/e5/frontier_points.csv`(NZ15·none·비공시·budget_ok 중 p50 최소) · 경계 일정 수 =
`out/e5/frontier_points.csv` · 가드 행 = `src/cap/e5_metrics.py:289`.

테스트 2개 추가(70 → 72). `test_epsilon_diagnostic_counts_are_the_ones_in_6_3`은 진단을 다시
돌려 32/4/25나 "4개가 한 묶음"이 바뀌면 실패한다. `test_hydrogen_share_in_the_ledger_is_reproducible`은
비용최소 계획이 바뀌어 34~100%가 움직이거나, 재현되지 않는 "30~42%"가 어느 쪽 문서로든 되살아나면
실패한다 — 가이드와 METHODOLOGY 양쪽을 같이 본다.

**생성 블록은 손대지 않았다.** 바뀐 1,000자는 전부 손으로 쓴 산문이다(§1 P1, §4.1 A-05 행,
§6.3 두 문단, §3.10·O9의 행 번호).

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | P1의 상태가 "4/32 생존"에서 "여덟 중 일곱에서 기술 축이 죽는다"로 정확해졌다. 약한 쪽으로 정확해진 것이지만, Arc가 `out/m8/summary.csv`를 열면 어차피 보이는 수다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계 (F16은 창 마감 — 아래는 다음 창으로)

- **미인용 부속 문서 9개**(신규 관측): 가이드가 이름을 부르지 않는 `docs/` 문서가
  `cross_model_check.md`, `literature_map.md`, `price_process_test.md`, `seed_stability.md`,
  `tech_band_upgrade.md`, `tech_cost_reconciliation.md`, `validation_backtest.md`,
  `validation_external.md`, `data_audit.md`로 아홉이다. §7이 검증 세 겹을 **서술만** 하고
  그것을 담은 파일을 가리키지 않는다 — F15가 §6.3에서 고친 것과 같은 결함이 §7에 남아 있다.
  다음 창의 첫 후보.
- **CCfD를 시험 가능하게 만드는 일**(백로그 유지, F13): D5에 행사가 행 + E5 격자에 `ccfd` 축.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드. §6.1이 "스윕이 현행 실행보다 낡았다"고
  이미 적고 있다.
- **MCI 사업소 상한 검증**(백로그 유지, O13): `prepare_raw.py` 석화 분기에 경고 한 줄.
- **③ 번호 오기**(백로그 유지, F13): 파이프라인 재실행 사이클에 같이 넣어라.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F16 (01:10) — §7이 하지 않은 검증 둘을 했다고 적고 있었고, 실제로 한 검증의 낙제 판정은 빠져 있었다

**한 일.** F15 인계의 첫 후보(“§7이 검증 세 겹을 서술만 하고 그것을 담은 파일을 가리키지 않는다”)를
집었다. 파일을 붙이려고 네 기록 문서를 열어 대조하니 **문제는 인용 부재가 아니었다** — §7의 서술
자체가 기록과 어긋났다. 안 한 검증 둘을 했다고 적고 있었고, 한 검증에서는 두 기업 중 하나가
기준을 초과하는데 그 판정이 통째로 없었다.

*(지도상 F16은 창 마감이나, 태스크 정의가 마감을 09:00 별도 태스크로 분리하고 이 창의 목적을
“가이드 개선·검증”으로 못박고 있어 실작업 사이클로 돌렸다. 스케줄은 건드리지 않았다.)*

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §7 back-test | “2020–2024 실적 생산량과 **에너지 가격**을 넣고 실제 배출과 **에너지 비용**의 재현 오차를 보고한다” | **비용은 재현한 적이 없다.** `docs/validation_backtest.md` §3이 세 줄로 명시한다 — 에너지 원단위 재현 불가(사업장별 에너지 소비 미공시), 석화 전면 미검증, **“비용 재현 없음: 실제 에너지 지출·CAPEX 실적 시계열이 없어 모형 비용 구조 자체는 후향 검증되지 않았다.”** 가이드가 그 문서를 인용하지 않는 동안, 문서가 “안 했다”고 적은 것을 가이드가 “했다”고 팔고 있었다 | `docs/validation_backtest.md` §3 |
| 2 | §7 external | “지표 ②를 공표된 LCOA/MAC 범위(**Vogl, Agora, IEA ISTR, Material Economics, MPP**)와 대조” | **그 대조는 하지 않았다.** `docs/validation_external.md` **§5 「아직 못 한 대조 (없는 것을 있다고 하지 않는다)」**의 첫 항목이 정확히 그 다섯 출처다: “수소환원 LCOA(US$/tCO₂) 수치를 아직 추출하지 않았다.” `data_gap_registry.md:124,137`도 「미추출」·「미시도」로 이중 기재한다. §3이 실제로 한 것은 NGFS 섀도 탄소가격(2030 US$150 / 2050 US$1,700)과 리트로핏 MAC으로 **자릿수만** 가둔 것이다. Vogl은 모형에 들어오지만 CAPEX·기술 파라미터 앵커이지 US$/tCO₂ 비교자가 아니다 | `docs/validation_external.md:152`, `docs/data_gap_registry.md:124,137` |
| 3 | §7 back-test | 판정 없음 — “재현 오차를 보고한다”로 끝 | **네 기업 중 둘만 대조 가능하고 그 둘 중 하나가 낙제다.** POSCO 평균 +1.0%·최대 2.7% 통과, **NSC 평균 +15.7%·최대 17.5%로 ±10% 기준 초과**. 게다가 원인 가설 배제까지 되어 있다 — 가동 중 EAF를 전부 넣어도 15.7%p 중 1.3%p만 닫히고 FY2029 신설분까지 넣어도 +10.2%가 남는다. 원인은 설비 목록이 아니라 주입값 `BF = 2.15 tCO₂/t` 자체다. LOTTE·MCI는 생산량 미공시라 대조가 성립조차 않는다 | `docs/validation_backtest.csv`, `docs/validation_backtest.md` §1·§2 |
| 4 | §7 external | 실적 대조 대상을 “광양 EAF, NSC 야하타, JFE 구라시키”로 셋 열거한 뒤 “이 층이 A-13의 4.2배를 찾았다” | **그 4.2배를 만든 프로젝트가 열거에 없다.** 4.2배는 고베제강 2016년 3고로 개수(47 천원/t능력) ↔ 우리 주입값 200에서 나온다. 열거된 셋은 전부 EAF이고 그중 어느 것도 개수 앵커가 아니다. Arc가 “그 4.2는 어느 프로젝트인가”를 물으면 §7의 답이 틀린다 | `docs/validation_external.md` §1·§1-1 |
| 5 | §7 머리 | “**Three layers**, all runnable.” 뒤에 굵은 글씨 층이 **넷** | **넷이다.** independent reimplementation이 네 번째 층으로 서 있는데 머리말은 셋을 셌다 | `docs/TECHNICAL_GUIDE.md` §7 자체 |

### 인용 부재로 남아 있던 것 (원래 F16의 목표)

각 층에 생성기와 기록 파일을 붙였다: `tests/test_consistency.py` / `scripts/validate_external.py` →
[`validation_external.md`] / `scripts/validate_backtest.py` → [`validation_backtest.md`] +
`validation_backtest.csv` / `scripts/cross_model_check.py` → [`cross_model_check.md`].
F15가 지목한 미인용 문서 9개 중 셋이 §7에서 해소됐다(`validation_external`·`validation_backtest`·
`cross_model_check`). 남은 여섯은 `literature_map`·`price_process_test`·`seed_stability`·
`tech_band_upgrade`·`tech_cost_reconciliation`·`data_audit`이다.

### 덧붙인 정량 사실 (전부 기록 문서에서)

- CAPEX 문헌 대조 판정: `steel_h2dri`·`steel_hyrex` 863은 DIW 858–1,089 **안**, `steel_eaf` 240은
  368–677의 **하단 아래 ×0.65** — 외부 대조의 두 번째 낙제인데 §7에 없었다.
- 석화 유일 외부 확인: NCC 연료전환 49% ↔ 미쓰이 오사카 공시 약 44%.
- 지표 ② 자릿수: 115–279 천원/tCO₂ = US$85–207.
- **NSC 낙제가 헤드라인을 움직이지 않는 이유**를 같은 자리에 적었다: 철강은 회사 공시 총량에
  재척도하므로 루트 표준은 수준이 아니라 배분 가중치이고, NSC 목록이 고로 10기 단일 구성이라
  가중이 균일해 수준이 상쇄된다(모형이 실제로 쓰는 `ef_inc` 1.846 vs 공시 1.856). 그리고 그 값이
  **수준을 정하는 곳은 재척도하지 않는 석화 2사인데, 그 둘이 정확히 대조 불가능한 둘이다.**
- 교차대조 분해가 **정성적**이라는 것을 명시했다 — 어느 요인이 감축단가 차이의 몇 %인지는
  계산되지 않았고(`cross_model_check.md` §4), 따라서 이 층이 지지하는 주장은 “같은 방향”이지
  “같은 수준”이 아니다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py     # 21 blocks, 115,805 chars (112,185 -> )
.venv/bin/python scripts/build_guide_page.py     # 38 sections, 383 table rows (불변)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                 # gate: OK (pytest 73 passed, audit ok 68/PARTIAL 4 불변)
```

수치 출처: 후향 오차 = `docs/validation_backtest.csv`(`err_pct`) · EAF 가설 배제 표 =
`docs/validation_backtest.md` §2 · CAPEX 실적·문헌 대조 = `docs/validation_external.md` §1·§1-1·§2 ·
LCOA 미대조 = `docs/validation_external.md:152` + `docs/data_gap_registry.md:124,137` ·
지표 ② 자릿수 = `docs/validation_external.md` §3 · 교차대조 미분해 = `docs/cross_model_check.md` §4.

테스트 1개 추가(72 → 73). `test_section_7_backtest_matches_the_backtest_record`는
`validation_backtest.csv`에서 기업별 평균·최대 오차를 다시 계산해 §7의 수와 대조하고,
대조 가능한 기업 집합이 {POSCO, NSC}에서 바뀌면 실패한다. 더해서 “and energy cost reported”가
되살아나거나 LCOA 공백 문장이 사라지거나 네 기록 파일 중 하나라도 §7에서 빠지면 실패한다.

**생성 블록은 손대지 않았다.** 21개 불변, 바뀐 3,600자는 전부 §7의 손으로 쓴 산문이다.

`data/~$CAP_data_collection_template.xlsx`(엑셀 잠금 파일) 삭제를 이번 커밋에 실었다 — 창 내내
uncommitted로 남아 게이트 ⑧을 WARN으로 붙들고 있던 것이다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **약해진 쪽으로 정확해짐** | §7이 팔던 검증 두 개가 실은 미시행이었다. Arc가 `validation_backtest.md`를 열면 어차피 보이는 것이고, 심사자가 먼저 찾으면 문서 전체의 신뢰가 걸린다. 대신 NSC 낙제가 헤드라인에 걸리지 않는 이유를 같은 자리에 붙여 방어 가능한 형태로 만들었다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | `scripts/gate.py` 8항목. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **미인용 부속 문서 6개**(F15 목록에서 셋 해소): `literature_map`·`price_process_test`·
  `seed_stability`·`tech_band_upgrade`·`tech_cost_reconciliation`·`data_audit`. 다만 §7에서 본 것을
  적어 두면 — **인용을 붙이는 작업이 곧 사실 대조 작업이다.** 붙일 곳을 열면 서술이 어긋나 있다.
  다음 후보는 §4.5(evidence grading) ↔ `tech_band_upgrade.md`·`data_audit.md`.
- **§7이 새로 드러낸 실질 공백 2건**(문서가 아니라 작업): (a) 문헌 LCOA 미추출 — Vogl·Agora·
  IEA ISTR·Material Economics·MPP 한 번의 수집이 `data_gap_registry.md:137`대로 세 공백을 동시에
  메운다. (b) 교차대조 요인별 정량 분해 미실시.
- **CCfD를 시험 가능하게 만드는 일**(백로그 유지, F13): D5에 행사가 행 + E5 격자에 `ccfd` 축.
- **§6.1 시드 스윕 재실행**(백로그 유지): E3–E5 × 5시드.
- **MCI 사업소 상한 검증**(백로그 유지, O13): `prepare_raw.py` 석화 분기에 경고 한 줄.
- **③ 번호 오기**(백로그 유지, F13): 파이프라인 재실행 사이클에 같이 넣어라.
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F17 (01:40) — 게이트가 스탬프 자신 때문에 빨갰고, 같은 DIW 표의 밴드가 저장소 안에서 두 값이었다

**한 일.** 창 시작 시 `gate: FAILED (tests)`였다. 지도보다 이것을 먼저 끝냈고, 그 원인이
F16의 실수가 아니라 **스탬프의 성질**이라는 것을 확인한 뒤 성질 쪽을 고쳤다. 그 다음 F16
인계의 첫 후보(§4.5 ↔ `tech_band_upgrade.md`·`data_audit.md` 인용)를 집었는데, F16에서와
똑같은 일이 났다 — **인용을 붙이러 연 곳의 서술이 어긋나 있었다.**

### 게이트 복구 (지도보다 먼저)

`docs/TECHNICAL_GUIDE.md`의 `GEN:stamp`가 커밋 SHA를 찍는다. 그 SHA를 담은 커밋이 만들어지는
순간 스탬프는 자기가 이름 부르지 못하는 커밋 하나를 남기고 낡는다. **커밋 전에 알 수 없는 것을
스탬프에 넣었다는 것이 결함이다.** F1이 SHA 대상을 상태 경로(`src`·`data`·`config.yaml`·`out`)로
좁혀 완화했지만 재발 빈도만 줄였다 — F16이 `data/~$...xlsx` 잠금 파일을 지웠고 다음 사이클에서
같은 실패가 났다. 스탬프를 그 경로들의 **내용 다이제스트**(추적 파일 70개, 844KB, sha256 앞 12자)로
바꿨다. 내용은 커밋 전에 알 수 있으므로 자기 자신을 낡게 만들지 않는다. **실증: 이번 커밋 직후
`build_tech_guide.py --check`가 `generated blocks current`다.**

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §7 external | `steel_h2dri` 863이 DIW **858–1,089** 안, `steel_eaf` 240은 **368–677** 하단 아래 | **같은 문서 §3.4가 같은 파라미터에 다른 밴드(863–1,095 / 370–835)를 찍고 있었다.** 원인 둘: (a) EUR 환율이 두 벌 — 모형 투입값을 만든 1,458(=USD 1,350 × EURUSD 1.08)과 `validate_external.py`가 따로 든 1,450, (b) EAF 밴드 상단 정의가 두 벌 — DIW 전 출처군(OECD2015 573 → 835)과 발표 프로젝트만(467 → 681). Arc가 두 절을 나란히 읽으면 어느 쪽이 우리 밴드인지 답이 없다 | `data/raw/tech_bands.csv` `derivation` 열 · `scripts/validate_external.py:34` |
| 2 | §7 external | `steel_h2dri` "범위 안"을 독립적 외부 일치처럼 읽히게 서술 | **구성상 하한에 앉은 것이다.** 우리 투입값 863은 밴드 하한과 **같은 문헌 수치**(DIW 592 €/t)를 환산·반올림한 값이다. 환율을 통일하자 판정이 "범위 밖 ×1.00"으로 뒤집혔는데, 그것은 대조 결과가 아니라 반올림 잔차 0.14의 그림자였다 | `docs/validation_external.md` §2 |
| 3 | §4.5 | "**가장 좋은 출처를 가진 파라미터가 밴드 없는 것들**" — 등급과 폭이 반대 방향으로 간다 | **T1에서 성립하지 않는다** (3행 중 2행이 밴드 보유 — K-ETS 유상할당 비율은 법령이 범위를 준다). 등급별 보유율은 T1 2/3 · T2 2/137 · T3 1/41 · T4 0/79 · T5 16/155로 단조가 아니다. 방어 가능한 진술은 더 좁다: 가장 믿는 출처가 폭을 하나도 기여하지 않으므로 **이 모형의 폭은 대체로 증거가 아니라 규약이다** | `docs/parameter_inventory.csv` |
| 4 | §4.5 머리 | "T5는 `[low, high]`를 **요구한다**" — 규약을 통제로 읽히게 | **155행 중 139행에서 지켜지지 않는다.** 같은 절의 생성 블록이 이미 그 수를 찍고 있었는데 산문이 규약을 사실처럼 팔았다. 415행 중 밴드 보유는 **21행**뿐이고, 지켜진 T5 16행은 전부 우리가 고른 수(`model_choice` 8 · `policy_assumption` 4 · `prep_injection` 4)다. **물리·비용 쪽(`technology` 36 · `facility` 68 · `price_path` 18)에는 하나도 없다** | `docs/parameter_inventory.csv` |
| 5 | §7 "한 명령" | `scripts/audit_data.py`를 실행만 시키고 판정 기록을 가리키지 않음 | 인용 부재. 88열 중 `ok` 68 · `PARTIAL` 4 · 설계상 정상 16 · `CONSTANT`/`UNUSED`/`EMPTY` 0을 붙였고, **PARTIAL 넷 중 셋이 지표 ③이 실제로 나누는 D6 재무 열**(`revenue` 95.5% · `capex_total` 50.0% · `net_debt` 40.9%)이라는 것과 미공시 시 대체 없이 `null`이라는 것을 같은 자리에 적었다 | `docs/data_audit.md` · `src/cap/e5_metrics.py:120,122` |

### 코드에서 고친 것 (가이드 결함의 뿌리)

- `scripts/validate_external.py`: 밴드를 스스로 만들지 않고 `data/raw/tech_bands.csv`에서 읽는다
  (`tech_map()`). 환율은 `USDKRW × EURUSD`로 한 곳에서 정한다. **밴드가 저장소에 한 벌만 남았다.**
- 같은 파일 `verdict()`: 표가 찍는 자리수로 비교. 반올림 잔차가 판정을 뒤집지 못한다.
- 같은 파일 앵커 표에 `EAF OECD 2015 (n=40)` 573 €/t = 835 천원/t을 실었다 — **밴드 상단 835가
  어디서 왔는지 문서만 보고는 추적되지 않았다**(`tech_bands.csv`의 `derivation` 열에만 있었다).
- `scripts/build_tech_guide.py`: `state_digest()` 도입, `gen_stamp()`가 SHA 대신 그것을 찍는다.
- `scripts/build_guide_page.py`: 푸터가 `commit xxx` 대신 `state xxx`.

### 덧붙인 정량 사실 (전부 기록 문서에서)

- 밴드 부재의 값: 증거 밴드로 다시 풀면 철강 파라미터 몫이 **21%→37%(POSCO)·23%→36%(NSC)**
  (±15% 폭), 석화 2사는 1%p 안에서 그대로. 규약(±30% 대칭)이 틀린 것은 폭이 아니라 **중심**이다
  — `tech.capex` 증거 승수 구간이 [1.00, 3.48]로 한쪽만 열려 있다.

### 검증

```
.venv/bin/python scripts/validate_external.py   # 159 lines (157 -> )
.venv/bin/python scripts/build_tech_guide.py    # 21 blocks, 119,608 chars (115,805 -> )
.venv/bin/python scripts/build_guide_page.py    # 38 sections, 383 table rows (불변)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                # gate: OK (pytest 75 passed, audit ok 68/PARTIAL 4 불변)
.venv/bin/python scripts/build_tech_guide.py --check   # 커밋 직후 current — 스탬프 수정의 실증
```

수치 출처: 밴드 = `data/raw/tech_bands.csv` · 환산 앵커 = `docs/validation_external.md` §2 ·
등급별 밴드 보유 = `docs/parameter_inventory.csv` · 파라미터 몫 이동 = `docs/tech_band_upgrade.md` §3 ·
열 감사 = `docs/data_audit.md` · 지표 ③ 계산 = `src/cap/e5_metrics.py:119–122`.

테스트 2개 추가(73 → 75). `test_stamp_is_a_content_digest_not_a_commit_sha`는 스탬프가 커밋으로
해석되면(=SHA 스탬프로 되돌아가면) 실패한다. `test_section_4_5_band_coverage_is_the_inventory_not_a_story`는
21/415·139/155·등급별 보유율을 인벤토리에서 다시 계산해 대조하고, T5 물리·비용 파라미터에 밴드가
하나라도 붙으면 §4.5 문장을 다시 쓰라고 실패한다.

**생성 블록은 손대지 않았다.** 21개 불변. 바뀐 3,800자는 §4.5·§7의 손으로 쓴 산문이고,
스탬프 블록만 생성기를 고쳐 바뀌었다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, PARTIAL 4` 불변. PARTIAL 4가 이제 가이드 본문에 이름으로 적힌다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | 밴드가 한 벌이 됐다. 그리고 "밴드가 없다"의 값이 처음으로 수로 붙었다(21%→37%) — 심사자가 "왜 ±30%냐"를 물을 때 답이 생겼다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 게이트가 스탬프 때문에 주기적으로 빨개지던 것이 끝났다 (커밋 직후 `--check` current로 실증) |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **미인용 부속 문서 4개**(F16 목록에서 `tech_band_upgrade`·`data_audit` 해소): `literature_map`·
  `price_process_test`·`seed_stability`·`tech_cost_reconciliation`. **인용을 붙이는 작업이 곧 사실
  대조 작업이라는 것이 F16·F17 두 번 연속 확인됐다** — 붙일 곳을 열면 서술이 어긋나 있다.
  다음 후보는 §6.1 ↔ `seed_stability`(§6.1이 스스로 낡았다고 적고 있다), §3.4 ↔
  `tech_cost_reconciliation`.
- **`D6.capex_total` 미소비 의심**(신규 관측): 스키마에 있고 감사가 `PARTIAL`(50%)로 분류하는데
  `src/cap/`에서 이 **입력 열**을 읽는 곳을 못 찾았다(`capex_total_bnkrw`는 모형이 계산한 별개 값).
  `UNUSED`여야 할 것이 `PARTIAL`로 분류되는 것이면 감사기의 판정 규칙 결함이다. 확인 필요.
- **§7이 드러낸 실질 공백 2건**(변동 없음): 문헌 LCOA 미추출 · 교차대조 요인별 정량 분해 미실시.
- **CCfD 시험 가능화**(백로그, F13) · **§6.1 시드 스윕 재실행**(백로그) · **MCI 사업소 상한 검증**
  (백로그, O13) · **③ 번호 오기**(백로그, F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F18 (02:12) — 감사가 "안 읽는 열"을 읽는다고 팔고 있었고, §7은 게이트의 이빨을 과장하고 있었다

**한 일.** F17 인계의 신규 관측(`D6.capex_total` 미소비 의심)을 확인했다. 의심이 맞았고,
원인은 데이터가 아니라 **감사기의 판정 규칙**이었다. 그것을 고치자 가이드 §7의 수가 바뀌었고,
그 문단을 다시 읽다가 **게이트가 하지 않는 일을 한다고 적은 문장**을 찾았다. 인계의 둘째
후보(§6.1 ↔ `seed_stability`)도 같이 집었고, 여기서도 같은 일이 났다 — 인용을 붙이러 연 곳의
서술이 어긋나 있었다. F16·F17·F18 세 번 연속이다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §7 audit | 감사 집계 `68 ok / 4 PARTIAL / 0 UNUSED`, 그중 `D6.capex_total` 50.0%를 "일부만 채워졌지만 소비되는" 열로 열거 | **엔진이 이 열을 읽지 않는다.** 지표 ⑥이 D6에서 읽는 것은 `ebitda`·`revenue`·`net_debt` 셋뿐이다. 바른 집계는 `68 ok / 3 PARTIAL / 1 UNUSED` | `docs/data_audit.csv` · `src/cap/e5_metrics.py:104-112` |
| 2 | §7 gate | "Eight checks: … data audit (**no unused or unsourced columns**) … Non-zero exit on any hard failure" — 미사용·미출처 컬럼이 게이트를 빨갛게 만든다고 읽힌다 | **만들지 않는다.** `audit_data.py`의 치명 조건은 `SYNTHETIC LEAK`와 `MISSING INPUT` 둘뿐이고, 지금 UNSOURCED 경고 4건(`PREP_ALLOC`·`PREP_BOTTOMUP`·`EST_D2A_V0`·`EST_D2B_V0`)이 선 채로 게이트가 초록이다. 심사자가 `gate.py` 한 번 돌리고 `data_audit.md` 끝을 보면 잡는다 | `scripts/audit_data.py:main` · `docs/data_audit.md` 경고 절 |
| 3 | §6.1 | ⑤ 변동계수 3–9%에 대해 "it is a lower bound, **so more simulation buys nothing**" | **CV는 표본오차다 — n_sims를 올리면 줄어든다.** 출처 문서가 바로 그 둘(자릿수 축소 / n_sims 상향)을 남은 선택지로 적는다. 우리는 자릿수를 택한 것이고, 그것은 "줄일 수 없다"가 아니다 | `docs/seed_stability.md` 판정 절 |
| 4 | §6.1 | 스윕이 재는 것을 "precision, not accuracy" 한 줄로만 한정 | **재지 않는 채널이 하나 더 있다** — 시드는 가격 경로만 바꾸고 계획 메뉴는 고정이므로 **계획 선택의 안정성은 이 스윕이 재지 않는다**(출처 문서가 명시). 그리고 §6.1이 이미 들고 있던 NSC 사례가 정확히 그 재지 않은 채널이다. 두 채널의 크기를 같은 자리에 붙였다: **계획 변경 −5.9%**(165.4 → 155.6) vs **시드 잡음 0.26%** | `docs/seed_stability.md` · `docs/seed_stability.csv` · `out/e5/metrics_company.csv` |
| 5 | §6.1 | `seed_stability.md` 미인용(F17 인계의 미인용 부속 문서 4개 중 하나) | 인용 부착 + 스윕 조건(E1·E2 공유, n_sims 10,000)을 명시 | `docs/seed_stability.md` |

### 코드에서 고친 것 (가이드 결함의 뿌리)

`scripts/audit_data.py`의 활용 판정은 컬럼 이름을 **엔진 소스 전체**에서 찾는다. 그래서
E5가 계산하는 동명 필드가 입력 열의 알리바이가 된다 — `best.capex_total`(`e5_metrics.py:377`)과
MCP 산출 열 목록(`mcp_server.py:122`)이 D6 입력 열의 매칭을 만들어 냈다. `NAME_COLLISION`에
충돌을 명시(사유 + 충돌 지점 파일:행)해 판정을 `UNUSED`로 되돌렸다.

**일반 결함은 남았고 그렇게 적었다**: 계산 필드와 이름이 겹치는 입력 열은 읽히지 않아도
사용으로 채점된다. 프레임 단위 데이터플로 추적으로만 닫히고 이 파이프라인은 그것을 하지
않는다 — 가이드 §7에 그 한계를 그대로 썼다. `mcp_server.py`를 `ENGINE`에서 빼는 안은
검토했다가 버렸다: 어느 입력 열도 "mcp_server에서만" 매칭되지 않아 아무것도 바뀌지 않는다.

### 새로 드러난 실질 공백 1건

`D6.capex_total`은 22 기업-연도 중 11개가 채워진 채 아무도 읽지 않는다. **CFO가 가장 먼저
할 비교 — "이 전환은 당신이 이미 돌리는 자본 프로그램의 몇 배인가" — 가 모형에 없는 유일한
분모다.** 코드 한 줄, 새 데이터 0. 이번 사이클에 넣지 않았다(E5 재실행이 필요하고, 창 규칙상
끝낼 수 없는 파이프라인은 시작하지 않는다). §7에 조용히 버리지 않고 이름으로 적었다.

### 검증

```
.venv/bin/python scripts/audit_data.py          # ok 68, UNUSED 1, PARTIAL 3, 설계상 정상 16
.venv/bin/python scripts/build_tech_guide.py    # 21 blocks, 122,320 chars (119,608 -> )
.venv/bin/python scripts/build_guide_page.py    # 38 sections, 383 table rows (불변)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                # gate: OK (pytest 77 passed)
```

수치 출처: 감사 집계·채움률 = `docs/data_audit.csv` · 게이트 치명 조건 = `scripts/audit_data.py:main` ·
UNSOURCED 4건 = `docs/data_audit.md` 경고 절 · D6 소비 열 = `src/cap/e5_metrics.py:104-112` ·
고정시드 NSC = `docs/seed_stability.csv`(seed 20260806) · 현행 NSC = `out/e5/metrics_company.csv` ·
CV·미측정 채널 = `docs/seed_stability.md`.

테스트 2개 추가(75 → 77). `test_section_7_audit_tally_is_the_audit_not_a_memory`는 §7의 집계를
`data_audit.csv`에서 다시 세고, UNUSED 집합이 바뀌거나 "no unused or" 서술이 되살아나면 실패한다.
`test_section_6_1_seed_sweep_matches_the_stability_record`는 두 채널의 크기를 CSV와 `out/`에서
다시 계산하고, 크기 비교가 뒤집히거나 "more simulation buys nothing"이 되살아나면 실패한다.

**생성 블록은 손대지 않았다.** 21개 불변. 바뀐 2,700자는 전부 §6.1·§7의 손으로 쓴 산문이다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **정확해짐 (약해진 쪽으로)** | `ok 68, UNUSED 1, PARTIAL 3` — "전부 쓴다"가 처음으로 거짓이 됐다. 거짓이 된 게 아니라 원래 거짓이었고 감사가 못 봤다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | §6.1이 "우리가 잰 것"과 "재지 않은 것"을 크기까지 붙여 구분한다. 심사자의 "그 CV가 오차막대냐"에 답이 생겼다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 감사기가 미소비 열을 다시 소비로 채점하지 않는다. MCP 표면 변화 없음 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **미인용 부속 문서 3개**(F17 목록에서 `seed_stability` 해소): `literature_map`·`price_process_test`·
  `tech_cost_reconciliation`. 다음 후보는 **§3.4 ↔ `tech_cost_reconciliation`**(F17이 지목했으나
  아직 안 열림), 그리고 §6.2의 GBM/OU 대조 ↔ `price_process_test`. **인용 부착 = 사실 대조라는
  것이 F16·F17·F18 세 번 연속 확인됐다.**
- **`D6.capex_total` 소비**(신규, 위 참조): `_affordability`에 `capex_total_to_own_capex` 한 줄.
  E5 재실행이 필요하니 파이프라인 재실행을 하는 사이클에 묶어라.
- **감사기의 이름 매칭 한계**(신규, 미해결): 계산 필드와 동명인 입력 열은 안 읽혀도 사용으로
  채점된다. 지금은 `NAME_COLLISION` 1건으로 막았다 — 다른 열도 같은 착각일 수 있다. 확인하려면
  입력 열 88개의 매칭 지점을 파일:행으로 뽑아 D* 표를 실제로 읽는 모듈인지 하나씩 봐야 한다.
- **§7이 드러낸 실질 공백 2건**(변동 없음): 문헌 LCOA 미추출 · 교차대조 요인별 정량 분해 미실시.
- **CCfD 시험 가능화**(백로그, F13) · **§6.1 시드 스윕 재실행**(백로그 — 이번에 §6.1이 스윕의
  낡음을 수로 붙였으므로 재실행의 값이 더 분명해졌다) · **MCI 사업소 상한 검증**(백로그, O13) ·
  **③ 번호 오기**(백로그, F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F19 (02:49) — A-10이 CCUS를 리트로핏이라 팔고 있었고, EFF 정합의 근거 두 칸이 출처 없는 가정이었다

**한 일.** F18 인계의 첫 후보(§3.4 ↔ `tech_cost_reconciliation`)를 열었다. 인용을 붙이러
열었더니 붙일 곳이 어긋나 있었다 — **F16·F17·F18·F19 네 번 연속**. 이번엔 어긋난 것이
가이드만이 아니었다: 정본인 `METHODOLOGY.md`의 A-10 줄이 틀렸고, 대조 상대인 EFF의 값 두 개가
출처 없는 가정이었으며, 그 사실을 모르고 쓴 "정합" 판정이 두 칸 있었다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.2 A-10 (+ `METHODOLOGY.md` A-10) | "Blast-furnace conversion is hydrogen-DRI only; **CCUS and efficiency are retrofits**" — 포집이 고로에 붙는 수단으로 읽힌다 | **CCUS는 수단 집합에 없다.** `prepare_raw.py:303`이 raw 17행에서 `steel_ccus`·`petchem_ccus`를 떨어뜨린다(사용자 결정 2026-08-06). 어떤 설비도 어떤 가격에도 채택할 수 없다. 그리고 **같은 두 문서의 §6.4가 "LOTTE의 공시 수단이 CCUS인데 우리가 D3에서 제외했다"고 적고 있었다** — 문서가 자기를 부정하고 있었다. A-10이 왜 네 기업 중 둘의 공시 좌표를 없애는지도 이제 한 자리에서 읽힌다(POSCO=BF→EAF 불허, LOTTE=CCUS 제외) | `scripts/prepare_raw.py:303` · `data/prepared/D3_tech_options.csv` · 빌드 로그 `[e2] warning: disclosed tech 'petchem_ccus' not in D3 — commitment dropped` |
| 2 | §3.4 | "One row per abatement measure available to a sector, **13 rows in total**" — 13이 수집량인 것처럼 읽힌다 | **raw는 17행이고 4행이 솔버에 닿지 않는다.** CCUS 2행(제외 결정) + `_alt` 2행(대안 출처 CAPEX 700·1,126 대 채택값 240·863, 민감도 전용). 수가 데이터이므로 `GEN:d3_excluded` 블록으로 옮겼다 — 손으로 적은 13이 지금까지 4행의 부재를 가리고 있었다 | `data/raw/tech_options.csv` (17행) · `data/prepared/D3_tech_options.csv` (13행) · `scripts/prepare_raw.py:301,303` |
| 3 | §3.4 밴드 주석 | `steel_eaf` 240이 밴드(370–835) 아래인 이유를 "기존 부지 재활용 대 그린필드"로 적고 그 근거를 `data/manifests/estimation_notes_D2_v0.md`에 인용 | **그 파일은 그린필드를 말하지 않는다.** 그린필드 귀속은 `data/raw/tech_bands.csv`의 `derivation` 열에 있다("그린필드 EAF 기준"). 인용을 옮겼다 | `data/raw/tech_bands.csv` · `data/manifests/estimation_notes_D2_v0.md:40` |
| 4 | §3.4 밴드 주석 | 240을 "문헌에 조용히 맞추지 않았다"는 증거로만 씀 — 그 값이 얼마나 낮은지는 없었다 | **우리가 이미 들고 있는 EAF 실적 6건 중 최저이고, 유일한 `partial_scope_comparator`다.** 나머지 5건은 1,314–2,899 천원/t(정부지원·물류·후속설비 포함 gross). **240은 방어 가능한 하한이지 EAF 중심값이 아니다** — 그렇게 적었다. 우리 결론에는 영향이 없다(`steel_eaf`는 아무 설비도 채택 못 하는 행), 영향은 그 전환을 허용하는 EFF 쪽에 있다 | `cap-efficient/data/technology_cost_evidence.csv` |
| 5 | §7 | 교차대조 절이 `tech_cost_reconciliation.md` 미인용 (F17·F18 인계의 미인용 부속 문서 3개 중 하나) | 인용 부착 + 아래 정정의 결과를 적었다: **수소환원 CAPEX ±4% 일치는 문헌 도출값 1개(우리, DIW DP2082)와 표준화 가정 1개(저쪽)의 일치이고, 독립 추출 2개의 일치가 아니다.** 보이는 것보다 약한 교차검증이며, 두 저장소 중 이 사실을 적는 곳은 지금 여기뿐이다 | `docs/tech_cost_reconciliation.md` · `cap-efficient/data/technologies.csv` |

### 부속 문서에서 고친 것 (가이드 결함의 뿌리)

`docs/tech_cost_reconciliation.md` (양 저장소 동일 사본):

- **§2 스크랩 EAF 판정 철회.** "정본 = EFF. EFF는 실제 프로젝트 6건의 총사업비/용량 환산"은
  사실이 아니다 — `cap-efficient/data/technologies.csv`의 `SCRAP_EAF` 행은
  `data_status=model_estimate`, 근거는 "용해설비·전력계통·부대설비 포함 표준화 비용 가정"이다.
  6건을 실제로 환산하면 **240 · 1,314 · 1,515 · 2,266 · 2,576 · 2,899**(중앙값 ≈1,890 ·
  평균 ≈1,802)이고 **560은 그 어떤 함수도 아니다.** 따라서 "FIN의 240은 하한 편의"라는 판정의
  근거도 사라졌다. 남는 사실만 남겼다.
- **§1 수소환원 판정 약화.** "EFF=프로젝트 기반"도 같은 착각이었다(`H2_DRI_EAF`도 `model_estimate`).
- **§5 추적 항목 3번 신설**: EFF 철강 CAPEX 2건의 출처 부재. 재도출·`source_note` 기재까지 이 표는
  두 값을 정합의 근거로 쓰지 않는다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py    # 22 blocks (21 -> ), 125,519 chars (122,320 -> )
.venv/bin/python scripts/build_guide_page.py    # 38 sections, 383 table rows (불변)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                # gate: OK (pytest 79 passed)
.venv/bin/python scripts/build_tech_guide.py --check   # 커밋 직후 current
```

수치 출처: 탈락 4행·행 수 = `data/raw/tech_options.csv` ↔ `data/prepared/D3_tech_options.csv` ·
CCUS 제외 지점 = `scripts/prepare_raw.py:303` · 그린필드 귀속 = `data/raw/tech_bands.csv` derivation ·
EAF 실적 6건 = `cap-efficient/data/technology_cost_evidence.csv` · EFF 값의 등급 =
`cap-efficient/data/technologies.csv` (`data_status`) · `_alt` CAPEX = `data/raw/tech_options.csv`.

테스트 2개 추가(77 → 79). `test_ccus_is_described_as_excluded_wherever_a10_is_stated`는 CCUS가
D3에 들어오거나 두 문서 중 하나라도 CCUS를 리트로핏 수단으로 되돌려 적으면 실패하고, 제외 지점을
파일:행으로 적지 않으면 실패한다. `test_section_3_4_excluded_rows_are_counted_not_typed`는 행 수와
탈락 이름을 raw ↔ prepared에서 다시 세고, EFF 6건의 최저값·범위·`partial_scope` 플래그가 바뀌거나
EFF 두 값에 출처가 붙으면 실패한다(=정정 문장을 갱신하라는 신호).

**생성 블록은 하나 늘었다** (21 → 22, `d3_excluded` 신설). 손으로 쓴 산문 변경은 §4.2 A-10 한 줄과
§7 한 문단이고, 나머지 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **정확해짐** | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변. 그와 별개로 **raw 17행 중 4행이 모형에 닿지 않는다는 사실이 처음으로 문서에 적혔다** — 감사는 prepared만 보므로 이것을 잡지 못한다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 가정 묶음 + base, 묶음당 16칸 (3묶음 미재계획 — F2) |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 반드시 묻는 "CCS는 넣었나"에 답이 생겼고(안 넣었다, 왜, 어디서), 교차검증의 강도를 과장하지 않는다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 8항목 그린, MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 383행) / SVG / 데이터 패키지 |

### 인계

- **미인용 부속 문서 2개**(F18 목록에서 `tech_cost_reconciliation` 해소): `literature_map` ·
  `price_process_test`. 다음 후보는 **§6.2의 GBM/OU 대조 ↔ `price_process_test`**. 인용 부착 =
  사실 대조가 **네 번 연속** 확인됐다 — 이 규칙성 자체가 남은 2개를 여는 이유다.
- **EFF 철강 CAPEX 2건 출처 부재**(신규, 미해결): `SCRAP_EAF` 560 · `H2_DRI_EAF` 900이
  `model_estimate`이고 외부 앵커가 없다. EFF에는 `technology_cost_evidence.csv`에 실적 6건이
  이미 있으므로 **비교가능 범위(`full_project` + `partial_scope`)만 골라 재도출하는 것이 데이터 0의
  작업이다.** 이것을 하면 H4 교차대조의 "정합" 판정 두 칸이 처음으로 근거를 갖는다.
- **`_alt` 행의 용도 미실현**(신규 관측): `steel_eaf_alt`·`steel_h2dri_alt`는 "민감도 전용"으로
  보존되는데 **어떤 민감도 축도 이 행을 쓰지 않는다**(11 묶음에 `tech_capex_alt`류가 없다).
  `tech.capex` 증거 승수 구간 [1.00, 3.48](F17)과 같은 문제를 다른 데이터로 재는 행이 이미
  저장소에 있다는 뜻이다.
- **`D6.capex_total` 소비**(F18): `_affordability`에 한 줄. E5 재실행이 필요하니 파이프라인
  재실행 사이클에 묶어라.
- **감사기의 이름 매칭 한계**(F18, 미해결) · **§7이 드러낸 실질 공백 2건**(문헌 LCOA 미추출 ·
  교차대조 요인별 정량 분해 미실시) · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F20 (03:23) — §6.2가 인용하는 산출물 셋이 헤드라인보다 오래됐고, H4 요인 목록에 확률과정이 없었다

**한 일.** F19 인계의 첫 후보(§6.2 GBM/OU 대조 ↔ `price_process_test`)를 열었다. 인용을
붙이러 열었더니 붙일 곳이 어긋나 있었다 — **F16·F17·F18·F19·F20 다섯 번 연속**. 이번에
어긋난 것은 문장이 아니라 **문장이 가리키는 파일들**이었다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6.2 | "zero rank reversals across thirteen perturbations (`out/m5/bundle_matrix.csv` …; `out/process/{gbm,ou,gbm_median}` …)" — 열세 개가 모두 §6의 헤드라인에 대해 잰 것처럼 읽힌다 | **셋 다 base보다 오래됐다.** `out/process/*` 2026-08-09 21:28 · `out/scenarios` 08-10 08:53 · `out/m8` 08-10 06:44 대 base `out/e5` 08-10 10:00. `process_alternative.py`의 `_link_shared`가 E1·E2를 base에 **심볼릭 링크**하므로, base가 다시 풀린 뒤 그 팔은 디스크 위에서 자기 입력과 어긋난 트리가 된다. 크기를 측정했다: 명목상 헤드라인과 같은 설정인 `gbm` 통제군이 **NSC에 대해 ② +6.27%(165.4 대 155.6), ③ +4.63%(34,488 대 32,961)** 어긋난다. 나머지 세 기업은 정확히 일치한다 | `out/process/gbm/e5/metrics_company.csv` ↔ `out/e5/metrics_company.csv` · mtime · `scripts/process_alternative.py:47` |
| 2 | §6.2 | "TCaR moves 41–48% on the price-process choice alone" — 그 41–48%가 확률과정 대안의 **범위**로 읽힌다 | **한 점이지 범위가 아니다.** OU 반감기 10년은 주입값이고, 짧을수록 TCaR은 더 줄고 무한대면 GBM으로 수렴한다(`process_alternative.md` 말미의 주의). 그리고 **자매 모형이 반감기 2.0년에 있다** — 같은 축 위, 다섯 배 빠른 곳. 따라서 41–48%는 이 채널의 **하한** | `docs/process_alternative.md` · `cap-efficient/data/price_process.json` |
| 3 | §6.2 | "untestable"이 §8·§9에만 수로 있었고 §6.2에는 없었다 (`price_process_test.md`는 F17 이래 세 사이클째 미인용) | 검정력을 그 자리에 적고 인용을 붙였다: 유한표본 단위근 검정의 검정력 **4.9–5.4%**(명목 5%), **월별 480관측(40년)으로도 80%에 미달**, τ가 n이 아니라 √n으로 커지므로 반감기 10년에는 약 **395년**이 필요하다 | `docs/price_process_test.md` · `docs/price_process_test.csv` |
| 4 | §7 (+ `docs/cross_model_check.md` §3·§4) | 구조 요인 목록이 "배출 경계 · 기술 집합 · 탄소비용 처리 · 설비 해상도 · 시나리오 정의" 다섯이고, TCaR 수준 비교 불가의 이유는 **분모**뿐이었다 | **분모를 맞춰도 남는 여섯 번째 요인이 있다 — 두 모형이 같은 난수 세계를 쓰지 않는다.** FIN은 GBM, EFF는 세 요인 전부 OU(전력 **반감기 2.0년** κ=0.35 · 수소 2.5 κ=0.28 · 자본 3.2 κ=0.22), 요인상관도 **단위행렬 대 0.55/0.25/0.35**, σ도 수소 0.250 대 0.18 · 자본비 0.060 대 0.14. 가이드 자신의 측정으로 이 채널이 **41–48%**이므로 목록에 있는 어떤 구조 요인보다 크다. 양쪽 다 데이터가 고른 값이 아니다 — FIN은 사전값(A-17), EFF는 파일 전체가 `illustrative_estimate` | `cap-efficient/data/price_process.json` · `out/e3/calibration_report.csv` · `docs/process_alternative.md` |

### 부속 코드에서 고친 것

- **`cap-efficient/docs/cross_model_check.md`가 조용히 낡아 있었다.** `cross_model_check.py`는
  "양 저장소 동일 사본"이라고 적으면서 `~/Documents/cap-efficient/docs`(별도 git 저장소)에만
  복사했다. **정작 이 저장소에 커밋되는 사본은 갱신되지 않았다.** 두 트리 모두에 쓰도록 고쳤다.
- `cross_model_check.py`에 `eff_file()`을 넣어 EFF 파일을 두 트리 중 존재하는 쪽에서 읽는다.

### 검증

```
.venv/bin/python scripts/cross_model_check.py     # docs/ + EFF 사본 2개
.venv/bin/python scripts/build_tech_guide.py      # 23 blocks (22 -> ), 129,266 chars (125,519 -> )
.venv/bin/python scripts/build_guide_page.py      # 38 sections, 387 table rows (383 -> )
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                  # gate: OK (pytest 81 passed)
.venv/bin/python scripts/build_tech_guide.py --check   # 커밋 직후 current
```

수치 출처: 드리프트·mtime = `out/process/gbm/e5/metrics_company.csv` ↔ `out/e5/metrics_company.csv` ·
심볼릭 링크 = `scripts/process_alternative.py:47` (`_link_shared(dst, ["e1","e2"])`) ·
검정력·임계값 = `docs/price_process_test.csv` · EFF κ·σ·상관 = `cap-efficient/data/price_process.json` ·
FIN σ·상관 = `out/e3/calibration_report.csv` · 41–48% = `docs/process_alternative.md` §2.

테스트 2개 추가(79 → 81). `test_diagnostic_drift_block_measures_the_real_gap`은 드리프트를
out/에서 다시 계산해 생성 블록의 수와 맞추고, **팔을 다시 돌리면 검사 방향이 뒤집혀
"모두 최신"이라고 적히는지를 본다**(같은 설정인데 0.05% 넘게 어긋나면 실패).
`test_cross_model_causes_name_the_price_process`는 EFF의 κ에서 반감기를 다시 계산해 두 문서와
맞추고, EFF가 GBM으로 바꾸거나 §7이 이 교란을 다시 분모 문제의 각주로 되돌리면 실패한다.

**생성 블록은 하나 늘었다** (22 → 23, `diagnostic_drift` 신설). 손으로 쓴 산문 변경은 §6.2 한
문단과 §7 한 문단이고, 드리프트 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **정확해짐 (약해진 쪽으로)** | 11 묶음 + base 불변이나, **그 묶음이 현행 base가 아닌 08:53 실행에 대해 잰 것**임이 처음으로 문서에 적혔다. 차이는 여전히 유효하고 수준만 어긋난다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "왜 GBM이냐"를 물으면 검정력 4.9–5.4%·40년 부족·395년 필요가 한 자리에 있다. 그리고 두 모형의 TCaR을 나란히 보여줄 때 섞이는 것이 무엇인지 처음으로 적혀 있다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 커밋되는 EFF 사본이 더 이상 낡지 않는다. 게이트 8항목 그린, MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 387행) / SVG / 데이터 패키지 |

### 인계

- **곁가지 산출물 재실행**(신규, 최우선): `out/process`(3팔) · `out/scenarios`(11묶음) ·
  `out/m8`이 전부 현행 base보다 오래됐다. **이번 사이클에 시작하지 않았다** — E3–E5 3회 +
  묶음 재평가는 30분 창에 끝난다는 보장이 없고, 창 규칙상 못 끝낼 파이프라인은 시작하지
  않는다. 파이프라인 재실행 사이클에 `D6.capex_total` 소비(F18)와 **함께 묶어라**.
  재실행하면 `diagnostic_drift` 블록이 저절로 "모두 최신"으로 바뀌고 새 테스트가 그쪽을 검사한다.
- **gate에 곁가지 낡음 검사 없음**(신규): gate의 `out/ vs data/raw`는 `out/e5/affordability.csv`
  하나만 본다. `out/process`·`out/scenarios`·`out/m8`은 아무도 보지 않아 F20까지 하루 넘게
  낡은 채 인용됐다. 지금은 가이드 블록이 드러내지만 게이트는 여전히 통과시킨다.
- **미인용 부속 문서 1개**(F19 목록에서 `price_process_test` 해소): `literature_map`.
  **인용 부착 = 사실 대조가 다섯 번 연속 확인됐다** — 마지막 하나를 여는 이유다.
- **EFF·FIN 확률과정 정량 분해 미실시**(신규): §3-5 표는 차이의 *크기*를 보이지만, TCaR 대조
  차이에서 이 채널이 몇 %인지는 계산하지 않았다. FIN을 EFF의 κ로 한 번 돌리면 그 몫이 나온다.
- **EFF 트리가 둘**(신규 관측): `~/Documents/cap-efficient`(별도 git, 08-11 20:53)와
  `cap-efficient/`(이 저장소, 08-10 09:08). `price_process.json`은 동일하나 **`cross_model_check.py`가
  읽는 `outputs/candidate_scenario_metrics.csv`는 별도 저장소 쪽이고, 그 값이 F19 이후 바뀌었다**
  (POSCO EFF ② 26.6 → 58.3, TCaR 9.6 → 10.3). 어느 트리가 정본인지 결정해야 한다.
- **EFF 철강 CAPEX 2건 출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) ·
  **`D6.capex_total` 소비**(F18) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F21 (03:52) — A-13의 판정이 앵커 하나 위에 서 있었고, 그 앵커는 열 사이클 전에 철회됐다

**한 일.** F20 인계의 마지막 미인용 부속 문서(`literature_map`)를 열었다. 인용을 붙이러
열었더니 붙일 곳이 어긋나 있었다 — **F16·F17·F18·F19·F20·F21 여섯 번 연속**. 이번에 어긋난
것은 판정 자체였고, 그 판정을 철회한 문서는 **2026-08-10부터 저장소 안에 있었다**.

최우선 인계(곁가지 산출물 재실행)는 이번에도 시작하지 않았다. E3–E5 3회 + 11묶음 재평가는
30분 창에 끝난다는 보장이 없고, 창 규칙상 못 끝낼 파이프라인은 시작하지 않는다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.1 A-13 · §7 | "**Fails external validation**: the injected blast-furnace replacement cost is 4.2× a disclosed actual (Kobe, 47)" — 두 곳 모두 **단일 관측**에 기댄 판정이고, "그 배수가 이 파라미터에 대한 전부"로 읽힌다 | **앵커는 셋이고 한 점으로 모이지 않는다.** 고베 실적 **47** · 문헌 개수 단가 €48/t = **70** · 기당 재조달비 300–1,000 M USD ÷ BF 능력 중앙값 5.02 Mt = **[81, 269]**. 우리 200은 앞의 둘보다 ×4.2·×2.9 크지만 **셋째 대역 안**이다. 판정은 우리 값의 오류가 아니라 **6배 산포**다. `validation_external.md` §1-1이 L1(08-10) 이래 이렇게 적고 있었고 **가이드만 옛 판정을 들고 나가고 있었다** | `docs/literature_map.md` §4-1 · `docs/validation_external.md` §1-1 · `cap-efficient/data/technology_cost_evidence.csv` · `data/prepared/D1a_facility_static.csv` |
| 2 | §7 (신규) | — (약점이 어디에도 없었다) | 약한 근거를 같은 문장에 붙였다: **ACCR 원문이 통화를 밝히지 않는다**(USD 가정). AUD였다면 대역이 **[52, 175]**로 내려가 우리 200이 **대역 위로 나간다** — 'inside the band'는 무조건적 판정이 아니다. 그리고 **NATCOMM의 H2-DRI-EAF 값이 VOGL_2018과 정확히 같으므로** 그 개수 단가도 2차 인용일 수 있다 — **3점이 아니라 2.5점** | `data/raw/source_register.csv` (`ACCR_BF_RELINE_2025`·`NATCOMM_APA_2026` quality_note) · `scripts/validate_external.py:67-71` |
| 3 | §4.3 표 · §4.3 산문 | `reline_cheap`의 Re-planned 칸이 "**not needed**" — 재계획이 필요 없는, 즉 **다 잰** 축으로 읽힌다 | **E2가 읽는 축이다.** `incumbent_capex_scale`은 `plancost.py:78`에서 좌초비용에 곱해지고 좌초비용은 E2 계획 탐색이 읽는다. 이 축의 본 효과는 **채택 시점을 앞당기는 것**이고, 계획 메뉴를 공유한 채 잰 2.0% / 0.3%는 **하한**이다. `build_scenario_page.py`의 `PARTIAL_EFFECT`가 이미 그렇게 적고 있었다 — 목록을 두 벌 두지 않고 그쪽을 읽는다 | `src/cap/plancost.py:78` · `scripts/build_scenario_page.py:36-41` · `scripts/run_scenarios.py:63-66` |
| 4 | §7 (신규) | 개수 검증이 **실패 방향 한 건**만 실려 있었다 | 같은 문서의 **반대 방향 결과**를 처음 적었다 — 재투자 창 타이밍(`last_reline_year + 15/20년`, CAPEX 피크 연도를 직접 정하는 내부 가정)은 두 외부 표본과 어긋나지 않는다: 2030년까지 도래 **8/17기·능력 48.9%** 대 NATCOMM 철강 자산 42%, 2026–2035 도래 **12/17기·능력 73.8%** 대 ACCR "개수 의사결정의 70% 이상". 표본이 달라 검증은 아니고, 우리가 조금 이른 것은 한일 노후 고로 표본과 방향이 맞는다 | `docs/validation_external.md` §1-2 |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py      # 24 blocks (23 -> ), 132,387 chars (129,266 -> )
.venv/bin/python scripts/build_guide_page.py      # 38 sections, 392 table rows (387 -> )
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                  # gate: OK (pytest 83 passed)
.venv/bin/python scripts/build_tech_guide.py --check   # 커밋 직후 current
```

수치 출처: 고베 47 = `cap-efficient/data/technology_cost_evidence.csv` (`COST_KOB_BF3_RELINE`) ·
우리 200·BF 17기·능력 중앙값 5.02 Mt = `data/prepared/D1a_facility_static.csv` ·
€48/t·300–1,000 M USD·환율 = `scripts/validate_external.py` (상수를 재선언하지 않고 import) ·
재투자 창 대조 = `docs/validation_external.md` §1-2 · 2.0%/0.3% = `out/m5/bundle_matrix.csv`.

테스트 2개 추가(81 → 83). `test_reline_verdict_uses_every_anchor_not_the_binding_one`은 세 앵커를
출처에서 다시 계산해 가이드 표와 맞추고, **우리 200이 ACCR 대역 밖으로 나가면 실패**하며(그러면
§7·§4.1을 다시 써야 한다), AUD 대역 [52, 175]가 빠지면 실패한다.
`test_reline_cheap_is_not_sold_as_a_completed_check`는 `PARTIAL_EFFECT` 연결이 끊기거나
`plancost.py`가 이 배수를 더 이상 읽지 않거나 표가 다시 "not needed"로 돌아가면 실패한다.

**생성 블록은 하나 늘었다** (23 → 24, `reline_anchors` 신설). 손으로 쓴 산문 변경은 §4.1 A-13
한 칸, §4.3 한 문장, §7 두 문단이고, 앵커 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **정확해짐 (약해진 쪽으로)** | 11 묶음 + base 불변이나, "다 잰 축"으로 읽히던 묶음이 하나 더 줄었다 — 재계획 미실시가 3건이 아니라 **3건 + 부분 1건**이다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "이 파라미터 틀린 거 아니냐"고 물으면 답이 "4.2배 틀렸다"가 아니라 "6배로 흩어져 있고 우리는 그 안이다, 다만 통화 가정에 걸려 있다"로 바뀐다. 방어 가능한 진술이고 과장이 아니다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 8항목 그린, MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 392행) / SVG / 데이터 패키지 |

### 인계

- **부속 문서 인용이 전부 붙었다.** F17 이후 6사이클 연속으로 "인용을 붙이러 열면 사실이
  어긋나 있다"가 확인됐고, 목록이 소진됐다. **다음 사이클부터는 이 탐지기가 없다** — 대신
  쓸 규칙이 필요하다. 후보: **가이드가 인용하는 부속 문서의 판정 날짜 대 가이드 문장의 판정**
  (이번 F21 결함이 정확히 그것이다 — 문서는 08-10에 판정을 바꿨고 가이드는 열흘 뒤까지 옛
  판정을 들고 있었다). 이것은 기계로 잡을 수 있다: 부속 문서 mtime > 가이드의 해당 문단 최종
  수정 커밋이면 경고.
- **`reline_cheap --replan` 미실시**(신규): §4.3이 이제 "하한"이라고 적지만 상한은 없다.
  `run_scenarios.py --replan reline_cheap` 1회면 채택 시점 이동분이 나온다. 그리고 **대역
  상단(×1.34)은 어떤 묶음도 돌린 적이 없다** — 저비용 쪽만 잰 비대칭 민감도다.
- **곁가지 산출물 재실행**(F20, 최우선 유지): `out/process`·`out/scenarios`·`out/m8`이 base보다
  오래됐다. `D6.capex_total` 소비(F18) · `reline_cheap --replan` · `carbon_slow`·`ppa_costly`·
  `retire_free` 재계획을 **한 번의 파이프라인 창에 묶어라**.
- **gate에 곁가지 낡음 검사 없음**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **EFF 트리가 둘**(F20) · **EFF·FIN 확률과정 정량 분해**(F20) ·
  **감사기의 이름 매칭 한계**(F18) · **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) ·
  **§6.1 시드 스윕 재실행** · **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F22 (04:30) — 교차대조의 유일한 수준 검사가 저장소 밖 파일에서 나오고 있었다

**한 일.** F21 인계는 "부속 문서 인용이 전부 붙었다 — 다음 사이클부터는 이 탐지기가
없다"였고, 대체 규칙으로 **부속 문서의 판정 날짜 대 가이드 문단의 최종 수정 시각**을
제안했다. 그것을 스크립트로 돌려 봤다(`git blame` × 부속 문서 최종 커밋). 8건이 걸렸으나
**대부분 노이즈였다** — 매 사이클 재생성되는 문서를 가리키는 포인터 문장들이 시간차만으로
걸린다. 규칙 자체는 만들지 않고(허용목록 유지비가 잡는 결함보다 크다) 그 목록을 손으로
읽었고, 거기서 §7의 실제 결함이 나왔다.

최우선 인계(곁가지 산출물 재실행)는 이번에도 시작하지 않았다 — 30분 창 규칙. 대신 그것을
**게이트가 말하게** 만들었다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §7 | "the claim this layer supports is that the two models point the same direction — **not that their levels agree**" — 교차대조가 수준 공간에서는 아무것도 재지 않은 것처럼 읽힌다 | **수준 공간의 검사가 하나 있고 C16 이래 있었다.** 우리 ②가 EFF의 실행가능 후보 대역 안에 드는가 — POSCO 115 대 [26.6, 155.9] **안**, NSC 156 대 [42.3, 152.3] **밖(위치 103%)**. 가이드는 이 검사를 한 번도 싣지 않았다 | `docs/cross_model_check.md` §3 · `out/e5/metrics_company.csv` · `cap-efficient/outputs/candidate_scenario_metrics.csv` |
| 2 | 검사 자체 (`cross_model_check.py`) | 대역이 **저장소 밖 파일**에서 나오고 있었다. F20이 `eff_file()` 헬퍼를 넣고도 후보 지표만은 `EFF / "outputs" / ...`로 직접 읽었다 | **두 사본이 어긋나 있고 판정이 뒤집힌다.** 같은 필터에서 NSC 대역이 커밋된 사본 **42.3~152.3**, 저장소 밖 사본 **78.5~184.6** — 우리 156은 앞에서 밖, 뒤에서 안. **F20이 판정을 "(POSCO)"에서 "(POSCO, NSC)"로 올린 근거가 저장소 밖 파일의 변화 하나였다.** 탐색 순서를 뒤집어 커밋된 사본을 정본으로 삼았다 — 재현 가능해지는 대신 NSC가 대역 밖이 된다 | `scripts/cross_model_check.py:37-50,110` · 두 트리 `outputs/candidate_scenario_metrics.csv` (사내 08-10 00:43 / 사외 08-11 22:17) |
| 3 | §7 (신규) | — (대역 검사가 대칭적 검사로 읽힐 자리) | 약한 근거를 같은 문장에 붙였다: **대역의 하단은 EFF 자신이 고른 값**이다(선택 규칙이 gross 최소). 우리 값은 정의상 그 아래로 내려갈 수 없으므로 **이 검사는 위쪽으로만 실패한다**. 폭도 **POSCO 5.9배 · NSC 3.6배**로 느슨하다. 이 검사가 지지하는 진술은 "수준이 일치한다"가 아니라 "EFF가 실행가능하다고 본 것 중 **비싼 쪽에** 우리가 있다" | `cap-efficient/outputs/candidate_scenario_metrics.csv` (`gross_cost_p50_kkrw_per_tco2`) |
| 4 | `docs/cross_model_check.md` §3 (양 사본) | "FIN의 값이 EFF의 실행가능 후보 분포 안에 있다 (POSCO, NSC)" 한 줄뿐 | 같은 문서에 대역 폭·한쪽 실패·트리 의존을 적었다. §3의 "어느 트리에서 나왔는가" 절이 두 사본의 대역을 나란히 적고 **판정이 뒤집히는 자리를 표시**한다 | 생성기가 씀 (`eff_divergence` 신설) |

### 게이트에 추가한 것

- **`sidecars` 검사(WARN)**: `out/process`·`out/scenarios`·`out/m8`·`out/m5`가 `out/e5`보다
  오래되면 이름과 시간차를 찍는다. F20 인계 항목이다 — 그 전까지 이 넷은 아무도 보지 않아
  하루 넘게 낡은 채 §6.2에 인용됐고 게이트는 그린이었다.
  현재: `STALE behind out/e5: process(13h), scenarios(1h), m8(3h), m5(1h)`.
  FAIL이 아닌 이유는 코드 결함이 아니라 **재실행 대기 상태**이기 때문이다.

### 검증

```
.venv/bin/python scripts/cross_model_check.py     # docs/ + EFF 사본 2개
.venv/bin/python scripts/build_tech_guide.py      # 25 blocks (24 -> ), 134,143 chars (132,387 -> )
.venv/bin/python scripts/build_guide_page.py      # 38 sections, 395 table rows (392 -> )
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                  # gate: OK (pytest 84 passed, sidecars WARN)
.venv/bin/python scripts/build_tech_guide.py --check   # 커밋 직후 current
```

수치 출처: 우리 ② 115·156 = `out/e5/metrics_company.csv` (NZ15·support=none) ·
EFF 대역 = `cap-efficient/outputs/candidate_scenario_metrics.csv`
(`scenario_id=ACCELERATED_15C`, 4개 실행가능 플래그 전부 참인 96개 후보의
`gross_cost_p50_kkrw_per_tco2` min/max) · 저장소 밖 대역 =
`~/Documents/cap-efficient/outputs/candidate_scenario_metrics.csv` (같은 필터, 99개).

테스트 1개 추가(83 → 84). `test_crossmodel_band_reads_the_committed_eff_copy`는 (a) 읽는
사본이 저장소 안인지, (b) §7 표의 대역이 그 사본에서 다시 계산한 값과 맞는지, (c) 우리
값이 EFF 채택값 아래로 내려가지 않는지(=하단이 EFF의 답이라는 서술의 전제), (d) §7이
"위쪽으로만 실패"와 "트리 의존"을 계속 적는지를 본다. **탐색 순서가 다시 뒤집히면 실패한다.**

**생성 블록은 하나 늘었다** (24 → 25, `crossmodel_band` 신설). 손으로 쓴 산문 변경은 §7
한 문단(대역 검사 도입문)이고, 대역 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 묶음 + base 불변. 다만 게이트가 이제 이 묶음이 base보다 1시간 낡았다고 말한다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선 (약해진 쪽으로)** | Arc가 "다른 모형과 대조했느냐"고 물으면 답이 "방향만"에서 "수준에서도 한 검사가 있다 — 우리 단가는 상대 모형이 실행가능하다고 본 대역 안(POSCO)·위(NSC)이고, 그 대역은 5.9배로 느슨하며 하단이 상대의 답이라 위쪽으로만 실패한다"로 바뀐다. 자랑이 아니라 방어 가능한 진술이다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 교차대조가 이 저장소만으로 재현된다. 게이트 9항목(8 → ), MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 395행) / SVG / 데이터 패키지 |

### 인계

- **EFF 트리 문제는 절반만 닫혔다**(F20 인계 갱신): 읽는 쪽은 커밋된 사본으로 고정했으나
  **두 사본이 여전히 다르다**. 저장소 밖 사본(08-11 22:17)이 더 새롭고, 그쪽이 맞다면
  커밋된 `cap-efficient/outputs/`를 갱신해야 한다 — 그러면 NSC가 다시 대역 안이 되고
  §7 표와 테스트가 자동으로 따라간다. **어느 쪽이 정본인지는 EFF를 누가 언제 돌렸는지의
  문제**이고 이 창에서 결정할 수 없다.
- **부속 문서 낡음 탐지기는 만들지 않았다**(F21 제안 기각): `git blame` × 부속 문서 최종
  커밋으로 8건이 걸렸으나 실효 1건, 나머지는 재생성 문서를 가리키는 포인터 문장이었다.
  허용목록 유지비가 잡는 결함보다 크다. 대신 **이번 결함이 걸린 경로**를 규칙으로 삼아라 —
  *가이드가 인용하는 문서의 표가 가이드에 없으면 왜 없는지 확인한다.*
  §7의 대역 표가 정확히 그 경우였다.
- **곁가지 산출물 재실행**(F20, 최우선 유지): 이제 게이트가 이름으로 말한다.
  `out/process`(13h)·`out/m8`(3h)·`out/scenarios`(1h)·`out/m5`(1h). `D6.capex_total` 소비(F18) ·
  `reline_cheap --replan`(F21) · 대역 상단(×1.34) 미측정(F21)을 **한 번의 파이프라인 창에 묶어라**.
- **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5), `get_affordability` 통화 경고(F8).

## F23 (05:00) — 가이드가 인용만 하고 표는 싣지 않은 문서를 전수로 훑었다

**한 일.** F22 인계가 규칙을 하나 남겼다 — *가이드가 인용하는 문서의 표가 가이드에
없으면 왜 없는지 확인한다.* `docs/*.md` 16개에 대해 인용 지점과 문서 본문을 대조했다.
`validation_backtest.md`는 판정·원인배제·못 한 검증까지 §7에 전부 실려 있었다(F가 이미
닫은 것). 걸린 것은 **`robustness_structural.md` 하나**다 — 가이드는 이 문서를 §1 P2
행에서 §3-1(헤지가 꼬리를 못 덮는다)만 한 번 인용하고, §1 순위 불변과 §2 꼬리 배수 표는
어디에도 없었다. 그 대조 중에 게이트 항목 수 오기 2건이 같이 나왔다.

최우선 인계(곁가지 재실행)는 이번에도 시작하지 않았다 — 30분 창 규칙. F20 이래 4사이클
연속이므로 인계에 그 사실을 적는다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6.2 | 순위 강건성의 근거로 **할인율 3.5/5/6.5% · GBM 대 평균회귀 · 충격 정규화 · 시나리오 묶음 13개**를 든다. 넷 다 **입력**을 흔들고 목적함수는 P50 최소화로 고정이다 — "무엇을 최적이라 부르는지"는 흔들어 본 적 없는 것처럼 읽힌다 | **흔들어 봤고 문서가 I2 이래 들고 있었다.** 같은 계획 메뉴에서 P90 최소화로 다시 고르면 **순위 불변**이고, 꼬리 배수는 POSCO ×1.5 · NSC ×1.4 대 MCI ×3.8 · LOTTE ×3.9. 최저~최고 기업 격차가 기대비용 2.4배에서 나쁜 경우 **6.4배**로 벌어진다. §1 P2 행이 이 문서를 인용하면서도 §3-1만 가져왔다 | `docs/robustness_structural.md` §1·§2 · `out/e5/frontier_points.csv` (NZ15·support=none·`budget_ok`·미공시) |
| 2 | §6.2 (신규) | — (약점이 없었다) | 약한 근거를 같은 자리에 붙였다: **P90은 우리 시뮬레이션의 P90**이므로 꼬리 배수의 절대값은 A-17 사전 변동성(h₂ 0.25, 단위상관)만큼만 믿을 수 있다 — 결론은 철강 대 석화의 **대비**다. 그리고 이것은 **재계획이 아니라 재선택**이다: E2가 P50 목적으로 만든 메뉴 밖의 계획은 어느 기준으로도 고를 수 없다(§4.3이 묶음에 붙인 것과 같은 천장, 같은 방향 — 기준의 영향을 **과소**평가한다) | `docs/robustness_structural.md` §4 · `scripts/robustness_structural.py` |
| 3 | §7 | "**Eight checks** … Five of the **eight** are hard" — 손으로 센 수 | **9항목이다.** F22가 `sidecars`를 아홉 번째로 넣었고 가이드가 따라오지 않았다. 외부 독자에게 검사 하나가 없는 것으로 보인다. 이제 `gate.CHECKS`/`gate.HARD`에서 생성한다 — 다음에 항목이 늘어도 틀릴 수 없다 | `scripts/gate.py:177-186` (`CHECKS`), `:31` (`HARD`) |
| 4 | §6.2 `diagnostic_drift` 블록 | "`scripts/gate.py` checks staleness for `out/e5` **only**" | **F22 이후 거짓이다.** `check_sidecars`가 `out/process`·`out/scenarios`·`out/m8`·`out/m5`를 이름과 시간차로 찍는다. 이 문단의 주제가 낡은 곁가지인데, 게이트가 그것을 말한다는 사실이 빠져 실제보다 어두운 그림을 팔고 있었다 | `scripts/gate.py:120-140` |

`docs/robustness_structural.md` 자체도 `out/e5`보다 낡아 POSCO의 P50 선택이 `P0043`으로
남아 있었다(현행 `P0042`). 재생성했고 **수치는 불변**이다 — 낡은 것은 계획 식별자뿐이었다.

### 검증

```
.venv/bin/python scripts/robustness_structural.py   # P0043 -> P0042, 수치 불변
.venv/bin/python scripts/build_tech_guide.py        # 27 blocks (25 -> ), 136,886 chars (134,143 -> )
.venv/bin/python scripts/build_guide_page.py        # 38 sections, 400 table rows (395 -> )
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                    # gate: OK (pytest 87 passed, sidecars WARN)
```

수치 출처: 115·156·242·279(P50)와 170·221·911·1,085(P90) = `out/e5/frontier_points.csv`를
`robustness_structural.pick`으로 재선택한 값 — 생성기가 로직을 다시 쓰지 않고 **import**한다 ·
9·5 = `scripts/gate.py`의 `CHECKS`·`HARD` · 2.4배·6.4배 = 같은 표의 최소/최대 비.

테스트 3개 추가(84 → 87).
`test_criterion_swap_carries_the_tail_not_just_the_ranking`은 네 기업의 P90 단가와 꼬리
배수를 `out/e5`에서 다시 계산해 가이드와 맞추고, **순위가 실제로 뒤집히면 "unchanged"
문장에서 실패하며**, 두 약점 문장(A-17 사전값 의존 · 재선택)이 지워지면 실패한다.
`test_gate_check_count_is_read_off_gate_not_hand_written`은 항목 수·hard 수·항목 이름을
`gate.py`에서 읽어 대조하고 옛 "Eight checks"가 되살아나면 실패한다.
`test_guide_does_not_say_the_gate_ignores_the_side_diagnostics`는 4번 오류의 회귀를 막는다.

**생성 블록은 둘 늘었다** (25 → 27, `criterion_swap`·`gate_checks` 신설). 손으로 쓴 산문
변경은 §6.2 두 문단(도입 + 약점)과 §7 한 문장이고, 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **개선** | 11 묶음 + base 불변이나, **묶음이 아닌 축**이 하나 문서화됐다 — 목적함수 교체(P50↔P90)는 재실행 없이 같은 메뉴에서 재선택으로 잰다. 분석툴로서 "위험선호를 바꾸면?"에 답이 생겼다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "위험회피 투자자라면 답이 달라지느냐"고 물으면 이제 답이 있다 — 지목 기업은 같고, 석화의 꼬리는 철강의 **2.6배 두껍다**(×3.9 대 ×1.5). 석화 문제가 수준이 아니라 분산이라는 §1 P2의 주장에 처음으로 수치가 붙었다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 게이트 항목 수가 가이드에서 생성된다 — 손으로 센 수가 게이트와 어긋날 자리가 사라졌다. 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 400행) / SVG / 데이터 패키지 |

### 인계

- **F22의 규칙은 이번으로 소진됐다.** `docs/*.md` 16개를 인용 지점 기준으로 전수 대조했고
  실효 결함은 `robustness_structural.md` 하나였다. **다음 규칙 후보**: 이번 결함 2·3·4가
  전부 **"생성될 수 있었는데 손으로 쓰여 있던 수"**였다 — 가이드에서 숫자가 들어간
  손글씨 문장을 훑어 생성 가능한 것을 골라내라. `Eight checks`는 9사이클 동안 맞다가
  F22 하루에 틀렸고, 아무 테스트도 잡지 못했다.
- **곁가지 산출물 재실행**(F20 이래 4사이클 연속 미실시, 최우선 유지):
  `out/process`(13h)·`out/m8`(3h)·`out/scenarios`(1h)·`out/m5`(1h). `D6.capex_total`
  소비(F18) · `reline_cheap --replan`(F21) · 대역 상단(×1.34) 미측정(F21)을
  **한 번의 파이프라인 창에 묶어라**. 30분 창에서는 시작하지 않는 것이 규칙이므로,
  이 항목은 **창 규칙을 바꾸지 않으면 영원히 밀린다** — 남은 사이클 중 하나를 통째로
  파이프라인에 쓰거나, 아니면 인계에서 내려라.
- **EFF 두 사본 불일치**(F22): 읽는 쪽은 커밋된 사본으로 고정했으나 저장소 밖 사본이
  더 새롭다. 정본 판정은 이 창에서 결정 불가.
- **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F24 (05:30) — 손으로 옮겨 적은 수치를 전수로 훑고, 재실행에 무너질 자리를 막았다

**한 일.** F23 인계가 남긴 규칙을 따랐다 — *가이드에서 숫자가 들어간 손글씨 문장을 훑어
생성 가능한 것을 골라내라.* 생성 블록 밖의 수치 문장 350줄에서 "저장소 객체의 개수"를
주장하는 것만 추려 실데이터와 대조했다. **대부분은 맞았다**: D파일 계수(D1a 23행 ·
D1b 69행 · `PREP_ALLOC` 57/`PREP_BOTTOMUP` 12 · `emissions_s2` 63/69 · D3 steel 7/petchem 6 ·
retrofit 9/13 · D5 7행 · D7 12행, `target` 3/`tech_commit` 7/`timing` 2, `high` 7/`mid` 5,
`coverage_pct` 12행 전부 결측), 파라미터 목록(T5 155 중 139 무대역 · 415 중 21 유대역),
데이터 사전(14파일 134열), `out/e5/gap.csv` 8행=4쌍, `disclosed_skipped.csv` 4행,
일본 사업소 NSC 27/MCI 8, m8의 4·25·32·7 — 전부 일치. **§5의 "85 columns across nine
files"는 과거형 서술**(고쳐진 옛 사전)이므로 결함이 아니다.

걸린 것은 둘이고, 둘 다 "지금 틀렸다"가 아니라 **"읽는 사람이 어느 쪽을 믿을지 알 수
없다" · "곧 틀려질 예약이 걸려 있다"**이다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.1 | 추정 용량을 "**16 of the 23 rows, all blast furnaces**"로 적는다. 같은 문서 §4.1의 A-13은 "200 thousand KRW/t for **all 17 blast furnaces**". 외부 독자는 16기인지 17기인지 판정할 수 없고, 어느 한쪽이 오기라고 읽는다 | **둘 다 맞다 — 추정된 16기는 고로 17기 중 16기다.** 빠진 한 기가 `POSCO_GWY_BF1`이고, 그것이 곱수 913.3을 고정한 **바로 그 교정점**이다. 따라서 공표 용량을 가진 유일한 고로가 상수를 맞추는 데 쓰였고, **추정값을 공표값과 대조할 수 있는 행은 하나도 없다**(표본 외 검증 0건). 나머지 공표 6행은 고로가 아니라 다른 기준(§3.0)이므로 대조에 쓸 수 없다. 이 사실이 §3.1에 없었다 | `data/prepared/D1a_facility_static.csv` (`unit_type`, `capacity_unit`의 `추정` 토큰) |
| 2 | §6.3 | ε-스윕 수치 네 개 — 32캡 · 헤드라인 비지배 **4**(한 묶음) · 대체 규약 **25** · 기술축 복귀 **8묶음 중 7**. 지금은 맞다 | **틀릴 것이 예약되어 있었다.** 이 넷은 `out/m8/summary.csv`에서 손으로 옮겨 적은 값이고, `out/m8`은 게이트 `sidecars` 검사가 **3시간 낡았다고 이름으로 부르는** 부속 산출물이다. 그 재실행은 F20 이래 최우선 인계다 — **인계를 실행하는 순간 네 수가 조용히 거짓이 되고**, 그때까지 아무 테스트도 잡지 못했다. F23의 `Eight checks`(9사이클 맞다가 하루에 틀림)와 같은 종류다. 이제 생성기가 읽는다 | `out/m8/summary.csv` (`caps_tried`, `nondominated_headline`, `nondominated_l2`) |
| 3 | §6.3 꼬리 문장 | "The counts above are the `caps_tried` … columns of that file" — 어느 열에서 왔는지만 말한다 | **언제 읽는지**를 붙였다: 빌드 시점에 읽으며, `out/m8`이 §6.2가 부르는 낡은 부속 산출물이므로 **재실행은 이 수들을 움직이고 주변 산문은 움직이지 않는다**. 독자가 수치의 수명을 알게 된다 | 생성기 도입 사실 |

**§1 P1 행은 생성하지 못했다.** 같은 네 수를 손으로 들고 있으나 마크다운 **표 셀 안**이라
`<!-- GEN -->` 마커가 표를 깬다. 대신 테스트가 m8에서 다시 계산해 대조한다 — 재실행 후
§6.3은 자동으로 따라가고 **§1은 실패로 손을 부른다**. 가정을 명시한다: 표 셀 생성을 위해
렌더러를 인라인 치환까지 확장하는 것은 이 결함 하나가 요구하는 것보다 큰 변경이라 하지
않았다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py    # 29 blocks (27 -> ), 137,681 chars (136,886 -> )
.venv/bin/python scripts/build_guide_page.py    # 38 sections, 400 table rows (불변)
.venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                # gate: OK (pytest 89 passed, sidecars WARN)
```

수치 출처: 16·17·23·6과 `POSCO_GWY_BF1` = `data/prepared/D1a_facility_static.csv` ·
32·4·25·7·8 = `out/m8/summary.csv` · 대조에 쓴 나머지 계수 = `data/prepared/D*.csv`,
`docs/parameter_inventory.csv`, `data/package/data_dictionary.csv`, `out/e5/gap.csv`,
`out/e2/disclosed_skipped.csv`, `data/raw/jp_site_emissions.csv`.

테스트 2개 추가(87 → 89).
`test_capacity_estimate_count_separates_estimated_from_all_blast_furnaces`는 세 수를 D1a에서
다시 세고, 교정점이 이름으로 실려 있는지, "대조 불가" 진술이 살아 있는지를 보며, 옛
문장이 되살아나면 실패한다. `test_epsilon_sweep_counts_are_read_off_m8_everywhere_they_appear`는
§6.3의 네 수와 **§1 P1의 같은 네 수**를 m8에서 다시 계산해 대조한다 —
**최우선 인계(부속 재실행)를 실행하면 이 테스트가 §1을 고치라고 실패한다.**

**생성 블록은 둘 늘었다** (27 → 29, `capacity_basis`·`frontier_degeneracy` 신설). 손으로 쓴
산문 변경은 §3.1 도입문 한 문장과 §6.3 꼬리 한 문장이고, 수치는 전부 생성기가 썼다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 묶음 + base 불변 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 §3.1의 용량 추정을 물으면 답이 "16행을 곱수로 추정했다"에서 "**추정을 검증할 수 있는 행이 0이다 — 공표 용량을 가진 유일한 고로가 곱수를 맞추는 데 쓰였기 때문**"으로 바뀐다. A-01의 무게가 처음으로 정확히 서술된다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 부속 산출물 재실행이 가이드를 조용히 낡게 만들 경로 하나가 닫혔다(§6.3 자동·§1 실패). 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 400행) / SVG / 데이터 패키지 |

### 인계

- **F23의 규칙도 이번으로 소진됐다.** 손글씨 수치를 전수로 훑었고 실효 결함은 위 둘이다.
  나머지는 전부 실데이터와 일치했다 — **가이드의 수치 정확도 자체는 더 팔 것이 없다.**
  **다음 규칙 후보**: 이번 결함 1은 *같은 문서의 두 곳이 같은 대상을 다르게 센* 경우였다.
  §3(데이터 정의)과 §4(가정 대장)가 같은 객체를 세는 자리를 짝지어 훑어라 — 고로 수가
  그랬듯, 두 절이 각각 맞으면서 함께 틀려 보이는 자리가 더 있을 수 있다.
- **곁가지 산출물 재실행**(F20 이래 5사이클 연속 미실시, 최우선 유지): `out/process`(13h) ·
  `out/m8`(3h) · `out/scenarios`(1h) · `out/m5`(1h). **이번 사이클이 그 재실행의 비용을
  하나 줄였다** — m8을 다시 돌리면 §6.3은 자동으로 갱신되고 §1만 손으로 고치면 된다.
  F23이 적은 판단은 그대로다: 30분 창에서 시작하지 않는 규칙을 지키면 이 항목은 영원히
  밀리므로, **남은 사이클 하나를 통째로 파이프라인에 쓰거나 인계에서 내려라.**
- **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건
  출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F25 (05:30) — §3과 §4가 같은 것을 다르게 세는 자리를 훑다가, 3일 낡은 산출물을 찾았다

**한 일.** F24 인계의 규칙을 따랐다 — *§3(데이터 정의)과 §4(가정 대장)가 같은 객체를
세는 자리를 짝지어 훑어라.* 짝을 하나씩 대조하는 중에 A-01의 곱수가 §3.1에서 913.3,
§3.1 필드표와 §4.2에서 913으로 갈리는 것이 나왔고, 그 대조를 코드까지 밀자
**코드는 5,480,000/6,000 = 913.3**인데 파라미터 대장이 913.0을 손으로 들고 있었다.
같은 방식으로 A-01의 "민감도 8위"를 `out/sensitivity/ranking.csv`와 맞춰보다가
**이번 사이클의 실제 결함**이 나왔다.

`scripts/sensitivity_screening.py`는 **자기 독스트링과 다른 곳에 쓴다.** 산출을
`outputs/sensitivity_{screening,ranking}.csv`로 쓰는데 그 디렉터리는 저장소에 없다.
소비자는 다섯이고 전부 `out/sensitivity/{ranking,screening}.csv`를 읽는다 —
`build_tech_guide.py:951`(가이드 §4.3), `build_evidence_page.py`, `data_section_table.py`,
`mcp_server.py`(도구 `get_sensitivity` + 데이터 패키지 매니페스트), `uncertainty_propagation.py`.
그래서 소비자가 읽던 두 파일은 **2026-08-09 10:27 판에 얼어붙어** 있었다. 그 사이
`3825a87 fix: allocation fallback zeroed POSCO's emissions`와 G1 사업소 배분이 들어가
`data/prepared/`가 08-10 05:19에 재생성됐다. **가이드 §4가 인용하던 민감도는 그 수정
이전 데이터의 것이다.**

재실행은 **4.2초**다. 낡음의 원인은 비용이 아니라 경로였다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.1 A-02 | "Moves abatement cost by up to **86%**" | **154%**다. 그리고 옛 값에서는 2위 `tech.emission_factor`(85.8)와 사실상 동률이라 "최대 파라미터"라는 주장이 근소했으나, 현재는 2위(`vol.h2` 45.1)의 **3.4배**다 — A-02의 주장은 약해진 게 아니라 훨씬 세졌고, 그것을 3일간 모르고 있었다 | `out/sensitivity/ranking.csv` (재실행 후) |
| 2 | §4.2 A-01 | "Sensitivity rank **8**" | **10위**다. `fac.capacity`는 14.0으로 내려가고 `vol.elec`·`tech.elec_intensity`·`price.re`가 위로 올라왔다 | 같은 파일 |
| 3 | §4.3 생성 블록 | top 5 = ef_inc 86 · emission_factor 86 · discount 42 · vol.h2 42 · h2_intensity 31 | **ef_inc 154 · vol.h2 45 · emission_factor 44 · discount 36 · vol.elec 33.** 생성 블록이었으나 **읽는 파일이 낡아** 자동 갱신이 무의미했다 — 생성이 신선을 보장하지 않는다는 반례다 | 같은 파일 |
| 4 | §4.3 (신규) | — | 스크린의 천장을 같은 자리에 붙였다: **E2 계획 메뉴를 고정한 채 재가격**하므로 위의 `reline_cheap`과 같은 종류의 과소평가이고, 섭동은 **대칭 ±30%** — §4.5가 문헌 밴드가 있는 세 칸에서 *중심*이 틀렸다고 보인 바로 그 규약이다 | `scripts/sensitivity_screening.py` |
| 5 | §3.1 필드표 · §4.2 A-01 | 곱수 "913 t/m³·yr" (§3.1 산문은 913.3) | **913.3**이다(코드 `T_PER_M3 = 5,480,000/6,000`). 한 문서가 같은 상수를 두 값으로 적고 있었다 | `scripts/prepare_raw.py:44` |
| 6 | §3.2 | "**All nine** columns are schema-required" | **10열**이다. 표가 9행인 것은 복합키 `facility_id`+`year`를 한 줄에 적었기 때문 — 그 사실을 문장에 넣었다 | `src/cap/schemas.py` `SCHEMAS['D1b_facility_panel']` |

**코드·문서 결함 3건**(가이드 밖):
- `check_sidecars`가 네 디렉터리를 손으로 적고 있었다. `out/sensitivity`는 목록에 없었고
  **목록에 있던 어느 것보다 낡았다**(24시간). 이제 `out/` 아래 base(e1–e5)가 아닌
  디렉터리를 전부 센다 — 새 곁가지가 생겨도 숨을 자리가 없다. WARN이 4개에서 9개로
  늘었다(`g2`·`m3`·`m4`·`seeds`·`uncertainty`가 처음 이름으로 불린다).
- `build_parameter_inventory.py:184`가 913.0을 손으로 적어 **대장이 모형이 쓰지 않는
  값을 등재**하고 있었다. 이제 코드와 같은 식으로 쓴다.
- `sensitivity_screening.py` 독스트링이 "인벤토리 밴드를 쓴다"고 주장했으나 구현은
  일률 ±30%다. 독스트링을 사실로 고쳤다.

### 검증

```
.venv/bin/python scripts/sensitivity_screening.py   # 4.2s, out/sensitivity/ 재생성
.venv/bin/python scripts/build_parameter_inventory.py
.venv/bin/python scripts/build_tech_guide.py        # 29 blocks, 138,309 chars (137,681 -> )
.venv/bin/python scripts/build_guide_page.py        # 38 sections, 400 table rows (불변)
.venv/bin/python scripts/build_evidence_page.py && .venv/bin/python scripts/build_site.py
.venv/bin/python scripts/gate.py                    # gate: OK (pytest 92 passed, sidecars WARN 9건)
```

수치 출처: 154·45·44·36·33·14.0·3.4배·순위 10 = `out/sensitivity/ranking.csv`(재실행분) ·
913.3 = `scripts/prepare_raw.py:44` · 10열 = `src/cap/schemas.py` ·
낡음 판정 = `out/sensitivity` 파일 시각 08-09 10:27 대 `data/prepared` 08-10 05:19,
`out/e5` 08-10 10:00.

테스트 3개 추가(89 → 92).
`test_sensitivity_ledger_numbers_track_the_screening_output`은 A-02의 이동폭·1위 배수와
A-01의 순위를 `ranking.csv`에서 다시 계산해 **가이드와 METHODOLOGY 양쪽**에 대조한다 —
한쪽만 고치면 실패하고, 1위 파라미터가 바뀌면 §4.1을 다시 쓰라고 실패한다.
`test_screening_writes_where_every_consumer_reads`는 생산자 경로가 다시 어긋나면 실패한다.
`test_gate_sidecar_check_enumerates_out_rather_than_a_hand_written_list`는 게이트가
낡은 곁가지를 하나라도 이름에서 빠뜨리면 실패한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | 11 묶음 + base 불변. 다만 OAT 스크린(25 파라미터)이 3일 만에 현행 데이터로 돌아왔다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "가장 약한 가정이 얼마나 아픈가"를 물으면 답이 "86%, 2위와 동률"에서 "**154%, 2위의 3.4배**"로 바뀐다. A-02가 압도적 단일 약점이라는 §1의 서사가 처음으로 수치로 뒷받침된다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | MCP `get_sensitivity`와 데이터 패키지 매니페스트가 3일 낡은 순위를 서빙하고 있었다 — 이제 현행. 게이트 9항목·MCP 11도구 불변이나 `sidecars`가 손목록에서 열거로 바뀌었다 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 400행 + evidence 페이지) / SVG / 데이터 패키지 |

### 인계

- **F24의 규칙(§3↔§4 짝 대조)은 절반만 소진됐다.** A-01·A-13·A-03·A-11·A-09·A-20의
  §3 대응 지점을 봤고 실효 결함은 위 5·6번이다. **다음 규칙 후보**: 이번 결함 1–3은
  전부 *생성 블록이 낡은 파일을 읽어* 생긴 것이다 — **생성기가 읽는 입력 파일의 시각을
  전수로 훑어라.** 생성은 신선을 보장하지 않는다. `build_tech_guide.py`가 읽는 경로
  목록을 뽑아 각각이 `out/e5`보다 새것인지 보면, 지금 §6.2·§6.3(process·m8)과
  §6.1(seeds)이 같은 상태일 가능성이 높다.
- **곁가지 산출물 재실행**(F20 이래 6사이클 연속 미실시, 최우선 유지): 게이트가 이제
  9개를 이름으로 부른다 — `process`(13h) · `m8`(3h) · `g2`(3h) · `m3`(3h) · `uncertainty`(3h) ·
  `m4`(2h) · `scenarios`(1h) · `m5`(1h) · `seeds`(18h). **이번 사이클이 그 중 하나를
  4.2초에 끝냈다** — 낡음의 원인이 전부 파이프라인 비용은 아니라는 증거다. 나머지도
  실행 비용을 먼저 재라. 30분 창에서 못 하는 것은 `process`·`scenarios`·`m8`뿐일 수 있다.
- **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건
  출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **§6.1 시드 스윕 재실행** ·
  **MCI 사업소 상한 검증**(O13) · **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F26 (06:00) — 좁은 정의를 넓은 정의로 광고한 문장, 그리고 그것을 고치는 비용이 절반으로 적혀 있던 이유

**한 일.** F25 인계의 규칙을 따랐다 — *생성기가 읽는 입력 파일의 시각을 전수로 훑어라.*
`build_tech_guide.py`가 읽는 `out/` 경로 14개를 뽑아 `out/e5`와 대조했다. 넷이 낡았다
(`m5` 1.1h · `m8` 3.3h · `process` 12.5h · `scenarios` 1.1h). 나머지 열은 현행이었고,
F25가 고친 `out/sensitivity`는 43.7시간 **앞서** 있었다 — 규칙이 겨눈 것은 맞았다.

낡음을 좇아 `out/m5`의 생산자를 찾다가 이번 사이클의 결함이 나왔다. `out/scenarios`를
새로 돌리고 `bundle_matrix.csv`를 재생성하는 과정에서, 저장된 값이 **`scenario == NZ15`
한 칸에서만** 재현된다는 것이 먼저 드러났다(16칸 전체로는 22개 대조 중 15개만 맞았고,
NZ15로 거르자 22/22 맞았다). 생산자를 열어보니 `SCEN, SUPP = "NZ15", "none"`이 코드에
박혀 있었다. **좁은 정의는 논문 §6의 규약이므로 틀리지 않았다. 틀린 것은 가이드였다.**

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.3 산문 | "Δ② and Δ③ are the largest move across the **sixteen** firm × scenario × support cells (4 × 2 × 2)" | **네 칸**이다 — 헤드라인 칸(`NZ15`·`support=none`)의 기업 4개. 두 정의는 멀다: `penalty_none`이 헤드라인에서 ③ **5.9%**, 16칸 전체에서 **241.8%**다. Arc가 "무엇에 대한 최대인가"를 물으면 답이 40배 달라진다 | `scripts/robustness_section_table.py:41`, `out/scenarios/summary.csv` |
| 2 | §4.3 산문 | "Two of those five have been re-planned" | 손으로 적힌 수였고, 재계획 하나가 끝나는 순간 거짓이 된다. 이번 사이클이 바로 그렇게 만들었다 — 지금은 5/5다 | `out/m5/bundle_matrix.csv` |
| 3 | §4.3 대체 문구 | "Run `python -m cap m5`" | **그런 단계가 없다.** CLI는 `e1–e5·render·all`이고 `out/m5`는 `robustness_section_table.py`가 쓴다. 지시대로 하면 아무것도 나오지 않는다 | `src/cap/__main__.py:13` |
| 4 | §4.3 · `run_scenarios.py` 3곳 · 논문 §6.2 | "Re-planning each costs about ten minutes" / "묶음당 ~10분" | **12~32분**이다(실측 6묶음, 중앙값 20). `retire_free` 32분이 가장 비싸다 — 제약을 푸는 묶음이 탐색 공간을 넓히므로 | `REPLAN_MINUTES`, 실행 로그 |
| 5 | 논문 §6.2 | "남은 둘(`carbon_slow`·`ppa_costly`)은 미착수다" | **셋**이었다. `retire_free`가 세는 데서 빠져 있었다 | `out/scenarios/summary.csv` |

1·2번은 **손으로 쓴 산문이 수치 주장을 하고 있던** 자리다. 문장을 지우고 범위 진술을
생성기로 옮겼다. `bundle_matrix.csv`에 16칸 최대 두 열(`d_m2_pct_all`·`d_tcar_pct_all`)을
새로 낸다 — 헤드라인 열은 논문 §6이 그 정의로 쓰였으므로 그대로 두고, 둘 다 싣는다.

### 미뤄둔 일을 했고, 판정이 다섯 번 뒤집혔다

4번을 실측하려 `--replan`을 한 번 돌린 것이 F20 이래 7사이클 밀린 항목의 시작이 됐다.
**E2 전용 다섯 축을 전부 재계획했다.**

| 묶음 | 재계획 전 ② / ③ | 재계획 후 ② / ③ |
|---|---|---|
| `retire_free` | **0.0% / 0.0%** ("모든 열에서 base와 같다") | **41.2% / 51.9%** — ②에서 **전 축 최대** |
| `carbon_slow` | **0.0% / 0.0%** | **39.0% / 99.7%** |
| `ppa_costly` | **0.0% / 0.0%** ("평가 단계에 아예 등장하지 않는다") | 1.4% / 8.1% |

**이 표의 '변화 0'은 한 번도 축의 성질이었던 적이 없었다** — 다섯 번 다 E2 공유라는 실행
규약의 산물이었다. 논문이 "폐쇄 상한은 평가 단계에 아예 등장하지 않는다"고 적었던 축이
②의 최대 축이다. 순위는 다섯 축 모두에서 보존됐다(`m5_bundle_rank_reversals` = 0) —
**결론의 순서는 견디고 조달부담의 크기는 견디지 않는다**가 이제 다섯 축에서 확인됐다.

논문 §6.2의 표 11행·산문 두 문단·제목과 §0 대장 3개 키(`m5_bundles_inert` 2→0,
`m5_bundles_headline_inert` 3→0, `m5_bundles_replanned` 4→7), §4 요약 한 항목을 다시 썼다.

### 동시 실행 (기록해 둘 것)

07:00경 **다른 세션이 같은 저장소에서 같은 F26을 돌리고 있었다.** `--replan penalty_none`
·`retire_free`·`carbon_fast`가 내 `ppa_costly`와 겹쳤고, 내 `retire_free`는 실행 중
종료됐다(exit 144). D17이 실증한 바로 그 위험이므로 **경쟁 쓰기를 더 넣지 않고 그쪽
체인이 끝나기를 기다렸다.** 결과적으로 두 세션이 다섯 축을 나눠 재계획한 셈이 됐고,
`REPLAN_MINUTES`는 그쪽이 6묶음을 잰 값(20)이 내 2묶음 값(16)보다 나으므로 그것을 남겼다.
`out/`은 깨지지 않았다 — `run_scenarios.py`의 부분 갱신 로직과 재계획본 보존 로직이
버텼다. **다만 이것은 운이 아니라 확인이 필요한 사실이고, 다음 창은 동시 실행 여부를
먼저 보라.**

### 검증

```
.venv/bin/python scripts/run_scenarios.py                    # 평가 전용 6묶음 갱신
.venv/bin/python scripts/run_scenarios.py --replan carbon_slow   # 16분 3초
.venv/bin/python scripts/run_scenarios.py --replan ppa_costly    # 21분 44초
.venv/bin/python scripts/robustness_section_table.py         # out/m5 재생성
.venv/bin/python scripts/build_tech_guide.py                 # 31 blocks, 140,714 chars
.venv/bin/python scripts/build_guide_page.py && scripts/build_site.py
.venv/bin/python scripts/gate.py                             # gate: OK (pytest 97 passed)
```

수치 출처: 241.8·47.8·41.2·51.9·39.0·99.7·1.4·8.1 = `out/m5/bundle_matrix.csv`(재생성분) ·
네 칸/16칸 = `scripts/robustness_section_table.py:41` × `out/scenarios/summary.csv` ·
CLI 단계 목록 = `src/cap/__main__.py:13` · 재계획 실측 = 실행 로그 6건.

테스트 2개 추가(92 → 97, 다른 세션분 포함). `test_axis_impact_deltas_state_the_cells_they_
are_maximised_over`는 16칸 최대 두 값을 `summary.csv`에서 다시 계산해 **대장 열과 가이드
문장 양쪽**에 대조하고, 생산자의 필터가 바뀌거나 옛 "sixteen" 문장이 되살아나면 실패한다.
`test_guide_never_names_a_pipeline_stage_that_does_not_exist`는 가이드나 생성기가 CLI에
없는 `python -m cap <단계>`를 부르면 실패한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **개선** | 11묶음 중 **다섯이 처음으로 계획 선택까지 다시 풀렸다**. "흔들어 봤다"고 표에 실려 있으면서 흔든 적 없던 행이 0개가 됐다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "그 최대는 무엇에 대한 최대인가"를 물으면 답이 있다. 그리고 답이 두 개다 — 헤드라인 칸과 16칸이 40배 갈린다는 것 자체가 팔 그림이다. 폐쇄 상한이 ②의 최대 축이라는 것은 새 결과다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변. 곁가지 STALE이 9개에서 `scenarios(0h)` 하나로 줄었다(두 세션 합산) |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 400행 + evidence) / SVG / 데이터 패키지 |

### 인계

- **F25의 규칙(생성기 입력 시각 전수 조사)은 소진됐다.** 14경로를 다 봤고 낡은 넷 중 둘
  (`m5`·`scenarios`)을 이번에 없앴다. 남은 둘은 `m8`(3.3h)·`process`(12.5h)다.
  **다음 규칙 후보**: 이번 결함 1·2·5는 전부 *같은 사실을 코드와 산문이 따로 들고 있다가
  갈린* 경우다(필터 상수, 재계획 수, 미착수 묶음 수). **가이드와 논문에서 "몇 개"를 세는
  문장을 전수로 뽑아, 그 수가 코드나 CSV에서 다시 세어지는지 하나씩 확인하라.** 다시
  세어지지 않는 수는 전부 2번과 같은 자리다.
- **동시 실행 확인을 사이클 시작 절차에 넣어라** (`pgrep -f run_scenarios`). 이번엔 버텼다.
- **`out/m8`·`out/process` 재실행**: 남은 두 곁가지. m8은 `frontier_tech_epsilon.py`,
  process는 `process_alternative.py`. **비용을 먼저 재라** — F26이 보인 것은 미루는 판단이
  대개 재본 적 없는 추정 위에 선다는 것이다.
- **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건
  출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13) ·
  **③ 번호 오기**(F13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F27 (08:30) — 게이트가 낡음을 "가장 새 파일"로 재고 있었다: 지울 수 없는 경고와 지워지지 않는 낡음

**먼저 밝힐 것 — 이 창에서 두 세션이 동시에 돌았다.** 같은 저장소·같은 작업트리에
스케줄 태스크가 둘 붙어 있었고, 다른 세션이 08:05에 `autopilot(F26)`으로 **두 세션의
작업이 섞인 트리를 통째로 커밋**했다(내 생성기 셋·테스트, 그쪽의 페이퍼 산문). 내용은
서로 모순되지 않고 게이트도 그린이므로 되돌리지 않았다. 이 절은 그 커밋 **이후** 남은
내 작업이다. 운영상 알아야 할 사실이므로 맨 앞에 적는다 — 시나리오 재계획 중에도
`ppa_costly`를 도는 프로세스와 내가 띄운 `carbon_fast`가 한때 나란히 `summary.csv`를
쓸 뻔했다(07:00에 정리).

**한 일.** F25 인계의 규칙("생성기가 읽는 입력 파일의 시각을 전수로 훑어라")을 실행했고,
그 훑기가 규칙 자체보다 큰 것을 찾았다 — **판정기가 틀렸다.**

`scripts/gate.py::check_sidecars`는 곁가지 디렉터리마다 `_newest`로 시각을 잡았다.
파일 하나만 다시 써도 디렉터리 전체가 최신으로 보인다. 실측된 거짓 그린 둘:

- `out/scenarios`가 "최신"이던 시각에 `disc65` 팔은 base보다 **43시간** 낡아 있었다
  (부분 재계획 중이었다). 게이트는 그린이었다.
- `out/uncertainty`가 "최신"이던 시각에 `decomposition_bands.csv`는 **이틀** 낡아 있었고,
  그 파일은 `g2_band_impact.py:44`를 거쳐 **가이드 §4.5의 밴드 열로 인용**되고 있었다.
  즉 §4.5는 신선한 `_conv` 열과 이틀 낡은 `_band` 열을 한 문장에 섞어 싣고 있었다.

이제 `_oldest_excluding`으로 **가장 낡은 파일**을 보고, 뒤처진 파일을 이름으로 부른다
(`scenarios(18h, disc35/e2/disclosed_skipped.csv)`). 예외는 `e1_local` 하나다 —
`out/e1`을 `copytree`한 사본이고 `copy2`가 원본 mtime을 보존하므로 정의상 원본과 같은
시각이다. 세면 그 묶음이 영원히 `0h STALE`로 뜨고, **재실행으로 지울 수 없는 경고는
읽히지 않는다.**

**곁가지 재실행 — 일곱 사이클의 전제가 틀렸다.** F20 이래 "비싸서 미룬다"였다. 실측:

| 산출물 | 밀린 시간 | 실제 재실행 비용 |
|---|---|---|
| `out/m3`·`out/m4`·`out/g2` | 2–3h | 각 **1초 미만** |
| `out/process` | 12.5h | **10초** |
| `out/seeds` | 18h | **17초** |
| `out/uncertainty` | 3h | **37초** |
| `out/m8` | 3.3h | **7분** |
| `out/scenarios` (재계획) | 43h | **묶음당 12–32분** ← 유일하게 비쌌다 |

일곱 사이클을 미룬 근거였던 "묶음당 약 10분"은 문서 넷에 적혀 있었고 실측은 12–32분이다.
`REPLAN_MINUTES`를 20으로 고치고 여섯 묶음의 실측치를 주석에 남겼다. **제약을 푸는
묶음(`retire_free`)이 가장 비싸다**(32분) — 탐색 공간이 넓어지니 당연하지만, 미루는
판단은 그 사실 없이 단일 낙관치에 기대고 있었다.

**I3-b 자동 해소.** `docs/process_alternative.md`가 인용하던 Nippon Steel GBM ② 165 ·
③ 34,488이 현행 156 · 32,961과 어긋나 있었다(F20 이래 미해결). 10초짜리 재실행이 닫았고,
`docs/uncertainty_propagation.md`의 "인용값 정합 경고"가 스스로 사라졌다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §6.1 변동계수 표 | ② 0.3–0.8% · ③ 1.1–1.8% · ⑤ 3–9% (손글씨) | **② 0.2–0.8% · ③ 1.1–1.9% · ⑤ 2.0–9.2%** — 세 행 전부 틀렸다. 시드 스윕이 18시간 낡아 있었고 다시 뜨자 NSC 행이 전부 바뀌었다 | `docs/seed_stability.csv` (재실행분) |
| 2 | §6.1 단서 | "스윕이 현행보다 낡았다 — NSC가 ② 165.4 · ③ 34,488" | 스윕을 다시 떠서 **어긋남이 사라졌다.** 단서가 거짓이 됐다. 이제 생성기가 고정 시드 행과 `out/e5`를 대조해 **판정 자체를 계산한다** | `docs/seed_stability.csv` × `out/e5` |
| 3 | §4.5 | "밴드를 넣으면 철강 파라미터 몫이 21%→37%(POSCO) · **23%**→36%(NSC)" | NSC의 규약 기준은 **21.4%**였다(23%는 낡음). 그리고 더 중요한 것: **지금 다시 재면 21%→21%, 아무것도 안 움직인다** | `out/g2/f3_compare.csv` |
| 4 | §4.5 결론 | "빠진 밴드는 비용 불확실성의 1/5과 1/3의 차이다" | **더 이상 참이 아니다.** 밴드가 넓고 한쪽으로 치우친 유일한 파라미터 `tech.capex`가 민감도 화면 재실행 뒤 9위→**13위**로 밀려 추첨 상위 10 밖으로 나갔다. 추첨되는 10 중 밴드 보유는 1개뿐이고 그것은 TCaR을 ~0 움직인다. **규약이 무해한 것이 아니라, 규약을 반증할 증거를 가진 자리가 검정 밖으로 나갔다** | `out/sensitivity/ranking.csv` × `out/uncertainty/decomposition_bands.csv` |
| 5 | §6.2 드리프트 표 | 곁가지를 **요약 파일** 시각으로 날짜 짓고 드리프트를 **대조 팔 하나**로만 쟀다 | 부분 재실행이 둘 다 다시 쓴다. 캠페인 도중 `out/scenarios`가 "08-12 06:14 작성 · 드리프트 0"으로 자기를 보고했고 그때 **12개 팔 중 6개가 base보다 낡아** 있었다. 이제 **가장 낡은 팔**로 날짜 짓고 `Arms behind base` 열을 싣는다 | 파일 시각 실측 |
| 6 | §4.3 생성 블록 | "16칸 최대는 B20과 `support=current` 칸에서 온다"(생성 블록 안의 손글씨 인과 주장) | 데이터에서 읽는다 — Mitsui Chemicals · `B20` · `support=current`. 맞는 주장이었으나 계속 맞을 이유가 없었다 | `out/scenarios/summary.csv` |
| 7 | §4.5 `tech.capex` 승수 | (첫 구현에서) [1.54, 3.48]× | **[1.00, 3.48]×.** 밴드가 붙은 기술 행이 둘인데 `field`로 키를 잡아 **마지막 행만** 남았다. 포락선이 주장이다 | `out/g2/bands.csv` |

**생성 블록 둘 신설**(29 → 31): `seed_cv`(§6.1 변동계수 표 + 신선도 판정),
`band_vs_convention`(§4.5 밴드 대 규약 + 검정이 닿지 못하는 이유).
**손글씨 표 하나와 문단 둘이 생성기로 넘어갔다** — 곁가지를 다시 돌릴 때마다 손으로
따라 고쳐야 하는 수치는 생성기의 것이다.

### 검증

```
.venv/bin/python scripts/gate.py     # gate: OK — pytest 97 passed, 곁가지 검사 PASS
```

**`곁가지 out/ vs out/e5`가 F20 이래 처음으로 PASS다.** 실질 검사 8개 전부 PASS
(남은 WARN은 미커밋 항목 하나뿐).

테스트 4개 추가·3개 개정(92 → 97).
- `test_seed_cv_table_is_recomputed_from_the_sweep_not_hand_written` — §6.1 세 행을
  `seed_stability.csv`에서 다시 센다.
- `test_diagnostic_drift_dates_each_sidecar_by_its_oldest_arm` — 요약 파일 시각으로
  되돌아가면 실패한다.
- `test_band_vs_convention_numbers_come_from_the_band_comparison` — 밴드 치환이 0일 때
  "규약은 무해하다"로 읽히게 두면 실패하고, `tech.capex`의 화면 순위를 대조한다.
- `test_guide_never_names_a_pipeline_stage_that_does_not_exist` — 가이드의 재현 지시가
  `cap.__main__.STAGES`에 없는 단계를 부르면 실패한다.
- 개정 3건: `test_gate_sidecar_check_enumerates_out_rather_than_a_hand_written_list`가
  기대값을 `_newest`로 만들고 있어 **테스트가 게이트와 같은 맹점을 공유했다** —
  `_oldest_excluding`으로 바꾸고 뒤처진 파일 이름까지 본다.
  `test_seed_sweep_staleness_is_described_as_it_is`는 어긋나는 회사를 `{NSC}`로 못박고
  있었다(스윕을 다시 뜨자 붉어졌다 — 실패가 아니라 사실이 바뀐 것) → 방향을 못박지
  않고 가이드 주장과 실제를 대조한다. `test_section_6_1_seed_sweep_matches_the_stability_record`는
  어느 파일에도 남지 않은 수치를 대조하고 있어 그 부분을 뺐다.

수치 출처: 변동계수 3행 = `docs/seed_stability.csv` · 21%/21%·[1.00, 3.48]× =
`out/g2/{f3_compare,bands}.csv` · 순위 13/25 = `out/sensitivity/ranking.csv` ·
추첨 10개 목록 = `out/uncertainty/decomposition_bands.csv`(이번에 `params` 열 신설) ·
곁가지 시각 = 파일 mtime 대 `out/e5`.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **개선** | 곁가지 9개가 전부 현행 base 위에 선다. 게이트의 곁가지 검사가 F20 이래 처음 PASS |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "가정 불확실성의 폭을 어떻게 아는가"를 물으면, 이제 §4.5가 **그 검정이 닿지 못하는 자리**를 스스로 말한다. 약한 근거를 같은 문장에서 밝히라는 이 문서의 원칙이 처음으로 *검정의 범위*에까지 적용됐다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 낡음 판정이 파일 단위가 됐고 뒤처진 파일을 이름으로 부른다. MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML(38절 400행) / SVG / 데이터 패키지 |

### 인계

- **동시 실행을 정리하라 (최우선).** 같은 저장소에 스케줄 태스크가 둘 붙어 있다. 이번엔
  운이 좋아 내용이 섞이고도 모순이 없었지만, 07:00에 두 `run_scenarios`가 같은
  `summary.csv`를 쓸 뻔했고 그것은 `out/` 정합을 깨는 사고였다(D17에서 실증된 종류).
  **하나로 줄이거나, `run_scenarios.py`에 잠금을 걸어라.**
- **다음 규칙 후보**: 이번 결함 1·3·5는 전부 *판정 코드가 낙관적 집계(`max`)를 쓴* 것이다.
  `scripts/` 전체에서 신선도·정합을 `max`/`newest`/`any`로 재는 자리를 훑어라 —
  하나라도 통과하면 통과로 치는 검사는 부분 실행 앞에서 전부 거짓 그린이 된다.
- **`reline_cheap` 재계획** (§4.3이 스스로 "lower bound"라고 적는 유일한 축, ~20분).
- **추첨 대상을 밴드 보유로도 열기** (§4.5가 이번에 드러낸 공백): 상위 10 순위만으로
  뽑으면 `tech.capex`처럼 증거가 있는 파라미터가 검정에서 빠진다.
- **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건
  출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F28 (08:34) — 상태 스탬프가 결과 847개 중 2개만 세고 있었다: `git ls-files`는 `.gitignore`를 통과하지 못한다

**한 일.** F27 인계의 규칙("판정 코드가 낙관적 집계 — `max`/`newest`/`any` — 를 쓰는 자리를
전수로 훑어라")을 실행했다. `scripts/` 전체에서 신선도·정합 판정 자리를 훑었고, 두 곳이
걸렸다. 하나는 낙관적 *집계*가 아니라 낙관적 *범위*였고, 그것이 더 컸다.

**결함 1 — 가이드의 상태 스탬프가 결과를 세지 않는다.** `build_tech_guide.state_digest()`는
`git ls-files src data config.yaml out`으로 파일 목록을 만들었다. `.gitignore:31`이
`out/**`를 지운다. 그래서 스탬프가 실제로 센 결과 파일은 **847개 중 2개**였다 —
우연히 추적되던 `out/sensitivity/{ranking,screening}.csv` 둘. 스탬프의 문장은
"Code, inputs, config **and results** (`src`, `data`, `config.yaml`, `out`) hash to …"
였다. **`python -m cap all`을 다시 돌려 가이드가 인용하는 모든 수치가 움직여도 이 스탬프는
그대로였고, `--check`도 스탬프 테스트도 그린이었다.** 가이드가 외부 독자에게 처음 보이는
문장이 "이 문서는 이 상태에 대응한다"이고, 그 문장이 검사하지 않는 것을 검사한다고
적고 있었다.

이제 결과는 디스크에서 열거한다(`out/**`, 숨김 경로 제외). 비용을 먼저 쟀다 —
**847파일 · 397MB · 0.55초**. 일곱 사이클을 미룬 근거가 재본 적 없는 추정이었다는
F27의 교훈을 그대로 적용했다. 덧붙여 가이드가 인용하는 파생 기록 `docs/*.csv` 넷
(`seed_stability`·`parameter_inventory`·`price_process_test`·`validation_backtest`)도
같은 이유로 스탬프 밖이었다 — 범위에 넣었다. 센 파일은 68개에서 **920개**가 됐다.

**결함 2 — `gate.check_freshness`가 `out/`을 가장 새 파일로 쟀다.** F27이 곁가지 검사에서
같은 것을 고쳤고, 이 검사는 그대로 남아 있었다. `out/e5`의 아홉 파일 중 하나만 다시 써도
out/ 전체가 입력보다 새것으로 보인다. 지금 e5는 아홉 파일이 같은 초에 쓰였으므로 거짓
그린은 아직 없었으나, 부분 재실행은 실측된 사건이다(F27이 `out/scenarios`에서 봤다).
검사의 방향은 비대칭이어야 한다 — **어느 입력이라도 새로우면 낡은 것이고, 모든 산출이
새로워야 새것이다.** `_oldest`로 바꾸고 뒤처진 파일을 이름으로 부른다.

**규칙을 다 훑고 남은 것(거짓 양성)**: `calibration.FACTOR_SERIES`의 `any`(생성기 §3.5)는
`calibrate()`의 `if not avail`과 같은 뜻이라 맞다. `gen_frontier_shape`의
`rank.max() == len(p)`는 묶음마다 프런티어 일정이 하나뿐이라(8/8) 현재 `min`과 같은 값을
낸다 — 일정이 둘 이상 오면 갈리므로 인계에 남긴다. `check_sidecars`의
`base = _newest("out/e5")`는 낙관이 아니라 보수 방향이라 그대로 둔다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | 머리말 상태 스탬프 | "Code, inputs, config and results (`src`, `data`, `config.yaml`, `out`) hash to `6c888ea3c1ea`" — **결과가 해시에 들어 있다는 주장** | `out/`은 gitignore 대상이고 목록은 `git ls-files`로 만들어졌다. 847개 중 2개만 셌다. 이제 920파일을 세고 `33d92a014e0b`이며, 무엇을 어떻게 세는지(디스크에서 읽는 이유)를 문장이 밝힌다 | `.gitignore:31` · `git ls-files out` (2줄) · `out/**` 실측 847 |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py   # 31 blocks, 140,835 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current (다이제스트가 자기 자신을 낡게 하지 않는다)
.venv/bin/python scripts/gate.py               # gate: OK
```

수치 출처: 847 = `find out -type f | wc -l` · 2 = `git ls-files out` · 397MB = `du -sh out` ·
0.55초 = sha256 전수 해시 실측 · 920 = `_state_files()` · §9.1 재계산(8/8 · 4 · 5 · 7)은
`out/e5/frontier_points.csv` × `out/e2/plan_index.csv`에서 독립으로 다시 세어 가이드와 일치.

테스트 2개 추가(97 → 99).
- `test_stamp_covers_the_results_it_claims_to_cover` — `out/` 아래 결과가 스탬프 목록에
  전부 들어 있는지 보고, 결과 파일 하나의 **내용**을 바꿔 다이제스트가 움직이는지 확인한
  뒤 내용과 **mtime까지** 되돌린다(안 되돌리면 그 파일이 e5의 최신이 되어 게이트의 곁가지
  검사가 아홉 디렉터리를 전부 STALE로 부른다 — 테스트가 게이트를 붉히면 안 된다).
- `test_freshness_measures_out_by_its_oldest_file_not_its_newest` — e5 파일 하나를 입력보다
  낡게 되돌려 게이트가 붉어지고 그 파일을 이름으로 부르는지 확인한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 192행, 곁가지 검사 PASS |
| ③ 인사이트 — 팔 수 있는 그림인가 | 유지 | §9.1의 8/8 · 4 · 5 · 7을 독립 재계산으로 확인 — Arc가 "프런티어가 무엇의 프런티어인가"를 물으면 답이 데이터에서 다시 세어진다 |
| ④ GitHub·MCP — 도구로 작동하는가 | **개선** | 상태 스탬프가 처음으로 결과를 실제로 센다. freshness 판정이 파일 단위가 됐다. 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **규칙(낙관적 집계·범위 훑기)은 절반 소진.** `scripts/`의 신선도·정합 판정은 다 봤다.
  **남은 절반은 `src/cap/`이다** — 모형 안에서 "하나라도 있으면 있다"로 판정하는 자리
  (`avail`·`applies_to`·`any`), 특히 조용한 탈락과 붙어 있는 것들.
- **`gen_frontier_shape`의 `rank.max()`**: "그 일정은 대리모형의 가장 비싼 계획"이라는
  문장을 묶음당 프런티어 일정이 둘 이상일 때도 참으로 만들려면 `min`이어야 한다.
  지금은 8/8이 단일 일정이라 갈리지 않는다 — 갈리는 날 조용히 틀린다.
- **동시 실행**(F27 최우선 인계, 미해결): 같은 저장소에 스케줄 태스크가 둘. 이번 사이클은
  `pgrep -f run_scenarios`로 시작 시 확인했고 비어 있었다.
- **`reline_cheap` 재계획**(~20분) · **추첨 대상을 밴드 보유로도 열기**(F27) ·
  **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건
  출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F29 (08:52) — 가이드가 "가용 연도는 시나리오마다 다르다"고 적고 있었다: 그 표는 헤더뿐이다

**한 일.** F28 인계의 규칙("낙관적 집계·범위 훑기의 남은 절반은 `src/cap/`")을 실행했다.
`avail`·`applies_to`·`any`·`.max()`를 전수로 봤고, 걸린 것은 낙관적 집계가 아니라
**존재하지 않는 입력이었다.**

**결함 — 시나리오 기술 가용성이 데이터에 없다.** `e1_constraints.py:103`은 D2b에서
`tech_avail_*` 경로를 골라 시나리오별 최초 가용 연도를 만든다. D2b의 변수는 여섯 개
(`elec_price`·`h2_price`·`co2_price`·`coal_price`·`gas_price`·`re_price`)뿐이고
`tech_avail_*`는 **한 줄도 없다.** 그래서 `out/e1/tech_availability.csv`는 헤더 한 줄이다.
E2의 `av.get(tech_id, tk.avail_year)`는 언제나 D3의 정적 `avail_year`로 떨어진다.
C4의 $\max(a^0_k, a^{0,\sigma,\text{region}}_k)$은 코드로는 계산되지만 **데이터가 그 항을
채운 적이 없다.**

가이드 §3.8의 다섯 번째 판정은 "이것은 test 2와 달리 시나리오마다 갈릴 수 있다 —
가용 연도가 시나리오 의존이므로"라고 적고 있었다. 갈릴 수 없다. 그리고 **같은 절의 생성
블록이 "두 시나리오에서 판정이 동일하다"고 적고 있었다** — 한 절이 자기를 부정한 채로
아홉 사이클을 지났다. 생성 블록 쪽 문장은 손으로 박혀 있어(측정된 값이 아니었다) 어긋남을
드러내지 못했다.

E1에 빈 표 경고를 넣었다가 **뺐다**: 출력이 한 글자도 바뀌지 않는 `print` 한 줄이
`src/cap/` mtime을 올려 게이트가 `cap all` 재실행(~20분)을 요구했다. 사이클 안에 못 끝낼
작업은 시작하지 않는다(§6). 사실은 가이드·METHODOLOGY·테스트 세 곳에 남았고 셋 다 검사된다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.8 test 5 | "unlike test 2 this one **can flip between scenarios**, since availability years are scenario-dependent" | 갈릴 수 없다. `out/e1/tech_availability.csv` 0행 — D2b에 `tech_avail_*` 없음. 모든 가용성 판정이 D3의 정적 `avail_year`로 환원된다. D2b 변수 여섯 개를 이름으로 적고 C4의 시나리오 항이 미사용임을 밝힌다 | `data/prepared/D2b_scenario_prices.csv` (6변수) · `out/e1/tech_availability.csv` (1줄) · `e2_milp.py:148` |
| 2 | §3.8 산문 | NSC 킴 2030은 "the scenario availability year E1 derives from D3" 때문 | D3의 `steel_h2dri.avail_year = 2030` + `build_years = 4`. 시나리오가 아니라 **모형 날짜**다 | `data/prepared/D3_tech_options.csv` |
| 3 | §3.8 생성 블록 | "the **two** `high` rows … Verdicts are identical across **both** scenarios." — 손으로 박은 수 | 측정한다: 침묵 탈락 3건 중 `high` 2건, 판정 갈림 0건. 그리고 **같은 이유**(가용성 표 0행)를 그 자리에서 적는다 | `scripts/build_tech_guide.py::gen_d7_enforcement` |
| 4 | §8 (신설 10) | 시나리오가 기술 도입 시점을 앞당긴다는 읽기를 막는 문장이 없었다 | 두 시나리오는 예산과 가격만 다르다. "야심찬 시나리오가 수소환원을 앞당긴다"는 이 모형이 낼 수 없는 그림이다 | 위와 동일 |
| 5 | §8 꼬리 문장 | "Claims **4, 7 and 8** carry their size in the sentence itself (71–73%, 2 of 4 firms, ~8%)" — 손으로 박은 목록·수치. 10번을 더하면 그대로 틀린다 | §8의 번호를 읽고 표가 덮는 것을 빼서 계산한다 → "Claims 4, 7, 8 and 10". 출처 없는 괄호 수치는 지웠다 | `gen_limits()` |

METHODOLOGY C4에도 같은 사실을 넣었다(정본이 가이드와 어긋나면 안 된다).

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 31 blocks, 142,175 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python scripts/gate.py                       # gate: OK — pytest 100 passed, 9항목 PASS
```

수치 출처: D2b 6변수 = `data/prepared/D2b_scenario_prices.csv` · 가용성 표 0행 =
`out/e1/tech_availability.csv` · 2030/4년 = `data/prepared/D3_tech_options.csv` ·
침묵 탈락 3·`high` 2 = `gen_d7_enforcement`의 판정 집합.

테스트 1개 추가(99 → 100).
- `test_availability_is_described_as_scenario_dependent_only_if_it_is` — **양방향이다.**
  표가 비어 있으면 가이드가 D2b 변수 여섯 개를 이름으로 적고 행 수를 세야 하고,
  D2b에 `tech_avail_*`가 생겨 표가 채워지면 "never binds"가 남아 있는 것으로 실패한다.
  덧붙여 §3.8의 2030/2034가 D3의 `avail_year + build_years`와 같은지 대조한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변. 다만 **공백 1건이 새로 이름을 얻었다**: D2b에 기술 가용성 경로가 없다 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | **정직해짐** | 시나리오 축이 기술 *시점*을 움직이지 못한다는 사실이 §8에 수치와 함께 적혔다. 도구의 능력이 준 것이 아니라 광고가 실제와 맞춰졌다 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 "시나리오가 다르면 무엇이 달라지는가"를 물으면 답이 "예산과 가격, 그리고 그것뿐"으로 정확해진다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **수집 대상 1건(새로 이름 얻음)**: D2b의 `tech_avail_*` 경로. NGFS/GCAM 시나리오에서
  기술 가용 연도를 뽑아 넣으면 C4의 시나리오 항이 처음으로 산다. 넣기 전까지 §8-10과
  §3.8 test 5의 문장이 정확하다 — 그리고 새 테스트가 넣는 순간 붉어져 문장을 부른다.
- **`src/cap/` 규칙 훑기 완료.** 남은 거짓 양성: `e2_milp.py:295`의 `has_incumbent = any(...)`
  는 warm-start 유무 판정이라 `any`가 맞다. `e5_metrics.py:229`의 `prof.capex_k.any()`는
  0-CAPEX 계획에서 peak_year를 None으로 두려는 것이라 맞다. `e2_milp.py:354`의
  `ppa_rows.max()`는 D7에 `ppa` 행이 없어 실행된 적이 없다(현재 항상 0.0).
- **mtime 교훈**: 출력이 안 바뀌는 편집이라도 `src/cap/`을 건드리면 게이트가 20분 재실행을
  요구한다. 사이클 안에 못 끝낼 것은 손대지 말고, 손댔다면 `git checkout` 후 **mtime까지**
  되돌려라(`touch -r`). F28이 테스트에서 겪은 것과 같은 함정이다.
- **`gen_frontier_shape`의 `rank.max()`**(F28, 미해결) · **동시 실행 정리**(F27 최우선,
  이번 사이클 시작 시 `pgrep` 비어 있었다) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F30 (02:07) — §7이 D6의 빈칸을 지표 ③의 결함으로 적고 있었다: 그 빈칸은 지표 ⑥에만 난다

**한 일.** F29 인계대로 새 지도가 없어 §7(검증)을 감사 파일과 한 줄씩 대조했다. 걸린 것은
집계가 아니라 **귀속**이다.

**결함 — 부분 채움 컬럼이 잘못된 지표에 붙어 있었다.** §7은 D6의 `revenue`(95.5%)와
`net_debt`(40.9%)를 두고 "metric ③ is built on both … so metric ③ is thinner than metric ①
for exactly the firms that disclose least"라고 적고 있었다. 인용된 두 줄이 스스로를 부정한다 —
`e5_metrics.py:120`은 `capex_total_to_revenue_pct`, `:122`는 `netdebt_to_ebitda_post`, 곧
**지표 ⑥(재무 부담)**이다. 지표 ③(TCaR = P90 − P50)은 D6를 한 줄도 읽지 않는다.
`docs/data_audit.md:27`은 처음부터 "지표 ⑥이 D6에서 읽는 것은 `ebitda`·`revenue`·`net_debt`
셋뿐"이라 적고 있었으므로, 가이드가 자기 감사 파일과 어긋난 채 서 있었다. Arc가 읽는
문장으로는 최악의 방향이다 — **리스크 지표가 미공시 재무데이터 위에 서 있다**는 읽기를
만든다. 실제로는 리스크 지표가 16행 전부 채워져 있고, 비는 것은 감당가능성 쪽이다.

빈칸을 세어 그 자리에 적었다: `out/e5/affordability.csv` 16행 중 **8행에 순부채 배수가 없고**
(POSCO·LOTTE, §3.7 경계 문제), **4행에 EBITDA 비율 자체가 없다**(LOTTE, 기준 EBITDA 음수).
`out/e5/metrics_company.csv`의 `tcar_bnkrw`는 16행 전부 채워져 있다. §3.10·§3.7·§8의 ⑥ 서술과
이제 일관된다(그쪽은 처음부터 맞게 적혀 있었다 — 어긋난 것은 §7뿐이었다).

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §7 감사 문단 | "**metric ③** is built on both: it divides by `revenue` … adds `net_debt` …" | 그 두 줄은 지표 ⑥이다. ③은 D6를 읽지 않는다 | `src/cap/e5_metrics.py:120,122` · `docs/data_audit.md:27` |
| 2 | §7 같은 문단 | "so **metric ③ is thinner than metric ①** for exactly the firms that disclose least" — 결측 수는 없이 | 얇아지는 것은 ⑥이다. 16행 중 순부채 배수 8행·EBITDA 비율 4행 결측, ③은 16/16 완전. 수를 문장에 넣었다 | `out/e5/affordability.csv` · `out/e5/metrics_company.csv` |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 31 blocks
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python scripts/gate.py                       # gate: OK — pytest 101 passed, 9항목 PASS
```

수치 출처: 8·4·16 = `out/e5/affordability.csv`의 `netdebt_to_ebitda_post`·
`capex_peak_to_ebitda` 결측 수 · 16/16 = `out/e5/metrics_company.csv`의 `tcar_bnkrw` ·
95.5·40.9·99.1 = `docs/data_audit.md:19-21` · 줄 번호 120/122 = `src/cap/e5_metrics.py` 실측.

테스트 1개 추가(100 → 101).
- `test_partial_d6_columns_are_attributed_to_the_metric_that_reads_them` — **양방향이다.**
  결측을 데이터에서 세어 §7의 수와 대조하고(수가 바뀌면 붉어진다), ③에 결측이 생기면
  "complete on all 16 rows" 문장으로 붉어지며, 인용된 `e5_metrics.py:120/122`가 실제로
  `revenue_latest_bnkrw`·`net_debt_bnkrw`를 만지는 줄인지 확인한다 — 줄 번호가 밀리면
  가이드의 링크가 낡았다고 말한다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음, 곁가지 검사 PASS |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | "미공시 재무데이터가 리스크 수치를 갉는다"는 잘못된 읽기가 사라졌다. 결측이 어느 지표의 어느 행에 나는지 수로 적혔다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **§7의 나머지 줄 인용은 미검사.** 이번에 검사기를 붙인 것은 `e5_metrics.py:120/122`
  두 줄뿐이다. 가이드 전체에 `#L<n>` 링크가 더 있고, 줄 번호는 코드가 움직이면 조용히
  낡는다 — 다음 사이클의 값싼 표적: 모든 `src/cap/*.py#L<n>` 링크를 한 테스트로 훑어
  가리키는 줄에 그 문장이 말하는 식별자가 있는지 본다.
- **`gen_frontier_shape`의 `rank.max()`**(F28, 미해결) · **D2b `tech_avail_*` 수집**(F29) ·
  **동시 실행 정리**(F27, 이번 사이클 시작 시 `pgrep` 비어 있었다) · **`reline_cheap` 재계획**
  (~20분) · **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F31 (03:56) — 가이드의 줄 인용 36건 중 아무도 검사하지 않았고, 이미 두 건이 밀려 있었다

**한 일.** 사이클 시작 시 작업 트리에 F30 이후의 미커밋 변경 3파일이 남아 있었다(앞선 실행이
커밋 전에 끊긴 것으로 보인다). §2 규칙대로 지도보다 **미완 작업을 먼저 완결**했다 — 내용을
한 줄씩 검증하고, 빌드·게이트를 다시 돌려 통과시킨 뒤 이 사이클의 성과로 커밋했다.

**결함 1 — `_gap`이 두 갈래로 적혀 있었다. 세 갈래다.** §2의 그림 설명은 "interpolates …
only while the disclosed point lies within the frontier's span …, and otherwise **clamps** to the
endpoint"였다. `e5_metrics.py:61-81`의 `_gap`은 구간 위쪽 밖이면 끝점으로 clamp하지만 **아래쪽
밖이면 NaN을 돌려준다** — 코드가 자기 docstring에 "np.interp clamping would fabricate a gap
there"라고 이유까지 적어 둔 의도적 선택이다. 세는 쪽도 같은 결함이었다: clamp만 세면 보간
다리와 NaN 다리가 한 덩어리가 되어, `out/e5/gap.csv`에 **빈칸으로 남은 다리가 "측정된 0"으로
읽힌다.** 이제 세 갈래를 각각 세고(cost 4/0/0, risk 3/1/0), 반대편에서 파일의 빈칸 수(0, 0)와
대조해 같은 사실을 두 번 말한다. §9 용어집은 처음부터 맞게 적고 있었다 — 어긋난 것은 §2뿐.

**결함 2 — 드롭되지 않은 것을 "한 줄 위에서 드롭된다"고 적고 있었다.** §4.3은 니폰스틸 두
기(Yawata·Hirohata)를 두고 "dropped one line earlier, at `e2_milp.py:325`"라 했다. `:325`는
루프 자신의 `if r.item_type == "tech_commit" and r.facility_id in cf.index …` **가드**다. 두 기는
그 블록에 **들어가지도 못하고**, 그래서 `dropped` 목록에 오르지도 않는다. 결론(¥630.2bn 공시가
§6.4에 보이지 않는다)은 같지만 기전이 달랐고, 링크를 따라간 사람은 드롭 코드가 아니라 조건문을
본다. 문장을 기전대로 고쳤다.

**결함 3·4 — 줄 번호 두 건이 조용히 밀려 있었다.** `ccfd=0`은 `e5_metrics.py:200`이 아니라
`:201`, 스케줄 키는 `:186-189`가 아니라 `:187-188`. 코드가 움직이면 줄 인용은 아무 소리 없이
낡는다. F30이 두 건에만 검사기를 붙였고 나머지 **34건은 아무도 보지 않았다.**

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §2 그림 설명 | `_gap`은 "interpolates … and otherwise **clamps** to the endpoint" — 두 갈래 | 세 갈래다. 구간 아래쪽 밖 → **NaN**(외삽하지 않는다). 세 갈래를 각각 세고 파일의 빈칸과 대조한다 | `src/cap/e5_metrics.py:61-81` docstring · `out/e5/gap.csv` |
| 2 | §2 같은 문단 | "**4 of 4** cost legs and **3 of 4** risk legs are clamped" — 나머지가 보간인지 NaN인지 말하지 않음 | cost 4 clamped / 0 interpolated / 0 unmeasurable, risk 3 / 1 / 0. 빈칸 대조 0·0 | `out/e5/frontier_points.csv` · `out/e5/gap.csv` |
| 3 | §4.3 | 니폰스틸 두 기는 "**dropped** one line earlier, at `e2_milp.py:325`" | `:325`는 루프의 `if … facility_id in cf.index` 가드다. 블록에 진입하지 못하는 것이지 드롭되는 것이 아니다 | `src/cap/e2_milp.py:325` |
| 4 | §6.2 | `ccfd=0` 자리 = `e5_metrics.py:200` | `:201` | `src/cap/e5_metrics.py` 실측 |
| 5 | §9 용어집 | 스케줄 키 = `e5_metrics.py:186-189` | `:187-188` | `src/cap/e5_metrics.py` 실측 |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 31 blocks, 143,149 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python scripts/gate.py                       # gate: OK — pytest 103 passed, 8 PASS + git WARN(커밋 전)
```

수치 출처: 다리 집계 4/0/0·3/1/0 = `out/e5/frontier_points.csv`의 프론티어 구간과
공시 좌표 비교 · 빈칸 0·0 = `out/e5/gap.csv`의 `gap_cost_bnkrw`·`gap_risk_bnkrw` 결측 수 ·
줄 번호 61·201·187·325 = `src/cap/*.py` 실측 · 인용 36건 = 가이드 정규식 훑기.

테스트 2개 추가(101 → 103).
- `test_gap_legs_are_counted_in_all_three_of_gap_s_branches` — **양방향이다.** 세 갈래를
  데이터에서 다시 세어 §2의 수와 대조하고(수가 바뀌면 붉어진다), "NaN rather than
  extrapolating"이 사라지면 붉어지며, 센 NaN 수가 파일의 빈칸 수와 같은지 반대편에서 본다.
- `test_every_source_line_citation_in_the_guide_points_at_what_it_claims` — **가이드 전체를
  훑는다.** (1) `file.py:N` 36건이 전부 존재하는 파일의 존재하는 줄인가. (2) 15건의 앵커는
  그 줄에 문장이 이름으로 말하는 식별자(`INC_CAPEX`·`ccfd=0`·`def _gap(`·
  `facility_id in cf.index` 등)가 실제로 있는가. 코드가 밀리면 이제 조용히 낡지 않는다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음, 곁가지 검사 PASS |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | 프론티어 갭이 "0"인지 "잴 수 없음"인지 구분되어 적힌다. 그리고 Arc가 코드 링크를 따라가면 문장이 말한 줄이 나온다 — 문서 신뢰의 값싼 기반 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **앵커 표는 15건뿐이다.** 인용 36건 중 나머지 21건은 "존재하는 줄인가"까지만 검사된다.
  다음 사이클의 값싼 표적: 앵커 표를 채워 각 인용이 무엇을 말하는지 식별자로 못박기.
- **`gen_frontier_shape`의 `rank.max()`**(F28, 미해결) · **D2b `tech_avail_*` 수집**(F29) ·
  **동시 실행 정리**(F27, 이번 사이클 시작 시 `pgrep` 비어 있었다) · **`reline_cheap` 재계획**
  (~20분) · **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).
- **게이트 소요 ~4분**(pytest 226s). 사이클 안에 두 번 돌릴 수 있다.

## F32 (06:13) — 앵커 표가 33건 중 15건만 덮고 있었고, 인용 3건은 정규식이 아예 보지 못했다

**한 일.** F31 인계의 값싼 표적을 끝까지 밀었다. F31이 만든 인용 검사기는 두 겹이었다 —
(1) 모든 `file.py:N`이 존재하는 파일의 존재하는 줄인가, (2) 앵커 표 15건은 그 줄에 문장이
말하는 식별자가 있는가. **(1)은 코드가 밀려도 통과한다.** 파일은 계속 그만큼 길고, 밀린
줄도 여전히 존재한다. 그래서 앵커 없는 인용은 F31 이후에도 조용히 다른 코드를 가리킬 수
있었다 — 검사기가 붙었다는 사실이 오히려 안심을 준 채로.

**결함 1 — 정규식이 쉼표로 이어 붙인 인용의 첫 구간만 읽었다.** 가이드는
`e2_milp.py:36-37,54-55`·`prepare_raw.py:100-107,190,211` 형식을 쓴다. F31의
`([a-z_]+\.py)[:#]L?(\d+)(?:-L?(\d+))?`는 `36-37`과 `100-107`에서 멈춘다. 곧
**`e2_milp.py:54-55`·`prepare_raw.py:190`·`:211` 3건은 존재 검사조차 받지 않고 있었다.**
쉼표 연속 형식을 펼쳐 읽게 고쳤다 — 인용 36건이 아니라 **39건**(고유 시작줄 33건)이다.

**결함 2 — 표가 완비인지 아무도 확인하지 않았다.** 표에 없는 인용이 몇 건인지 세는 곳이
없었으므로, 새 인용이 앵커 없이 들어와도 테스트는 조용했다. 이제 **인용된 시작줄 집합 −
표 = 공집합**을 단언한다. 앵커 없는 인용을 새로 쓰면 그 자리에서 붉어지고, 실패 메시지가
"그 줄이 말하는 식별자를 표에 적어라"라고 말한다. 표를 15건 → **33건(전건)**으로 채웠다.

**전건 실측 결과: 이번 사이클에 밀린 인용은 없다.** 33건 전부 앵커가 그 줄에 있다. F31이
두 건을 손으로 잡은 뒤 코드가 움직이지 않았다는 뜻이고, 이제 다음 이동은 손이 아니라
테스트가 잡는다.

### 가이드에 바뀐 것

산문 한 문단(§7, freshness 문단 뒤). **수치 없음** — 인용 수를 적으면 그 수가 또 낡는다.
"이 문서의 모든 `file.py:N`은 테스트가 두 겹으로 검사한다: 파일이 존재하고 충분히 길며,
그 줄에 문장이 이름으로 말하는 식별자가 아직 있다. 예외 인용은 없다"를 적고, **왜** 그
검사가 있는지를 적었다 — 링크만 밀린 문장은 틀린 문장보다 나쁘다. 따라간 독자가 맞는
문장들까지 믿지 않게 되기 때문이다(F31에서 손으로 두 건 발견).

사실 오류 수정은 이번 사이클 0건이다. 전건 대조에서 어긋난 것이 없었다 — 검사 범위를
15/33에서 33/33으로 넓힌 것이 이번의 성과다.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 31 blocks, 143,887 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python -m pytest tests/test_tech_guide.py -q # 48 passed
.venv/bin/python scripts/gate.py                       # gate: OK
```

수치 출처: 인용 39건·고유 시작줄 33건 = `docs/TECHNICAL_GUIDE.md` 정규식 훑기 ·
앵커 33건의 식별자 = `src/cap/*.py`·`scripts/*.py` 실측(전건 대조 OK).

테스트는 늘리지 않았다(48건 유지) — 기존
`test_every_source_line_citation_in_the_guide_points_at_what_it_claims`를 세 겹으로
넓혔다. 새 겹은 **완비성**이다: 인용은 있는데 표에 없으면 실패.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음, 곁가지 검사 PASS |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 어느 코드 링크를 따라가든 문장이 말한 줄이 나온다. 그리고 §7이 그 사실을 독자에게 말한다 — 문서 신뢰의 값싼 기반 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **인용 검사는 이제 완비다. 다음 값싼 표적은 `docs/*.md`·`web/` 쪽 링크다** — 가이드가
  거는 상대 링크(`../src/cap/...`, `mcp_server.md`, `data_audit.md`)가 존재하는 파일을
  가리키는지는 아무도 보지 않는다. 같은 훑기 한 번이면 된다.
- `prepare_raw.py:57-59`에 **죽은 루프**가 있다 — `for fid in ...: pass`. 제외 사유를
  기록하려던 흔적으로 보이고 아무 일도 하지 않는다. 가이드의 서술에는 영향 없다(제외는
  60-62줄이 한다). 창 밖 코드 수정으로 인계.
- **`gen_frontier_shape`의 `rank.max()`**(F28, 미해결) · **D2b `tech_avail_*` 수집**(F29) ·
  **`reline_cheap` 재계획**(~20분) · **추첨 대상을 밴드 보유로도 열기**(F27) ·
  **EFF 두 사본 불일치**(F22) · **EFF·FIN 확률과정 정량 분해**(F20) ·
  **EFF 철강 CAPEX 2건 출처 부재**(F19) · **`_alt` 행의 용도 미실현**(F19) ·
  **감사기의 이름 매칭 한계**(F18) · **§7이 드러낸 실질 공백 2건** ·
  **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F33 (08:00) — §3이 세는 "입력 9개"는 준비 디렉터리의 10개가 아니었고, 열 번째 파일은 어디에서도 검사되지 않는다

**한 일.** F32 인계의 값싼 표적(가이드가 거는 상대 링크 전수 확인)부터 시작했다. 링크
25건 — `data_gap_registry.md`·`../METHODOLOGY.md`·`figures/frontier_gap.svg`·
`../src/cap/e5_metrics.py#L377` 등 — 은 **전부 존재한다**. 결함 0건이었다. 그래서 같은
훑기를 한 칸 넓혀 **가이드가 백틱으로 부르는 파일 경로 80건**을 대조했고, 거기서 이번
사이클의 실물이 나왔다.

**결함 — `data/prepared/`에는 파일이 10개인데 `SCHEMAS`에는 9개뿐이고, 가이드의 모든 셈이
그 9 위에서 이루어지면서 그 사실을 말하지 않았다.** 빠진 것은 `D3b_tech_bands.csv`다.
§3.4는 그 파일을 입력처럼 서술하고 밴드 표까지 생성한다. 그런데 그것은 `SCHEMAS`의 키가
아니므로:

- `load_input`이 이름으로 거부한다(`schemas.py:66-67`) — 적재 시 열 검사도 숫자 검사도 없다.
- 감사기는 `for name in sorted(S.SCHEMAS)`로 돈다(`audit_data.py:146`) — **8개 열이 세지지도,
  `source_id`가 확인되지도 않는다.** 파일이 사라져도 `MISSING INPUT`이 아니다.
- 공개 패키지 빌더의 전달 목록은 D2a–D7뿐이다(`build_data_package.py:160-162`) — D3b는
  집계로 대체되는 것이 아니라 **그냥 없다**.
- 준비 단계에서도 선택적이다: `data/raw/tech_bands.csv`가 없으면 `prepare_raw.py:423`이
  로그만 남기고 밴드 없이 진행한다.
- `src/cap/` 어디도 읽지 않는다. 소비자는 `scripts/`의 밴드 진단 두 개
  (`uncertainty_propagation.py:189`, `g2_band_impact.py:33`)와 가이드 생성기뿐이다.

그래서 §3 머리글의 "9 files", §3.10의 "two of the nine input files", §7의 "88 columns across
the 9 input files"는 **전부 맞는 수인데 무엇 위의 수인지가 없었다.** §3.4를 읽고 파일을
세어 본 독자는 10을 얻고, 그 차이가 어디서 오는지 문서에서 찾지 못한다. Arc가 데이터셋
정의를 자세히 묻고 있는 창에서 이것은 셈의 오류가 아니라 **경계의 누락**이다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3 머리글 (인벤토리 표 뒤) | 준비 입력이 9개인 것처럼 읽힌다 — 열 번째 파일에 대한 언급 없음 | `data/prepared/`는 10개다. `D3b_tech_bands.csv`는 스키마 밖이라 적재 검사·감사·패키지 어디에도 들어가지 않고, 생성 자체가 선택적이다 | `src/cap/schemas.py:14` (9키) · `ls data/prepared` (10파일) · `scripts/prepare_raw.py:423` |
| 2 | §3.4 밴드 절 | D3b를 다른 입력과 같은 지위로 서술 | 스키마 밖 유일 파일임을 첫 문단에서 밝힌다 | `src/cap/schemas.py:66-67` |
| 3 | §3.10 | "Two of the **nine input files** are replaced by aggregates" — 9의 근거 없음, D3b의 행방 없음 | "nine **schema-enforced** input files". 열 번째는 대체도 탑재도 아닌 **부재** — 밴드는 패키지가 아니라 저장소에서만 재현된다 | `scripts/build_data_package.py:160-162` · `ls data/package` (D3b 없음) |
| 4 | §7 감사 문단 | "88 columns across the 9 input files" — 감사 범위가 스키마임을 말하지 않음 | 감사는 `SCHEMAS`를 돈다. D3b의 8개 열은 세지도 `source_id`를 보지도 않으며, 소실이 `MISSING INPUT`이 아니다 | `scripts/audit_data.py:146` · `head -1 data/prepared/D3b_tech_bands.csv` (8열) |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 31 blocks, 145,294 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python -m pytest tests/test_tech_guide.py -q # 49 passed
.venv/bin/python scripts/gate.py                       # gate: OK
```

수치 출처: 파일 10 vs 스키마 9 = `data/prepared/` 목록과 `src/cap/schemas.py`의 `SCHEMAS`
키 · D3b 8열 = `data/prepared/D3b_tech_bands.csv` 헤더 · 패키지 부재 = `data/package/` 목록 ·
링크 25건 = `docs/TECHNICAL_GUIDE.md` 정규식 훑기 후 `test -e` 전수(전건 존재).

테스트 1개 추가(48 → 49).
`test_every_prepared_file_outside_schemas_is_declared_as_outside` — **양방향이다.**
`data/prepared/*.csv` − `SCHEMAS`를 계산해 스키마 밖 파일이 있으면 가이드가 그 파일을
이름으로 부르는지 보고, 그 "감사 밖·패키지 밖"이 지금도 사실인지 `docs/data_audit.md`와
`data/package/`에서 반대편으로 확인한다. 스키마가 늘거나 줄면 §3.10·§7의 입력 파일 수가
같이 낡으므로 그 수도 `len(SCHEMAS)`와 대조한다. 준비 파일이 전부 스키마 안으로 들어오면
이번에 쓴 문단이 남아 있는 것 자체가 실패다.

F32의 인용 완비성 검사가 곧바로 값을 했다 — 새로 쓴 `prepare_raw.py:423`이 앵커 표에 없다고
붉어졌다. 앵커(`"tech_bands.csv 없음"`)를 추가했다. 인용 34건 전건 앵커 유지.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | **개선** | 감사 범위가 준비 디렉터리 전체가 아니라 `SCHEMAS`라는 사실이 문서에 처음 적혔다. `audit` 자체는 `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 데이터셋 경계를 물으면 "9개"의 근거와 열 번째 파일의 지위가 같은 절에 있다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 (패키지가 D3b를 담지 않는다는 사실이 이제 명시된다) |

### 인계

- **`test_generated_blocks_are_current`가 빌드 직후 한 번 붉어졌다가 재실행하면 통과한다.**
  스탬프 다이제스트가 `out/`을 디스크에서 읽는데(`build_tech_guide.py:101,109`) 생성기 자신이
  모델 코드를 실행하며 `out/`을 건드리는 것으로 보인다 — 즉 **스탬프가 한 실행 뒤처진다.**
  이번 사이클 시작 시 게이트의 `1 failed`도 같은 것으로 보인다(HEAD 상태에서 그 테스트를
  단독 실행하면 통과). 다음 사이클의 값싼 표적: 다이제스트가 자기 실행의 부산물을 세지
  않게 하거나, 생성기가 `out/`에 쓰지 않게 하기.
- **상대 링크는 전건 존재하지만 아무도 검사하지 않는다** — 이번에는 손으로 훑었다.
  `test_*`로 굳히려면 25건 `test -e` 한 줄이면 된다. (이번 사이클은 D3b 쪽이 실물이라 그쪽을
  택했다.)
- **`gen_frontier_shape`의 `rank.max()`**(F28, 미해결) · **D2b `tech_avail_*` 수집**(F29) ·
  **`prepare_raw.py:57-59`의 죽은 루프**(F32) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F34 (09:45) — §4.4의 A-id 색인은 손으로 적혀 있었고, *large*로 등급된 가정 두 개가 어느 색인에도 없었다

**한 일.** 작업 트리에 F33 이후의 **미완 작업**이 있었다 — `docs/TECHNICAL_GUIDE.md`의 빈
`<!-- GEN:ledger_map -->` 블록과 `scripts/build_tech_guide.py`의 초안 생성기가 커밋되지
않은 채였고, 빌드도 테스트도 붙지 않았다. 지도보다 이것을 먼저 완결했다.

**결함 — §4.4의 A-id 색인은 손으로 적힌 문장이었고, 표가 자라는 동안 그대로 남아 낡았다.**
§4.1이 5행에서 8행으로 자라는 사이 색인 문장은 다섯 개만 셌다. 그래서:

- **A-05**(수소 외부 조달, *Large* — 계획별 비용 분산의 34–100%)와 **A-07**(경매 비중,
  ③를 99.7% 움직이는 축)이 **어느 색인에도 없었다.**
- **A-19**는 §4.2로 안내되었으나 그 표에 있던 적이 없다 — §4.1에 있다.

그러면서 §4 머리글은 "Every A-id in that ledger appears somewhere in this guide"라고 적고
있었다. Arc가 가정 목록을 따라 읽으면 *large*로 등급된 가정 두 개가 색인에서 사라지고,
하나는 없는 곳을 가리킨다.

**초안 생성기 자체의 결함 2건도 고쳤다.**

1. `missing = ledger - tabled - minor`는 **항상 공집합**이었다 — 바로 위에서
   `minor = ledger - tabled`로 계산했기 때문이다. 즉 "원장에 있는데 어느 색인에도 없다"는
   경고 가지가 죽어 있었고, 더 나쁘게는 **METHODOLOGY에 새 id가 들어오고 가이드가 그것을
   한 번도 쓰지 않으면 그 id가 조용히 `minor`로 흡수되어** 색인이 "위 문단에 있다"고
   거짓을 말했을 것이다. 손으로 적은 색인이 낡은 것과 정확히 같은 결함을 새 자리에 옮겨
   심는 셈이다. 고침: `minor`를 **§4.4 첫 문단이 실제로 이름으로 부르는 id**에서 읽는다
   (`build_tech_guide.py:1932-1933`). 그러면 `missing`이 살아나고, 그때는 색인이 주장 대신
   결함을 인쇄한다.
2. `"{ids} are the three above"`에 **3이 하드코딩**되어 있었다 — 부차 id가 넷이 되는 순간
   낡는 수다. `len(minor)`로 바꿨다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §4.4 색인 문장 | "**A-02, A-03, A-13, A-17, A-24**는 §4.1" — 다섯 개 | §4.1 표는 **8행**이다. **A-05·A-07**이 빠져 있었다 | `docs/TECHNICAL_GUIDE.md` §4.1 표 행 8건 실측 |
| 2 | §4.4 색인 문장 | "**A-19**는 §4.2" | A-19는 **§4.1**에 있다. §4.2 표에 나온 적이 없다 | `docs/TECHNICAL_GUIDE.md:850`(§4.1 행) |
| 3 | §4 머리글 | "map is generated from the tables rather than typed" — 실제로는 생성물이 아니었고(블록이 비어 있었다), 부차 id는 표에서 나오지도 않는다 | 표에서 읽는 부분과 §4.4 첫 문단에서 읽는 부분을 구분해 적었다. 그리고 색인이 낡으면 주장 대신 결함을 인쇄한다고 밝혔다 | `scripts/build_tech_guide.py:1940-1958` |

생성된 색인은 이제 **원장 24개 전건을 배치한다**: §4.1 8개 · §4.2 13개 · §4.4 문단 3개.

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 32 blocks, 146,359 chars
.venv/bin/python -m pytest tests/ -q                   # 105 passed (test_tech_guide 50)
.venv/bin/python scripts/gate.py                       # gate: OK (9항목, 231초)
```

수치 출처: §4.1·§4.2 표 행 = `docs/TECHNICAL_GUIDE.md` 정규식 훑기(각 8·13행) ·
원장 24개 = `METHODOLOGY.md`의 `A-\d\d` 고유값 · 부차 3개 = §4.4 첫 문단이 이름으로 부르는
id · §3에 다시 나오는 16개 = §3 본문 훑기.

테스트 1개 추가(49 → 50).
`test_the_ledger_map_places_every_methodology_identifier_in_the_right_section` —
**생성기의 파서를 빌려 쓰지 않는다.** 표·§4.4 문단·원장을 테스트가 따로 읽어 합집합이
원장과 정확히 일치하는지 보고(빠짐도 잉여도 실패), 표와 문단이 겹치지 않는지 보고, 그
다음 생성된 블록이 그 사실을 그대로 말하는지 본다. 파서가 틀리면 두 결과가 어긋나므로
붉어진다. 색인이 자기 결함을 인쇄한 상태(`in no index here`)로 커밋되는 것도 실패다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit 줄 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc의 "주요 가정이 무엇인가"에 대해 원장 24개가 전건 배치된다. 특히 *large* 두 개(A-05·A-07)가 색인에 처음 들어왔다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 항목 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **게이트의 900초 타임아웃이 이 머신에서 실제로 발동한다 — 코드 결함이 아니라 부하 때문에.**
  이번 사이클 게이트는 `[FAIL] pytest tests/ TimeoutExpired ... after 900 seconds`로 한 번
  붉었다. 원인은 테스트가 아니라 머신이다: 그 시각 **load average 146**, 다른 앱의 프로세스
  795개(Codex 렌더러 34, Dia 26, dart-mcp 20), **여유 메모리 페이지 1,944개(≈30MB)**였다.
  그 상태에서 pytest는 22분 동안 CPU를 1분 46초밖에 쓰지 못했다 — 계산이 아니라 페이징을
  기다린 것이다. 파일별로 나눠 8개 전건 통과를 먼저 확인했고(`test_pipeline.py` 15건이
  274초로 가장 무겁다), 부하가 5로 내려간 뒤 게이트를 다시 돌리자 **같은 스위트가 231초에
  105건 통과**했다. **다음 사이클이 같은 실패를 보면 코드를 의심하기 전에 `uptime`과
  `vm_stat`을 먼저 볼 것.** 게이트의 900초는 이 머신이 한가할 때의 4배 여유이므로 값 자체는
  타당하다.
- **F33이 보고한 스탬프 한 실행 지연은 이번에 재현되지 않았다** — 빌드 직후
  `test_generated_blocks_are_current` 포함 50건이 한 번에 통과했다. 조건부 재현으로 보이며
  다음에 다시 붉어지면 그때 조건을 적을 것.
- **상대 링크 25건 전수 `test -e`**(F33 인계, 여전히 손 검사만) ·
  **`gen_frontier_shape`의 `rank.max()`**(F28) · **D2b `tech_avail_*` 수집**(F29) ·
  **`prepare_raw.py:57-59`의 죽은 루프**(F32) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F35 (02:20) — §5가 인쇄한 유상할당 램프는 `config.yaml`의 것이었고, 모델은 그 4년을 쓰지 않는다

**한 일.** 시작 시 게이트 `gate: OK`, 작업 트리 깨끗, 미완 작업 없음. 지도(§5-C)는 F16에서
끝나 있으므로 우선순위 규칙에 따라 인계 목록에서 골랐다. 먼저 F33·F34가 두 번 미룬
**상대 링크 전수 검사**를 손으로 마쳤다 — 24개 고유 링크 전건 존재, `.py#L` 형식 4건은
F32의 인용 검사가 이미 덮고 있다. 결함 없음. 그래서 검사만 남기는 대신, 아직 아무도 보지
않은 §5(Configuration)를 데이터·코드와 대조했고 거기서 결함이 나왔다.

**결함 — §5의 A-07 램프는 모델이 적용하는 일정이 아니라 `config.yaml`이 적어 둔 램프였다.**

`plancost.auction_share`는 config 램프를 보간한 **뒤 D5의 확정 할당 창에서 덮어쓴다**
(`plancost.py:45-61`). D5의 유일한 `auction_share` 행은 2026–2030 15%이므로 실효 일정은
2025 10% · **2026–2030 15%** · 2035 30% · 2040 50% · 2045 75% · 2050 100%다. 그런데 §5는
config 앵커만 인쇄해 그 사이를 보간하게 만들었다 — 즉 Arc가 §5에서 2027년을 읽으면 **12%**를
얻지만 모델은 **15%**를 쓴다. 네 해(2026–2029)가 어긋났다.

작은 어긋남이 아니다. **A-07은 §4.3이 재계획까지 해서 잰 축 중 ③를 99.7% 움직이는
가장 큰 축이다.** 그리고 §3.6("2026–2029의 보간 0.11–0.14를 평평한 0.15로 올린다")과
§4.1의 A-07 행("confirmed K-ETS Phase 4 allocation plan (15% non-power, 2026–2030)")은
**옳게 적혀 있었다.** 가이드가 자기 자신과 어긋나 있었고, 어긋난 쪽은 하필 Arc가 "무슨
설정으로 돌렸나"를 보러 오는 절이다.

**두 번째 결함, 같은 문장.** "everything after 2030 is an assumption"은 2030 이전이 전부
확정이라는 뜻이 된다. 확정 창은 2026–2030뿐이고 **2025는 그 밖**이다 — D5에 2025 행이
없어 config의 10%가 그대로 쓰인다. 확정과 가정의 경계가 앞쪽 끝에서 틀려 있었다.

**고침.** 생성기가 `plancost.auction_share`와 `_d5_auction_windows`를 직접 불러 **실효
일정**을 인쇄한다(`build_tech_guide.py:695-740`). 확정 구간은 D5의 창에서 읽어 표시하고,
확정 밖 config 앵커는 이름으로 불러 가정이라 적고, 덮여 사라지는 config 값(2026–2029의
11–14%)이 모델에 닿지 않는다는 사실을 같은 문단에서 밝힌다. 새 함수를 만들지 않고 모델이
쓰는 함수를 그대로 부르므로 다음에 D5가 바뀌면 문서가 따라 움직인다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §5 A-07 램프 | "2025 10% → 2030 15% → …" — 보간하면 2026–2029가 **11·12·13·14%** | 모델의 실효값은 그 네 해 모두 **15%**. D5가 config를 덮는다 | `data/prepared/D5_policy_support.csv` 2행(2026–2030, 15%) · `src/cap/plancost.py:59-61` · `auction_share()` 실행값 |
| 2 | §5 같은 문장 | "everything after 2030 is an assumption" — 2030 이전은 확정이라는 뜻 | 확정은 **2026–2030뿐**. **2025의 10%는 가정**이고 D5에 2025 행이 없다 | `data/prepared/D5_policy_support.csv`에 `auction_share`의 `valid_from`은 2026 |
| 3 | §5 문단 전체 | 무엇이 config이고 무엇이 모델의 값인지 구분 없음 | 두 값이 갈라진다는 사실 자체를 §3.6과 같은 말로 적었다 | — |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 32 blocks, 146,790 chars
.venv/bin/python scripts/build_tech_guide.py --check   # generated blocks current
.venv/bin/python scripts/gate.py                       # gate: OK (106 passed, 226초)
```

수치 출처: 실효 유상할당 = `src/cap/plancost.py`의 `auction_share(2025..2050, cfg)` 실행값 ·
확정 창 = `data/prepared/D5_policy_support.csv`의 `auction_share` 행 ·
config 램프 = `config.yaml:65-70` · A-07의 99.7% = `docs/TECHNICAL_GUIDE.md` §4.3 표(F-이전 사이클
검증됨).

**함께 대조하고 결함이 없던 것들**(다음 사이클이 다시 세지 않도록 적어 둔다):
상대 링크 24건 전건 존재 · D1b 69행 = 23시설 × 2022–2024 · `energy_naphtha` 69행 전건 공란 ·
`emissions_s2` 63/69 비영 · BF 17기 · 재라이닝 도래 8/17(용량 48.9%)·12/17(73.8%) ·
`re_price` 한국 175,000 / 일본 198,000 평평 · D2b 결측 2건(한국 `h2_price` 2025)뿐 ·
일본 2025 h2 10,239.6 ≈ 10,240 · D5 7행 구성 · 능력 추정 승수 5,480,000/6,000 = 913.3 ·
§5 표의 나머지 15행 = `config.yaml` 정본.

테스트 1개 추가(50 → 51).
`test_section_5_auction_ramp_is_the_schedule_the_model_applies` — **생성기의 계산을 빌리지
않는다.** 블록에 인쇄된 일정을 문자열에서 다시 읽어 연도별로 펼치고, `auction_share()`가
실제로 내는 26개 값과 전건 대조한다(어긋난 해를 인쇄·실제와 함께 보고). 확정이라 이름 붙인
해의 집합이 D5의 창과 다르면 실패하고, 확정 밖 config 앵커를 가정이라 부르지 않아도 실패한다.
되돌림 검사로 옛 문장을 넣어 보니 `(2026, 0.11, 0.15) … (2029, 0.14, 0.15)`로 붉어진다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | "무슨 설정으로 돌렸나"에 대해 §5가 이제 **모델이 쓰는 값**을 말한다. ③의 최대 축이 4년치 틀린 채 나가지 않는다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **`docs/parameter_inventory.csv`가 2025년 유상할당 10%를 T1로 등급하고 출처를
  `KETS_P4_CONFIRM_2025`로 적는다** — 그런데 Phase 4의 확정 창은 2026–2030이고 2025는 그
  밖이다(이번 사이클에서 D5로 확인). 그 두 행이 §4.5가 "the two banded T1 rows are the 2025
  and 2030 K-ETS auction shares"라고 말하는 근거이므로, 인벤토리가 틀렸다면 §4.5도 함께
  틀린다. **가이드는 인벤토리에 충실하므로 이번에는 건드리지 않았다** — 다음 사이클이
  인벤토리 쪽을 확인할 것(2025년 값이 Phase 3 확정이라면 `source_id`가 틀린 것이고,
  아니라면 등급이 T5여야 한다). 창 밖(코드/데이터) 항목이라 여기 남긴다.
- **상대 링크 24건은 이번에 전수 확인했고 전건 존재한다** — F33·F34 인계에서 내린다.
  `test -e`로 굳히지 않은 이유: 결함이 없어 붉어질 일이 없고, 링크가 깨지면 `.py` 인용은
  F32의 검사가, 문서 링크는 사람이 곧 본다. 값싼 항목이 필요하면 다음 사이클이 한 줄로 넣으면 된다.
- **`gen_frontier_shape`의 `rank.max()`**(F28) · **D2b `tech_avail_*` 수집**(F29) ·
  **`prepare_raw.py:57-59`의 죽은 루프**(F32) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F36 (03:05) — §5의 "Frontier grid 10 points"는 경계의 크기가 아니었고, 그 주석은 §6.3의 다른 진단 이름이었다

**한 일.** 시작 시 작업 트리에 F35의 미완 작업(생성기·테스트·블록)이 커밋되지 않은 채
있어 §1.3에 따라 그것을 먼저 검증했다 — 빌드 재현, `test_tech_guide` 51건 통과, 코드
(`plancost.py:45-61`)로 문장 재확인. 그 사이 **F35 사이클이 병렬로 살아 있었고**
02:25에 스스로 커밋·푸쉬·로그를 마쳤다(`382172d`). 중복 작업을 버리고 이번을 F36으로
잡았다. 지도(§5-C)는 F16에서 끝나므로 우선순위 규칙에 따라 F35가 열어 둔 §5를 이어
읽었고, 표의 나머지 행에서 결함이 나왔다.

**결함 — `milp.frontier_points`는 경계의 점 개수가 아닌데 §5는 "Frontier grid | 10 points"라
적었다.**

그 설정이 실제로 하는 일은 `e2_milp.py:388`에 있다: E2가 최소위험 해와 최소비용 해 사이에
**위험** 목표를 10개 벌려 계획을 열거한다. 그 다음 두 번의 축소가 일어난다.

1. **중복 제거.** firm × scenario당 실제로 남는 계획은 10개가 아니라 **3–9개**다
   (`out/e2/plan_index.csv`: POSCO NZ15 3 … MCI B20 9). 8묶음 합계 48.
2. **E5의 재구성.** §6이 보고하는 경계는 E2의 계획 위에 서지 않는다 — E5가 계약 차원을
   버리고 13개 기술 일정 × 10개 계약 변형으로 후보를 다시 만든다(§6.3에 이미 적혀 있다).
   그 결과 묶음당 후보 10–31개, **비지배점 2–8개**(`out/e5/frontier_points.csv`, 16묶음 76점).

즉 §5를 읽은 Arc는 "경계가 10점"이라고 믿게 되는데 어느 묶음도 10점이 아니다. 가장 많은
묶음이 8점이다.

**두 번째 결함, 같은 행의 주석.** "ε-constraint sweep"은 §6.3에서 **다른 것**의 이름이다 —
`scripts/frontier_tech_epsilon.py`가 **누적배출**에 32개 상한을 거는 별도 진단이고
파이프라인 밖이며 `out/m8`에 쓴다. §5의 10점은 **위험** 축이고 E2 안에 있다. 같은 문서가
같은 말로 축도 파일도 다른 두 가지를 가리키고 있었다.

**고침.** 행을 `_frontier_grid_row()`가 생성한다(`build_tech_guide.py:742-769`). 이름을
"E2 enumeration grid"로 바꾸고, 값에 "per firm × scenario"를 붙여 전체 개수가 아님을
밝히고, 축이 위험임을 명시해 §6.3과 구별하고, 두 축소를 `out/e2/plan_index.csv`와
`out/e5/frontier_points.csv`에서 읽어 같은 칸에 적는다. 손으로 적은 수는 없다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §5 표 행 이름 | "**Frontier grid**" — 경계의 크기로 읽힌다 | E2의 **계획 열거** 격자다. §6의 경계는 E5가 다시 만든 후보에서 고른 **2–8점/묶음** | `src/cap/e2_milp.py:388` · `out/e5/frontier_points.csv`(`on_frontier`, 16묶음 76점) |
| 2 | §5 표 행 값 | "10 points" — 10개가 존재한다는 뜻 | 10은 **시도 횟수의 상한**이다. 중복 제거 뒤 firm × scenario당 **3–9개**만 남는다 | `out/e2/plan_index.csv` 48행, 8묶음 groupby |
| 3 | §5 표 행 주석 | "ε-constraint sweep" — §6.3이 **누적배출 32상한**의 별도 진단에 쓰는 이름 | §5의 것은 **위험** 축이고 E2 안이다. 축과 파일을 같은 칸에서 구별했다 | `docs/TECHNICAL_GUIDE.md` §6.3 · `scripts/frontier_tech_epsilon.py` → `out/m8/summary.csv` |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 32 blocks, 146,983 chars
.venv/bin/python -m pytest tests/test_tech_guide.py -q # 52 passed
.venv/bin/python scripts/gate.py                       # gate: OK
```

수치 출처: 격자 10 = `config.yaml:33`(`milp.frontier_points`) ·
중복 제거 후 3–9 = `out/e2/plan_index.csv`를 `(company_id, scenario)`로 groupby ·
비지배 2–8 = `out/e5/frontier_points.csv`의 `on_frontier`를
`(company_id, scenario, support)`로 groupby(16묶음, 76점) · 후보 10–31 = 같은 파일 전체(268행).

테스트 1개 추가(51 → 52).
`test_section_5_does_not_call_the_e2_grid_the_size_of_the_frontier` — 생성기의 계산을 빌리지
않는다. 인쇄된 두 범위를 문자열에서 다시 읽어 두 CSV에서 따로 센 값과 대조하고, 격자 크기가
config와 같은지, 축("risk")을 말하는지 본다. 되돌림 검사: 옛 행 `| Frontier grid | 10 points |
ε-constraint sweep |`은 행 탐색(`ε points`)에서 걸려 붉어지고, 비지배 범위를 2–7로 한 자리
흔들면 대조에서 붉어진다.

**함께 대조하고 결함이 없던 것들**: §-참조 183건 전건이 실재 절을 가리킨다(32개 고유,
dangling 0) · §6.2의 "TCaR 41–48%"·"석화 ② 71–73%" = `docs/process_alternative.md` §2·§3 표 ·
"thirteen perturbations" = m5 11묶음 + `ou`·`gbm_median` 2 · `rank_preserved = 1` 11/11 =
`out/m5/bundle_matrix.csv` · §6.4의 NSC gap 1,255/4,651 = `out/e5/gap.csv`(1254.55/4651.15) ·
"disclosed_skipped.csv는 4행, NSC 없음" = 실측 4행(LOTTE×2, POSCO×2) · §3.8의 12행 분해
(target 3 / tech_commit 7 / timing 2, `high` 7 / `mid` 5, 강제 2 · 사유기록 2 · 무흔적 3) ·
§2의 "2% MIP gap, 60초" = `config.yaml:38,42` · NSC 기미쓰 +8년 클램프가 §3.8에 이미 적혀 있다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | approach 질문의 핵심 산출물인 "경계"의 크기가 §5에서 처음으로 실제 값이다. §5와 §6.3이 같은 말을 다른 뜻으로 쓰던 충돌도 없어졌다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **§4.5의 "the two banded T1 rows … where the statute itself states a range"는 틀렸다 —
  이번 사이클에 근거까지 모아 두었으니 다음 사이클이 한 번에 고칠 수 있다.** 두 행의 밴드는
  `FIN.config.auction_share.2025` 0.1 → [0.06, 0.14], `.2030` 0.15 → [0.09, 0.21]로
  **양쪽 정확히 ±40%의 대칭 관행**이다(`docs/parameter_inventory.csv:2-3`). 법령이 말한
  범위가 아니다 — 인용된 `KETS_P4_CONFIRM_2025`는 발전외 **15%라는 점값**을 말한다
  (`data/raw/source_register.csv:17`). 게다가 그 출처의 `reporting_start`는 **2026-01-01**이라
  2025년 행은 출처 자신의 기간 **밖**이다(F35 인계가 지목한 것과 같은 행). 고치면 §4.5의
  결론("the width in this model is mostly convention rather than evidence")이 오히려
  **강해진다** — 관행이 아니라고 남겨 둔 마지막 두 행마저 관행이기 때문이다. 시간이 모자라
  이번에 손대지 않았다(게이트 재실행 비용). 인벤토리 쪽 등급·`source_id` 수정은 창 밖.
- **게이트 전체는 10분을 넘겼지만 pytest는 아니었다** — pytest는 226초(107건)로 정상이고,
  나머지 8항목이 나머지를 썼다. F34가 본 것은 pytest 자체가 페이징으로 막힌 경우였으므로
  **증상이 다르다**: 다음에 게이트가 느리면 `[PASS] pytest` 줄의 초 수를 먼저 볼 것.
  226초면 원인은 pytest 밖이다. 게이트를 배경으로 돌리고 그 사이 다른 검증을 진행하면 된다.
- **동시 실행 주의.** 이 창에서 F35와 F36이 겹쳐 돌았다. 시작 시 `git log`로 마지막 커밋을
  먼저 보고 `ITERATION_LOG.md` 꼬리와 대조할 것 — 작업 트리가 더러운 것이 "미완"이 아니라
  "다른 창이 작업 중"일 수 있다.
- **`gen_frontier_shape`의 `rank.max()`**(F28) · **D2b `tech_avail_*` 수집**(F29) ·
  **`prepare_raw.py:57-59`의 죽은 루프**(F32) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).

## F37 (06:18) — §4.5가 "법령이 범위를 말한 곳"이라 부른 T1 두 행은 나머지와 같은 ±40% 관행이었다

**이 절은 F38이 커밋 `7134afb`에서 복원해 적는다.** F37 사이클은 코드·가이드·테스트·웹을
커밋하고 푸쉬했으나 `ITERATION_LOG.md`에 절을 남기지 않았다(커밋 파일 목록에 이 파일이 없다).
사실 관계는 커밋 메시지와 그 diff에서 확인한 것만 적는다.

**결함.** §4.5는 밴드를 가진 T1 두 행을 "증거가 폭을 말한" 반례로 들었다 — "the 2025 and 2030
K-ETS auction shares, where the statute itself states a range". 세 가지가 틀렸다.

1. 두 밴드는 중심값의 정확히 ±40%이고(0.1 → [0.06, 0.14], 0.15 → [0.09, 0.21]), 같은 램프의
   post-2030 T5 네 행이 쓰는 규칙과 같다. 증거에서 읽은 폭이 아니라 대칭 관행이다.
2. 인용된 `KETS_P4_CONFIRM_2025`은 범위가 아니라 점값(발전외 15%)을 말하고, 관보가 아니라
   언론 기사다(`data/raw/source_register.csv`의 licence·quality_note).
3. 2025년 행은 그 출처 자신의 보고 기간(2026-01-01 ~ 2030-12-31) 밖이다.

**고침.** 계층별 밴드 수(손으로 적혀 있었다), 각 T1 행의 양쪽 폭, 같은 규칙을 쓰는 형제 행,
출처 기간 밖 여부를 `gen_band_coverage`가 인벤토리와 레지스터에서 읽는다
(`scripts/build_tech_guide.py`, +61줄). 정정은 §4.5의 결론을 약화시키지 않고 강화한다 —
관행이 아니라고 남겨 둔 마지막 두 행마저 관행이므로 "the width in this model is mostly
convention rather than evidence"가 예외 없이 성립한다.

**검증(커밋 메시지 기록).** build_tech_guide 33블록 148,352자 재현 ·
`pytest tests/test_tech_guide.py` 53건 · `gate.py` 9항목 OK(pytest 108건 240.76s).

### 인계

- **로그 절 누락은 이번 창에서 두 번째 종류의 미완이다**(F35는 커밋 전 작업 트리, F37은 커밋 후
  로그). 다음 사이클은 `git log`의 마지막 F번호와 `ITERATION_LOG.md`의 마지막 `## F` 절을
  **둘 다** 보고 시작할 것 — 하나만 보면 F37을 F37로 다시 하거나, 없었던 일로 지나친다.

## F38 (08:31) — §3.5의 "factor가 부르지 않는 계열"과 "아무도 읽지 않는 계열"은 같은 집합이 아니었다

**한 일.** 시작 시 `git log`(마지막 `7134afb` = F37)와 `ITERATION_LOG.md` 꼬리(마지막 절 F36)가
어긋났다 — F37이 로그 절을 남기지 않았다. §1.3에 따라 그것을 먼저 완결하고(위 절), 이번을
F38로 잡았다. 게이트는 시작 시 `gate: OK`(pytest 108건 228.09s). 지도(§5-C)는 F16에서 끝나므로
우선순위 규칙에 따라 데이터셋 절을 계속 읽었고, Arc가 가장 자세히 요구한 §3에서 결함이 나왔다.

**결함 1 — §3.5가 손으로 적은 계열 수가 두 가지 이유로 틀렸다.**

`series_id` 행이 "11 of the 18 series present are named by no factor"라고 적었다. 실제로
`FACTOR_SERIES`가 부르는 이름은 6개이고 그중 D4에 있는 것은 5개다(`equip_import_idx`는 없다).
따라서 factor가 부르지 않는 현존 계열은 **13개**다. 11은 어느 셈으로도 나오지 않는다 —
18 − 7(생성기가 세는 "calibration이 이름으로 여는 계열", `electrolyzer_capex` 포함)의 잔재로
보인다. 같은 절의 GEN 블록은 "12 of them are read by nothing"을 맞게 인쇄하고 있었으므로,
독자는 같은 절에서 11·12·13 중 두 수를 서로 다른 뜻으로 마주쳤다.

**결함 2 — 그 옆의 절대 규칙이 §3.5 자신의 결론과 모순된다.**

"**a series no factor names is never opened, however long it is.**" 이것이 참이면
`electrolyzer_capex`는 열리지 않아야 한다 — 어느 factor도 그것을 부르지 않는다. 그런데
`calibrate`가 `wide.get("electrolyzer_capex")`로 직접 열고(`src/cap/calibration.py:99-113`),
그 마지막 관측이 수소 가격 경로의 앵커다. §3.5는 스무 줄 뒤에서 바로 그 사실을 결론으로
쓴다("the electrolyzer capex path — which sets the hydrogen price … is anchored on a two-point
series"). 즉 이 절은 자기가 세운 규칙의 반례 위에 자기 결론을 세우고 있었다. 표의
"Read as" 열도 산문이 "that mapping"이라 부르는 `FACTOR_SERIES`가 아니다 — 생성기가
`electrolyzer_capex → ez`를 따로 붙인다(`build_tech_guide.py:473`).

**고침.** 수를 산문에서 걷어내 `gen_price_series`로 옮겼다. 블록이 이제 factor가 부르는 현존
계열 수, 부르지 않는 수, 그 차이가 왜 "읽히지 않는 수"와 다른지를 D4와 `FACTOR_SERIES`에서
읽어 한 자리에 적는다. 산문의 규칙은 "never opened **for volatility**"로 한정하고, 예외를
이름과 코드 줄로 밝히고, "Read as" 열이 factor 지도 **더하기** 그 직접 읽기임을 말한다.

### 가이드에서 고친 사실 오류

| # | 어디 | 적혀 있던 것 | 실제 | 출처 |
|---|---|---|---|---|
| 1 | §3.5 `series_id` 행 | "**11** of the 18 series present are named by no factor" | **13**이다 — `FACTOR_SERIES`의 이름 6개 중 D4에 있는 것은 5개(`equip_import_idx` 부재) | `data/prepared/D4_price_history.csv`(18계열) · `src/cap/calibration.py:24-26` |
| 2 | 같은 행 | 수를 손으로 적었다 | 수를 GEN 블록으로 옮겼고 산문에는 남기지 않는다 | `scripts/build_tech_guide.py:484-490`(`gen_price_series`) |
| 3 | §3.5 산문 | "a series no factor names is **never opened**, however long it is" — 예외 없는 규칙 | 변동성 추정에 한해 참이다. `electrolyzer_capex`는 어느 factor도 부르지 않는데 `calibrate`가 직접 열고, 수소 가격 경로의 앵커다 | `src/cap/calibration.py:99-113` · 같은 절 §3.5 말미 |
| 4 | §3.5 산문 | "The 'Read as' column below is **that mapping**"(= `FACTOR_SERIES`) | 그 지도 **더하기** 직접 읽기 하나(`ez`)다 | `scripts/build_tech_guide.py:473` |

### 검증

```
.venv/bin/python scripts/build_tech_guide.py           # 33 blocks, 149,001 chars
.venv/bin/python scripts/build_site.py                 # web/guide.html 재생성
.venv/bin/python -m pytest tests/test_tech_guide.py -q # 54 passed
.venv/bin/python -m pytest tests/ -q                   # 109 passed (306.01s)
.venv/bin/python scripts/gate.py                       # gate: OK
```

**게이트가 한 번 붉었고, 원인은 이 변경이 아니라 호스트였다 — 다음 사이클을 위해 적는다.**
변경 뒤 첫 게이트가 `[FAIL] pytest tests/ TimeoutExpired ... 900 seconds`로 끝났다. 같은
스위트가 사이클 시작 시 228초에 통과했으므로 증상만 보면 이번 변경이 의심된다. 그러나 그
프로세스는 **CPU 268초를 쓰면서 벽시계로 2시간 39분**을 썼다(`3% cpu 2:39:10 total`) —
즉 계산이 아니라 대기였다. 확인해 보니 `Google Drive`가 13시간째 CPU 100%를 점유했고
load average가 1/5/15분에 9 / 170 / **275**였다. 부하가 가라앉은 뒤 같은 스위트가 306초에
**109건 전건 통과**했다. 판별 순서: `[FAIL] pytest`를 보면 (1) 그 줄이 초과(Timeout)인지
실패(assert)인지, (2) 초과면 `ps -o time,%cpu`로 CPU 시간과 벽시계를 비교, (3) `uptime`의
load average — 이 셋이면 코드 결함과 호스트 부하를 섞지 않는다.

**게이트의 pytest 하위 프로세스는 타임아웃 뒤에도 살아남는다.** 900초 초과로 게이트가 끝난
뒤에도 그 pytest(PID 97316)가 1시간 8분째 돌고 있었고, 다음 진단 실행과 경쟁했다. 재현
실행 전에 `ps -ax | grep [p]ytest`로 고아 프로세스를 먼저 죽일 것 — 그러지 않으면 두 번째
실행도 느려서 "재현되었다"고 잘못 읽는다.

수치 출처: 현존 18계열·99관측 = `data/prepared/D4_price_history.csv` ·
factor가 부르는 6개 이름과 그중 현존 5개 = `src/cap/calibration.py:24-26`(`FACTOR_SERIES`) ·
직접 읽히는 1개 = `src/cap/calibration.py:99` · 6관측 문턱을 넘는 3개(`smp_monthly` 19,
`indus_tariff` 10, `jepx_spot` 7) = 같은 CSV의 계열별 관측 수.

테스트 1개 추가(53 → 54).
`test_section_3_5_does_not_confuse_named_by_no_factor_with_read_by_nothing` — 생성기의 계산을
빌리지 않는다. D4에서 계열을 직접 세고, factor 지도는 `cap.calibration`에서 import하고,
직접 열리는 계열은 `calibration.py` 원문에서 `wide.get("...")`를 정규식으로 뽑아 확인한 뒤,
인쇄된 세 수와 대조한다. 산문에 `\d+ of the \d+ series` 꼴이 남아 있으면 실패하고, 규칙이
변동성으로 한정되지 않거나 예외를 이름으로 밝히지 않아도 실패한다. 되돌림 검사: `HEAD`의
옛 본문에 같은 검사를 걸면 세 곳에서 붉어진다(`11 of the 18 series` 검출 · 한정 문구 부재 ·
예외 이름 부재).

F32의 줄 인용 검사에 `calibration.py:99 → electrolyzer_capex` 앵커를 추가했다 — 새 인용이
"존재하는 줄"만 통과하고 무엇을 가리키는지는 아무도 보지 않는 상태로 들어가지 않도록.

**함께 대조하고 결함이 없던 것들**: D1b 10열 전부 스키마 필수(§3.2의 "All ten columns")·
`emissions_s2` 비영 63/69 · `re_price` 한국 175,000 / 일본 198,000 KRW/MWh 단일값 ·
§3.5의 "electricity clears it three times over … mean of the three series' annualised
volatilities"(`calibration.py:76-86`의 `np.mean(fv)`) · §3.6의 유상할당 문장(config
2025 0.10 → 2030 0.15 보간 0.11–0.14를 D5의 15%가 2026–2030에 덮어쓴다, `config.yaml:65-71` ·
`D5_policy_support.csv`) · `auction_share_power` 50%가 별도 키로 격리된다.

### 사용자 5개 점검

| 점검 | 판정 | 근거 |
|---|---|---|
| ① 데이터 — 가짜 없고 전부 쓰는가 | 유지 | gate audit `ok 68, UNUSED 1, PARTIAL 3` 불변 |
| ② 시나리오 — 분석툴로 쓸 수 있는가 | 유지 | `out/scenarios/summary.csv` 12묶음 |
| ③ 인사이트 — 팔 수 있는 그림인가 | **개선** | Arc가 가장 자세히 물은 데이터셋 절에서, 어느 계열이 실제로 모델에 들어가는지가 처음으로 한 벌의 수로 맞는다. 지표 ③(TCaR)의 근거를 세는 절이라 특히 그렇다 |
| ④ GitHub·MCP — 도구로 작동하는가 | 유지 | 게이트 9항목·MCP 11도구 불변 |
| ⑤ 산출물 — 다른 형식이 있는가 | 유지 | md / HTML / SVG / 데이터 패키지 |

### 인계

- **`equip_import_idx`는 `FACTOR_SERIES`가 부르지만 D4에 없다.** capex factor가 사전값
  0.06으로 떨어지는 두 이유 중 하나다(다른 하나는 `constr_cost_idx` 3관측). 계열을 구하면
  capex 변동성이 처음으로 추정치가 되고 상관행렬의 항등도 하나 풀린다 — 창 밖(데이터 수집)이라
  여기 남긴다.
- **동시 실행 — 이 커밋은 두 창의 작업을 함께 담는다.** 이 사이클이 호스트 부하로 게이트를
  기다리는 동안(위 문단) **다른 창이 같은 작업 트리에서 같은 블록을 이어 고쳤다**(파일
  mtime 14:43–14:52). 그 창은 이 사이클이 인계로 남기려던 것 — "12개가 D2b의 어느 앵커로
  쓰였는지 §3.5가 말하지 않는다" — 을 바로 구현했다: `kau_krw`가
  `scripts/uncertainty_propagation.py:49`에서 읽히므로 **어느 코드도 읽지 않는 계열은 11개**,
  그중 3개(`re_ppa_*`)는 D2b `re_price` 앵커와 `source_id`를 공유하고 나머지 9개는 어느
  시나리오 파일과도 키를 공유하지 않는다는 문장이 같은 블록에 들어갔다. 그 창은 이 사이클이
  추가한 검사도 새 문구에 맞춰 고쳤고(`the calibration leaves 12 and not 13 unopened`),
  `uncertainty_propagation.py:49 → kau_krw` 앵커를 F32 표에 넣었다. 커밋 시점의 트리는
  `build_tech_guide.py --check` 통과 · `tests/test_tech_guide.py` **55건** 통과다.
  다음 창은 이 커밋 뒤의 `git log`와 로그 절을 함께 보고 자기 번호를 정할 것.
- **§4.5의 밴드 서술은 F37이 생성기로 옮겼다** — `docs/parameter_inventory.csv`의 T1 두 행
  등급·`source_id` 수정은 여전히 창 밖 미결이다(F36 인계).
- **`gen_frontier_shape`의 `rank.max()`**(F28) · **D2b `tech_avail_*` 수집**(F29) ·
  **`prepare_raw.py:57-59`의 죽은 루프**(F32) · **`reline_cheap` 재계획**(~20분) ·
  **추첨 대상을 밴드 보유로도 열기**(F27) · **EFF 두 사본 불일치**(F22) ·
  **EFF·FIN 확률과정 정량 분해**(F20) · **EFF 철강 CAPEX 2건 출처 부재**(F19) ·
  **`_alt` 행의 용도 미실현**(F19) · **감사기의 이름 매칭 한계**(F18) ·
  **§7이 드러낸 실질 공백 2건** · **CCfD 시험 가능화**(F13) · **MCI 사업소 상한 검증**(O13).
- **미해결 코드 수정 5건**(창 밖, 변동 없음): D6 통화 환산(F1), 광양 2고로 능력(F3),
  미등록 `facility_id` 조용한 탈락(F4), `FACTOR_SERIES` 부재 계열(F5),
  `get_affordability` 통화 경고(F8).
