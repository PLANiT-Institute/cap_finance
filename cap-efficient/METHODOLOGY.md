# 방법론과 구현 범위

## 0. 시나리오 계보와 실행 게이트

모델은 `scenario_definitions.csv`를 먼저 읽고 활성 경로만 계산한다. 기업 공시경로는 `corporate_disclosed`, 가속경로는 `internal_stress`로 분류한다. 후자의 1.5°C 표시는 스트레스 강도를 뜻할 뿐 GCAM 정합을 주장하지 않는다. 공식 GCAM 경로는 [GCAM의 기후제약](https://jgcri.github.io/gcam-doc/policies.html)과 [Hector 온도 계산](https://jgcri.github.io/gcam-doc/hector.html)을 사용한 실행 DB에서 추출되어야 한다. `gcam_run_manifest.csv`는 공식 GCAM 9.1 릴리스 자산·configuration·query·target XML의 SHA256을, `gcam_query_manifest.csv`는 온도·강제력·탄소가격·철강 생산/투입/배출·전력/수소가격과 두 변환입력의 추출상태를 고정한다. target-finder 실행·온도 기준·원시단위·DB/CSV hash가 검증되기 전에는 `GCAM_15C`와 `GCAM_2C`가 실행 집합에 들어갈 수 없다.

## 1. 스케줄 최적화

기업마다 3~5개 시설 블록의 기술 조합을 기업 공시경로에서 한 번 완전열거한다. 전환연도는 `설비 재투자연도 + 계획별 이동연도`와 기술 가용연도 중 늦은 값이다. 공시경로의 모든 이정표 탄소예산과 계획별 최소 H₂-DRI BF 용량비중을 지키는 조합 중 중앙 가격경로의 순비용이 가장 낮은 것을 선택한다.

선택된 `(facility_id, technology_id, transition_year)` 집합은 SHA256 기반 `portfolio_id`로 고정한다. 다른 활성 시나리오에서는 설비·기술·전환연도를 다시 고르지 않고 가격·정책·탄소예산만 바꿔 평가한다. 각 연도의 실제 배출과 시나리오 예산뿐 아니라 스크랩·수소·증분계통 수요, 기술별 공사기간과 회사 동시공사 한도, 독립 실패확률 결합값을 검사한다. 하나라도 미충족이면 비용과 위험은 보존하되 효율경계·추천 후보에서는 제외한다. 이 공급·실행 한도는 아직 `model_estimate` 스크리닝 값이다.

기존 BF 블록 후보는 `BF_RELINE`, `SCRAP_EAF`, `H2_DRI_EAF`, 기존 EAF는 `EAF_RENEWABLE`이다. 일본 기업은 공통 기술비용에 `capex_cost_index=1.10`을 적용한다.

## 2. 전환비용

```text
실제 증분 현금비용(지원 전)
  = 증분 고정 OPEX + 증분 전력비 + 수소비
  + 계약 프리미엄(PPA·수소계약·고정 EPC) + 전환연도 증분 CAPEX

실제 순현금비용(지원 후)
  = 실제 증분 현금비용 − CAPEX 보조 − CCfD OPEX 보전

인식된 탄소비용 회피가치
  = (기준배출 − 전환기술 배출) × 탄소가격 × 비용 인식률

경제적 순비용
  = 실제 순현금비용 − 인식된 탄소비용 회피가치
```

NPV 십억원을 누적 회피배출 MtCO₂로 나눈 수치는 천원/tCO₂다. 절대 P50/P90 NPV, 시나리오 고유 회피배출 분모, 공시경로 고정 포트폴리오의 공통 분모를 모두 보존한다. 현재 두 활성 경로의 분석기간이 같아 고정 포트폴리오의 두 분모는 같지만, 향후 GCAM 기간경계가 달라져도 비교 근거를 잃지 않도록 별도 필드로 유지한다.
대시보드 비용 브리지는 P50 순비용에 가장 가까운 모의경로의 CAPEX·고정비·전력·수소·계약 프리미엄·탄소가치·정책지원을 표시하고, 보간 중앙값과의 미세한 차이는 `중앙값 보정`으로 완전히 조정한다. 설비 표의 비용은 확률 P50 배분값이 아니라 동일 기준가격 경로에서 설비를 단독 평가한 순비용 NPV다.

## 3. 확률과 계약

전력·수소입력·건설비에 상관 정규 충격과 평균회귀를 적용한다. PPA, 수소 장기계약, 고정가 EPC는 해당 비중의 가격을 중앙경로에 계약 프리미엄을 더한 값으로 고정한다. 나머지 비중은 확률경로에 노출한다.

## 4. 비교 지표

1. 정렬 CAPEX와 피크 투자연도
2. 순전환비용 P50
3. TCaR = P90−P50, 기본 계획의 단독분산 진단비중, 정밀 후보의 정확한 Shapley 분산배분
4. 동일 `portfolio_id`의 `to − from` signed ΔP50(공통분모)·ΔTCaR·Δ절대 P50/P90 NPV·Δ순현금비용·Δ탄소회피가치·Δ정책지원·ΔCAPEX
5. 가격경로별 최저 대안 대비 계획 고정의 중앙 추가비용
6. P90 순비용 / EBITDA, P90 순비용 / 연간 CAPEX

효율경계는 해당 시나리오의 탄소예산·자원·동시공사·실패위험 제약을 모두 충족한 P1–P7 중 P50과 TCaR의 비지배점이다. `CURRENT`는 경계를 구성하지 않고, 동일 위험 이하 비용 gap과 동일 비용 이하 위험 gap 및 경로 실패의 진단 대상이 된다.
기본 계획표의 TCaR 환산 영향은 기존 호환성을 위해 전력·수소입력·건설 CAPEX를 각각 단독 변동한 진단치를 유지한다. 정밀 shortlist는 같은 seed의 공통난수로 세 요인의 모든 8개 부분집합을 재평가하고, 전체 비용강도 분산을 정확한 Shapley 값으로 배분한다. 상관·비선형 상호작용 때문에 개별 기여는 음수가 될 수 있으나 합계는 전체 분산과 일치하며 `shapley_reconciliation_delta`로 감사한다.

## 5. 생성 후보와 강건 선택

P1–P7의 전환시점·계약 프로필을 템플릿으로 사용하되, 각 시설에 허용된 기술을 다시 배정한다. H₂-DRI 최소비중과 공시계획의 고정 전환달력을 적용한 뒤 `(시설, 기술, 전환연도)` 집합을 물리 포트폴리오로 고정한다. 계약조건이 다른 의사결정 패키지는 `candidate_id`가 다르지만 물리 전환이 같으면 `physical_portfolio_id`가 같다. 현재 17개 시설에서 기업별 조합을 합쳐 910개 후보를 생성한다.

세 단계로 계산한다.

1. 모든 후보를 모든 활성 시나리오에 같은 물리 포트폴리오로 적용해 중앙가격 비용과 탄소·스크랩·수소·계통·공사·실패 제약을 스크리닝한다.
2. 회사당 최대 64개를 선택해 시나리오별 100개 Monte Carlo 경로로 P50·TCaR·현금비용·정책지원·탄소가치·절대 NPV를 다시 계산한다. 이 후보 샘플은 전력·수소·건설 요인분해를 별도로 실행하지 않는다.
3. 모든 활성 시나리오에서 적격인 후보 중 결정론 최악비용이 낮은 후보와 각 시나리오 최저비용 후보를 합쳐 seed 독립 shortlist를 고정한다. 이 후보만 실행 요청의 전체 경로 수로 다시 모의하고, 전력·수소입력·건설 CAPEX의 8개 부분집합 Shapley 분산기여율, 시설별 비용·감축, 연도별 자원 수요·공급여유를 계산한다.

공식 국가 자원 벤치마크는 회사별 제약 입력과 분리한다. 국가 수소공급(MtH₂ 또는 MtH₂-eq), 계통설비(GW·MW·MVA), 스크랩 발생·가공(Mt/년)은 단위와 범위가 서로 다르므로 회사별 TWh·Mt 공급한도로 변환하지 않는다. 대시보드와 보고서는 이를 방향성·검증 우선순위의 별도 맥락으로만 표시한다.

후보 (p), 시나리오 (s)의 비용을 (C_{p,s}), 해당 비교집합의 시나리오별 feasible 최저 비용을 (C_s^*)라 두면 `regret = C_{p,s} − C_s^*`, `max_regret = max_s(regret)`다. 모든 시나리오에서 feasible한 후보 중 최대후회와 최악 TCaR가 비지배인 집합을 강건경계로 표시한다. 정밀 결과의 기준점은 전체 910개가 아니라 고정된 정밀 shortlist 안의 최저 적격안이다. 위험회피도 λ의 추천점은 `max_regret + λ × worst_TCaR`를 최소화하며 현재 λ=0, 1, 4를 출력한다. 이는 기후경로 확률을 가정하지 않는 minimax-regret 진단이지 투자자의 효용함수 추정이 아니다.

## 6. 데이터 품질과 다음 단계

- 공식: 기업 생산·배출·재무 총량, 감축률, 일부 확정 전환사업
- 보정/추정: 시설 블록 배분, 개별 집약도, 재투자연도, 공통 기술비용, 환율, 계약, 지원율, 가속 경로
- 경계 불일치: 환경값은 철강/국내 기준인 반면 일본 재무는 연결 기준이며 Kobe 14.3MtCO₂는 목표 경계

다음 구현은 실제 BF/EAF별 공개 패널과 품질등급, 국가별 장기 전력·수소 경로, 스크랩·계통·공사기간 제약을 붙인 뒤 완전열거를 MILP로 교체하는 순서가 적절하다.

수백 개 후보와 현실 제약의 구조는 구현했지만 공급한도·실패확률·전환비용은 여전히 `model_estimate`다. 공식 GCAM 1.5°C/2.0°C가 비활성인 동안 강건경계는 기업 공시경로와 내부 스트레스 두 경로에 대해서만 유효하다. 다음 단계는 공식 GCAM 실행환경과 query export를 확보하고, 실제 BF/EAF 공개 패널과 국가별 공급제약으로 후보 입력을 보정한 뒤 MILP/확률최적화로 확장하는 것이다.

---

## 상호 참조 — FIN 형식 명세

최적화 정식화(집합·결정변수·목적함수·제약)와 가정 대장(A-01…A-21)의 정본은 FIN 저장소의
[`METHODOLOGY.md`](https://github.com/PLANiT-Institute/cap_finance/blob/main/METHODOLOGY.md)다.
이 문서는 EFF(증거 레지스트리·강건 선별)의 구현 범위를 기술하며, 두 모형의 교차대조는
`docs/cross_model_check.md`에 둔다. AUTOPILOT §3의 공유 산출물 규약에 따른다.
