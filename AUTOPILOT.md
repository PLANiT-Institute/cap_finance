# AUTOPILOT v2 — CAP 이중구현 학술 완성 프로토콜

> 매 사이클의 시스템 지시. **단일 저장소**(2026-08-10 통합) 안에 두 구현이 나란히 산다.
> - **FIN** = 저장소 루트 `~/Documents/GitHub/cap_finance` — 시설 MILP·계약 격자·위험 경계·석유화학 포함
> - **EFF** = `cap-efficient/` 하위디렉터리 — stdlib 전용·증거 레지스트리·강건 선별·철강 4사
>
> **이중구현은 유지된다.** 통합은 폴더 관리 문제를 없앤 것이고, H4 교차대조의 전제인
> **코드 독립성**은 그대로다: EFF는 `cap.*`를 import하지 않고 FIN은 `cap_efficient.*`를
> import하지 않는다. 공유는 CSV 산출물과 크로스워크로만. 이 규칙이 깨지면 교차대조는
> 순환논증이 되고 §7의 "독립 구현과의 교차대조" 방어가 무효가 된다.
>
> **기간**: 2026-08-10 00:41 → 09:00, 30분 × 17사이클 (D1–D17).
> **기준**: "돌아간다"가 아니라 **"심사자가 반박하지 못한다"**. 이전 v1의 완료 기준은 전부 무효.

---

## 0. 미션 — 9시에 도달해야 하는 상태

1. **결론을 좌우하는 파라미터가 전부 T3 이상 증거**로 뒷받침되고, 그렇지 못한 것은 민감도 분석으로 결론 불변성이 입증돼 있다.
2. **검증 3층 통과**: 내부 일관성(회계 항등식·질량/에너지 균형) → 외부 대조(실제 프로젝트 비용·문헌 LCOA 범위·두 모형 교차) → 후향 검증(2020–2024 실적 재현).
3. **형식 명세 존재**: 집합·파라미터·결정변수·목적함수·제약이 수식으로 기술된 `METHODOLOGY.md`, 모든 가정에 식별자 부여.
4. **민감도·강건성 부속서**: 할인율(3.5/5/6.5%), 상위 파라미터 OAT, 구조 대안(마진 정의·경계 정의·탄소 처리) 결과가 표로 존재.
5. **재현 패키지**: 데이터 패키지(라이선스 준수) + 시드 고정 + 환경 핀 + 1명령 재실행 + MCP 서버로 조회 가능.
6. **워킹 페이퍼 초안**: `paper/working_paper.md` — 연구질문·방법·데이터·결과·한계·반증가능 주장.
7. **Vercel 사이트**가 위 전부를 탐색 가능하게 묶는다.

---

## 1. 증거 등급 (모든 수치에 필수)

| 등급 | 정의 | 예 |
|---|---|---|
| **T1** | 규제·검증·법정 공시 | GIR 명세서 배출량, 감사받은 재무제표, 관보 고시 |
| **T2** | 기업 1차 공시 | IR 데이터북, 보도자료, 지속가능경영보고서 |
| **T3** | 동료심사·공적기관 | peer-reviewed 논문, IEA·IEAGHG·DIW·NEDO 보고서 |
| **T4** | 업계·시장 인용 | 트레이드 프레스, 컨설팅, 2차 인용 |
| **T5** | 모델 추정 | 우리가 만든 값 — **근거식 + [low, central, high] 범위 필수** |

**규칙**
- 모든 파라미터 행에 `evidence_tier`, `value_low`, `value_high`, `source_id`, `vintage`(자료 연도) 컬럼.
- **T5는 반드시 범위를 갖는다.** 범위 없는 T5는 결함으로 간주하고 즉시 수정.
- 민감도 상위 10 파라미터(§4-C에서 확정)는 **T3 이상 필수**. 불가능하면 그 사실과 결론 영향이 페이퍼 한계절에 명시돼야 한다.
- 인용은 `source_register`의 `source_id`로만. 본문에 URL 직접 기입 금지.

## 2. 사이클 프로토콜 (30분)

1. **상태(2분)**: 양 저장소 `ITERATION_LOG.md` 최신 + `git status`. 미완 작업 우선 완결.
2. **선택(2분)**: §4 워크스트림에서 §5 사이클 지도가 지정한 항목. 지도와 어긋나면 로그에 사유.
3. **실행(20분)**: 다음을 모두 만족해야 "완료".
   - 수치를 바꿨으면 **누가 봐도 추적 가능**: PREP_LOG 또는 해당 CSV의 source/tier 컬럼 갱신.
   - 코드를 바꿨으면 **테스트 추가 또는 갱신**. FIN `pytest tests/ -q`, EFF `pytest tests -q` + `validate-data` 그린.
   - 파이프라인 영향 시 최소 `--sims 2000` 재실행으로 방향 확인, 사이클 여유 시 20000.
   - **결론 변화 확인**: 헤드라인 표(기업×시나리오 ②③) 전후 비교를 로그에 숫자로 남긴다.
4. **커밋·푸쉬(3분)**: `autopilot(C{N}): <내용>`. 양 저장소 각각.
5. **로그(3분)**: `## Cycle N (HH:MM) — 한 일 / 검증(수치·테스트) / 결론 영향 / 인계 / 백로그 변경`.

**금지**: 출처 없는 수치 신규 도입. 테스트 레드 상태 커밋. 로그 누락.
**중단 규칙**: 같은 오류 2사이클 → 백로그 하단으로 강등, 원인 가설 기록, 다음 항목.
**웹 수집 규칙**: 접근 실패한 출처는 `data_gap_registry`에 `access_blocked` 사유와 대체 경로를 남긴다. 추측으로 채우지 않는다.

## 3. 동기화 규약 (통합 후)

- **커밋은 하나**: 이제 한 저장소이므로 사이클당 커밋 1개. FIN·EFF 양쪽을 건드렸으면 한 커밋에 담고 메시지에 둘 다 밝힌다.
- **코드 독립성은 불가침**: EFF 코드는 `cap.*`를, FIN 코드는 `cap_efficient.*`를 import하지 않는다. 이를 강제하는 테스트는 `tests/test_independence.py`.
- **정본**: 데이터 증거·성숙도 = EFF / 최적화 방법론·석유화학 = FIN. 개선은 상대 구현에 **소비 경로**(변환 스크립트 + CSV)로 전달, 맹목적 복사 금지.
- **공유 산출물**(양쪽 동일 사본 유지): `data/crosswalk_facilities.csv`, `data/crosswalk_scenarios.csv`, `docs/tech_cost_reconciliation.md`, `docs/cross_model_check.md`, `AUTOPILOT.md`(루트 정본, `cap-efficient/AUTOPILOT.md`는 사본), `METHODOLOGY.md`(FIN 정본, EFF는 참조 링크).
- **매 사이클 1개 교차대조**: 값 하나를 양 모형에서 확인. 2배 이상 차이는 백로그 항목 생성.
- **비공개 원칙**: 시설 단위 산출은 설계서 §8-2. 공개 패키지·페이퍼 본문은 기업 집계, 시설 결과는 부속서 취급.
- **폐기**: `~/Documents/GitHub/cap-rebuild`는 `archive/cap_kj_v1`의 오래된 부분집합으로 확인됨(2026-08-10). 저장소에 넣지 않는다.

---

## 4. 워크스트림 (완료 시 [x])

### F. 신뢰도 기반 공사 (최우선 — 이후 모든 작업의 순서를 결정)

- [x] **F1. 파라미터 인벤토리**: FIN·EFF의 모든 입력 파라미터를 한 표로(`docs/parameter_inventory.csv`): id, 모형, 값, 단위, tier, 범위, source_id, vintage, 사용처(어느 방정식). 누락·무출처 항목 목록화.
  - *완료 기준*: 전 파라미터 행 존재, tier 부여율 100%, 무출처 항목이 명시적으로 `T5-norange`로 표시됨.
- [x] **F2. 사전 민감도 스크리닝**: 각 파라미터를 ±30%(또는 tier 범위) 흔들어 헤드라인 4지표(②·③·gap·경계 점수) 변화 측정 → `outputs/sensitivity_screening.csv` + 상위 10 랭킹.
  - *완료 기준*: 랭킹 표 존재, 상위 10의 현재 tier 명시, T3 미만인 것이 §5 데이터 사이클의 작업 목록이 됨.
- [x] **F3. 불확실성 범위 도입** (D5): `scripts/uncertainty_propagation.py` + `docs/uncertainty_propagation.md`. 상위 10 파라미터를 동시에 추첨(200회 × 가격경로 20,000)해 TCaR을 **가격분 / 파라미터분 / 결합 / 상호작용**으로 분해한다. 분위수는 가법적이 아니므로 잔차를 열로 남긴다. 결과: 파라미터분이 결합 TCaR의 **철강 41~44%, 석화 26~27%**(±30% 폭). **추첨 폭은 근거가 아니라 규약**이다 — 인벤토리 415행 중 밴드 보유 18행. 그래서 ±15%도 함께 돌려 선형성(×2.08~2.14)을 보고한다 → **G9 신설**.
  - *남은 것*: 밴드가 T3 근거로 채워지면(G2·G3·G9) 이 표의 파라미터분이 비로소 절대값으로 인용 가능해진다. 계획 재선택 경로는 여전히 밖에 있다(추첨마다 MILP 재해 불가).

### G. 데이터 승급 (F2 랭킹 순서로 진행)

- [ ] **G1. 시설 실측 배출**: 한국 GIR 명세서 사업장 배출량(공공데이터포털), 일본 SHK/EEGS 사업소별 배출량 확보 → 현재의 "회사 합계 × 능력가중 배분"을 실측으로 대체. **모형 최대 약점.**
  - *완료 기준*: 최소 POSCO 2사업장·NSC 3제철소 실측 확보, 배분 규칙 대비 오차 보고, 미확보분은 사유 기록.
- [ ] **G2. 에너지 원단위**: 현재 루트 표준값 주입(BF 13.5 GJ/t 등) → 기업 환경데이터·문헌 범위로 교체하고 [low, high] 부여.
- [ ] **G3. 설비 능력**: 내용적×913 t/m³/yr 단일 계수 → 공표 능력 우선, 미공표만 계수 추정. **EFF 블록 대비 12% 괴리(C2 §5-1)를 이 작업으로 해소.**
- [ ] **G4. 시나리오 실데이터**: NGFS Phase 5 / GCAM 공개 산출 접근 재시도. 실패 시 IEA WEO·국가 로드맵 앵커로 **단조성·정합성 검증된** 경로 재구축 + EST 라벨 유지.
- [~] **G5. 가격 시계열 심화** (D4 부분완료, **목적 축소**): 검정 부분은 끝났다 — `scripts/price_process_test.py` + `docs/price_process_test.md`. ADF·Hurst를 유한표본 임계값과 함께 돌리고 **검정력**을 계산한 결과, 반감기 10년 OU를 80% 검정력으로 잡으려면 월별 **약 4,740obs(≈395년)**가 필요하다. **GBM 유지 결정, 근거는 검정 결과가 아니라 검정 불가능성.** 남은 것은 σ 정밀화 목적의 월별 수집뿐이고 익명 HTTP로는 막혔다(`data_gap_registry.md` G5).
- [ ] **G8. 확률과정 이원화 해소** (D4 신설, 교차대조 발견): FIN은 GBM, EFF `data/price_process.json`은 κ=0.35/yr(반감기 2.0년) OU다. σ는 0.242 대 0.22로 붙어 있으나 **과정이 다르고**, H4 교차대조의 위험 지표 차이에 이 몫이 섞여 있다. 어느 쪽도 데이터로 지지되지 않으므로(위 검정력), 값을 맞추는 것이 아니라 **차이를 분해해 보고**하는 것이 완료 기준이다.
- [ ] **G9. 증거 밴드 부재** (D5 신설, F3의 전제): `docs/parameter_inventory.csv` 415행 중 `value_low`/`value_high`를 가진 것이 **18행**뿐이다. F3의 파라미터분은 그래서 ±30%라는 **규약** 위에 서 있고 폭에 선형이다(×2.08~2.14). 완료 기준은 "밴드를 다 채운다"가 아니라 **F2 상위 10에 대해 T3 이상 근거 밴드를 붙이거나, 못 붙이는 것은 그 사실과 결론 영향을 명시**하는 것이다(§1 규칙과 동일). G2·G3와 같은 수집으로 상당수가 해결된다.
- [ ] **I3-b. `docs/process_alternative.md` 재실행** (D5 신설, 자동 대조로 발견): 그 문서의 GBM TCaR 열이 현재 `out/e5/metrics_company.csv`와 어긋난다 — **Nippon Steel 인용 34,488 대 현재 32,973 (+4.6%)**, 나머지 3사는 일치. 문서 생성 이후의 파이프라인 변경이 반영되지 않았다는 뜻이고, F3 §3의 '과정 선택분'이 그만큼 오차를 안는다. `scripts/uncertainty_propagation.py::_cited_vs_current`가 이 어긋남을 매 실행 자동 경고한다.
- [ ] **G6. 마진·재무**: 포스코 별도 실적의 톤당 마진을 사업부문 공시로 정합화, 석화 스프레드의 변동비 차감 처리 명시.

### H. 검증 (학술 방어의 핵심)

- [x] **H1. 내부 일관성 테스트**: 회계 항등식(총비용 = 구성요소 합), 질량·에너지 균형(생산량×원단위 = 에너지 소비), 배출 정합(EF×생산 = 배출), 단위 왕복 변환. → `tests/test_consistency.py`.
- [x] **H2. 후향 검증(back-test)**: 2020–2024 실제 생산·에너지가격을 입력했을 때 모형이 실제 배출·에너지비용을 얼마나 재현하는가. 오차율 보고.
  - *완료 기준*: `docs/validation_backtest.md`, 기업별 배출 재현 오차 ±10% 이내 목표, 벗어나면 원인 규명.
- [x] **H3. 외부 대조**: (a) 실제 프로젝트 비용(광양 EAF, NSC 야하타, JFE 구라시키 — EFF `technology_cost_evidence`)과 모형 CAPEX 비교, (b) 문헌 LCOA/MAC 범위(Vogl, Agora, IEA ISTR, Material Economics, MPP)와 우리 ② 비교.
  - *완료 기준*: `docs/validation_external.md`에 비교표 + 우리 값이 문헌 범위 안/밖 판정과 사유.
- [x] **H4. 두 모형 교차대조(C4 승격)**: POSCO·NSC의 배출경로·CAPEX·비용을 양 모형에서 대조, 차이를 **구조 요인별로 분해**(경계 정의, 마진 처리, 탄소 처리, 시설 해상도).
  - *완료 기준*: `docs/cross_model_check.md`, 각 차이에 정량 기여도.

### I. 강건성 (심사자가 반드시 묻는 것)

- [x] **I1. 할인율**: 3.5 / 5.0 / 6.5% 전 파이프라인 재실행, 헤드라인 표 3종 → 결론(순위·gap 부호) 불변 여부.
- [x] **I2. 구조 대안**: (a) 비용 정의 P50 vs P90 기준, (b) retire 상한 10/20/30%, (c) 무상할당 램프 3종(현행/보수/공격), (d) 예산 위반 페널티 수준. 각각 결론 영향.
- [x] **I3. 확률과정 대안**: GBM vs 평균회귀(OU) — G5 검정 결과 반영, TCaR 차이 보고.
- [x] **I4. 표본 안정성**: 시드 5개 반복, 헤드라인 지표 표준편차 보고(EFF는 이미 seed 반복 있음 — 방식 이식).

### J. 형식화·공개

- [x] **J1. `METHODOLOGY.md`**: 집합·파라미터·변수·목적함수·제약 수식 기술, 각 가정에 `A-01` 형태 식별자, 근거·영향 명시. E2 대리목적함수와 E4 정본의 관계를 정면 기술.
- [x] **J2. 데이터 패키지**: `data/package/` 공개 CSV + 병합 데이터 사전 + 출처 등록부 공개분 + LICENSE(재배포 불가 출처 제외/구간화) + `manifest.json`(SHA256).
- [x] **J3. MCP 서버**: EFF `cap_efficient/mcp_server.py` stdlib JSON-RPC — `list_companies`, `get_plan_metrics`, `get_frontier`, `get_parameter(id)`(tier·출처 반환), `get_validation_summary`, `get_data_package_manifest`. 시설 단위는 기본 거부.
- [ ] **J4. 워킹 페이퍼**: `paper/working_paper.md` — 연구질문, 선행연구 대비 기여(자본배분 경계·TCaR·공시 gap), 방법, 데이터·신뢰등급, 결과, 강건성, 한계, **반증가능 주장 목록**.
- [ ] **J5. README 재작성 양쪽**: 30초 스타트, 아키텍처, 산출물 표, 신뢰등급 요약, 인용 방법(BibTeX), 라이선스.
- [ ] **J6. 사이트 확장**: 방법론·검증·강건성·데이터 패키지 페이지 추가, 랜딩에서 전부 도달 가능.

### L. 문헌 좌표화 (2026-08-10 신설 — "우리 발견이 얼마나 더 좋아질 수 있나")

우리가 만든 숫자를 우리끼리만 검증하는 것으로는 부족하다. **다른 보고서·논문이 이미 아는 것을
우리가 모르고 있는지**, 그리고 **우리가 아는 것 중 그들이 모르는 것이 정확히 무엇인지**를 분리한다.

- [x] **L1. 선행연구 지도**: 우리 3대 주장(① 자본배분 효율경계 ② TCaR = P90−P50 ③ 공시-모형 gap) 각각에 대해 문헌이 도달한 지점을 표로. 대상: Vogl et al.(철강 전환비용), Agora/Material Economics(MAC), IEA ISTR, MPP, NGFS Phase 5, TCFD/ISSB 전환계획 문헌, 좌초자산 문헌(Caldecott, Semieniuk). → `docs/literature_map.md`
  - *완료 기준*: 주장별로 "이미 있음 / 부분 있음 / 우리가 처음"이 근거와 함께 판정되고, 각 판정에 인용 `source_id`. — **D2 충족**. 다만 판정 일부가 본문 없이 스니펫 수준이며 그 목록은 `data_gap_registry.md`에 있다.
- [x] **L2. 문헌 방법 이식**: 탄소가격 불확실성의 확률화를 채택. — **D6 완료.** `kau_krw`(K-ETS 2015–2023) 실측 연변동성 36.3%로 확률 축 신설, TCaR 증분 철강 +2,444/+1,223 · 석화 −100/−37 (십억원). FC4가 **업종별로 갈렸다** — 철강은 반증 실패(정책분이 파라미터분과 동급), 석화는 반증. → `docs/uncertainty_propagation.md` §4, `docs/literature_map.md` §2.
  - **헤드라인은 아직 결정론이다.** 확률 축은 진단으로만 켰고(`shocks['co2']` opt-in), `config.yaml`은 무수정 — 채택하면 e5 전면 재실행이 필요하다. → **L2-b 신설**.
- [ ] **L2-b. 탄소가격 확률화의 헤드라인 채택 판단** (D6 신설): 정책 축을 e3 FACTORS에 넣어 헤드라인 ③을 다시 정의할지. 걸림돌 둘 — (a) `calibration.calibrate`의 corr가 `dropna()`로 계산돼 4번째 계열을 넣으면 elec–capex 상관이 바뀌고 **헤드라인이 L2와 무관한 이유로 움직인다**, (b) `kau_krw`(연, 2015–2023)와 `smp_monthly`(월, 2025–)의 관측 구간이 겹치지 않아 탄소–전력 상관을 추정할 수 없다.
  - *완료 기준*: (a)를 분리해 corr 추정 규약을 계열쌍별 pairwise로 바꾸고 전후 헤드라인 비교표, 또는 채택하지 않기로 하고 그 이유를 방법 절에 기록.
- [ ] **G7. 개수 이력 외부 데이터베이스**: GEM Global Blast Furnace Tracker로 D1a `last_reline_year`·능력의 출처를 개별 수집에서 공개 DB로 승격. (L1에서 `NATCOMM_APA_2026`이 쓰는 것을 우리는 안 쓰고 있음을 확인.)
  - *완료 기준*: 채택 전후 헤드라인 4지표 비교표 + 왜 바뀌었는지 메커니즘 설명.
- [ ] **L3. 기여 진술 정밀화**: L1·L2 이후 "이 논문이 아니면 알 수 없는 것" 한 문단. 반증가능 형태로.

### M. 논문화 (J4 확장 — 최우선 산출물)

- [x] **M1. 골격**: `paper/working_paper.md` — 연구질문 1문장, 기여 3개, 반증가능 주장 FC1–FC5, 절 구조, 타깃 저널(1순위 Energy Policy). 본문 수치는 §0 대장에만 적고 `tests/test_paper_numbers.py`가 `out/`과의 일치를 강제한다.
- [x] **M2. 방법 절** (D7): `paper/working_paper.md` 「본문 §3. 방법」 3.1–3.8. METHODOLOGY의 수식을 논문 문체로 옮기고, **대리와 정본의 관계를 서술이 아니라 측정으로** 기술했다 — E2 대리비용과 E4 정본 P50의 순위상관이 철강 −0.05~0.00 / 석화 0.20~0.73이고, 대리 최저비용 계획이 정본 최저인 묶음은 **0/8**이다. 재정렬의 원인이 몬테카를로가 아님도 대조군으로 보였다(ρ(중앙경로, P50) ≥ 0.94). 부수 발견: 51개 후보 중 정본 기준 서로 다른 것은 **43개**(CCfD만 다른 쌍이 묶음마다 1개) → 페이퍼 §4 경계 퇴화 진단 강화, METHODOLOGY §9-9 신설.
  - *남은 것*: 후보집합 밖에 대해서는 말할 수 없다는 한계(페이퍼 §6-10)는 **M8이 아니면 좁혀지지 않는다**.
- [ ] **M3. 데이터 절**: D1–D7 출처·연도·신뢰등급 표, 공백 4건과 그 처리(data_gap_registry 연결).
- [ ] **M4. 결과 절**: 헤드라인 표 3종 + 경계 그림 + λ 접점 + 정책 wedge. 모든 수치는 `out/`에서 자동 인용.
- [ ] **M5. 강건성·한계 절**: I1–I4 + F3 결과, 반증가능 주장별로 "이렇게 나오면 우리가 틀린 것".
- [ ] **M6. 서론·선행연구 절**: L1 지도를 산문으로.
- [ ] **M7. 초록·기여**: 마지막에.
- [ ] **M8. 경계 퇴화 해소** (D3 신설, **M5보다 먼저 답이 필요**): 16개 (기업×시나리오×지원) 묶음 중 14개에서 효율경계 위 점들이 **같은 기술 일정**을 공유한다 — 경계를 따라 움직이는 것은 PPA 비중뿐이다. E2가 만드는 기술 일정이 기업당 1–5개로 얇은 것인지(계획 생성 문제), 재투자 창과 A-10이 자유도를 없앤 것인지(모형 경계에 관한 발견) 가른다. ε-constraint를 기술 일정 축에도 걸어 재확인. 후자로 확정되면 논문 제목의 "자본배분"을 "계약·자본 혼합 배분"으로 정정한다.

### K. 잔여 (v1 이월)

- [ ] K1. FIN 회귀 테스트에 retire·전력 이원화 케이스 추가(A3).
- [ ] K2. EFF에 FIN의 신규 시계열 이식(B3).
- [ ] K3. 시나리오 정의 대조표(C3) — G4에 흡수 가능.
- [ ] K4. EFF에 부분감축 수단(수소취입·HBI·스크랩) 도입 검토.

---

## 5-A. 오늘 창 (2026-08-10 01:00 → 09:00, D1–D17) — **현행 지도**

사용자 지시(2026-08-10 00:41): 30분 간격, 9시까지, **오늘 창만**. 목표 4개 —
**데이터 갱신·검증 / 문헌으로 발견 개선 / MCP·GitHub 도구 작동 / 논문화**.
§5-0의 5개 점검(①~⑤)은 매 사이클 그대로 유지한다.

| 사이클 | 시각 | 항목 | 목표 대응 |
|---|---|---|---|
| D1 | 01:00 | ④ 게이트 스크립트화 + 통합 후 전체 재실행 | MCP·GitHub |
| D2 | 01:30 | **L1** 선행연구 지도 | 문헌 |
| D3 | 02:00 | **M1** 페이퍼 골격·연구질문·반증가능 주장 | 논문 |
| D4 | 02:30 | **G5** 월별 가격 + ADF/Hurst 검정 | 데이터 |
| D5 | 03:00 | **F3** 파라미터 불확실성 전파 (TCaR 분해) | 데이터·검증 |
| D6 | 03:30 | **L2** 문헌 방법 1개 이식 + 결론 영향 | 문헌 |
| D7 | 04:00 | **M2** 방법 절 | 논문 |
| D8 | 04:30 | **G3** 설비 능력 정합 (EFF 12% 괴리 해소) | 데이터 |
| D9 | 05:00 | **M3** 데이터 절 + 신뢰등급 표 | 논문 |
| D10 | 05:30 | **G2** 에너지 원단위 범위 승급 | 데이터 |
| D11 | 06:00 | **M4** 결과 절 + 표·그림 | 논문 |
| D12 | 06:30 | **G4** 시나리오 실데이터 재시도 | 데이터 |
| D13 | 07:00 | **L3** 기여 진술 + 문헌 대비 우리 값 위치 | 문헌 |
| D14 | 07:30 | **M5** 강건성·한계 절 | 논문 |
| D15 | 08:00 | **M6** 서론·선행연구 절 | 논문 |
| D16 | 08:30 | **J5·J6** README·사이트 + ⑤ 산출물 | MCP·GitHub |
| D17 | 09:00 | 총점검 + 창 요약 + 스케줄 종료 | 전부 |

**밀릴 때 우선순위**: M(논문) > L(문헌) > ④게이트 > G(데이터) > 나머지.
**앞설 때**: G6·K항.

## 5-0. 사이클 지도 개정 (C10 → , 2026-08-09 11:00 → 19:00) — *이력, 완료됨*

사용자가 매 사이클 점검할 5개 질문을 지정했다. 아래 지도는 그 5개를 향해 재정렬한 것이며,
§5의 원 지도(C6–C26)는 학술 완성 백로그로 계속 유효하다 — 밀린 항목은 이 지도 뒤에 붙는다.

| 점검 | 질문 | 판정 근거 |
|---|---|---|
| ① 데이터 | 가짜가 없고 전부 쓰는가 | `scripts/audit_data.py` — 합성 누출 시 실행 실패, UNUSED/EMPTY/무출처 계수 |
| ② 시나리오 | 시나리오 분석툴로 쓸 수 있는가 | `scripts/run_scenarios.py` 묶음 수 + 순위 역전 판정 |
| ③ 인사이트 | 컨설턴트로서 팔 수 있는 그림인가 | 조달부담(⑥)·경계·gap이 한 화면에서 의사결정으로 이어지는가 |
| ④ GitHub·MCP | 도구로 작동하는가 | `python -m cap all` 무인자 성공 + MCP `tools/list` 응답 + 푸쉬 |
| ⑤ 다른 비주얼 | 다른 형식의 산출물이 있는가 | 정적 보고서 외 대화형·덱·데이터 패키지 |

| 사이클 | 항목 | 점검 대응 |
|---|---|---|
| C10 | 데이터 감사 + 지표 ⑥ 조달부담 + CAPEX 시점 수정 | ①③ |
| C11 | D5 유상할당 정본화, H1 내부 일관성 테스트, MCP 서버 | ①④ |
| C12 | 시나리오 러너 + 대화형 시나리오 페이지 | ②③⑤ |
| C13 | H3 외부 대조 (실제 프로젝트 비용·문헌 LCOA) | ① |
| C14 | H2 후향 검증 2020–2024 | ① |
| C15 | J1 METHODOLOGY 형식화 | ④ |
| C16 | J2 데이터 패키지 + manifest | ①④⑤ |
| C17+ | I1 replan 강건성 → J4 워킹 페이퍼 → 잔여 | ②① |

## 5. 사이클 지도 (C6 → C26, 22:35 → 09:00)

| 사이클 | 시각(목표) | 항목 | 산출물 |
|---|---|---|---|
| C6 | 22:35 | F1 파라미터 인벤토리 | `docs/parameter_inventory.csv` |
| C7 | 23:05 | F2 사전 민감도 스크리닝 | `outputs/sensitivity_screening.csv` + 상위 10 |
| C8 | 23:35 | H1 내부 일관성 테스트 | `tests/test_consistency.py` 그린 |
| C9 | 00:05 | G1 시설 실측 배출 수집(웹) | raw 갱신 + 배분 오차 보고 |
| C10 | 00:35 | G1 적용 + G3 능력 정합 | 12% 괴리 해소 또는 사유 |
| C11 | 01:05 | G5 월별 가격 + 평균회귀 검정 | 캘리브레이션 재실행 |
| C12 | 01:35 | G4 시나리오 실데이터 | D2 재구축, 단조성 검증 |
| C13 | 02:05 | G2·G6 원단위·마진 승급 | tier 상승분 로그 |
| C14 | 02:35 | H2 후향 검증 | `docs/validation_backtest.md` |
| C15 | 03:05 | H3 외부 대조 | `docs/validation_external.md` |
| C16 | 03:35 | H4 두 모형 교차대조 | `docs/cross_model_check.md` |
| C17 | 04:05 | I1 할인율 강건성 | 3종 헤드라인 표 |
| C18 | 04:35 | I2 구조 대안 | 대안별 결론 영향표 |
| C19 | 05:05 | F3 파라미터 불확실성 전파 | TCaR 분해 확장 |
| C20 | 05:35 | I3·I4 확률과정·표본 안정성 | 보고서 반영 |
| C21 | 06:05 | J1 METHODOLOGY 형식화 | 수식 명세 |
| C22 | 06:35 | J2 데이터 패키지 | `data/package/` + manifest |
| C23 | 07:05 | J3 MCP 서버 | 서버 + 등록 문서 |
| C24 | 07:35 | J4 워킹 페이퍼 초안 | `paper/working_paper.md` |
| C25 | 08:05 | J5·J6 README·사이트 확장 | 배포 갱신 |
| C26 | 08:35 | 총점검 | 전 게이트 재확인, 최종 푸쉬, 10시간 요약 |

**밀릴 때 우선순위**: H(검증) > G1·G5(핵심 데이터) > I1(할인율) > J1(방법론) > J4(페이퍼) > 나머지.
**앞설 때**: K항 + EFF 부분감축 수단 도입.

## 6. 품질 게이트 (매 사이클)

- [ ] 두 저장소 테스트 그린, 커밋 완료
- [ ] **재현성**: 테스트·실행이 산출물을 공유하지 않는가(out vs out_test), 보고서 수치가 실데이터 실행에서 나온 것인가
- [ ] 신규·변경 수치에 tier + source_id + (T5면) 범위
- [ ] ITERATION_LOG에 **헤드라인 지표 전후 숫자** 기록
- [ ] 결론 서사 점검: "기대값에서 싸 보이는 계획은 꼬리에서 비싸다" / "지출 부족 = 에너지 리스크 포지션" — 뒤집히면 버그 우선 의심, 진짜면 명시적으로 기록하고 페이퍼에 반영

## 7. 학술적 방어 체크리스트 (C26에서 전항 확인)

- [ ] 연구질문이 한 문장으로 서술되고, 답이 검증 가능한 형태인가
- [ ] 선행연구 대비 기여가 명시적인가 (자본배분 효율경계·TCaR·공시 gap의 결합)
- [ ] 모든 결정변수·제약이 수식으로 기술됐는가
- [ ] 데이터 출처·연도·등급이 표로 존재하는가
- [ ] 결론을 좌우하는 가정이 식별되고 민감도로 검증됐는가
- [ ] 후향 검증 결과가 제시됐는가
- [ ] 독립 구현(EFF)과의 교차대조가 있는가
- [ ] 한계와 **반증가능한 주장**이 명시됐는가
- [ ] 재현 가능한가 (데이터·코드·시드·환경)
